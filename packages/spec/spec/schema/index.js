/* eslint-disable @typescript-eslint/no-require-imports */
// This file is autogenerated by the hyperschema compiler
// Schema Version: 1
/* eslint-disable camelcase */
/* eslint-disable quotes */
/* eslint-disable space-before-function-paren */

const { c } = require('hyperschema/runtime')

const VERSION = 1

// eslint-disable-next-line no-unused-vars
let version = VERSION

// @peartube/empty
const encoding0 = {
  preencode(state, m) {

  },
  encode(state, m) {

  },
  decode(state) {
    return {
    }
  }
}

// @peartube/error
const encoding1 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte

    if (m.code) c.uint.preencode(state, m.code)
    c.string.preencode(state, m.message)
  },
  encode(state, m) {
    const flags = m.code ? 1 : 0

    c.uint.encode(state, flags)

    if (m.code) c.uint.encode(state, m.code)
    c.string.encode(state, m.message)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      code: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      message: c.string.decode(state)
    }
  }
}

// @peartube/peer-performance-metric
const encoding2 = {
  preencode(state, m) {
    const flags =
      (m.latencyMs ? 1 : 0) |
      (m.handshakeDurationMs ? 2 : 0) |
      (m.socketStability ? 4 : 0) |
      (m.socketStabilityObserved ? 8 : 0) |
      (m.handshakeSuccesses ? 16 : 0) |
      (m.handshakeFailures ? 32 : 0) |
      (m.handshakes ? 64 : 0) |
      (m.udxThroughputBps ? 128 : 0) |
      (m.observedAt ? 256 : 0)

    c.string.preencode(state, m.peerId)
    c.uint.preencode(state, flags)

    if (m.latencyMs) c.uint.preencode(state, m.latencyMs)
    if (m.handshakeDurationMs) c.uint.preencode(state, m.handshakeDurationMs)
    if (m.socketStability) c.uint.preencode(state, m.socketStability)
    if (m.handshakeSuccesses) c.uint.preencode(state, m.handshakeSuccesses)
    if (m.handshakeFailures) c.uint.preencode(state, m.handshakeFailures)
    if (m.handshakes) c.uint.preencode(state, m.handshakes)
    if (m.udxThroughputBps) c.uint.preencode(state, m.udxThroughputBps)
    if (m.observedAt) c.uint.preencode(state, m.observedAt)
  },
  encode(state, m) {
    const flags =
      (m.latencyMs ? 1 : 0) |
      (m.handshakeDurationMs ? 2 : 0) |
      (m.socketStability ? 4 : 0) |
      (m.socketStabilityObserved ? 8 : 0) |
      (m.handshakeSuccesses ? 16 : 0) |
      (m.handshakeFailures ? 32 : 0) |
      (m.handshakes ? 64 : 0) |
      (m.udxThroughputBps ? 128 : 0) |
      (m.observedAt ? 256 : 0)

    c.string.encode(state, m.peerId)
    c.uint.encode(state, flags)

    if (m.latencyMs) c.uint.encode(state, m.latencyMs)
    if (m.handshakeDurationMs) c.uint.encode(state, m.handshakeDurationMs)
    if (m.socketStability) c.uint.encode(state, m.socketStability)
    if (m.handshakeSuccesses) c.uint.encode(state, m.handshakeSuccesses)
    if (m.handshakeFailures) c.uint.encode(state, m.handshakeFailures)
    if (m.handshakes) c.uint.encode(state, m.handshakes)
    if (m.udxThroughputBps) c.uint.encode(state, m.udxThroughputBps)
    if (m.observedAt) c.uint.encode(state, m.observedAt)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      peerId: r0,
      latencyMs: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      handshakeDurationMs: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      socketStability: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      socketStabilityObserved: (flags & 8) !== 0,
      handshakeSuccesses: (flags & 16) !== 0 ? c.uint.decode(state) : 0,
      handshakeFailures: (flags & 32) !== 0 ? c.uint.decode(state) : 0,
      handshakes: (flags & 64) !== 0 ? c.uint.decode(state) : 0,
      udxThroughputBps: (flags & 128) !== 0 ? c.uint.decode(state) : 0,
      observedAt: (flags & 256) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/resource-budget-state
const encoding3 = {
  preencode(state, m) {
    const flags =
      (m.memoryPressure ? 1 : 0) |
      (m.cpuPressure ? 2 : 0) |
      (m.maxFanout ? 4 : 0) |
      (m.maxRequestsPerWindow ? 8 : 0) |
      (m.maxFeedEntries ? 16 : 0) |
      (m.maxConcurrentSync ? 32 : 0) |
      (m.maxConcurrentProofs ? 64 : 0) |
      (m.maxConcurrentFetches ? 128 : 0)

    c.string.preencode(state, m.role)
    c.uint.preencode(state, flags)

    if (m.memoryPressure) c.uint.preencode(state, m.memoryPressure)
    if (m.cpuPressure) c.uint.preencode(state, m.cpuPressure)
    if (m.maxFanout) c.uint.preencode(state, m.maxFanout)
    if (m.maxRequestsPerWindow) c.uint.preencode(state, m.maxRequestsPerWindow)
    if (m.maxFeedEntries) c.uint.preencode(state, m.maxFeedEntries)
    if (m.maxConcurrentSync) c.uint.preencode(state, m.maxConcurrentSync)
    if (m.maxConcurrentProofs) c.uint.preencode(state, m.maxConcurrentProofs)
    if (m.maxConcurrentFetches) c.uint.preencode(state, m.maxConcurrentFetches)
  },
  encode(state, m) {
    const flags =
      (m.memoryPressure ? 1 : 0) |
      (m.cpuPressure ? 2 : 0) |
      (m.maxFanout ? 4 : 0) |
      (m.maxRequestsPerWindow ? 8 : 0) |
      (m.maxFeedEntries ? 16 : 0) |
      (m.maxConcurrentSync ? 32 : 0) |
      (m.maxConcurrentProofs ? 64 : 0) |
      (m.maxConcurrentFetches ? 128 : 0)

    c.string.encode(state, m.role)
    c.uint.encode(state, flags)

    if (m.memoryPressure) c.uint.encode(state, m.memoryPressure)
    if (m.cpuPressure) c.uint.encode(state, m.cpuPressure)
    if (m.maxFanout) c.uint.encode(state, m.maxFanout)
    if (m.maxRequestsPerWindow) c.uint.encode(state, m.maxRequestsPerWindow)
    if (m.maxFeedEntries) c.uint.encode(state, m.maxFeedEntries)
    if (m.maxConcurrentSync) c.uint.encode(state, m.maxConcurrentSync)
    if (m.maxConcurrentProofs) c.uint.encode(state, m.maxConcurrentProofs)
    if (m.maxConcurrentFetches) c.uint.encode(state, m.maxConcurrentFetches)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      role: r0,
      memoryPressure: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      cpuPressure: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      maxFanout: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      maxRequestsPerWindow: (flags & 8) !== 0 ? c.uint.decode(state) : 0,
      maxFeedEntries: (flags & 16) !== 0 ? c.uint.decode(state) : 0,
      maxConcurrentSync: (flags & 32) !== 0 ? c.uint.decode(state) : 0,
      maxConcurrentProofs: (flags & 64) !== 0 ? c.uint.decode(state) : 0,
      maxConcurrentFetches: (flags & 128) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/resource-allocation-state
const encoding4 = {
  preencode(state, m) {
    const flags =
      (m.feedIndexers ? 1 : 0) |
      (m.autobaseLinearizationBuffers ? 2 : 0) |
      (m.activeSwarmConnections ? 4 : 0) |
      (m.maxConcurrentSync ? 8 : 0) |
      (m.maxConcurrentProofs ? 16 : 0) |
      (m.maxConcurrentFetches ? 32 : 0) |
      (m.memoryPressure ? 64 : 0) |
      (m.cpuPressure ? 128 : 0)

    c.uint.preencode(state, flags)

    if (m.feedIndexers) c.uint.preencode(state, m.feedIndexers)
    if (m.autobaseLinearizationBuffers) c.uint.preencode(state, m.autobaseLinearizationBuffers)
    if (m.activeSwarmConnections) c.uint.preencode(state, m.activeSwarmConnections)
    if (m.maxConcurrentSync) c.uint.preencode(state, m.maxConcurrentSync)
    if (m.maxConcurrentProofs) c.uint.preencode(state, m.maxConcurrentProofs)
    if (m.maxConcurrentFetches) c.uint.preencode(state, m.maxConcurrentFetches)
    if (m.memoryPressure) c.uint.preencode(state, m.memoryPressure)
    if (m.cpuPressure) c.uint.preencode(state, m.cpuPressure)
  },
  encode(state, m) {
    const flags =
      (m.feedIndexers ? 1 : 0) |
      (m.autobaseLinearizationBuffers ? 2 : 0) |
      (m.activeSwarmConnections ? 4 : 0) |
      (m.maxConcurrentSync ? 8 : 0) |
      (m.maxConcurrentProofs ? 16 : 0) |
      (m.maxConcurrentFetches ? 32 : 0) |
      (m.memoryPressure ? 64 : 0) |
      (m.cpuPressure ? 128 : 0)

    c.uint.encode(state, flags)

    if (m.feedIndexers) c.uint.encode(state, m.feedIndexers)
    if (m.autobaseLinearizationBuffers) c.uint.encode(state, m.autobaseLinearizationBuffers)
    if (m.activeSwarmConnections) c.uint.encode(state, m.activeSwarmConnections)
    if (m.maxConcurrentSync) c.uint.encode(state, m.maxConcurrentSync)
    if (m.maxConcurrentProofs) c.uint.encode(state, m.maxConcurrentProofs)
    if (m.maxConcurrentFetches) c.uint.encode(state, m.maxConcurrentFetches)
    if (m.memoryPressure) c.uint.encode(state, m.memoryPressure)
    if (m.cpuPressure) c.uint.encode(state, m.cpuPressure)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      feedIndexers: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      autobaseLinearizationBuffers: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      activeSwarmConnections: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      maxConcurrentSync: (flags & 8) !== 0 ? c.uint.decode(state) : 0,
      maxConcurrentProofs: (flags & 16) !== 0 ? c.uint.decode(state) : 0,
      maxConcurrentFetches: (flags & 32) !== 0 ? c.uint.decode(state) : 0,
      memoryPressure: (flags & 64) !== 0 ? c.uint.decode(state) : 0,
      cpuPressure: (flags & 128) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/identity
const encoding5 = {
  preencode(state, m) {
    c.string.preencode(state, m.publicKey)
    state.end++ // max flag is 32 so always one byte

    if (m.driveKey) c.string.preencode(state, m.driveKey)
    if (m.name) c.string.preencode(state, m.name)
    if (m.avatar) c.string.preencode(state, m.avatar)
    if (m.seedPhrase) c.string.preencode(state, m.seedPhrase)
    if (m.createdAt) c.uint.preencode(state, m.createdAt)
  },
  encode(state, m) {
    const flags =
      (m.driveKey ? 1 : 0) |
      (m.name ? 2 : 0) |
      (m.avatar ? 4 : 0) |
      (m.seedPhrase ? 8 : 0) |
      (m.createdAt ? 16 : 0) |
      (m.isActive ? 32 : 0)

    c.string.encode(state, m.publicKey)
    c.uint.encode(state, flags)

    if (m.driveKey) c.string.encode(state, m.driveKey)
    if (m.name) c.string.encode(state, m.name)
    if (m.avatar) c.string.encode(state, m.avatar)
    if (m.seedPhrase) c.string.encode(state, m.seedPhrase)
    if (m.createdAt) c.uint.encode(state, m.createdAt)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      publicKey: r0,
      driveKey: (flags & 1) !== 0 ? c.string.decode(state) : null,
      name: (flags & 2) !== 0 ? c.string.decode(state) : null,
      avatar: (flags & 4) !== 0 ? c.string.decode(state) : null,
      seedPhrase: (flags & 8) !== 0 ? c.string.decode(state) : null,
      createdAt: (flags & 16) !== 0 ? c.uint.decode(state) : 0,
      isActive: (flags & 32) !== 0
    }
  }
}

// @peartube/create-identity-request
const encoding6 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte

    if (m.name) c.string.preencode(state, m.name)
    if (m.avatar) c.string.preencode(state, m.avatar)
  },
  encode(state, m) {
    const flags = (m.name ? 1 : 0) | (m.avatar ? 2 : 0)

    c.uint.encode(state, flags)

    if (m.name) c.string.encode(state, m.name)
    if (m.avatar) c.string.encode(state, m.avatar)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      name: (flags & 1) !== 0 ? c.string.decode(state) : null,
      avatar: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/create-identity-response.identity
const encoding7_0 = c.frame(encoding5)

// @peartube/create-identity-response
const encoding7 = {
  preencode(state, m) {
    encoding7_0.preencode(state, m.identity)
  },
  encode(state, m) {
    encoding7_0.encode(state, m.identity)
  },
  decode(state) {
    const r0 = encoding7_0.decode(state)

    return {
      identity: r0
    }
  }
}

// @peartube/get-identity-request
const encoding8 = encoding0

// @peartube/get-identity-response.identity
const encoding9_0 = encoding7_0

// @peartube/get-identity-response
const encoding9 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte

    if (m.identity) encoding9_0.preencode(state, m.identity)
  },
  encode(state, m) {
    const flags = m.identity ? 1 : 0

    c.uint.encode(state, flags)

    if (m.identity) encoding9_0.encode(state, m.identity)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      identity: (flags & 1) !== 0 ? encoding9_0.decode(state) : null
    }
  }
}

// @peartube/get-identities-request
const encoding10 = encoding0

// @peartube/get-identities-response.identities
const encoding11_0 = c.array(c.frame(encoding5))

// @peartube/get-identities-response
const encoding11 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte

    if (m.identities) encoding11_0.preencode(state, m.identities)
  },
  encode(state, m) {
    const flags = m.identities ? 1 : 0

    c.uint.encode(state, flags)

    if (m.identities) encoding11_0.encode(state, m.identities)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      identities: (flags & 1) !== 0 ? encoding11_0.decode(state) : null
    }
  }
}

// @peartube/set-active-identity-request
const encoding12 = {
  preencode(state, m) {
    c.string.preencode(state, m.publicKey)
  },
  encode(state, m) {
    c.string.encode(state, m.publicKey)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      publicKey: r0
    }
  }
}

// @peartube/set-active-identity-response
const encoding13 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte
  },
  encode(state, m) {
    const flags = m.success ? 1 : 0

    c.uint.encode(state, flags)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0
    }
  }
}

// @peartube/recover-identity-request
const encoding14 = {
  preencode(state, m) {
    c.string.preencode(state, m.seedPhrase)
    state.end++ // max flag is 1 so always one byte

    if (m.name) c.string.preencode(state, m.name)
  },
  encode(state, m) {
    const flags = m.name ? 1 : 0

    c.string.encode(state, m.seedPhrase)
    c.uint.encode(state, flags)

    if (m.name) c.string.encode(state, m.name)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      seedPhrase: r0,
      name: (flags & 1) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/recover-identity-response.identity
const encoding15_0 = encoding7_0

// @peartube/recover-identity-response
const encoding15 = {
  preencode(state, m) {
    encoding15_0.preencode(state, m.identity)
  },
  encode(state, m) {
    encoding15_0.encode(state, m.identity)
  },
  decode(state) {
    const r0 = encoding15_0.decode(state)

    return {
      identity: r0
    }
  }
}

// @peartube/bootstrap-device-request
const encoding16 = {
  preencode(state, m) {
    c.string.preencode(state, m.mnemonic)
  },
  encode(state, m) {
    c.string.encode(state, m.mnemonic)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      mnemonic: r0
    }
  }
}

// @peartube/bootstrap-device-response
const encoding17 = {
  preencode(state, m) {
    c.buffer.preencode(state, m.proof)
    c.string.preencode(state, m.identityPublicKey)
  },
  encode(state, m) {
    c.buffer.encode(state, m.proof)
    c.string.encode(state, m.identityPublicKey)
  },
  decode(state) {
    const r0 = c.buffer.decode(state)
    const r1 = c.string.decode(state)

    return {
      proof: r0,
      identityPublicKey: r1
    }
  }
}

// @peartube/attest-device-request
const encoding18 = {
  preencode(state, m) {
    c.buffer.preencode(state, m.proof)
    c.buffer.preencode(state, m.devicePublicKey)
  },
  encode(state, m) {
    c.buffer.encode(state, m.proof)
    c.buffer.encode(state, m.devicePublicKey)
  },
  decode(state) {
    const r0 = c.buffer.decode(state)
    const r1 = c.buffer.decode(state)

    return {
      proof: r0,
      devicePublicKey: r1
    }
  }
}

// @peartube/attest-device-response
const encoding19 = {
  preencode(state, m) {
    c.buffer.preencode(state, m.proof)
  },
  encode(state, m) {
    c.buffer.encode(state, m.proof)
  },
  decode(state) {
    const r0 = c.buffer.decode(state)

    return {
      proof: r0
    }
  }
}

// @peartube/verify-attestation-request
const encoding20 = encoding19

// @peartube/verify-attestation-response
const encoding21 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte
    c.string.preencode(state, m.identityPublicKey)
    c.string.preencode(state, m.devicePublicKey)
  },
  encode(state, m) {
    const flags = m.valid ? 1 : 0

    c.uint.encode(state, flags)
    c.string.encode(state, m.identityPublicKey)
    c.string.encode(state, m.devicePublicKey)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      valid: (flags & 1) !== 0,
      identityPublicKey: c.string.decode(state),
      devicePublicKey: c.string.decode(state)
    }
  }
}

// @peartube/provision-publisher-catalog-request
const encoding22 = {
  preencode(state, m) {
    c.string.preencode(state, m.publisherId)
    c.buffer.preencode(state, m.genesisRootKey)
  },
  encode(state, m) {
    c.string.encode(state, m.publisherId)
    c.buffer.encode(state, m.genesisRootKey)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.buffer.decode(state)

    return {
      publisherId: r0,
      genesisRootKey: r1
    }
  }
}

// @peartube/provision-publisher-catalog-response
const encoding23 = {
  preencode(state, m) {
    state.end++ // max flag is 32 so always one byte
    c.string.preencode(state, m.publisherId)
    c.buffer.preencode(state, m.catalogBootstrapKey)

    if (m.errorCode) c.string.preencode(state, m.errorCode)
    if (m.error) c.string.preencode(state, m.error)
    c.buffer.preencode(state, m.localWriterKey)
    c.buffer.preencode(state, m.localSignerKey)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.errorCode ? 2 : 0) |
      (m.error ? 4 : 0) |
      (m.writable ? 8 : 0) |
      (m.namespaceInitialized ? 16 : 0) |
      (m.admitted ? 32 : 0)

    c.uint.encode(state, flags)
    c.string.encode(state, m.publisherId)
    c.buffer.encode(state, m.catalogBootstrapKey)

    if (m.errorCode) c.string.encode(state, m.errorCode)
    if (m.error) c.string.encode(state, m.error)
    c.buffer.encode(state, m.localWriterKey)
    c.buffer.encode(state, m.localSignerKey)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      publisherId: c.string.decode(state),
      catalogBootstrapKey: c.buffer.decode(state),
      errorCode: (flags & 2) !== 0 ? c.string.decode(state) : null,
      error: (flags & 4) !== 0 ? c.string.decode(state) : null,
      localWriterKey: c.buffer.decode(state),
      localSignerKey: c.buffer.decode(state),
      writable: (flags & 8) !== 0,
      namespaceInitialized: (flags & 16) !== 0,
      admitted: (flags & 32) !== 0
    }
  }
}

// @peartube/prepare-publisher-root-operation-request
const encoding24 = {
  preencode(state, m) {
    c.string.preencode(state, m.publisherId)
    c.string.preencode(state, m.recordType)
    c.buffer.preencode(state, m.body)
    state.end++ // max flag is 8 so always one byte

    if (m.displaySummaryJson) c.string.preencode(state, m.displaySummaryJson)
    if (m.issuedAt) c.uint.preencode(state, m.issuedAt)
    if (m.expiresAt) c.uint.preencode(state, m.expiresAt)
    if (m.expiresInMs) c.uint.preencode(state, m.expiresInMs)
    c.string.preencode(state, m.intentId)
    c.buffer.preencode(state, m.signerPublicKey)
    c.uint.preencode(state, m.intentExpiresAt)
  },
  encode(state, m) {
    const flags =
      (m.displaySummaryJson ? 1 : 0) |
      (m.issuedAt ? 2 : 0) |
      (m.expiresAt ? 4 : 0) |
      (m.expiresInMs ? 8 : 0)

    c.string.encode(state, m.publisherId)
    c.string.encode(state, m.recordType)
    c.buffer.encode(state, m.body)
    c.uint.encode(state, flags)

    if (m.displaySummaryJson) c.string.encode(state, m.displaySummaryJson)
    if (m.issuedAt) c.uint.encode(state, m.issuedAt)
    if (m.expiresAt) c.uint.encode(state, m.expiresAt)
    if (m.expiresInMs) c.uint.encode(state, m.expiresInMs)
    c.string.encode(state, m.intentId)
    c.buffer.encode(state, m.signerPublicKey)
    c.uint.encode(state, m.intentExpiresAt)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.buffer.decode(state)
    const flags = c.uint.decode(state)

    return {
      publisherId: r0,
      recordType: r1,
      body: r2,
      displaySummaryJson: (flags & 1) !== 0 ? c.string.decode(state) : null,
      issuedAt: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      expiresAt: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      expiresInMs: (flags & 8) !== 0 ? c.uint.decode(state) : 0,
      intentId: c.string.decode(state),
      signerPublicKey: c.buffer.decode(state),
      intentExpiresAt: c.uint.decode(state)
    }
  }
}

// @peartube/prepare-publisher-root-operation-response
const encoding25 = {
  preencode(state, m) {
    state.end++ // max flag is 16 so always one byte

    if (m.publisherId) c.string.preencode(state, m.publisherId)
    if (m.recordType) c.string.preencode(state, m.recordType)
    c.buffer.preencode(state, m.unsignedBytes)
    c.buffer.preencode(state, m.candidateRecordId)
    c.uint.preencode(state, m.bodyLength)
    c.uint.preencode(state, m.issuedAt)
    c.uint.preencode(state, m.expiresAt)
    if (m.displaySummaryJson) c.string.preencode(state, m.displaySummaryJson)
    if (m.error) c.string.preencode(state, m.error)
    c.string.preencode(state, m.intentId)
    c.buffer.preencode(state, m.signerPublicKey)
    c.uint.preencode(state, m.intentExpiresAt)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.publisherId ? 2 : 0) |
      (m.recordType ? 4 : 0) |
      (m.displaySummaryJson ? 8 : 0) |
      (m.error ? 16 : 0)

    c.uint.encode(state, flags)

    if (m.publisherId) c.string.encode(state, m.publisherId)
    if (m.recordType) c.string.encode(state, m.recordType)
    c.buffer.encode(state, m.unsignedBytes)
    c.buffer.encode(state, m.candidateRecordId)
    c.uint.encode(state, m.bodyLength)
    c.uint.encode(state, m.issuedAt)
    c.uint.encode(state, m.expiresAt)
    if (m.displaySummaryJson) c.string.encode(state, m.displaySummaryJson)
    if (m.error) c.string.encode(state, m.error)
    c.string.encode(state, m.intentId)
    c.buffer.encode(state, m.signerPublicKey)
    c.uint.encode(state, m.intentExpiresAt)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      publisherId: (flags & 2) !== 0 ? c.string.decode(state) : null,
      recordType: (flags & 4) !== 0 ? c.string.decode(state) : null,
      unsignedBytes: c.buffer.decode(state),
      candidateRecordId: c.buffer.decode(state),
      bodyLength: c.uint.decode(state),
      issuedAt: c.uint.decode(state),
      expiresAt: c.uint.decode(state),
      displaySummaryJson: (flags & 8) !== 0 ? c.string.decode(state) : null,
      error: (flags & 16) !== 0 ? c.string.decode(state) : null,
      intentId: c.string.decode(state),
      signerPublicKey: c.buffer.decode(state),
      intentExpiresAt: c.uint.decode(state)
    }
  }
}

// @peartube/submit-publisher-root-operation-request.allowedSigners
const encoding26_7 = c.array(c.buffer)

// @peartube/submit-publisher-root-operation-request
const encoding26 = {
  preencode(state, m) {
    c.string.preencode(state, m.publisherId)
    c.string.preencode(state, m.recordType)
    c.buffer.preencode(state, m.unsignedBytes)
    c.buffer.preencode(state, m.candidateRecordId)
    state.end++ // max flag is 2 so always one byte

    if (m.displaySummaryJson) c.string.preencode(state, m.displaySummaryJson)
    c.buffer.preencode(state, m.signer)
    c.buffer.preencode(state, m.signature)
    if (m.allowedSigners) encoding26_7.preencode(state, m.allowedSigners)
    c.string.preencode(state, m.intentId)
    c.buffer.preencode(state, m.signerPublicKey)
  },
  encode(state, m) {
    const flags = (m.displaySummaryJson ? 1 : 0) | (m.allowedSigners ? 2 : 0)

    c.string.encode(state, m.publisherId)
    c.string.encode(state, m.recordType)
    c.buffer.encode(state, m.unsignedBytes)
    c.buffer.encode(state, m.candidateRecordId)
    c.uint.encode(state, flags)

    if (m.displaySummaryJson) c.string.encode(state, m.displaySummaryJson)
    c.buffer.encode(state, m.signer)
    c.buffer.encode(state, m.signature)
    if (m.allowedSigners) encoding26_7.encode(state, m.allowedSigners)
    c.string.encode(state, m.intentId)
    c.buffer.encode(state, m.signerPublicKey)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.buffer.decode(state)
    const r3 = c.buffer.decode(state)
    const flags = c.uint.decode(state)

    return {
      publisherId: r0,
      recordType: r1,
      unsignedBytes: r2,
      candidateRecordId: r3,
      displaySummaryJson: (flags & 1) !== 0 ? c.string.decode(state) : null,
      signer: c.buffer.decode(state),
      signature: c.buffer.decode(state),
      allowedSigners: (flags & 2) !== 0 ? encoding26_7.decode(state) : null,
      intentId: c.string.decode(state),
      signerPublicKey: c.buffer.decode(state)
    }
  }
}

// @peartube/submit-publisher-root-operation-response
const encoding27 = {
  preencode(state, m) {
    state.end++ // max flag is 32 so always one byte

    if (m.reason) c.string.preencode(state, m.reason)
    if (m.publisherId) c.string.preencode(state, m.publisherId)
    if (m.recordType) c.string.preencode(state, m.recordType)
    c.buffer.preencode(state, m.recordId)
    c.buffer.preencode(state, m.signer)
    c.buffer.preencode(state, m.signature)
    c.string.preencode(state, m.intentId)
    c.buffer.preencode(state, m.signerPublicKey)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.valid ? 2 : 0) |
      (m.reason ? 4 : 0) |
      (m.publisherId ? 8 : 0) |
      (m.recordType ? 16 : 0) |
      (m.complete ? 32 : 0)

    c.uint.encode(state, flags)

    if (m.reason) c.string.encode(state, m.reason)
    if (m.publisherId) c.string.encode(state, m.publisherId)
    if (m.recordType) c.string.encode(state, m.recordType)
    c.buffer.encode(state, m.recordId)
    c.buffer.encode(state, m.signer)
    c.buffer.encode(state, m.signature)
    c.string.encode(state, m.intentId)
    c.buffer.encode(state, m.signerPublicKey)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      valid: (flags & 2) !== 0,
      reason: (flags & 4) !== 0 ? c.string.decode(state) : null,
      publisherId: (flags & 8) !== 0 ? c.string.decode(state) : null,
      recordType: (flags & 16) !== 0 ? c.string.decode(state) : null,
      recordId: c.buffer.decode(state),
      signer: c.buffer.decode(state),
      signature: c.buffer.decode(state),
      intentId: c.string.decode(state),
      signerPublicKey: c.buffer.decode(state),
      complete: (flags & 32) !== 0
    }
  }
}

// @peartube/channel
const encoding28 = {
  preencode(state, m) {
    c.string.preencode(state, m.publicKey)
    state.end++ // max flag is 16 so always one byte

    if (m.name) c.string.preencode(state, m.name)
    if (m.description) c.string.preencode(state, m.description)
    if (m.avatar) c.string.preencode(state, m.avatar)
    if (m.videoCount) c.uint.preencode(state, m.videoCount)
    if (m.subscriberCount) c.uint.preencode(state, m.subscriberCount)
  },
  encode(state, m) {
    const flags =
      (m.name ? 1 : 0) |
      (m.description ? 2 : 0) |
      (m.avatar ? 4 : 0) |
      (m.videoCount ? 8 : 0) |
      (m.subscriberCount ? 16 : 0)

    c.string.encode(state, m.publicKey)
    c.uint.encode(state, flags)

    if (m.name) c.string.encode(state, m.name)
    if (m.description) c.string.encode(state, m.description)
    if (m.avatar) c.string.encode(state, m.avatar)
    if (m.videoCount) c.uint.encode(state, m.videoCount)
    if (m.subscriberCount) c.uint.encode(state, m.subscriberCount)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      publicKey: r0,
      name: (flags & 1) !== 0 ? c.string.decode(state) : null,
      description: (flags & 2) !== 0 ? c.string.decode(state) : null,
      avatar: (flags & 4) !== 0 ? c.string.decode(state) : null,
      videoCount: (flags & 8) !== 0 ? c.uint.decode(state) : 0,
      subscriberCount: (flags & 16) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/get-channel-request
const encoding29 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte

    if (m.publicKey) c.string.preencode(state, m.publicKey)
  },
  encode(state, m) {
    const flags = m.publicKey ? 1 : 0

    c.uint.encode(state, flags)

    if (m.publicKey) c.string.encode(state, m.publicKey)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      publicKey: (flags & 1) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-channel-response.channel
const encoding30_0 = c.frame(encoding28)

// @peartube/get-channel-response
const encoding30 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte

    if (m.channel) encoding30_0.preencode(state, m.channel)
  },
  encode(state, m) {
    const flags = m.channel ? 1 : 0

    c.uint.encode(state, flags)

    if (m.channel) encoding30_0.encode(state, m.channel)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      channel: (flags & 1) !== 0 ? encoding30_0.decode(state) : null
    }
  }
}

// @peartube/update-channel-request
const encoding31 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.name) c.string.preencode(state, m.name)
    if (m.description) c.string.preencode(state, m.description)
    if (m.avatar) c.string.preencode(state, m.avatar)
  },
  encode(state, m) {
    const flags = (m.name ? 1 : 0) | (m.description ? 2 : 0) | (m.avatar ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.name) c.string.encode(state, m.name)
    if (m.description) c.string.encode(state, m.description)
    if (m.avatar) c.string.encode(state, m.avatar)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      name: (flags & 1) !== 0 ? c.string.decode(state) : null,
      description: (flags & 2) !== 0 ? c.string.decode(state) : null,
      avatar: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/update-channel-response.channel
const encoding32_0 = encoding30_0

// @peartube/update-channel-response
const encoding32 = {
  preencode(state, m) {
    encoding32_0.preencode(state, m.channel)
  },
  encode(state, m) {
    encoding32_0.encode(state, m.channel)
  },
  decode(state) {
    const r0 = encoding32_0.decode(state)

    return {
      channel: r0
    }
  }
}

// @peartube/content-artwork
const encoding33 = {
  preencode(state, m) {
    c.string.preencode(state, m.role)
    state.end++ // max flag is 8 so always one byte

    if (m.blobId) c.string.preencode(state, m.blobId)
    if (m.blobsCoreKey) c.string.preencode(state, m.blobsCoreKey)
    if (m.mimeType) c.string.preencode(state, m.mimeType)
    if (m.remoteUrl) c.string.preencode(state, m.remoteUrl)
  },
  encode(state, m) {
    const flags =
      (m.blobId ? 1 : 0) | (m.blobsCoreKey ? 2 : 0) | (m.mimeType ? 4 : 0) | (m.remoteUrl ? 8 : 0)

    c.string.encode(state, m.role)
    c.uint.encode(state, flags)

    if (m.blobId) c.string.encode(state, m.blobId)
    if (m.blobsCoreKey) c.string.encode(state, m.blobsCoreKey)
    if (m.mimeType) c.string.encode(state, m.mimeType)
    if (m.remoteUrl) c.string.encode(state, m.remoteUrl)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      role: r0,
      blobId: (flags & 1) !== 0 ? c.string.decode(state) : null,
      blobsCoreKey: (flags & 2) !== 0 ? c.string.decode(state) : null,
      mimeType: (flags & 4) !== 0 ? c.string.decode(state) : null,
      remoteUrl: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/channel-source
const encoding34 = {
  preencode(state, m) {
    c.string.preencode(state, m.provider)
    c.string.preencode(state, m.identityKey)
    state.end++ // max flag is 8 so always one byte

    if (m.sourceId) c.string.preencode(state, m.sourceId)
    if (m.identityUrl) c.string.preencode(state, m.identityUrl)
    if (m.handle) c.string.preencode(state, m.handle)
    if (m.displayName) c.string.preencode(state, m.displayName)
  },
  encode(state, m) {
    const flags =
      (m.sourceId ? 1 : 0) | (m.identityUrl ? 2 : 0) | (m.handle ? 4 : 0) | (m.displayName ? 8 : 0)

    c.string.encode(state, m.provider)
    c.string.encode(state, m.identityKey)
    c.uint.encode(state, flags)

    if (m.sourceId) c.string.encode(state, m.sourceId)
    if (m.identityUrl) c.string.encode(state, m.identityUrl)
    if (m.handle) c.string.encode(state, m.handle)
    if (m.displayName) c.string.encode(state, m.displayName)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      provider: r0,
      identityKey: r1,
      sourceId: (flags & 1) !== 0 ? c.string.decode(state) : null,
      identityUrl: (flags & 2) !== 0 ? c.string.decode(state) : null,
      handle: (flags & 4) !== 0 ? c.string.decode(state) : null,
      displayName: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/channel-catalog-profile.sources
const encoding35_11 = c.array(c.frame(encoding34))
// @peartube/channel-catalog-profile.artwork
const encoding35_12 = c.array(c.frame(encoding33))

// @peartube/channel-catalog-profile
const encoding35 = {
  preencode(state, m) {
    const flags =
      (m.description ? 1 : 0) |
      (m.profileKind ? 2 : 0) |
      (m.mediaProvider ? 4 : 0) |
      (m.mediaId ? 8 : 0) |
      (m.originalLanguage ? 16 : 0) |
      (m.releaseDate ? 32 : 0) |
      (m.releaseYear ? 64 : 0) |
      (m.createdAt ? 128 : 0) |
      (m.updatedAt ? 256 : 0) |
      (m.sources ? 512 : 0) |
      (m.artwork ? 1024 : 0)

    c.string.preencode(state, m.channelKey)
    c.string.preencode(state, m.name)
    c.uint.preencode(state, flags)

    if (m.description) c.string.preencode(state, m.description)
    if (m.profileKind) c.string.preencode(state, m.profileKind)
    if (m.mediaProvider) c.string.preencode(state, m.mediaProvider)
    if (m.mediaId) c.string.preencode(state, m.mediaId)
    if (m.originalLanguage) c.string.preencode(state, m.originalLanguage)
    if (m.releaseDate) c.uint.preencode(state, m.releaseDate)
    if (m.releaseYear) c.uint.preencode(state, m.releaseYear)
    if (m.createdAt) c.uint.preencode(state, m.createdAt)
    if (m.updatedAt) c.uint.preencode(state, m.updatedAt)
    if (m.sources) encoding35_11.preencode(state, m.sources)
    if (m.artwork) encoding35_12.preencode(state, m.artwork)
  },
  encode(state, m) {
    const flags =
      (m.description ? 1 : 0) |
      (m.profileKind ? 2 : 0) |
      (m.mediaProvider ? 4 : 0) |
      (m.mediaId ? 8 : 0) |
      (m.originalLanguage ? 16 : 0) |
      (m.releaseDate ? 32 : 0) |
      (m.releaseYear ? 64 : 0) |
      (m.createdAt ? 128 : 0) |
      (m.updatedAt ? 256 : 0) |
      (m.sources ? 512 : 0) |
      (m.artwork ? 1024 : 0)

    c.string.encode(state, m.channelKey)
    c.string.encode(state, m.name)
    c.uint.encode(state, flags)

    if (m.description) c.string.encode(state, m.description)
    if (m.profileKind) c.string.encode(state, m.profileKind)
    if (m.mediaProvider) c.string.encode(state, m.mediaProvider)
    if (m.mediaId) c.string.encode(state, m.mediaId)
    if (m.originalLanguage) c.string.encode(state, m.originalLanguage)
    if (m.releaseDate) c.uint.encode(state, m.releaseDate)
    if (m.releaseYear) c.uint.encode(state, m.releaseYear)
    if (m.createdAt) c.uint.encode(state, m.createdAt)
    if (m.updatedAt) c.uint.encode(state, m.updatedAt)
    if (m.sources) encoding35_11.encode(state, m.sources)
    if (m.artwork) encoding35_12.encode(state, m.artwork)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      channelKey: r0,
      name: r1,
      description: (flags & 1) !== 0 ? c.string.decode(state) : null,
      profileKind: (flags & 2) !== 0 ? c.string.decode(state) : null,
      mediaProvider: (flags & 4) !== 0 ? c.string.decode(state) : null,
      mediaId: (flags & 8) !== 0 ? c.string.decode(state) : null,
      originalLanguage: (flags & 16) !== 0 ? c.string.decode(state) : null,
      releaseDate: (flags & 32) !== 0 ? c.uint.decode(state) : 0,
      releaseYear: (flags & 64) !== 0 ? c.uint.decode(state) : 0,
      createdAt: (flags & 128) !== 0 ? c.uint.decode(state) : 0,
      updatedAt: (flags & 256) !== 0 ? c.uint.decode(state) : 0,
      sources: (flags & 512) !== 0 ? encoding35_11.decode(state) : null,
      artwork: (flags & 1024) !== 0 ? encoding35_12.decode(state) : null
    }
  }
}

// @peartube/channel-catalog-group-summary
const encoding36 = {
  preencode(state, m) {
    c.string.preencode(state, m.id)
    c.string.preencode(state, m.kind)
    c.string.preencode(state, m.title)
    c.uint.preencode(state, m.itemCount)
    state.end++ // max flag is 1 so always one byte

    if (m.seasonNumber) c.uint.preencode(state, m.seasonNumber)
  },
  encode(state, m) {
    const flags = m.seasonNumber ? 1 : 0

    c.string.encode(state, m.id)
    c.string.encode(state, m.kind)
    c.string.encode(state, m.title)
    c.uint.encode(state, m.itemCount)
    c.uint.encode(state, flags)

    if (m.seasonNumber) c.uint.encode(state, m.seasonNumber)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const r3 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      id: r0,
      kind: r1,
      title: r2,
      itemCount: r3,
      seasonNumber: (flags & 1) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/channel-catalog-item
const encoding37 = {
  preencode(state, m) {
    const flags =
      (m.description ? 1 : 0) |
      (m.contentKind ? 2 : 0) |
      (m.publicBeeKey ? 4 : 0) |
      (m.sourceProvider ? 8 : 0) |
      (m.sourceVideoId ? 16 : 0) |
      (m.identityUrl ? 32 : 0) |
      (m.sourceCreatorId ? 64 : 0) |
      (m.sourceCreatorUrl ? 128 : 0) |
      (m.sourcePublishedAt ? 256 : 0) |
      (m.mediaProvider ? 512 : 0) |
      (m.mediaId ? 1024 : 0) |
      (m.seasonNumber ? 2048 : 0) |
      (m.episodeNumber ? 4096 : 0) |
      (m.originalAirDate ? 8192 : 0) |
      (m.duration ? 16384 : 0) |
      (m.blobId ? 32768 : 0) |
      (m.blobsCoreKey ? 65536 : 0) |
      (m.mimeType ? 131072 : 0) |
      (m.thumbnailUrl ? 262144 : 0) |
      (m.thumbnailBlobId ? 524288 : 0) |
      (m.thumbnailBlobsCoreKey ? 1048576 : 0) |
      (m.thumbnailMimeType ? 2097152 : 0) |
      (m.provenanceVersion ? 4194304 : 0) |
      (m.contentFingerprint ? 8388608 : 0) |
      (m.publicationState ? 16777216 : 0)

    c.string.preencode(state, m.id)
    c.string.preencode(state, m.title)
    c.uint.preencode(state, flags)

    if (m.description) c.string.preencode(state, m.description)
    if (m.contentKind) c.string.preencode(state, m.contentKind)
    c.string.preencode(state, m.channelKey)
    if (m.publicBeeKey) c.string.preencode(state, m.publicBeeKey)
    if (m.sourceProvider) c.string.preencode(state, m.sourceProvider)
    if (m.sourceVideoId) c.string.preencode(state, m.sourceVideoId)
    if (m.identityUrl) c.string.preencode(state, m.identityUrl)
    if (m.sourceCreatorId) c.string.preencode(state, m.sourceCreatorId)
    if (m.sourceCreatorUrl) c.string.preencode(state, m.sourceCreatorUrl)
    if (m.sourcePublishedAt) c.uint.preencode(state, m.sourcePublishedAt)
    if (m.mediaProvider) c.string.preencode(state, m.mediaProvider)
    if (m.mediaId) c.string.preencode(state, m.mediaId)
    if (m.seasonNumber) c.uint.preencode(state, m.seasonNumber)
    if (m.episodeNumber) c.uint.preencode(state, m.episodeNumber)
    if (m.originalAirDate) c.uint.preencode(state, m.originalAirDate)
    if (m.duration) c.uint.preencode(state, m.duration)
    if (m.blobId) c.string.preencode(state, m.blobId)
    if (m.blobsCoreKey) c.string.preencode(state, m.blobsCoreKey)
    if (m.mimeType) c.string.preencode(state, m.mimeType)
    if (m.thumbnailUrl) c.string.preencode(state, m.thumbnailUrl)
    if (m.thumbnailBlobId) c.string.preencode(state, m.thumbnailBlobId)
    if (m.thumbnailBlobsCoreKey) c.string.preencode(state, m.thumbnailBlobsCoreKey)
    if (m.thumbnailMimeType) c.string.preencode(state, m.thumbnailMimeType)
    if (m.provenanceVersion) c.string.preencode(state, m.provenanceVersion)
    if (m.contentFingerprint) c.string.preencode(state, m.contentFingerprint)
    if (m.publicationState) c.string.preencode(state, m.publicationState)
  },
  encode(state, m) {
    const flags =
      (m.description ? 1 : 0) |
      (m.contentKind ? 2 : 0) |
      (m.publicBeeKey ? 4 : 0) |
      (m.sourceProvider ? 8 : 0) |
      (m.sourceVideoId ? 16 : 0) |
      (m.identityUrl ? 32 : 0) |
      (m.sourceCreatorId ? 64 : 0) |
      (m.sourceCreatorUrl ? 128 : 0) |
      (m.sourcePublishedAt ? 256 : 0) |
      (m.mediaProvider ? 512 : 0) |
      (m.mediaId ? 1024 : 0) |
      (m.seasonNumber ? 2048 : 0) |
      (m.episodeNumber ? 4096 : 0) |
      (m.originalAirDate ? 8192 : 0) |
      (m.duration ? 16384 : 0) |
      (m.blobId ? 32768 : 0) |
      (m.blobsCoreKey ? 65536 : 0) |
      (m.mimeType ? 131072 : 0) |
      (m.thumbnailUrl ? 262144 : 0) |
      (m.thumbnailBlobId ? 524288 : 0) |
      (m.thumbnailBlobsCoreKey ? 1048576 : 0) |
      (m.thumbnailMimeType ? 2097152 : 0) |
      (m.provenanceVersion ? 4194304 : 0) |
      (m.contentFingerprint ? 8388608 : 0) |
      (m.publicationState ? 16777216 : 0)

    c.string.encode(state, m.id)
    c.string.encode(state, m.title)
    c.uint.encode(state, flags)

    if (m.description) c.string.encode(state, m.description)
    if (m.contentKind) c.string.encode(state, m.contentKind)
    c.string.encode(state, m.channelKey)
    if (m.publicBeeKey) c.string.encode(state, m.publicBeeKey)
    if (m.sourceProvider) c.string.encode(state, m.sourceProvider)
    if (m.sourceVideoId) c.string.encode(state, m.sourceVideoId)
    if (m.identityUrl) c.string.encode(state, m.identityUrl)
    if (m.sourceCreatorId) c.string.encode(state, m.sourceCreatorId)
    if (m.sourceCreatorUrl) c.string.encode(state, m.sourceCreatorUrl)
    if (m.sourcePublishedAt) c.uint.encode(state, m.sourcePublishedAt)
    if (m.mediaProvider) c.string.encode(state, m.mediaProvider)
    if (m.mediaId) c.string.encode(state, m.mediaId)
    if (m.seasonNumber) c.uint.encode(state, m.seasonNumber)
    if (m.episodeNumber) c.uint.encode(state, m.episodeNumber)
    if (m.originalAirDate) c.uint.encode(state, m.originalAirDate)
    if (m.duration) c.uint.encode(state, m.duration)
    if (m.blobId) c.string.encode(state, m.blobId)
    if (m.blobsCoreKey) c.string.encode(state, m.blobsCoreKey)
    if (m.mimeType) c.string.encode(state, m.mimeType)
    if (m.thumbnailUrl) c.string.encode(state, m.thumbnailUrl)
    if (m.thumbnailBlobId) c.string.encode(state, m.thumbnailBlobId)
    if (m.thumbnailBlobsCoreKey) c.string.encode(state, m.thumbnailBlobsCoreKey)
    if (m.thumbnailMimeType) c.string.encode(state, m.thumbnailMimeType)
    if (m.provenanceVersion) c.string.encode(state, m.provenanceVersion)
    if (m.contentFingerprint) c.string.encode(state, m.contentFingerprint)
    if (m.publicationState) c.string.encode(state, m.publicationState)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      id: r0,
      title: r1,
      description: (flags & 1) !== 0 ? c.string.decode(state) : null,
      contentKind: (flags & 2) !== 0 ? c.string.decode(state) : null,
      channelKey: c.string.decode(state),
      publicBeeKey: (flags & 4) !== 0 ? c.string.decode(state) : null,
      sourceProvider: (flags & 8) !== 0 ? c.string.decode(state) : null,
      sourceVideoId: (flags & 16) !== 0 ? c.string.decode(state) : null,
      identityUrl: (flags & 32) !== 0 ? c.string.decode(state) : null,
      sourceCreatorId: (flags & 64) !== 0 ? c.string.decode(state) : null,
      sourceCreatorUrl: (flags & 128) !== 0 ? c.string.decode(state) : null,
      sourcePublishedAt: (flags & 256) !== 0 ? c.uint.decode(state) : 0,
      mediaProvider: (flags & 512) !== 0 ? c.string.decode(state) : null,
      mediaId: (flags & 1024) !== 0 ? c.string.decode(state) : null,
      seasonNumber: (flags & 2048) !== 0 ? c.uint.decode(state) : 0,
      episodeNumber: (flags & 4096) !== 0 ? c.uint.decode(state) : 0,
      originalAirDate: (flags & 8192) !== 0 ? c.uint.decode(state) : 0,
      duration: (flags & 16384) !== 0 ? c.uint.decode(state) : 0,
      blobId: (flags & 32768) !== 0 ? c.string.decode(state) : null,
      blobsCoreKey: (flags & 65536) !== 0 ? c.string.decode(state) : null,
      mimeType: (flags & 131072) !== 0 ? c.string.decode(state) : null,
      thumbnailUrl: (flags & 262144) !== 0 ? c.string.decode(state) : null,
      thumbnailBlobId: (flags & 524288) !== 0 ? c.string.decode(state) : null,
      thumbnailBlobsCoreKey: (flags & 1048576) !== 0 ? c.string.decode(state) : null,
      thumbnailMimeType: (flags & 2097152) !== 0 ? c.string.decode(state) : null,
      provenanceVersion: (flags & 4194304) !== 0 ? c.string.decode(state) : null,
      contentFingerprint: (flags & 8388608) !== 0 ? c.string.decode(state) : null,
      publicationState: (flags & 16777216) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-content-catalog-request
const encoding38 = {
  preencode(state, m) {
    c.string.preencode(state, m.channelKey)
    state.end++ // max flag is 1 so always one byte

    if (m.publicBeeKey) c.string.preencode(state, m.publicBeeKey)
  },
  encode(state, m) {
    const flags = m.publicBeeKey ? 1 : 0

    c.string.encode(state, m.channelKey)
    c.uint.encode(state, flags)

    if (m.publicBeeKey) c.string.encode(state, m.publicBeeKey)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      channelKey: r0,
      publicBeeKey: (flags & 1) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-content-catalog-response.profile
const encoding39_3 = c.frame(encoding35)
// @peartube/get-content-catalog-response.groups
const encoding39_4 = c.array(c.frame(encoding36))

// @peartube/get-content-catalog-response
const encoding39 = {
  preencode(state, m) {
    state.end++ // max flag is 16 so always one byte

    if (m.errorCode) c.string.preencode(state, m.errorCode)
    if (m.error) c.string.preencode(state, m.error)
    if (m.profile) encoding39_3.preencode(state, m.profile)
    if (m.groups) encoding39_4.preencode(state, m.groups)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.errorCode ? 2 : 0) |
      (m.error ? 4 : 0) |
      (m.profile ? 8 : 0) |
      (m.groups ? 16 : 0)

    c.uint.encode(state, flags)

    if (m.errorCode) c.string.encode(state, m.errorCode)
    if (m.error) c.string.encode(state, m.error)
    if (m.profile) encoding39_3.encode(state, m.profile)
    if (m.groups) encoding39_4.encode(state, m.groups)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      errorCode: (flags & 2) !== 0 ? c.string.decode(state) : null,
      error: (flags & 4) !== 0 ? c.string.decode(state) : null,
      profile: (flags & 8) !== 0 ? encoding39_3.decode(state) : null,
      groups: (flags & 16) !== 0 ? encoding39_4.decode(state) : null
    }
  }
}

// @peartube/get-content-items-request
const encoding40 = {
  preencode(state, m) {
    c.string.preencode(state, m.channelKey)
    state.end++ // max flag is 8 so always one byte

    if (m.publicBeeKey) c.string.preencode(state, m.publicBeeKey)
    c.string.preencode(state, m.groupId)
    if (m.cursor) c.string.preencode(state, m.cursor)
    if (m.limit) c.uint.preencode(state, m.limit)
  },
  encode(state, m) {
    const flags =
      (m.publicBeeKey ? 1 : 0) | (m.cursor ? 2 : 0) | (m.limit ? 4 : 0) | (m.limitProvided ? 8 : 0)

    c.string.encode(state, m.channelKey)
    c.uint.encode(state, flags)

    if (m.publicBeeKey) c.string.encode(state, m.publicBeeKey)
    c.string.encode(state, m.groupId)
    if (m.cursor) c.string.encode(state, m.cursor)
    if (m.limit) c.uint.encode(state, m.limit)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      channelKey: r0,
      publicBeeKey: (flags & 1) !== 0 ? c.string.decode(state) : null,
      groupId: c.string.decode(state),
      cursor: (flags & 2) !== 0 ? c.string.decode(state) : null,
      limit: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      limitProvided: (flags & 8) !== 0
    }
  }
}

// @peartube/get-content-items-response.group
const encoding41_3 = c.frame(encoding36)
// @peartube/get-content-items-response.items
const encoding41_4 = c.array(c.frame(encoding37))

// @peartube/get-content-items-response
const encoding41 = {
  preencode(state, m) {
    state.end++ // max flag is 32 so always one byte

    if (m.errorCode) c.string.preencode(state, m.errorCode)
    if (m.error) c.string.preencode(state, m.error)
    if (m.group) encoding41_3.preencode(state, m.group)
    if (m.items) encoding41_4.preencode(state, m.items)
    if (m.nextCursor) c.string.preencode(state, m.nextCursor)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.errorCode ? 2 : 0) |
      (m.error ? 4 : 0) |
      (m.group ? 8 : 0) |
      (m.items ? 16 : 0) |
      (m.nextCursor ? 32 : 0)

    c.uint.encode(state, flags)

    if (m.errorCode) c.string.encode(state, m.errorCode)
    if (m.error) c.string.encode(state, m.error)
    if (m.group) encoding41_3.encode(state, m.group)
    if (m.items) encoding41_4.encode(state, m.items)
    if (m.nextCursor) c.string.encode(state, m.nextCursor)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      errorCode: (flags & 2) !== 0 ? c.string.decode(state) : null,
      error: (flags & 4) !== 0 ? c.string.decode(state) : null,
      group: (flags & 8) !== 0 ? encoding41_3.decode(state) : null,
      items: (flags & 16) !== 0 ? encoding41_4.decode(state) : null,
      nextCursor: (flags & 32) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/media-page-request
const encoding42 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.cursor) c.string.preencode(state, m.cursor)
    if (m.limit) c.uint.preencode(state, m.limit)
  },
  encode(state, m) {
    const flags = (m.cursor ? 1 : 0) | (m.limit ? 2 : 0) | (m.limitProvided ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.cursor) c.string.encode(state, m.cursor)
    if (m.limit) c.uint.encode(state, m.limit)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      cursor: (flags & 1) !== 0 ? c.string.decode(state) : null,
      limit: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      limitProvided: (flags & 4) !== 0
    }
  }
}

// @peartube/media-rendition-descriptor
const encoding43 = {
  preencode(state, m) {
    c.string.preencode(state, m.renditionId)
    c.string.preencode(state, m.purpose)
    c.string.preencode(state, m.format)
    c.string.preencode(state, m.coreKey)
    c.uint.preencode(state, m.coreLength)
    c.string.preencode(state, m.treeHash)
    c.uint.preencode(state, m.byteLength)
    state.end++ // max flag is 1 so always one byte

    if (m.segmentIndexId) c.string.preencode(state, m.segmentIndexId)
  },
  encode(state, m) {
    const flags = m.segmentIndexId ? 1 : 0

    c.string.encode(state, m.renditionId)
    c.string.encode(state, m.purpose)
    c.string.encode(state, m.format)
    c.string.encode(state, m.coreKey)
    c.uint.encode(state, m.coreLength)
    c.string.encode(state, m.treeHash)
    c.uint.encode(state, m.byteLength)
    c.uint.encode(state, flags)

    if (m.segmentIndexId) c.string.encode(state, m.segmentIndexId)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const r3 = c.string.decode(state)
    const r4 = c.uint.decode(state)
    const r5 = c.string.decode(state)
    const r6 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      renditionId: r0,
      purpose: r1,
      format: r2,
      coreKey: r3,
      coreLength: r4,
      treeHash: r5,
      byteLength: r6,
      segmentIndexId: (flags & 1) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/media-availability.reasonCodes
const encoding44_11 = c.array(c.string)

// @peartube/media-availability
const encoding44 = {
  preencode(state, m) {
    c.string.preencode(state, m.state)
    state.end++ // max flag is 4 so always one byte

    if (m.renditionId) c.string.preencode(state, m.renditionId)
    c.uint.preencode(state, m.observedAt)
    c.uint.preencode(state, m.expiresAt)
    c.uint.preencode(state, m.requiredRangeCount)
    c.uint.preencode(state, m.reachableRangeCount)
    c.uint.preencode(state, m.independentPeerCount)
    c.uint.preencode(state, m.completePeerCount)
    c.uint.preencode(state, m.measuredLatencyMs)
    encoding44_11.preencode(state, m.reasonCodes)
  },
  encode(state, m) {
    const flags = (m.renditionId ? 1 : 0) | (m.offlinePlayable ? 2 : 0) | (m.archivePledged ? 4 : 0)

    c.string.encode(state, m.state)
    c.uint.encode(state, flags)

    if (m.renditionId) c.string.encode(state, m.renditionId)
    c.uint.encode(state, m.observedAt)
    c.uint.encode(state, m.expiresAt)
    c.uint.encode(state, m.requiredRangeCount)
    c.uint.encode(state, m.reachableRangeCount)
    c.uint.encode(state, m.independentPeerCount)
    c.uint.encode(state, m.completePeerCount)
    c.uint.encode(state, m.measuredLatencyMs)
    encoding44_11.encode(state, m.reasonCodes)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      state: r0,
      renditionId: (flags & 1) !== 0 ? c.string.decode(state) : null,
      observedAt: c.uint.decode(state),
      expiresAt: c.uint.decode(state),
      requiredRangeCount: c.uint.decode(state),
      reachableRangeCount: c.uint.decode(state),
      independentPeerCount: c.uint.decode(state),
      completePeerCount: c.uint.decode(state),
      measuredLatencyMs: c.uint.decode(state),
      offlinePlayable: (flags & 2) !== 0,
      archivePledged: (flags & 4) !== 0,
      reasonCodes: encoding44_11.decode(state)
    }
  }
}

// @peartube/media-source-coordinates
const encoding45 = {
  preencode(state, m) {
    c.string.preencode(state, m.contentKind)
    c.string.preencode(state, m.mediaProvider)
    c.string.preencode(state, m.mediaId)
    state.end++ // max flag is 2 so always one byte

    if (m.seasonNumber) c.uint.preencode(state, m.seasonNumber)
    if (m.episodeNumber) c.uint.preencode(state, m.episodeNumber)
  },
  encode(state, m) {
    const flags = (m.seasonNumber ? 1 : 0) | (m.episodeNumber ? 2 : 0)

    c.string.encode(state, m.contentKind)
    c.string.encode(state, m.mediaProvider)
    c.string.encode(state, m.mediaId)
    c.uint.encode(state, flags)

    if (m.seasonNumber) c.uint.encode(state, m.seasonNumber)
    if (m.episodeNumber) c.uint.encode(state, m.episodeNumber)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      contentKind: r0,
      mediaProvider: r1,
      mediaId: r2,
      seasonNumber: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      episodeNumber: (flags & 2) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/media-publication-source.selectionReasonCodes
const encoding46_11 = encoding44_11
// @peartube/media-publication-source.rejectionReasonCodes
const encoding46_12 = encoding44_11
// @peartube/media-publication-source.introductionPublisherIds
const encoding46_13 = encoding44_11
// @peartube/media-publication-source.introductionIndexIds
const encoding46_14 = encoding44_11
// @peartube/media-publication-source.moderationFeedIds
const encoding46_15 = encoding44_11
// @peartube/media-publication-source.claimConflictIds
const encoding46_16 = encoding44_11
// @peartube/media-publication-source.provenanceClaimIds
const encoding46_17 = encoding44_11
// @peartube/media-publication-source.availability
const encoding46_29 = c.frame(encoding44)
// @peartube/media-publication-source.mediaCoordinates
const encoding46_30 = c.frame(encoding45)

// @peartube/media-publication-source
const encoding46 = {
  preencode(state, m) {
    const flags =
      (m.manifestId ? 1 : 0) |
      (m.renditionId ? 2 : 0) |
      (m.score ? 4 : 0) |
      (m.availabilityScore ? 8 : 0) |
      (m.formatSupport ? 16 : 0) |
      (m.moderationPenalty ? 32 : 0) |
      (m.preferred ? 64 : 0) |
      (m.selected ? 128 : 0) |
      (m.eligible ? 256 : 0) |
      (m.selectionReasonCodes ? 512 : 0) |
      (m.rejectionReasonCodes ? 1024 : 0) |
      (m.introductionPublisherIds ? 2048 : 0) |
      (m.introductionIndexIds ? 4096 : 0) |
      (m.moderationFeedIds ? 8192 : 0) |
      (m.claimConflictIds ? 16384 : 0) |
      (m.provenanceClaimIds ? 32768 : 0) |
      (m.scoreLocalCompleteness ? 65536 : 0) |
      (m.scoreStartupReachability ? 131072 : 0) |
      (m.scorePeerEvidence ? 262144 : 0) |
      (m.scoreFormatSupport ? 524288 : 0) |
      (m.scoreStartupLatency ? 1048576 : 0) |
      (m.scoreUserOverride ? 2097152 : 0) |
      (m.archiveState ? 4194304 : 0) |
      (m.cacheState ? 8388608 : 0) |
      (m.availabilityState ? 16777216 : 0) |
      (m.stale ? 33554432 : 0) |
      (m.incomplete ? 67108864 : 0) |
      (m.availability ? 134217728 : 0) |
      (m.mediaCoordinates ? 268435456 : 0)

    c.string.preencode(state, m.publicationId)
    c.string.preencode(state, m.publisherId)
    c.uint.preencode(state, flags)

    if (m.manifestId) c.string.preencode(state, m.manifestId)
    if (m.renditionId) c.string.preencode(state, m.renditionId)
    if (m.score) c.uint.preencode(state, m.score)
    if (m.availabilityScore) c.uint.preencode(state, m.availabilityScore)
    if (m.formatSupport) c.uint.preencode(state, m.formatSupport)
    if (m.moderationPenalty) c.uint.preencode(state, m.moderationPenalty)
    if (m.selectionReasonCodes) encoding46_11.preencode(state, m.selectionReasonCodes)
    if (m.rejectionReasonCodes) encoding46_12.preencode(state, m.rejectionReasonCodes)
    if (m.introductionPublisherIds) encoding46_13.preencode(state, m.introductionPublisherIds)
    if (m.introductionIndexIds) encoding46_14.preencode(state, m.introductionIndexIds)
    if (m.moderationFeedIds) encoding46_15.preencode(state, m.moderationFeedIds)
    if (m.claimConflictIds) encoding46_16.preencode(state, m.claimConflictIds)
    if (m.provenanceClaimIds) encoding46_17.preencode(state, m.provenanceClaimIds)
    if (m.scoreLocalCompleteness) c.uint.preencode(state, m.scoreLocalCompleteness)
    if (m.scoreStartupReachability) c.uint.preencode(state, m.scoreStartupReachability)
    if (m.scorePeerEvidence) c.uint.preencode(state, m.scorePeerEvidence)
    if (m.scoreFormatSupport) c.uint.preencode(state, m.scoreFormatSupport)
    if (m.scoreStartupLatency) c.uint.preencode(state, m.scoreStartupLatency)
    if (m.scoreUserOverride) c.uint.preencode(state, m.scoreUserOverride)
    if (m.archiveState) c.string.preencode(state, m.archiveState)
    if (m.cacheState) c.string.preencode(state, m.cacheState)
    if (m.availabilityState) c.string.preencode(state, m.availabilityState)
    if (m.availability) encoding46_29.preencode(state, m.availability)
    if (m.mediaCoordinates) encoding46_30.preencode(state, m.mediaCoordinates)
  },
  encode(state, m) {
    const flags =
      (m.manifestId ? 1 : 0) |
      (m.renditionId ? 2 : 0) |
      (m.score ? 4 : 0) |
      (m.availabilityScore ? 8 : 0) |
      (m.formatSupport ? 16 : 0) |
      (m.moderationPenalty ? 32 : 0) |
      (m.preferred ? 64 : 0) |
      (m.selected ? 128 : 0) |
      (m.eligible ? 256 : 0) |
      (m.selectionReasonCodes ? 512 : 0) |
      (m.rejectionReasonCodes ? 1024 : 0) |
      (m.introductionPublisherIds ? 2048 : 0) |
      (m.introductionIndexIds ? 4096 : 0) |
      (m.moderationFeedIds ? 8192 : 0) |
      (m.claimConflictIds ? 16384 : 0) |
      (m.provenanceClaimIds ? 32768 : 0) |
      (m.scoreLocalCompleteness ? 65536 : 0) |
      (m.scoreStartupReachability ? 131072 : 0) |
      (m.scorePeerEvidence ? 262144 : 0) |
      (m.scoreFormatSupport ? 524288 : 0) |
      (m.scoreStartupLatency ? 1048576 : 0) |
      (m.scoreUserOverride ? 2097152 : 0) |
      (m.archiveState ? 4194304 : 0) |
      (m.cacheState ? 8388608 : 0) |
      (m.availabilityState ? 16777216 : 0) |
      (m.stale ? 33554432 : 0) |
      (m.incomplete ? 67108864 : 0) |
      (m.availability ? 134217728 : 0) |
      (m.mediaCoordinates ? 268435456 : 0)

    c.string.encode(state, m.publicationId)
    c.string.encode(state, m.publisherId)
    c.uint.encode(state, flags)

    if (m.manifestId) c.string.encode(state, m.manifestId)
    if (m.renditionId) c.string.encode(state, m.renditionId)
    if (m.score) c.uint.encode(state, m.score)
    if (m.availabilityScore) c.uint.encode(state, m.availabilityScore)
    if (m.formatSupport) c.uint.encode(state, m.formatSupport)
    if (m.moderationPenalty) c.uint.encode(state, m.moderationPenalty)
    if (m.selectionReasonCodes) encoding46_11.encode(state, m.selectionReasonCodes)
    if (m.rejectionReasonCodes) encoding46_12.encode(state, m.rejectionReasonCodes)
    if (m.introductionPublisherIds) encoding46_13.encode(state, m.introductionPublisherIds)
    if (m.introductionIndexIds) encoding46_14.encode(state, m.introductionIndexIds)
    if (m.moderationFeedIds) encoding46_15.encode(state, m.moderationFeedIds)
    if (m.claimConflictIds) encoding46_16.encode(state, m.claimConflictIds)
    if (m.provenanceClaimIds) encoding46_17.encode(state, m.provenanceClaimIds)
    if (m.scoreLocalCompleteness) c.uint.encode(state, m.scoreLocalCompleteness)
    if (m.scoreStartupReachability) c.uint.encode(state, m.scoreStartupReachability)
    if (m.scorePeerEvidence) c.uint.encode(state, m.scorePeerEvidence)
    if (m.scoreFormatSupport) c.uint.encode(state, m.scoreFormatSupport)
    if (m.scoreStartupLatency) c.uint.encode(state, m.scoreStartupLatency)
    if (m.scoreUserOverride) c.uint.encode(state, m.scoreUserOverride)
    if (m.archiveState) c.string.encode(state, m.archiveState)
    if (m.cacheState) c.string.encode(state, m.cacheState)
    if (m.availabilityState) c.string.encode(state, m.availabilityState)
    if (m.availability) encoding46_29.encode(state, m.availability)
    if (m.mediaCoordinates) encoding46_30.encode(state, m.mediaCoordinates)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      publicationId: r0,
      publisherId: r1,
      manifestId: (flags & 1) !== 0 ? c.string.decode(state) : null,
      renditionId: (flags & 2) !== 0 ? c.string.decode(state) : null,
      score: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      availabilityScore: (flags & 8) !== 0 ? c.uint.decode(state) : 0,
      formatSupport: (flags & 16) !== 0 ? c.uint.decode(state) : 0,
      moderationPenalty: (flags & 32) !== 0 ? c.uint.decode(state) : 0,
      preferred: (flags & 64) !== 0,
      selected: (flags & 128) !== 0,
      eligible: (flags & 256) !== 0,
      selectionReasonCodes: (flags & 512) !== 0 ? encoding46_11.decode(state) : null,
      rejectionReasonCodes: (flags & 1024) !== 0 ? encoding46_12.decode(state) : null,
      introductionPublisherIds: (flags & 2048) !== 0 ? encoding46_13.decode(state) : null,
      introductionIndexIds: (flags & 4096) !== 0 ? encoding46_14.decode(state) : null,
      moderationFeedIds: (flags & 8192) !== 0 ? encoding46_15.decode(state) : null,
      claimConflictIds: (flags & 16384) !== 0 ? encoding46_16.decode(state) : null,
      provenanceClaimIds: (flags & 32768) !== 0 ? encoding46_17.decode(state) : null,
      scoreLocalCompleteness: (flags & 65536) !== 0 ? c.uint.decode(state) : 0,
      scoreStartupReachability: (flags & 131072) !== 0 ? c.uint.decode(state) : 0,
      scorePeerEvidence: (flags & 262144) !== 0 ? c.uint.decode(state) : 0,
      scoreFormatSupport: (flags & 524288) !== 0 ? c.uint.decode(state) : 0,
      scoreStartupLatency: (flags & 1048576) !== 0 ? c.uint.decode(state) : 0,
      scoreUserOverride: (flags & 2097152) !== 0 ? c.uint.decode(state) : 0,
      archiveState: (flags & 4194304) !== 0 ? c.string.decode(state) : null,
      cacheState: (flags & 8388608) !== 0 ? c.string.decode(state) : null,
      availabilityState: (flags & 16777216) !== 0 ? c.string.decode(state) : null,
      stale: (flags & 33554432) !== 0,
      incomplete: (flags & 67108864) !== 0,
      availability: (flags & 134217728) !== 0 ? encoding46_29.decode(state) : null,
      mediaCoordinates: (flags & 268435456) !== 0 ? encoding46_30.decode(state) : null
    }
  }
}

// @peartube/media-claim-provenance
const encoding47 = {
  preencode(state, m) {
    c.string.preencode(state, m.claimId)
    c.string.preencode(state, m.claimType)
    c.string.preencode(state, m.issuerId)
    state.end++ // max flag is 16 so always one byte

    if (m.subjectEntityId) c.string.preencode(state, m.subjectEntityId)
    if (m.confidence) c.uint.preencode(state, m.confidence)
    if (m.sourceRank) c.uint.preencode(state, m.sourceRank)
    if (m.issuedAt) c.uint.preencode(state, m.issuedAt)
  },
  encode(state, m) {
    const flags =
      (m.subjectEntityId ? 1 : 0) |
      (m.confidence ? 2 : 0) |
      (m.sourceRank ? 4 : 0) |
      (m.revoked ? 8 : 0) |
      (m.issuedAt ? 16 : 0)

    c.string.encode(state, m.claimId)
    c.string.encode(state, m.claimType)
    c.string.encode(state, m.issuerId)
    c.uint.encode(state, flags)

    if (m.subjectEntityId) c.string.encode(state, m.subjectEntityId)
    if (m.confidence) c.uint.encode(state, m.confidence)
    if (m.sourceRank) c.uint.encode(state, m.sourceRank)
    if (m.issuedAt) c.uint.encode(state, m.issuedAt)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      claimId: r0,
      claimType: r1,
      issuerId: r2,
      subjectEntityId: (flags & 1) !== 0 ? c.string.decode(state) : null,
      confidence: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      sourceRank: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      revoked: (flags & 8) !== 0,
      issuedAt: (flags & 16) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/media-conflict-summary.claimIds
const encoding48_3 = encoding44_11

// @peartube/media-conflict-summary
const encoding48 = {
  preencode(state, m) {
    c.string.preencode(state, m.conflictId)
    c.string.preencode(state, m.claimType)
    c.string.preencode(state, m.subjectEntityId)
    state.end++ // max flag is 2 so always one byte

    if (m.claimIds) encoding48_3.preencode(state, m.claimIds)
    if (m.preferredClaimId) c.string.preencode(state, m.preferredClaimId)
  },
  encode(state, m) {
    const flags = (m.claimIds ? 1 : 0) | (m.preferredClaimId ? 2 : 0)

    c.string.encode(state, m.conflictId)
    c.string.encode(state, m.claimType)
    c.string.encode(state, m.subjectEntityId)
    c.uint.encode(state, flags)

    if (m.claimIds) encoding48_3.encode(state, m.claimIds)
    if (m.preferredClaimId) c.string.encode(state, m.preferredClaimId)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      conflictId: r0,
      claimType: r1,
      subjectEntityId: r2,
      claimIds: (flags & 1) !== 0 ? encoding48_3.decode(state) : null,
      preferredClaimId: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/media-agent-summary
const encoding49 = {
  preencode(state, m) {
    c.string.preencode(state, m.entityId)
    state.end++ // max flag is 4 so always one byte

    if (m.localClusterId) c.string.preencode(state, m.localClusterId)
    if (m.displayName) c.string.preencode(state, m.displayName)
    if (m.claimCount) c.uint.preencode(state, m.claimCount)
  },
  encode(state, m) {
    const flags = (m.localClusterId ? 1 : 0) | (m.displayName ? 2 : 0) | (m.claimCount ? 4 : 0)

    c.string.encode(state, m.entityId)
    c.uint.encode(state, flags)

    if (m.localClusterId) c.string.encode(state, m.localClusterId)
    if (m.displayName) c.string.encode(state, m.displayName)
    if (m.claimCount) c.uint.encode(state, m.claimCount)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      entityId: r0,
      localClusterId: (flags & 1) !== 0 ? c.string.decode(state) : null,
      displayName: (flags & 2) !== 0 ? c.string.decode(state) : null,
      claimCount: (flags & 4) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/media-contribution-summary
const encoding50 = {
  preencode(state, m) {
    c.string.preencode(state, m.agentEntityId)
    c.string.preencode(state, m.role)
    state.end++ // max flag is 2 so always one byte

    if (m.workEntityId) c.string.preencode(state, m.workEntityId)
    if (m.publicationId) c.string.preencode(state, m.publicationId)
    c.string.preencode(state, m.claimId)
  },
  encode(state, m) {
    const flags = (m.workEntityId ? 1 : 0) | (m.publicationId ? 2 : 0)

    c.string.encode(state, m.agentEntityId)
    c.string.encode(state, m.role)
    c.uint.encode(state, flags)

    if (m.workEntityId) c.string.encode(state, m.workEntityId)
    if (m.publicationId) c.string.encode(state, m.publicationId)
    c.string.encode(state, m.claimId)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      agentEntityId: r0,
      role: r1,
      workEntityId: (flags & 1) !== 0 ? c.string.decode(state) : null,
      publicationId: (flags & 2) !== 0 ? c.string.decode(state) : null,
      claimId: c.string.decode(state)
    }
  }
}

// @peartube/media-entity-summary.availability
const encoding51_7 = encoding46_29
// @peartube/media-entity-summary.sources
const encoding51_8 = c.array(c.frame(encoding46))
// @peartube/media-entity-summary.renditions
const encoding51_9 = c.array(c.frame(encoding43))
// @peartube/media-entity-summary.genres
const encoding51_13 = encoding44_11

// @peartube/media-entity-summary
const encoding51 = {
  preencode(state, m) {
    const flags =
      (m.localClusterId ? 1 : 0) |
      (m.title ? 2 : 0) |
      (m.subtitle ? 4 : 0) |
      (m.claimCount ? 8 : 0) |
      (m.conflictCount ? 16 : 0) |
      (m.availability ? 32 : 0) |
      (m.sources ? 64 : 0) |
      (m.renditions ? 128 : 0) |
      (m.releaseYear ? 256 : 0) |
      (m.runtimeMinutes ? 512 : 0) |
      (m.overview ? 1024 : 0) |
      (m.genres ? 2048 : 0) |
      (m.posterBlobId ? 4096 : 0) |
      (m.posterBlobsCoreKey ? 8192 : 0) |
      (m.posterMimeType ? 16384 : 0) |
      (m.posterUrl ? 32768 : 0)

    c.string.preencode(state, m.entityId)
    c.string.preencode(state, m.entityKind)
    c.uint.preencode(state, flags)

    if (m.localClusterId) c.string.preencode(state, m.localClusterId)
    if (m.title) c.string.preencode(state, m.title)
    if (m.subtitle) c.string.preencode(state, m.subtitle)
    if (m.claimCount) c.uint.preencode(state, m.claimCount)
    if (m.conflictCount) c.uint.preencode(state, m.conflictCount)
    if (m.availability) encoding51_7.preencode(state, m.availability)
    if (m.sources) encoding51_8.preencode(state, m.sources)
    if (m.renditions) encoding51_9.preencode(state, m.renditions)
    if (m.releaseYear) c.uint.preencode(state, m.releaseYear)
    if (m.runtimeMinutes) c.uint.preencode(state, m.runtimeMinutes)
    if (m.overview) c.string.preencode(state, m.overview)
    if (m.genres) encoding51_13.preencode(state, m.genres)
    if (m.posterBlobId) c.string.preencode(state, m.posterBlobId)
    if (m.posterBlobsCoreKey) c.string.preencode(state, m.posterBlobsCoreKey)
    if (m.posterMimeType) c.string.preencode(state, m.posterMimeType)
    if (m.posterUrl) c.string.preencode(state, m.posterUrl)
  },
  encode(state, m) {
    const flags =
      (m.localClusterId ? 1 : 0) |
      (m.title ? 2 : 0) |
      (m.subtitle ? 4 : 0) |
      (m.claimCount ? 8 : 0) |
      (m.conflictCount ? 16 : 0) |
      (m.availability ? 32 : 0) |
      (m.sources ? 64 : 0) |
      (m.renditions ? 128 : 0) |
      (m.releaseYear ? 256 : 0) |
      (m.runtimeMinutes ? 512 : 0) |
      (m.overview ? 1024 : 0) |
      (m.genres ? 2048 : 0) |
      (m.posterBlobId ? 4096 : 0) |
      (m.posterBlobsCoreKey ? 8192 : 0) |
      (m.posterMimeType ? 16384 : 0) |
      (m.posterUrl ? 32768 : 0)

    c.string.encode(state, m.entityId)
    c.string.encode(state, m.entityKind)
    c.uint.encode(state, flags)

    if (m.localClusterId) c.string.encode(state, m.localClusterId)
    if (m.title) c.string.encode(state, m.title)
    if (m.subtitle) c.string.encode(state, m.subtitle)
    if (m.claimCount) c.uint.encode(state, m.claimCount)
    if (m.conflictCount) c.uint.encode(state, m.conflictCount)
    if (m.availability) encoding51_7.encode(state, m.availability)
    if (m.sources) encoding51_8.encode(state, m.sources)
    if (m.renditions) encoding51_9.encode(state, m.renditions)
    if (m.releaseYear) c.uint.encode(state, m.releaseYear)
    if (m.runtimeMinutes) c.uint.encode(state, m.runtimeMinutes)
    if (m.overview) c.string.encode(state, m.overview)
    if (m.genres) encoding51_13.encode(state, m.genres)
    if (m.posterBlobId) c.string.encode(state, m.posterBlobId)
    if (m.posterBlobsCoreKey) c.string.encode(state, m.posterBlobsCoreKey)
    if (m.posterMimeType) c.string.encode(state, m.posterMimeType)
    if (m.posterUrl) c.string.encode(state, m.posterUrl)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      entityId: r0,
      entityKind: r1,
      localClusterId: (flags & 1) !== 0 ? c.string.decode(state) : null,
      title: (flags & 2) !== 0 ? c.string.decode(state) : null,
      subtitle: (flags & 4) !== 0 ? c.string.decode(state) : null,
      claimCount: (flags & 8) !== 0 ? c.uint.decode(state) : 0,
      conflictCount: (flags & 16) !== 0 ? c.uint.decode(state) : 0,
      availability: (flags & 32) !== 0 ? encoding51_7.decode(state) : null,
      sources: (flags & 64) !== 0 ? encoding51_8.decode(state) : null,
      renditions: (flags & 128) !== 0 ? encoding51_9.decode(state) : null,
      releaseYear: (flags & 256) !== 0 ? c.uint.decode(state) : 0,
      runtimeMinutes: (flags & 512) !== 0 ? c.uint.decode(state) : 0,
      overview: (flags & 1024) !== 0 ? c.string.decode(state) : null,
      genres: (flags & 2048) !== 0 ? encoding51_13.decode(state) : null,
      posterBlobId: (flags & 4096) !== 0 ? c.string.decode(state) : null,
      posterBlobsCoreKey: (flags & 8192) !== 0 ? c.string.decode(state) : null,
      posterMimeType: (flags & 16384) !== 0 ? c.string.decode(state) : null,
      posterUrl: (flags & 32768) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-media-catalog-response.items
const encoding52_3 = c.array(c.frame(encoding51))

// @peartube/get-media-catalog-response
const encoding52 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (m.errorCode) c.string.preencode(state, m.errorCode)
    if (m.error) c.string.preencode(state, m.error)
    encoding52_3.preencode(state, m.items)
    if (m.nextCursor) c.string.preencode(state, m.nextCursor)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) | (m.errorCode ? 2 : 0) | (m.error ? 4 : 0) | (m.nextCursor ? 8 : 0)

    c.uint.encode(state, flags)

    if (m.errorCode) c.string.encode(state, m.errorCode)
    if (m.error) c.string.encode(state, m.error)
    encoding52_3.encode(state, m.items)
    if (m.nextCursor) c.string.encode(state, m.nextCursor)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      errorCode: (flags & 2) !== 0 ? c.string.decode(state) : null,
      error: (flags & 4) !== 0 ? c.string.decode(state) : null,
      items: encoding52_3.decode(state),
      nextCursor: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-media-entity-request
const encoding53 = {
  preencode(state, m) {
    c.string.preencode(state, m.entityId)
    state.end++ // max flag is 2 so always one byte
  },
  encode(state, m) {
    const flags = (m.includeClaims ? 1 : 0) | (m.includeConflicts ? 2 : 0)

    c.string.encode(state, m.entityId)
    c.uint.encode(state, flags)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      entityId: r0,
      includeClaims: (flags & 1) !== 0,
      includeConflicts: (flags & 2) !== 0
    }
  }
}

// @peartube/get-media-entity-response.entity
const encoding54_3 = c.frame(encoding51)
// @peartube/get-media-entity-response.claims
const encoding54_4 = c.array(c.frame(encoding47))
// @peartube/get-media-entity-response.conflicts
const encoding54_5 = c.array(c.frame(encoding48))

// @peartube/get-media-entity-response
const encoding54 = {
  preencode(state, m) {
    state.end++ // max flag is 32 so always one byte

    if (m.errorCode) c.string.preencode(state, m.errorCode)
    if (m.error) c.string.preencode(state, m.error)
    if (m.entity) encoding54_3.preencode(state, m.entity)
    if (m.claims) encoding54_4.preencode(state, m.claims)
    if (m.conflicts) encoding54_5.preencode(state, m.conflicts)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.errorCode ? 2 : 0) |
      (m.error ? 4 : 0) |
      (m.entity ? 8 : 0) |
      (m.claims ? 16 : 0) |
      (m.conflicts ? 32 : 0)

    c.uint.encode(state, flags)

    if (m.errorCode) c.string.encode(state, m.errorCode)
    if (m.error) c.string.encode(state, m.error)
    if (m.entity) encoding54_3.encode(state, m.entity)
    if (m.claims) encoding54_4.encode(state, m.claims)
    if (m.conflicts) encoding54_5.encode(state, m.conflicts)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      errorCode: (flags & 2) !== 0 ? c.string.decode(state) : null,
      error: (flags & 4) !== 0 ? c.string.decode(state) : null,
      entity: (flags & 8) !== 0 ? encoding54_3.decode(state) : null,
      claims: (flags & 16) !== 0 ? encoding54_4.decode(state) : null,
      conflicts: (flags & 32) !== 0 ? encoding54_5.decode(state) : null
    }
  }
}

// @peartube/get-media-collection-request
const encoding55 = encoding53

// @peartube/get-media-collection-response.entity
const encoding56_3 = encoding54_3
// @peartube/get-media-collection-response.claims
const encoding56_4 = encoding54_4
// @peartube/get-media-collection-response.conflicts
const encoding56_5 = encoding54_5

// @peartube/get-media-collection-response
const encoding56 = {
  preencode(state, m) {
    state.end++ // max flag is 32 so always one byte

    if (m.errorCode) c.string.preencode(state, m.errorCode)
    if (m.error) c.string.preencode(state, m.error)
    if (m.entity) encoding56_3.preencode(state, m.entity)
    if (m.claims) encoding56_4.preencode(state, m.claims)
    if (m.conflicts) encoding56_5.preencode(state, m.conflicts)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.errorCode ? 2 : 0) |
      (m.error ? 4 : 0) |
      (m.entity ? 8 : 0) |
      (m.claims ? 16 : 0) |
      (m.conflicts ? 32 : 0)

    c.uint.encode(state, flags)

    if (m.errorCode) c.string.encode(state, m.errorCode)
    if (m.error) c.string.encode(state, m.error)
    if (m.entity) encoding56_3.encode(state, m.entity)
    if (m.claims) encoding56_4.encode(state, m.claims)
    if (m.conflicts) encoding56_5.encode(state, m.conflicts)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      errorCode: (flags & 2) !== 0 ? c.string.decode(state) : null,
      error: (flags & 4) !== 0 ? c.string.decode(state) : null,
      entity: (flags & 8) !== 0 ? encoding56_3.decode(state) : null,
      claims: (flags & 16) !== 0 ? encoding56_4.decode(state) : null,
      conflicts: (flags & 32) !== 0 ? encoding56_5.decode(state) : null
    }
  }
}

// @peartube/get-media-agent-request
const encoding57 = encoding53

// @peartube/get-media-agent-response.entity
const encoding58_3 = c.frame(encoding49)
// @peartube/get-media-agent-response.claims
const encoding58_4 = encoding54_4
// @peartube/get-media-agent-response.conflicts
const encoding58_5 = encoding54_5

// @peartube/get-media-agent-response
const encoding58 = {
  preencode(state, m) {
    state.end++ // max flag is 32 so always one byte

    if (m.errorCode) c.string.preencode(state, m.errorCode)
    if (m.error) c.string.preencode(state, m.error)
    if (m.entity) encoding58_3.preencode(state, m.entity)
    if (m.claims) encoding58_4.preencode(state, m.claims)
    if (m.conflicts) encoding58_5.preencode(state, m.conflicts)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.errorCode ? 2 : 0) |
      (m.error ? 4 : 0) |
      (m.entity ? 8 : 0) |
      (m.claims ? 16 : 0) |
      (m.conflicts ? 32 : 0)

    c.uint.encode(state, flags)

    if (m.errorCode) c.string.encode(state, m.errorCode)
    if (m.error) c.string.encode(state, m.error)
    if (m.entity) encoding58_3.encode(state, m.entity)
    if (m.claims) encoding58_4.encode(state, m.claims)
    if (m.conflicts) encoding58_5.encode(state, m.conflicts)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      errorCode: (flags & 2) !== 0 ? c.string.decode(state) : null,
      error: (flags & 4) !== 0 ? c.string.decode(state) : null,
      entity: (flags & 8) !== 0 ? encoding58_3.decode(state) : null,
      claims: (flags & 16) !== 0 ? encoding58_4.decode(state) : null,
      conflicts: (flags & 32) !== 0 ? encoding58_5.decode(state) : null
    }
  }
}

// @peartube/get-media-collection-items-request
const encoding59 = {
  preencode(state, m) {
    c.string.preencode(state, m.collectionEntityId)
    state.end++ // max flag is 4 so always one byte

    if (m.cursor) c.string.preencode(state, m.cursor)
    if (m.limit) c.uint.preencode(state, m.limit)
  },
  encode(state, m) {
    const flags = (m.cursor ? 1 : 0) | (m.limit ? 2 : 0) | (m.limitProvided ? 4 : 0)

    c.string.encode(state, m.collectionEntityId)
    c.uint.encode(state, flags)

    if (m.cursor) c.string.encode(state, m.cursor)
    if (m.limit) c.uint.encode(state, m.limit)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      collectionEntityId: r0,
      cursor: (flags & 1) !== 0 ? c.string.decode(state) : null,
      limit: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      limitProvided: (flags & 4) !== 0
    }
  }
}

// @peartube/get-media-collection-items-response.items
const encoding60_3 = encoding52_3

// @peartube/get-media-collection-items-response
const encoding60 = {
  preencode(state, m) {
    state.end++ // max flag is 16 so always one byte

    if (m.errorCode) c.string.preencode(state, m.errorCode)
    if (m.error) c.string.preencode(state, m.error)
    if (m.items) encoding60_3.preencode(state, m.items)
    if (m.nextCursor) c.string.preencode(state, m.nextCursor)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.errorCode ? 2 : 0) |
      (m.error ? 4 : 0) |
      (m.items ? 8 : 0) |
      (m.nextCursor ? 16 : 0)

    c.uint.encode(state, flags)

    if (m.errorCode) c.string.encode(state, m.errorCode)
    if (m.error) c.string.encode(state, m.error)
    if (m.items) encoding60_3.encode(state, m.items)
    if (m.nextCursor) c.string.encode(state, m.nextCursor)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      errorCode: (flags & 2) !== 0 ? c.string.decode(state) : null,
      error: (flags & 4) !== 0 ? c.string.decode(state) : null,
      items: (flags & 8) !== 0 ? encoding60_3.decode(state) : null,
      nextCursor: (flags & 16) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-agent-contributions-request
const encoding61 = {
  preencode(state, m) {
    c.string.preencode(state, m.agentEntityId)
    state.end++ // max flag is 4 so always one byte

    if (m.cursor) c.string.preencode(state, m.cursor)
    if (m.limit) c.uint.preencode(state, m.limit)
  },
  encode(state, m) {
    const flags = (m.cursor ? 1 : 0) | (m.limit ? 2 : 0) | (m.limitProvided ? 4 : 0)

    c.string.encode(state, m.agentEntityId)
    c.uint.encode(state, flags)

    if (m.cursor) c.string.encode(state, m.cursor)
    if (m.limit) c.uint.encode(state, m.limit)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      agentEntityId: r0,
      cursor: (flags & 1) !== 0 ? c.string.decode(state) : null,
      limit: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      limitProvided: (flags & 4) !== 0
    }
  }
}

// @peartube/get-agent-contributions-response.items
const encoding62_3 = c.array(c.frame(encoding50))

// @peartube/get-agent-contributions-response
const encoding62 = {
  preencode(state, m) {
    state.end++ // max flag is 16 so always one byte

    if (m.errorCode) c.string.preencode(state, m.errorCode)
    if (m.error) c.string.preencode(state, m.error)
    if (m.items) encoding62_3.preencode(state, m.items)
    if (m.nextCursor) c.string.preencode(state, m.nextCursor)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.errorCode ? 2 : 0) |
      (m.error ? 4 : 0) |
      (m.items ? 8 : 0) |
      (m.nextCursor ? 16 : 0)

    c.uint.encode(state, flags)

    if (m.errorCode) c.string.encode(state, m.errorCode)
    if (m.error) c.string.encode(state, m.error)
    if (m.items) encoding62_3.encode(state, m.items)
    if (m.nextCursor) c.string.encode(state, m.nextCursor)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      errorCode: (flags & 2) !== 0 ? c.string.decode(state) : null,
      error: (flags & 4) !== 0 ? c.string.decode(state) : null,
      items: (flags & 8) !== 0 ? encoding62_3.decode(state) : null,
      nextCursor: (flags & 16) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-publication-sources-request
const encoding63 = {
  preencode(state, m) {
    c.string.preencode(state, m.entityId)
    state.end++ // max flag is 4 so always one byte

    if (m.cursor) c.string.preencode(state, m.cursor)
    if (m.limit) c.uint.preencode(state, m.limit)
  },
  encode(state, m) {
    const flags = (m.cursor ? 1 : 0) | (m.limit ? 2 : 0) | (m.limitProvided ? 4 : 0)

    c.string.encode(state, m.entityId)
    c.uint.encode(state, flags)

    if (m.cursor) c.string.encode(state, m.cursor)
    if (m.limit) c.uint.encode(state, m.limit)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      entityId: r0,
      cursor: (flags & 1) !== 0 ? c.string.decode(state) : null,
      limit: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      limitProvided: (flags & 4) !== 0
    }
  }
}

// @peartube/get-publication-sources-response.items
const encoding64_3 = encoding51_8

// @peartube/get-publication-sources-response
const encoding64 = {
  preencode(state, m) {
    state.end++ // max flag is 16 so always one byte

    if (m.errorCode) c.string.preencode(state, m.errorCode)
    if (m.error) c.string.preencode(state, m.error)
    if (m.items) encoding64_3.preencode(state, m.items)
    if (m.nextCursor) c.string.preencode(state, m.nextCursor)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.errorCode ? 2 : 0) |
      (m.error ? 4 : 0) |
      (m.items ? 8 : 0) |
      (m.nextCursor ? 16 : 0)

    c.uint.encode(state, flags)

    if (m.errorCode) c.string.encode(state, m.errorCode)
    if (m.error) c.string.encode(state, m.error)
    if (m.items) encoding64_3.encode(state, m.items)
    if (m.nextCursor) c.string.encode(state, m.nextCursor)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      errorCode: (flags & 2) !== 0 ? c.string.decode(state) : null,
      error: (flags & 4) !== 0 ? c.string.decode(state) : null,
      items: (flags & 8) !== 0 ? encoding64_3.decode(state) : null,
      nextCursor: (flags & 16) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-entity-artwork-request
const encoding65 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte

    if (m.entityId) c.string.preencode(state, m.entityId)
    if (m.publicationId) c.string.preencode(state, m.publicationId)
  },
  encode(state, m) {
    const flags = (m.entityId ? 1 : 0) | (m.publicationId ? 2 : 0)

    c.uint.encode(state, flags)

    if (m.entityId) c.string.encode(state, m.entityId)
    if (m.publicationId) c.string.encode(state, m.publicationId)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      entityId: (flags & 1) !== 0 ? c.string.decode(state) : null,
      publicationId: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-entity-artwork-response
const encoding66 = {
  preencode(state, m) {
    state.end++ // max flag is 16 so always one byte

    if (m.errorCode) c.string.preencode(state, m.errorCode)
    if (m.error) c.string.preencode(state, m.error)
    if (m.url) c.string.preencode(state, m.url)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.errorCode ? 2 : 0) |
      (m.error ? 4 : 0) |
      (m.exists ? 8 : 0) |
      (m.url ? 16 : 0)

    c.uint.encode(state, flags)

    if (m.errorCode) c.string.encode(state, m.errorCode)
    if (m.error) c.string.encode(state, m.error)
    if (m.url) c.string.encode(state, m.url)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      errorCode: (flags & 2) !== 0 ? c.string.decode(state) : null,
      error: (flags & 4) !== 0 ? c.string.decode(state) : null,
      exists: (flags & 8) !== 0,
      url: (flags & 16) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/media-playback-attempt
const encoding67 = {
  preencode(state, m) {
    c.string.preencode(state, m.publicationId)
    state.end++ // max flag is 1 so always one byte

    if (m.errorCode) c.string.preencode(state, m.errorCode)
  },
  encode(state, m) {
    const flags = m.errorCode ? 1 : 0

    c.string.encode(state, m.publicationId)
    c.uint.encode(state, flags)

    if (m.errorCode) c.string.encode(state, m.errorCode)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      publicationId: r0,
      errorCode: (flags & 1) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/prepare-media-playback-request
const encoding68 = {
  preencode(state, m) {
    c.string.preencode(state, m.entityId)
    state.end++ // max flag is 1 so always one byte

    if (m.publicationId) c.string.preencode(state, m.publicationId)
  },
  encode(state, m) {
    const flags = m.publicationId ? 1 : 0

    c.string.encode(state, m.entityId)
    c.uint.encode(state, flags)

    if (m.publicationId) c.string.encode(state, m.publicationId)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      entityId: r0,
      publicationId: (flags & 1) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/prepare-media-playback-response.attempts
const encoding69_8 = c.array(c.frame(encoding67))
// @peartube/prepare-media-playback-response.sources
const encoding69_9 = encoding51_8

// @peartube/prepare-media-playback-response
const encoding69 = {
  preencode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.errorCode ? 2 : 0) |
      (m.error ? 4 : 0) |
      (m.retry ? 8 : 0) |
      (m.publicationId ? 16 : 0) |
      (m.renditionId ? 32 : 0) |
      (m.coreKey ? 64 : 0) |
      (m.url ? 128 : 0) |
      (m.attempts ? 256 : 0) |
      (m.sources ? 512 : 0)

    c.uint.preencode(state, flags)

    if (m.errorCode) c.string.preencode(state, m.errorCode)
    if (m.error) c.string.preencode(state, m.error)
    if (m.retry) c.string.preencode(state, m.retry)
    if (m.publicationId) c.string.preencode(state, m.publicationId)
    if (m.renditionId) c.string.preencode(state, m.renditionId)
    if (m.coreKey) c.string.preencode(state, m.coreKey)
    if (m.url) c.string.preencode(state, m.url)
    if (m.attempts) encoding69_8.preencode(state, m.attempts)
    if (m.sources) encoding69_9.preencode(state, m.sources)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.errorCode ? 2 : 0) |
      (m.error ? 4 : 0) |
      (m.retry ? 8 : 0) |
      (m.publicationId ? 16 : 0) |
      (m.renditionId ? 32 : 0) |
      (m.coreKey ? 64 : 0) |
      (m.url ? 128 : 0) |
      (m.attempts ? 256 : 0) |
      (m.sources ? 512 : 0)

    c.uint.encode(state, flags)

    if (m.errorCode) c.string.encode(state, m.errorCode)
    if (m.error) c.string.encode(state, m.error)
    if (m.retry) c.string.encode(state, m.retry)
    if (m.publicationId) c.string.encode(state, m.publicationId)
    if (m.renditionId) c.string.encode(state, m.renditionId)
    if (m.coreKey) c.string.encode(state, m.coreKey)
    if (m.url) c.string.encode(state, m.url)
    if (m.attempts) encoding69_8.encode(state, m.attempts)
    if (m.sources) encoding69_9.encode(state, m.sources)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      errorCode: (flags & 2) !== 0 ? c.string.decode(state) : null,
      error: (flags & 4) !== 0 ? c.string.decode(state) : null,
      retry: (flags & 8) !== 0 ? c.string.decode(state) : null,
      publicationId: (flags & 16) !== 0 ? c.string.decode(state) : null,
      renditionId: (flags & 32) !== 0 ? c.string.decode(state) : null,
      coreKey: (flags & 64) !== 0 ? c.string.decode(state) : null,
      url: (flags & 128) !== 0 ? c.string.decode(state) : null,
      attempts: (flags & 256) !== 0 ? encoding69_8.decode(state) : null,
      sources: (flags & 512) !== 0 ? encoding69_9.decode(state) : null
    }
  }
}

// @peartube/get-claim-provenance-request
const encoding70 = {
  preencode(state, m) {
    c.string.preencode(state, m.claimId)
  },
  encode(state, m) {
    c.string.encode(state, m.claimId)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      claimId: r0
    }
  }
}

// @peartube/get-claim-provenance-response.claim
const encoding71_3 = c.frame(encoding47)

// @peartube/get-claim-provenance-response
const encoding71 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (m.errorCode) c.string.preencode(state, m.errorCode)
    if (m.error) c.string.preencode(state, m.error)
    if (m.claim) encoding71_3.preencode(state, m.claim)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) | (m.errorCode ? 2 : 0) | (m.error ? 4 : 0) | (m.claim ? 8 : 0)

    c.uint.encode(state, flags)

    if (m.errorCode) c.string.encode(state, m.errorCode)
    if (m.error) c.string.encode(state, m.error)
    if (m.claim) encoding71_3.encode(state, m.claim)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      errorCode: (flags & 2) !== 0 ? c.string.decode(state) : null,
      error: (flags & 4) !== 0 ? c.string.decode(state) : null,
      claim: (flags & 8) !== 0 ? encoding71_3.decode(state) : null
    }
  }
}

// @peartube/set-source-preference-request
const encoding72 = {
  preencode(state, m) {
    c.string.preencode(state, m.entityId)
    c.string.preencode(state, m.publicationId)
    state.end++ // max flag is 1 so always one byte
  },
  encode(state, m) {
    const flags = m.preferred ? 1 : 0

    c.string.encode(state, m.entityId)
    c.string.encode(state, m.publicationId)
    c.uint.encode(state, flags)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      entityId: r0,
      publicationId: r1,
      preferred: (flags & 1) !== 0
    }
  }
}

// @peartube/set-source-preference-response
const encoding73 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.errorCode) c.string.preencode(state, m.errorCode)
    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.errorCode ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.errorCode) c.string.encode(state, m.errorCode)
    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      errorCode: (flags & 2) !== 0 ? c.string.decode(state) : null,
      error: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/video-immutable-publication
const encoding74 = {
  preencode(state, m) {
    c.string.preencode(state, m.publicationId)
    state.end++ // max flag is 4 so always one byte

    if (m.manifestId) c.string.preencode(state, m.manifestId)
    if (m.renditionId) c.string.preencode(state, m.renditionId)
    if (m.publisherId) c.string.preencode(state, m.publisherId)
  },
  encode(state, m) {
    const flags = (m.manifestId ? 1 : 0) | (m.renditionId ? 2 : 0) | (m.publisherId ? 4 : 0)

    c.string.encode(state, m.publicationId)
    c.uint.encode(state, flags)

    if (m.manifestId) c.string.encode(state, m.manifestId)
    if (m.renditionId) c.string.encode(state, m.renditionId)
    if (m.publisherId) c.string.encode(state, m.publisherId)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      publicationId: r0,
      manifestId: (flags & 1) !== 0 ? c.string.decode(state) : null,
      renditionId: (flags & 2) !== 0 ? c.string.decode(state) : null,
      publisherId: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/video.immutablePublication
const encoding75_24 = c.frame(encoding74)

// @peartube/video
const encoding75 = {
  preencode(state, m) {
    const flags =
      (m.description ? 1 : 0) |
      (m.path ? 2 : 0) |
      (m.duration ? 4 : 0) |
      (m.thumbnail ? 8 : 0) |
      (m.channelKey ? 16 : 0) |
      (m.channelName ? 32 : 0) |
      (m.createdAt ? 64 : 0) |
      (m.views ? 128 : 0) |
      (m.category ? 256 : 0) |
      (m.blobId ? 512 : 0) |
      (m.blobsCoreKey ? 1024 : 0) |
      (m.mimeType ? 2048 : 0) |
      (m.availability ? 4096 : 0) |
      (m.thumbnailBlobId ? 8192 : 0) |
      (m.thumbnailBlobsCoreKey ? 16384 : 0) |
      (m.thumbnailMimeType ? 32768 : 0) |
      (m.publicBeeKey ? 65536 : 0) |
      (m.byteAvailability ? 131072 : 0) |
      (m.hasHeadBlock ? 262144 : 0) |
      (m.contiguousBlocks ? 524288 : 0) |
      (m.readyForPlayback ? 1048576 : 0) |
      (m.publicationId ? 2097152 : 0) |
      (m.immutablePublication ? 4194304 : 0)

    c.string.preencode(state, m.id)
    c.string.preencode(state, m.title)
    c.uint.preencode(state, flags)

    if (m.description) c.string.preencode(state, m.description)
    if (m.path) c.string.preencode(state, m.path)
    if (m.duration) c.uint.preencode(state, m.duration)
    if (m.thumbnail) c.string.preencode(state, m.thumbnail)
    if (m.channelKey) c.string.preencode(state, m.channelKey)
    if (m.channelName) c.string.preencode(state, m.channelName)
    if (m.createdAt) c.uint.preencode(state, m.createdAt)
    if (m.views) c.uint.preencode(state, m.views)
    if (m.category) c.string.preencode(state, m.category)
    if (m.blobId) c.string.preencode(state, m.blobId)
    if (m.blobsCoreKey) c.string.preencode(state, m.blobsCoreKey)
    if (m.mimeType) c.string.preencode(state, m.mimeType)
    if (m.availability) c.string.preencode(state, m.availability)
    if (m.thumbnailBlobId) c.string.preencode(state, m.thumbnailBlobId)
    if (m.thumbnailBlobsCoreKey) c.string.preencode(state, m.thumbnailBlobsCoreKey)
    if (m.thumbnailMimeType) c.string.preencode(state, m.thumbnailMimeType)
    if (m.publicBeeKey) c.string.preencode(state, m.publicBeeKey)
    if (m.byteAvailability) c.string.preencode(state, m.byteAvailability)
    if (m.contiguousBlocks) c.uint.preencode(state, m.contiguousBlocks)
    if (m.publicationId) c.string.preencode(state, m.publicationId)
    if (m.immutablePublication) encoding75_24.preencode(state, m.immutablePublication)
  },
  encode(state, m) {
    const flags =
      (m.description ? 1 : 0) |
      (m.path ? 2 : 0) |
      (m.duration ? 4 : 0) |
      (m.thumbnail ? 8 : 0) |
      (m.channelKey ? 16 : 0) |
      (m.channelName ? 32 : 0) |
      (m.createdAt ? 64 : 0) |
      (m.views ? 128 : 0) |
      (m.category ? 256 : 0) |
      (m.blobId ? 512 : 0) |
      (m.blobsCoreKey ? 1024 : 0) |
      (m.mimeType ? 2048 : 0) |
      (m.availability ? 4096 : 0) |
      (m.thumbnailBlobId ? 8192 : 0) |
      (m.thumbnailBlobsCoreKey ? 16384 : 0) |
      (m.thumbnailMimeType ? 32768 : 0) |
      (m.publicBeeKey ? 65536 : 0) |
      (m.byteAvailability ? 131072 : 0) |
      (m.hasHeadBlock ? 262144 : 0) |
      (m.contiguousBlocks ? 524288 : 0) |
      (m.readyForPlayback ? 1048576 : 0) |
      (m.publicationId ? 2097152 : 0) |
      (m.immutablePublication ? 4194304 : 0)

    c.string.encode(state, m.id)
    c.string.encode(state, m.title)
    c.uint.encode(state, flags)

    if (m.description) c.string.encode(state, m.description)
    if (m.path) c.string.encode(state, m.path)
    if (m.duration) c.uint.encode(state, m.duration)
    if (m.thumbnail) c.string.encode(state, m.thumbnail)
    if (m.channelKey) c.string.encode(state, m.channelKey)
    if (m.channelName) c.string.encode(state, m.channelName)
    if (m.createdAt) c.uint.encode(state, m.createdAt)
    if (m.views) c.uint.encode(state, m.views)
    if (m.category) c.string.encode(state, m.category)
    if (m.blobId) c.string.encode(state, m.blobId)
    if (m.blobsCoreKey) c.string.encode(state, m.blobsCoreKey)
    if (m.mimeType) c.string.encode(state, m.mimeType)
    if (m.availability) c.string.encode(state, m.availability)
    if (m.thumbnailBlobId) c.string.encode(state, m.thumbnailBlobId)
    if (m.thumbnailBlobsCoreKey) c.string.encode(state, m.thumbnailBlobsCoreKey)
    if (m.thumbnailMimeType) c.string.encode(state, m.thumbnailMimeType)
    if (m.publicBeeKey) c.string.encode(state, m.publicBeeKey)
    if (m.byteAvailability) c.string.encode(state, m.byteAvailability)
    if (m.contiguousBlocks) c.uint.encode(state, m.contiguousBlocks)
    if (m.publicationId) c.string.encode(state, m.publicationId)
    if (m.immutablePublication) encoding75_24.encode(state, m.immutablePublication)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      id: r0,
      title: r1,
      description: (flags & 1) !== 0 ? c.string.decode(state) : null,
      path: (flags & 2) !== 0 ? c.string.decode(state) : null,
      duration: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      thumbnail: (flags & 8) !== 0 ? c.string.decode(state) : null,
      channelKey: (flags & 16) !== 0 ? c.string.decode(state) : null,
      channelName: (flags & 32) !== 0 ? c.string.decode(state) : null,
      createdAt: (flags & 64) !== 0 ? c.uint.decode(state) : 0,
      views: (flags & 128) !== 0 ? c.uint.decode(state) : 0,
      category: (flags & 256) !== 0 ? c.string.decode(state) : null,
      blobId: (flags & 512) !== 0 ? c.string.decode(state) : null,
      blobsCoreKey: (flags & 1024) !== 0 ? c.string.decode(state) : null,
      mimeType: (flags & 2048) !== 0 ? c.string.decode(state) : null,
      availability: (flags & 4096) !== 0 ? c.string.decode(state) : null,
      thumbnailBlobId: (flags & 8192) !== 0 ? c.string.decode(state) : null,
      thumbnailBlobsCoreKey: (flags & 16384) !== 0 ? c.string.decode(state) : null,
      thumbnailMimeType: (flags & 32768) !== 0 ? c.string.decode(state) : null,
      publicBeeKey: (flags & 65536) !== 0 ? c.string.decode(state) : null,
      byteAvailability: (flags & 131072) !== 0 ? c.string.decode(state) : null,
      hasHeadBlock: (flags & 262144) !== 0,
      contiguousBlocks: (flags & 524288) !== 0 ? c.uint.decode(state) : 0,
      readyForPlayback: (flags & 1048576) !== 0,
      publicationId: (flags & 2097152) !== 0 ? c.string.decode(state) : null,
      immutablePublication: (flags & 4194304) !== 0 ? encoding75_24.decode(state) : null
    }
  }
}

// @peartube/list-videos-request
const encoding76 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (m.channelKey) c.string.preencode(state, m.channelKey)
    if (m.publicBeeKey) c.string.preencode(state, m.publicBeeKey)
    if (m.limit) c.uint.preencode(state, m.limit)
    if (m.offset) c.uint.preencode(state, m.offset)
  },
  encode(state, m) {
    const flags =
      (m.channelKey ? 1 : 0) | (m.publicBeeKey ? 2 : 0) | (m.limit ? 4 : 0) | (m.offset ? 8 : 0)

    c.uint.encode(state, flags)

    if (m.channelKey) c.string.encode(state, m.channelKey)
    if (m.publicBeeKey) c.string.encode(state, m.publicBeeKey)
    if (m.limit) c.uint.encode(state, m.limit)
    if (m.offset) c.uint.encode(state, m.offset)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      channelKey: (flags & 1) !== 0 ? c.string.decode(state) : null,
      publicBeeKey: (flags & 2) !== 0 ? c.string.decode(state) : null,
      limit: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      offset: (flags & 8) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/list-videos-response.videos
const encoding77_0 = c.array(c.frame(encoding75))

// @peartube/list-videos-response
const encoding77 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte

    if (m.videos) encoding77_0.preencode(state, m.videos)
  },
  encode(state, m) {
    const flags = m.videos ? 1 : 0

    c.uint.encode(state, flags)

    if (m.videos) encoding77_0.encode(state, m.videos)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      videos: (flags & 1) !== 0 ? encoding77_0.decode(state) : null
    }
  }
}

// @peartube/get-video-url-request
const encoding78 = {
  preencode(state, m) {
    c.string.preencode(state, m.channelKey)
    c.string.preencode(state, m.videoId)
    state.end++ // max flag is 8 so always one byte

    if (m.publicBeeKey) c.string.preencode(state, m.publicBeeKey)
    if (m.blobId) c.string.preencode(state, m.blobId)
    if (m.blobsCoreKey) c.string.preencode(state, m.blobsCoreKey)
    if (m.mimeType) c.string.preencode(state, m.mimeType)
  },
  encode(state, m) {
    const flags =
      (m.publicBeeKey ? 1 : 0) |
      (m.blobId ? 2 : 0) |
      (m.blobsCoreKey ? 4 : 0) |
      (m.mimeType ? 8 : 0)

    c.string.encode(state, m.channelKey)
    c.string.encode(state, m.videoId)
    c.uint.encode(state, flags)

    if (m.publicBeeKey) c.string.encode(state, m.publicBeeKey)
    if (m.blobId) c.string.encode(state, m.blobId)
    if (m.blobsCoreKey) c.string.encode(state, m.blobsCoreKey)
    if (m.mimeType) c.string.encode(state, m.mimeType)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      channelKey: r0,
      videoId: r1,
      publicBeeKey: (flags & 1) !== 0 ? c.string.decode(state) : null,
      blobId: (flags & 2) !== 0 ? c.string.decode(state) : null,
      blobsCoreKey: (flags & 4) !== 0 ? c.string.decode(state) : null,
      mimeType: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-video-url-response
const encoding79 = {
  preencode(state, m) {
    c.string.preencode(state, m.url)
  },
  encode(state, m) {
    c.string.encode(state, m.url)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      url: r0
    }
  }
}

// @peartube/prepare-playback-request
const encoding80 = encoding78

// @peartube/selected-blob-warmup
const encoding81 = {
  preencode(state, m) {
    const flags =
      (m.blobsCoreKey ? 1 : 0) |
      (m.blobId ? 2 : 0) |
      (m.peerCount ? 4 : 0) |
      (m.hasHeadBlock ? 8 : 0) |
      (m.contiguousBlocks ? 16 : 0) |
      (m.readyForPlayback ? 32 : 0) |
      (m.blobPeerIdsJson ? 64 : 0) |
      (m.sourceFeedPeerIdsJson ? 128 : 0) |
      (m.sourceRelayPeerIdsJson ? 256 : 0) |
      (m.retainedDiscoveryLabel ? 512 : 0) |
      (m.retainedDiscoveryStatus ? 1024 : 0) |
      (m.feedRelayAlsoBlobPeer ? 2048 : 0) |
      (m.promotedPeerHintsJson ? 4096 : 0) |
      (m.error ? 8192 : 0)

    c.uint.preencode(state, flags)

    if (m.blobsCoreKey) c.string.preencode(state, m.blobsCoreKey)
    if (m.blobId) c.string.preencode(state, m.blobId)
    if (m.peerCount) c.uint.preencode(state, m.peerCount)
    if (m.contiguousBlocks) c.uint.preencode(state, m.contiguousBlocks)
    if (m.blobPeerIdsJson) c.string.preencode(state, m.blobPeerIdsJson)
    if (m.sourceFeedPeerIdsJson) c.string.preencode(state, m.sourceFeedPeerIdsJson)
    if (m.sourceRelayPeerIdsJson) c.string.preencode(state, m.sourceRelayPeerIdsJson)
    if (m.retainedDiscoveryLabel) c.string.preencode(state, m.retainedDiscoveryLabel)
    if (m.retainedDiscoveryStatus) c.string.preencode(state, m.retainedDiscoveryStatus)
    if (m.promotedPeerHintsJson) c.string.preencode(state, m.promotedPeerHintsJson)
    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags =
      (m.blobsCoreKey ? 1 : 0) |
      (m.blobId ? 2 : 0) |
      (m.peerCount ? 4 : 0) |
      (m.hasHeadBlock ? 8 : 0) |
      (m.contiguousBlocks ? 16 : 0) |
      (m.readyForPlayback ? 32 : 0) |
      (m.blobPeerIdsJson ? 64 : 0) |
      (m.sourceFeedPeerIdsJson ? 128 : 0) |
      (m.sourceRelayPeerIdsJson ? 256 : 0) |
      (m.retainedDiscoveryLabel ? 512 : 0) |
      (m.retainedDiscoveryStatus ? 1024 : 0) |
      (m.feedRelayAlsoBlobPeer ? 2048 : 0) |
      (m.promotedPeerHintsJson ? 4096 : 0) |
      (m.error ? 8192 : 0)

    c.uint.encode(state, flags)

    if (m.blobsCoreKey) c.string.encode(state, m.blobsCoreKey)
    if (m.blobId) c.string.encode(state, m.blobId)
    if (m.peerCount) c.uint.encode(state, m.peerCount)
    if (m.contiguousBlocks) c.uint.encode(state, m.contiguousBlocks)
    if (m.blobPeerIdsJson) c.string.encode(state, m.blobPeerIdsJson)
    if (m.sourceFeedPeerIdsJson) c.string.encode(state, m.sourceFeedPeerIdsJson)
    if (m.sourceRelayPeerIdsJson) c.string.encode(state, m.sourceRelayPeerIdsJson)
    if (m.retainedDiscoveryLabel) c.string.encode(state, m.retainedDiscoveryLabel)
    if (m.retainedDiscoveryStatus) c.string.encode(state, m.retainedDiscoveryStatus)
    if (m.promotedPeerHintsJson) c.string.encode(state, m.promotedPeerHintsJson)
    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      blobsCoreKey: (flags & 1) !== 0 ? c.string.decode(state) : null,
      blobId: (flags & 2) !== 0 ? c.string.decode(state) : null,
      peerCount: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      hasHeadBlock: (flags & 8) !== 0,
      contiguousBlocks: (flags & 16) !== 0 ? c.uint.decode(state) : 0,
      readyForPlayback: (flags & 32) !== 0,
      blobPeerIdsJson: (flags & 64) !== 0 ? c.string.decode(state) : null,
      sourceFeedPeerIdsJson: (flags & 128) !== 0 ? c.string.decode(state) : null,
      sourceRelayPeerIdsJson: (flags & 256) !== 0 ? c.string.decode(state) : null,
      retainedDiscoveryLabel: (flags & 512) !== 0 ? c.string.decode(state) : null,
      retainedDiscoveryStatus: (flags & 1024) !== 0 ? c.string.decode(state) : null,
      feedRelayAlsoBlobPeer: (flags & 2048) !== 0,
      promotedPeerHintsJson: (flags & 4096) !== 0 ? c.string.decode(state) : null,
      error: (flags & 8192) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/peer-warmup
const encoding82 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (m.peerCount) c.uint.preencode(state, m.peerCount)
    if (m.elapsedMs) c.uint.preencode(state, m.elapsedMs)
  },
  encode(state, m) {
    const flags =
      (m.peerCount ? 1 : 0) | (m.retained ? 2 : 0) | (m.timedOut ? 4 : 0) | (m.elapsedMs ? 8 : 0)

    c.uint.encode(state, flags)

    if (m.peerCount) c.uint.encode(state, m.peerCount)
    if (m.elapsedMs) c.uint.encode(state, m.elapsedMs)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      peerCount: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      retained: (flags & 2) !== 0,
      timedOut: (flags & 4) !== 0,
      elapsedMs: (flags & 8) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/prepare-playback-response.selectedBlobWarmup
const encoding83_4 = c.frame(encoding81)
// @peartube/prepare-playback-response.peerWarmup
const encoding83_5 = c.frame(encoding82)

// @peartube/prepare-playback-response
const encoding83 = {
  preencode(state, m) {
    state.end++ // max flag is 32 so always one byte

    if (m.url) c.string.preencode(state, m.url)
    if (m.stats) encoding83_1.preencode(state, m.stats)
    if (m.selectedBlobWarmup) encoding83_4.preencode(state, m.selectedBlobWarmup)
    if (m.peerWarmup) encoding83_5.preencode(state, m.peerWarmup)
  },
  encode(state, m) {
    const flags =
      (m.url ? 1 : 0) |
      (m.stats ? 2 : 0) |
      (m.warmupStarted ? 4 : 0) |
      (m.peerWarmupStarted ? 8 : 0) |
      (m.selectedBlobWarmup ? 16 : 0) |
      (m.peerWarmup ? 32 : 0)

    c.uint.encode(state, flags)

    if (m.url) c.string.encode(state, m.url)
    if (m.stats) encoding83_1.encode(state, m.stats)
    if (m.selectedBlobWarmup) encoding83_4.encode(state, m.selectedBlobWarmup)
    if (m.peerWarmup) encoding83_5.encode(state, m.peerWarmup)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      url: (flags & 1) !== 0 ? c.string.decode(state) : null,
      stats: (flags & 2) !== 0 ? encoding83_1.decode(state) : null,
      warmupStarted: (flags & 4) !== 0,
      peerWarmupStarted: (flags & 8) !== 0,
      selectedBlobWarmup: (flags & 16) !== 0 ? encoding83_4.decode(state) : null,
      peerWarmup: (flags & 32) !== 0 ? encoding83_5.decode(state) : null
    }
  }
}

// @peartube/web-prepare-playback-response.selectedBlobWarmup
const encoding84_8 = encoding83_4
// @peartube/web-prepare-playback-response.peerWarmup
const encoding84_9 = encoding83_5

// @peartube/web-prepare-playback-response
const encoding84 = {
  preencode(state, m) {
    const flags =
      (m.transcoded ? 1 : 0) |
      (m.audioCodec ? 2 : 0) |
      (m.videoCodec ? 4 : 0) |
      (m.transcodeError ? 8 : 0) |
      (m.stats ? 16 : 0) |
      (m.warmupStarted ? 32 : 0) |
      (m.peerWarmupStarted ? 64 : 0) |
      (m.selectedBlobWarmup ? 128 : 0) |
      (m.peerWarmup ? 256 : 0)

    c.string.preencode(state, m.url)
    c.uint.preencode(state, flags)

    if (m.audioCodec) c.string.preencode(state, m.audioCodec)
    if (m.videoCodec) c.string.preencode(state, m.videoCodec)
    if (m.transcodeError) c.string.preencode(state, m.transcodeError)
    if (m.stats) encoding84_5.preencode(state, m.stats)
    if (m.selectedBlobWarmup) encoding84_8.preencode(state, m.selectedBlobWarmup)
    if (m.peerWarmup) encoding84_9.preencode(state, m.peerWarmup)
  },
  encode(state, m) {
    const flags =
      (m.transcoded ? 1 : 0) |
      (m.audioCodec ? 2 : 0) |
      (m.videoCodec ? 4 : 0) |
      (m.transcodeError ? 8 : 0) |
      (m.stats ? 16 : 0) |
      (m.warmupStarted ? 32 : 0) |
      (m.peerWarmupStarted ? 64 : 0) |
      (m.selectedBlobWarmup ? 128 : 0) |
      (m.peerWarmup ? 256 : 0)

    c.string.encode(state, m.url)
    c.uint.encode(state, flags)

    if (m.audioCodec) c.string.encode(state, m.audioCodec)
    if (m.videoCodec) c.string.encode(state, m.videoCodec)
    if (m.transcodeError) c.string.encode(state, m.transcodeError)
    if (m.stats) encoding84_5.encode(state, m.stats)
    if (m.selectedBlobWarmup) encoding84_8.encode(state, m.selectedBlobWarmup)
    if (m.peerWarmup) encoding84_9.encode(state, m.peerWarmup)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      url: r0,
      transcoded: (flags & 1) !== 0,
      audioCodec: (flags & 2) !== 0 ? c.string.decode(state) : null,
      videoCodec: (flags & 4) !== 0 ? c.string.decode(state) : null,
      transcodeError: (flags & 8) !== 0 ? c.string.decode(state) : null,
      stats: (flags & 16) !== 0 ? encoding84_5.decode(state) : null,
      warmupStarted: (flags & 32) !== 0,
      peerWarmupStarted: (flags & 64) !== 0,
      selectedBlobWarmup: (flags & 128) !== 0 ? encoding84_8.decode(state) : null,
      peerWarmup: (flags & 256) !== 0 ? encoding84_9.decode(state) : null
    }
  }
}

// @peartube/get-video-data-request
const encoding85 = {
  preencode(state, m) {
    c.string.preencode(state, m.channelKey)
    c.string.preencode(state, m.videoId)
    state.end++ // max flag is 1 so always one byte

    if (m.publicBeeKey) c.string.preencode(state, m.publicBeeKey)
  },
  encode(state, m) {
    const flags = m.publicBeeKey ? 1 : 0

    c.string.encode(state, m.channelKey)
    c.string.encode(state, m.videoId)
    c.uint.encode(state, flags)

    if (m.publicBeeKey) c.string.encode(state, m.publicBeeKey)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      channelKey: r0,
      videoId: r1,
      publicBeeKey: (flags & 1) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-video-data-response.video
const encoding86_0 = c.frame(encoding75)

// @peartube/get-video-data-response
const encoding86 = {
  preencode(state, m) {
    encoding86_0.preencode(state, m.video)
  },
  encode(state, m) {
    encoding86_0.encode(state, m.video)
  },
  decode(state) {
    const r0 = encoding86_0.decode(state)

    return {
      video: r0
    }
  }
}

// @peartube/upload-video-request
const encoding87 = {
  preencode(state, m) {
    const flags =
      (m.description ? 1 : 0) |
      (m.category ? 2 : 0) |
      (m.skipThumbnailGeneration ? 4 : 0) |
      (m.contentKind ? 8 : 0) |
      (m.seriesId ? 16 : 0) |
      (m.seriesTitle ? 32 : 0) |
      (m.mediaProvider ? 64 : 0) |
      (m.mediaId ? 128 : 0) |
      (m.seasonNumber ? 256 : 0) |
      (m.episodeNumber ? 512 : 0) |
      (m.expectedEpisodeCount ? 1024 : 0)

    c.string.preencode(state, m.filePath)
    c.string.preencode(state, m.title)
    c.uint.preencode(state, flags)

    if (m.description) c.string.preencode(state, m.description)
    if (m.category) c.string.preencode(state, m.category)
    if (m.contentKind) c.string.preencode(state, m.contentKind)
    if (m.seriesId) c.string.preencode(state, m.seriesId)
    if (m.seriesTitle) c.string.preencode(state, m.seriesTitle)
    if (m.mediaProvider) c.string.preencode(state, m.mediaProvider)
    if (m.mediaId) c.string.preencode(state, m.mediaId)
    if (m.seasonNumber) c.uint.preencode(state, m.seasonNumber)
    if (m.episodeNumber) c.uint.preencode(state, m.episodeNumber)
    if (m.expectedEpisodeCount) c.uint.preencode(state, m.expectedEpisodeCount)
  },
  encode(state, m) {
    const flags =
      (m.description ? 1 : 0) |
      (m.category ? 2 : 0) |
      (m.skipThumbnailGeneration ? 4 : 0) |
      (m.contentKind ? 8 : 0) |
      (m.seriesId ? 16 : 0) |
      (m.seriesTitle ? 32 : 0) |
      (m.mediaProvider ? 64 : 0) |
      (m.mediaId ? 128 : 0) |
      (m.seasonNumber ? 256 : 0) |
      (m.episodeNumber ? 512 : 0) |
      (m.expectedEpisodeCount ? 1024 : 0)

    c.string.encode(state, m.filePath)
    c.string.encode(state, m.title)
    c.uint.encode(state, flags)

    if (m.description) c.string.encode(state, m.description)
    if (m.category) c.string.encode(state, m.category)
    if (m.contentKind) c.string.encode(state, m.contentKind)
    if (m.seriesId) c.string.encode(state, m.seriesId)
    if (m.seriesTitle) c.string.encode(state, m.seriesTitle)
    if (m.mediaProvider) c.string.encode(state, m.mediaProvider)
    if (m.mediaId) c.string.encode(state, m.mediaId)
    if (m.seasonNumber) c.uint.encode(state, m.seasonNumber)
    if (m.episodeNumber) c.uint.encode(state, m.episodeNumber)
    if (m.expectedEpisodeCount) c.uint.encode(state, m.expectedEpisodeCount)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      filePath: r0,
      title: r1,
      description: (flags & 1) !== 0 ? c.string.decode(state) : null,
      category: (flags & 2) !== 0 ? c.string.decode(state) : null,
      skipThumbnailGeneration: (flags & 4) !== 0,
      contentKind: (flags & 8) !== 0 ? c.string.decode(state) : null,
      seriesId: (flags & 16) !== 0 ? c.string.decode(state) : null,
      seriesTitle: (flags & 32) !== 0 ? c.string.decode(state) : null,
      mediaProvider: (flags & 64) !== 0 ? c.string.decode(state) : null,
      mediaId: (flags & 128) !== 0 ? c.string.decode(state) : null,
      seasonNumber: (flags & 256) !== 0 ? c.uint.decode(state) : 0,
      episodeNumber: (flags & 512) !== 0 ? c.uint.decode(state) : 0,
      expectedEpisodeCount: (flags & 1024) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/upload-video-response.video
const encoding88_0 = encoding86_0

// @peartube/upload-video-response
const encoding88 = {
  preencode(state, m) {
    encoding88_0.preencode(state, m.video)
  },
  encode(state, m) {
    encoding88_0.encode(state, m.video)
  },
  decode(state) {
    const r0 = encoding88_0.decode(state)

    return {
      video: r0
    }
  }
}

// @peartube/download-video-request
const encoding89 = {
  preencode(state, m) {
    c.string.preencode(state, m.channelKey)
    c.string.preencode(state, m.videoId)
    c.string.preencode(state, m.destPath)
    state.end++ // max flag is 1 so always one byte

    if (m.publicBeeKey) c.string.preencode(state, m.publicBeeKey)
  },
  encode(state, m) {
    const flags = m.publicBeeKey ? 1 : 0

    c.string.encode(state, m.channelKey)
    c.string.encode(state, m.videoId)
    c.string.encode(state, m.destPath)
    c.uint.encode(state, flags)

    if (m.publicBeeKey) c.string.encode(state, m.publicBeeKey)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      channelKey: r0,
      videoId: r1,
      destPath: r2,
      publicBeeKey: (flags & 1) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/download-video-response
const encoding90 = {
  preencode(state, m) {
    state.end++ // max flag is 16 so always one byte

    if (m.filePath) c.string.preencode(state, m.filePath)
    if (m.size) c.uint.preencode(state, m.size)
    if (m.error) c.string.preencode(state, m.error)
    if (m.data) c.string.preencode(state, m.data)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.filePath ? 2 : 0) |
      (m.size ? 4 : 0) |
      (m.error ? 8 : 0) |
      (m.data ? 16 : 0)

    c.uint.encode(state, flags)

    if (m.filePath) c.string.encode(state, m.filePath)
    if (m.size) c.uint.encode(state, m.size)
    if (m.error) c.string.encode(state, m.error)
    if (m.data) c.string.encode(state, m.data)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      filePath: (flags & 2) !== 0 ? c.string.decode(state) : null,
      size: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      error: (flags & 8) !== 0 ? c.string.decode(state) : null,
      data: (flags & 16) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/delete-video-request
const encoding91 = {
  preencode(state, m) {
    c.string.preencode(state, m.videoId)
  },
  encode(state, m) {
    c.string.encode(state, m.videoId)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      videoId: r0
    }
  }
}

// @peartube/delete-video-response
const encoding92 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte

    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.error ? 2 : 0)

    c.uint.encode(state, flags)

    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      error: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/start-livestream-request
const encoding93 = {
  preencode(state, m) {
    state.end++ // max flag is 16 so always one byte

    if (m.channelKey) c.string.preencode(state, m.channelKey)
    if (m.title) c.string.preencode(state, m.title)
    if (m.targetFragmentDurationMs) c.uint.preencode(state, m.targetFragmentDurationMs)
    if (m.width) c.uint.preencode(state, m.width)
    if (m.height) c.uint.preencode(state, m.height)
  },
  encode(state, m) {
    const flags =
      (m.channelKey ? 1 : 0) |
      (m.title ? 2 : 0) |
      (m.targetFragmentDurationMs ? 4 : 0) |
      (m.width ? 8 : 0) |
      (m.height ? 16 : 0)

    c.uint.encode(state, flags)

    if (m.channelKey) c.string.encode(state, m.channelKey)
    if (m.title) c.string.encode(state, m.title)
    if (m.targetFragmentDurationMs) c.uint.encode(state, m.targetFragmentDurationMs)
    if (m.width) c.uint.encode(state, m.width)
    if (m.height) c.uint.encode(state, m.height)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      channelKey: (flags & 1) !== 0 ? c.string.decode(state) : null,
      title: (flags & 2) !== 0 ? c.string.decode(state) : null,
      targetFragmentDurationMs: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      width: (flags & 8) !== 0 ? c.uint.decode(state) : 0,
      height: (flags & 16) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/start-livestream-response
const encoding94 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (m.videoId) c.string.preencode(state, m.videoId)
    if (m.liveCoreKey) c.string.preencode(state, m.liveCoreKey)
    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) | (m.videoId ? 2 : 0) | (m.liveCoreKey ? 4 : 0) | (m.error ? 8 : 0)

    c.uint.encode(state, flags)

    if (m.videoId) c.string.encode(state, m.videoId)
    if (m.liveCoreKey) c.string.encode(state, m.liveCoreKey)
    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      videoId: (flags & 2) !== 0 ? c.string.decode(state) : null,
      liveCoreKey: (flags & 4) !== 0 ? c.string.decode(state) : null,
      error: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/stop-livestream-request
const encoding95 = encoding91

// @peartube/livestream-status
const encoding96 = {
  preencode(state, m) {
    c.string.preencode(state, m.state)
    state.end++ // max flag is 64 so always one byte

    if (m.videoId) c.string.preencode(state, m.videoId)
    if (m.liveCoreKey) c.string.preencode(state, m.liveCoreKey)
    if (m.mediaBlocks) c.uint.preencode(state, m.mediaBlocks)
    if (m.durationMs) c.uint.preencode(state, m.durationMs)
    if (m.peerCount) c.uint.preencode(state, m.peerCount)
    if (m.startedAt) c.uint.preencode(state, m.startedAt)
    if (m.endedAt) c.uint.preencode(state, m.endedAt)
  },
  encode(state, m) {
    const flags =
      (m.videoId ? 1 : 0) |
      (m.liveCoreKey ? 2 : 0) |
      (m.mediaBlocks ? 4 : 0) |
      (m.durationMs ? 8 : 0) |
      (m.peerCount ? 16 : 0) |
      (m.startedAt ? 32 : 0) |
      (m.endedAt ? 64 : 0)

    c.string.encode(state, m.state)
    c.uint.encode(state, flags)

    if (m.videoId) c.string.encode(state, m.videoId)
    if (m.liveCoreKey) c.string.encode(state, m.liveCoreKey)
    if (m.mediaBlocks) c.uint.encode(state, m.mediaBlocks)
    if (m.durationMs) c.uint.encode(state, m.durationMs)
    if (m.peerCount) c.uint.encode(state, m.peerCount)
    if (m.startedAt) c.uint.encode(state, m.startedAt)
    if (m.endedAt) c.uint.encode(state, m.endedAt)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      state: r0,
      videoId: (flags & 1) !== 0 ? c.string.decode(state) : null,
      liveCoreKey: (flags & 2) !== 0 ? c.string.decode(state) : null,
      mediaBlocks: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      durationMs: (flags & 8) !== 0 ? c.uint.decode(state) : 0,
      peerCount: (flags & 16) !== 0 ? c.uint.decode(state) : 0,
      startedAt: (flags & 32) !== 0 ? c.uint.decode(state) : 0,
      endedAt: (flags & 64) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/stop-livestream-response.status
const encoding97_1 = c.frame(encoding96)

// @peartube/stop-livestream-response
const encoding97 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.status) encoding97_1.preencode(state, m.status)
    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.status ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.status) encoding97_1.encode(state, m.status)
    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      status: (flags & 2) !== 0 ? encoding97_1.decode(state) : null,
      error: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-livestream-status-request
const encoding98 = encoding91

// @peartube/get-livestream-status-response.status
const encoding99_0 = encoding97_1

// @peartube/get-livestream-status-response
const encoding99 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte

    if (m.status) encoding99_0.preencode(state, m.status)
    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.status ? 1 : 0) | (m.error ? 2 : 0)

    c.uint.encode(state, flags)

    if (m.status) encoding99_0.encode(state, m.status)
    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      status: (flags & 1) !== 0 ? encoding99_0.decode(state) : null,
      error: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/prepare-live-playback-request
const encoding100 = {
  preencode(state, m) {
    c.string.preencode(state, m.liveCoreKey)
  },
  encode(state, m) {
    c.string.encode(state, m.liveCoreKey)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      liveCoreKey: r0
    }
  }
}

// @peartube/prepare-live-playback-response
const encoding101 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.url) c.string.preencode(state, m.url)
    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.url ? 1 : 0) | (m.isLive ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.url) c.string.encode(state, m.url)
    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      url: (flags & 1) !== 0 ? c.string.decode(state) : null,
      isLive: (flags & 2) !== 0,
      error: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/update-video-metadata-request
const encoding102 = {
  preencode(state, m) {
    c.string.preencode(state, m.channelKey)
    c.string.preencode(state, m.videoId)
    state.end++ // max flag is 4 so always one byte

    if (m.title) c.string.preencode(state, m.title)
    if (m.description) c.string.preencode(state, m.description)
    if (m.category) c.string.preencode(state, m.category)
  },
  encode(state, m) {
    const flags = (m.title ? 1 : 0) | (m.description ? 2 : 0) | (m.category ? 4 : 0)

    c.string.encode(state, m.channelKey)
    c.string.encode(state, m.videoId)
    c.uint.encode(state, flags)

    if (m.title) c.string.encode(state, m.title)
    if (m.description) c.string.encode(state, m.description)
    if (m.category) c.string.encode(state, m.category)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      channelKey: r0,
      videoId: r1,
      title: (flags & 1) !== 0 ? c.string.decode(state) : null,
      description: (flags & 2) !== 0 ? c.string.decode(state) : null,
      category: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/update-video-metadata-response
const encoding103 = encoding92

// @peartube/subscription
const encoding104 = {
  preencode(state, m) {
    c.string.preencode(state, m.channelKey)
    state.end++ // max flag is 4 so always one byte

    if (m.channelName) c.string.preencode(state, m.channelName)
    if (m.avatar) c.string.preencode(state, m.avatar)
    if (m.subscribedAt) c.uint.preencode(state, m.subscribedAt)
  },
  encode(state, m) {
    const flags = (m.channelName ? 1 : 0) | (m.avatar ? 2 : 0) | (m.subscribedAt ? 4 : 0)

    c.string.encode(state, m.channelKey)
    c.uint.encode(state, flags)

    if (m.channelName) c.string.encode(state, m.channelName)
    if (m.avatar) c.string.encode(state, m.avatar)
    if (m.subscribedAt) c.uint.encode(state, m.subscribedAt)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      channelKey: r0,
      channelName: (flags & 1) !== 0 ? c.string.decode(state) : null,
      avatar: (flags & 2) !== 0 ? c.string.decode(state) : null,
      subscribedAt: (flags & 4) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/subscribe-channel-request
const encoding105 = {
  preencode(state, m) {
    c.string.preencode(state, m.channelKey)
  },
  encode(state, m) {
    c.string.encode(state, m.channelKey)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      channelKey: r0
    }
  }
}

// @peartube/subscribe-channel-response
const encoding106 = encoding13

// @peartube/unsubscribe-channel-request
const encoding107 = encoding105

// @peartube/unsubscribe-channel-response
const encoding108 = encoding13

// @peartube/get-subscriptions-request
const encoding109 = encoding0

// @peartube/get-subscriptions-response.subscriptions
const encoding110_0 = c.array(c.frame(encoding104))

// @peartube/get-subscriptions-response
const encoding110 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte

    if (m.subscriptions) encoding110_0.preencode(state, m.subscriptions)
  },
  encode(state, m) {
    const flags = m.subscriptions ? 1 : 0

    c.uint.encode(state, flags)

    if (m.subscriptions) encoding110_0.encode(state, m.subscriptions)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      subscriptions: (flags & 1) !== 0 ? encoding110_0.decode(state) : null
    }
  }
}

// @peartube/playlist
const encoding111 = {
  preencode(state, m) {
    c.string.preencode(state, m.id)
    state.end++ // max flag is 8 so always one byte

    if (m.name) c.string.preencode(state, m.name)
    if (m.description) c.string.preencode(state, m.description)
    if (m.createdAt) c.uint.preencode(state, m.createdAt)
    if (m.updatedAt) c.uint.preencode(state, m.updatedAt)
  },
  encode(state, m) {
    const flags =
      (m.name ? 1 : 0) | (m.description ? 2 : 0) | (m.createdAt ? 4 : 0) | (m.updatedAt ? 8 : 0)

    c.string.encode(state, m.id)
    c.uint.encode(state, flags)

    if (m.name) c.string.encode(state, m.name)
    if (m.description) c.string.encode(state, m.description)
    if (m.createdAt) c.uint.encode(state, m.createdAt)
    if (m.updatedAt) c.uint.encode(state, m.updatedAt)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      id: r0,
      name: (flags & 1) !== 0 ? c.string.decode(state) : null,
      description: (flags & 2) !== 0 ? c.string.decode(state) : null,
      createdAt: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      updatedAt: (flags & 8) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/playlist-item
const encoding112 = {
  preencode(state, m) {
    c.string.preencode(state, m.playlistId)
    c.string.preencode(state, m.videoKey)
    state.end++ // max flag is 4 so always one byte

    if (m.channelKey) c.string.preencode(state, m.channelKey)
    if (m.videoId) c.string.preencode(state, m.videoId)
    if (m.addedAt) c.uint.preencode(state, m.addedAt)
  },
  encode(state, m) {
    const flags = (m.channelKey ? 1 : 0) | (m.videoId ? 2 : 0) | (m.addedAt ? 4 : 0)

    c.string.encode(state, m.playlistId)
    c.string.encode(state, m.videoKey)
    c.uint.encode(state, flags)

    if (m.channelKey) c.string.encode(state, m.channelKey)
    if (m.videoId) c.string.encode(state, m.videoId)
    if (m.addedAt) c.uint.encode(state, m.addedAt)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      playlistId: r0,
      videoKey: r1,
      channelKey: (flags & 1) !== 0 ? c.string.decode(state) : null,
      videoId: (flags & 2) !== 0 ? c.string.decode(state) : null,
      addedAt: (flags & 4) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/history-entry
const encoding113 = {
  preencode(state, m) {
    const flags =
      (m.eventId ? 1 : 0) |
      (m.channelKey ? 2 : 0) |
      (m.videoId ? 4 : 0) |
      (m.videoKey ? 8 : 0) |
      (m.title ? 16 : 0) |
      (m.duration ? 32 : 0) |
      (m.position ? 64 : 0) |
      (m.completed ? 128 : 0) |
      (m.timestamp ? 256 : 0) |
      (m.identity ? 512 : 0) |
      (m.saved ? 1024 : 0) |
      (m.order ? 2048 : 0)

    c.uint.preencode(state, flags)

    if (m.eventId) c.string.preencode(state, m.eventId)
    if (m.channelKey) c.string.preencode(state, m.channelKey)
    if (m.videoId) c.string.preencode(state, m.videoId)
    if (m.videoKey) c.string.preencode(state, m.videoKey)
    if (m.title) c.string.preencode(state, m.title)
    if (m.duration) c.uint.preencode(state, m.duration)
    if (m.position) c.uint.preencode(state, m.position)
    if (m.timestamp) c.uint.preencode(state, m.timestamp)
    if (m.identity) encoding113_9.preencode(state, m.identity)
    if (m.order) encoding113_11.preencode(state, m.order)
  },
  encode(state, m) {
    const flags =
      (m.eventId ? 1 : 0) |
      (m.channelKey ? 2 : 0) |
      (m.videoId ? 4 : 0) |
      (m.videoKey ? 8 : 0) |
      (m.title ? 16 : 0) |
      (m.duration ? 32 : 0) |
      (m.position ? 64 : 0) |
      (m.completed ? 128 : 0) |
      (m.timestamp ? 256 : 0) |
      (m.identity ? 512 : 0) |
      (m.saved ? 1024 : 0) |
      (m.order ? 2048 : 0)

    c.uint.encode(state, flags)

    if (m.eventId) c.string.encode(state, m.eventId)
    if (m.channelKey) c.string.encode(state, m.channelKey)
    if (m.videoId) c.string.encode(state, m.videoId)
    if (m.videoKey) c.string.encode(state, m.videoKey)
    if (m.title) c.string.encode(state, m.title)
    if (m.duration) c.uint.encode(state, m.duration)
    if (m.position) c.uint.encode(state, m.position)
    if (m.timestamp) c.uint.encode(state, m.timestamp)
    if (m.identity) encoding113_9.encode(state, m.identity)
    if (m.order) encoding113_11.encode(state, m.order)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      eventId: (flags & 1) !== 0 ? c.string.decode(state) : null,
      channelKey: (flags & 2) !== 0 ? c.string.decode(state) : null,
      videoId: (flags & 4) !== 0 ? c.string.decode(state) : null,
      videoKey: (flags & 8) !== 0 ? c.string.decode(state) : null,
      title: (flags & 16) !== 0 ? c.string.decode(state) : null,
      duration: (flags & 32) !== 0 ? c.uint.decode(state) : 0,
      position: (flags & 64) !== 0 ? c.uint.decode(state) : 0,
      completed: (flags & 128) !== 0,
      timestamp: (flags & 256) !== 0 ? c.uint.decode(state) : 0,
      identity: (flags & 512) !== 0 ? encoding113_9.decode(state) : null,
      saved: (flags & 1024) !== 0,
      order: (flags & 2048) !== 0 ? encoding113_11.decode(state) : null
    }
  }
}

// @peartube/personal-progress-identity
const encoding114 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.entityRef) c.string.preencode(state, m.entityRef)
    if (m.editionRef) c.string.preencode(state, m.editionRef)
    if (m.memberRef) c.string.preencode(state, m.memberRef)
  },
  encode(state, m) {
    const flags = (m.entityRef ? 1 : 0) | (m.editionRef ? 2 : 0) | (m.memberRef ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.entityRef) c.string.encode(state, m.entityRef)
    if (m.editionRef) c.string.encode(state, m.editionRef)
    if (m.memberRef) c.string.encode(state, m.memberRef)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      entityRef: (flags & 1) !== 0 ? c.string.decode(state) : null,
      editionRef: (flags & 2) !== 0 ? c.string.decode(state) : null,
      memberRef: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/personal-progress-order
const encoding115 = {
  preencode(state, m) {
    c.uint.preencode(state, m.playbackGeneration)
    c.uint.preencode(state, m.lamport)
    c.string.preencode(state, m.writerKey)
    state.end++ // max flag is 1 so always one byte
  },
  encode(state, m) {
    const flags = m.tombstone ? 1 : 0

    c.uint.encode(state, m.playbackGeneration)
    c.uint.encode(state, m.lamport)
    c.string.encode(state, m.writerKey)
    c.uint.encode(state, flags)
  },
  decode(state) {
    const r0 = c.uint.decode(state)
    const r1 = c.uint.decode(state)
    const r2 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      playbackGeneration: r0,
      lamport: r1,
      writerKey: r2,
      tombstone: (flags & 1) !== 0
    }
  }
}

// @peartube/resume-entry
const encoding116 = {
  preencode(state, m) {
    const flags =
      (m.channelKey ? 1 : 0) |
      (m.videoId ? 2 : 0) |
      (m.position ? 4 : 0) |
      (m.duration ? 8 : 0) |
      (m.completed ? 16 : 0) |
      (m.updatedAt ? 32 : 0) |
      (m.identity ? 64 : 0) |
      (m.saved ? 128 : 0) |
      (m.order ? 256 : 0)

    c.string.preencode(state, m.videoKey)
    c.uint.preencode(state, flags)

    if (m.channelKey) c.string.preencode(state, m.channelKey)
    if (m.videoId) c.string.preencode(state, m.videoId)
    if (m.position) c.uint.preencode(state, m.position)
    if (m.duration) c.uint.preencode(state, m.duration)
    if (m.updatedAt) c.uint.preencode(state, m.updatedAt)
    if (m.identity) encoding116_7.preencode(state, m.identity)
    if (m.order) encoding116_9.preencode(state, m.order)
  },
  encode(state, m) {
    const flags =
      (m.channelKey ? 1 : 0) |
      (m.videoId ? 2 : 0) |
      (m.position ? 4 : 0) |
      (m.duration ? 8 : 0) |
      (m.completed ? 16 : 0) |
      (m.updatedAt ? 32 : 0) |
      (m.identity ? 64 : 0) |
      (m.saved ? 128 : 0) |
      (m.order ? 256 : 0)

    c.string.encode(state, m.videoKey)
    c.uint.encode(state, flags)

    if (m.channelKey) c.string.encode(state, m.channelKey)
    if (m.videoId) c.string.encode(state, m.videoId)
    if (m.position) c.uint.encode(state, m.position)
    if (m.duration) c.uint.encode(state, m.duration)
    if (m.updatedAt) c.uint.encode(state, m.updatedAt)
    if (m.identity) encoding116_7.encode(state, m.identity)
    if (m.order) encoding116_9.encode(state, m.order)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      videoKey: r0,
      channelKey: (flags & 1) !== 0 ? c.string.decode(state) : null,
      videoId: (flags & 2) !== 0 ? c.string.decode(state) : null,
      position: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      duration: (flags & 8) !== 0 ? c.uint.decode(state) : 0,
      completed: (flags & 16) !== 0,
      updatedAt: (flags & 32) !== 0 ? c.uint.decode(state) : 0,
      identity: (flags & 64) !== 0 ? encoding116_7.decode(state) : null,
      saved: (flags & 128) !== 0,
      order: (flags & 256) !== 0 ? encoding116_9.decode(state) : null
    }
  }
}

// @peartube/personal-device
const encoding117 = {
  preencode(state, m) {
    c.string.preencode(state, m.keyHex)
    state.end++ // max flag is 4 so always one byte

    if (m.deviceName) c.string.preencode(state, m.deviceName)
    if (m.addedAt) c.uint.preencode(state, m.addedAt)
  },
  encode(state, m) {
    const flags = (m.deviceName ? 1 : 0) | (m.addedAt ? 2 : 0) | (m.self ? 4 : 0)

    c.string.encode(state, m.keyHex)
    c.uint.encode(state, flags)

    if (m.deviceName) c.string.encode(state, m.deviceName)
    if (m.addedAt) c.uint.encode(state, m.addedAt)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      keyHex: r0,
      deviceName: (flags & 1) !== 0 ? c.string.decode(state) : null,
      addedAt: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      self: (flags & 4) !== 0
    }
  }
}

// @peartube/personal-setting
const encoding118 = {
  preencode(state, m) {
    c.string.preencode(state, m.key)
    state.end++ // max flag is 1 so always one byte

    if (m.value) c.string.preencode(state, m.value)
  },
  encode(state, m) {
    const flags = m.value ? 1 : 0

    c.string.encode(state, m.key)
    c.uint.encode(state, flags)

    if (m.value) c.string.encode(state, m.value)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      key: r0,
      value: (flags & 1) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-playlists-request
const encoding119 = encoding0

// @peartube/get-playlists-response.playlists
const encoding120_0 = c.array(c.frame(encoding111))

// @peartube/get-playlists-response
const encoding120 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte

    if (m.playlists) encoding120_0.preencode(state, m.playlists)
  },
  encode(state, m) {
    const flags = m.playlists ? 1 : 0

    c.uint.encode(state, flags)

    if (m.playlists) encoding120_0.encode(state, m.playlists)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      playlists: (flags & 1) !== 0 ? encoding120_0.decode(state) : null
    }
  }
}

// @peartube/get-playlist-items-request
const encoding121 = {
  preencode(state, m) {
    c.string.preencode(state, m.playlistId)
  },
  encode(state, m) {
    c.string.encode(state, m.playlistId)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      playlistId: r0
    }
  }
}

// @peartube/get-playlist-items-response.items
const encoding122_0 = c.array(c.frame(encoding112))

// @peartube/get-playlist-items-response
const encoding122 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte

    if (m.items) encoding122_0.preencode(state, m.items)
  },
  encode(state, m) {
    const flags = m.items ? 1 : 0

    c.uint.encode(state, flags)

    if (m.items) encoding122_0.encode(state, m.items)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      items: (flags & 1) !== 0 ? encoding122_0.decode(state) : null
    }
  }
}

// @peartube/create-playlist-request
const encoding123 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte

    if (m.name) c.string.preencode(state, m.name)
    if (m.description) c.string.preencode(state, m.description)
  },
  encode(state, m) {
    const flags = (m.name ? 1 : 0) | (m.description ? 2 : 0)

    c.uint.encode(state, flags)

    if (m.name) c.string.encode(state, m.name)
    if (m.description) c.string.encode(state, m.description)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      name: (flags & 1) !== 0 ? c.string.decode(state) : null,
      description: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/create-playlist-response
const encoding124 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte

    if (m.id) c.string.preencode(state, m.id)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.id ? 2 : 0)

    c.uint.encode(state, flags)

    if (m.id) c.string.encode(state, m.id)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      id: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/update-playlist-request
const encoding125 = {
  preencode(state, m) {
    c.string.preencode(state, m.id)
    state.end++ // max flag is 2 so always one byte

    if (m.name) c.string.preencode(state, m.name)
    if (m.description) c.string.preencode(state, m.description)
  },
  encode(state, m) {
    const flags = (m.name ? 1 : 0) | (m.description ? 2 : 0)

    c.string.encode(state, m.id)
    c.uint.encode(state, flags)

    if (m.name) c.string.encode(state, m.name)
    if (m.description) c.string.encode(state, m.description)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      id: r0,
      name: (flags & 1) !== 0 ? c.string.decode(state) : null,
      description: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/update-playlist-response
const encoding126 = encoding13

// @peartube/delete-playlist-request
const encoding127 = {
  preencode(state, m) {
    c.string.preencode(state, m.id)
  },
  encode(state, m) {
    c.string.encode(state, m.id)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      id: r0
    }
  }
}

// @peartube/delete-playlist-response
const encoding128 = encoding13

// @peartube/add-to-playlist-request
const encoding129 = {
  preencode(state, m) {
    c.string.preencode(state, m.playlistId)
    state.end++ // max flag is 4 so always one byte

    if (m.channelKey) c.string.preencode(state, m.channelKey)
    if (m.videoId) c.string.preencode(state, m.videoId)
    if (m.videoKey) c.string.preencode(state, m.videoKey)
  },
  encode(state, m) {
    const flags = (m.channelKey ? 1 : 0) | (m.videoId ? 2 : 0) | (m.videoKey ? 4 : 0)

    c.string.encode(state, m.playlistId)
    c.uint.encode(state, flags)

    if (m.channelKey) c.string.encode(state, m.channelKey)
    if (m.videoId) c.string.encode(state, m.videoId)
    if (m.videoKey) c.string.encode(state, m.videoKey)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      playlistId: r0,
      channelKey: (flags & 1) !== 0 ? c.string.decode(state) : null,
      videoId: (flags & 2) !== 0 ? c.string.decode(state) : null,
      videoKey: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/add-to-playlist-response
const encoding130 = encoding13

// @peartube/remove-from-playlist-request
const encoding131 = {
  preencode(state, m) {
    c.string.preencode(state, m.playlistId)
    c.string.preencode(state, m.videoKey)
  },
  encode(state, m) {
    c.string.encode(state, m.playlistId)
    c.string.encode(state, m.videoKey)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)

    return {
      playlistId: r0,
      videoKey: r1
    }
  }
}

// @peartube/remove-from-playlist-response
const encoding132 = encoding13

// @peartube/log-watch-history-request
const encoding133 = {
  preencode(state, m) {
    const flags =
      (m.channelKey ? 1 : 0) |
      (m.videoId ? 2 : 0) |
      (m.videoKey ? 4 : 0) |
      (m.title ? 8 : 0) |
      (m.duration ? 16 : 0) |
      (m.position ? 32 : 0) |
      (m.completed ? 64 : 0) |
      (m.timestamp ? 128 : 0) |
      (m.identity ? 256 : 0) |
      (m.saved ? 512 : 0) |
      (m.playbackGeneration ? 1024 : 0) |
      (m.tombstone ? 2048 : 0)

    c.uint.preencode(state, flags)

    if (m.channelKey) c.string.preencode(state, m.channelKey)
    if (m.videoId) c.string.preencode(state, m.videoId)
    if (m.videoKey) c.string.preencode(state, m.videoKey)
    if (m.title) c.string.preencode(state, m.title)
    if (m.duration) c.uint.preencode(state, m.duration)
    if (m.position) c.uint.preencode(state, m.position)
    if (m.timestamp) c.uint.preencode(state, m.timestamp)
    if (m.identity) encoding133_8.preencode(state, m.identity)
    if (m.playbackGeneration) c.uint.preencode(state, m.playbackGeneration)
  },
  encode(state, m) {
    const flags =
      (m.channelKey ? 1 : 0) |
      (m.videoId ? 2 : 0) |
      (m.videoKey ? 4 : 0) |
      (m.title ? 8 : 0) |
      (m.duration ? 16 : 0) |
      (m.position ? 32 : 0) |
      (m.completed ? 64 : 0) |
      (m.timestamp ? 128 : 0) |
      (m.identity ? 256 : 0) |
      (m.saved ? 512 : 0) |
      (m.playbackGeneration ? 1024 : 0) |
      (m.tombstone ? 2048 : 0)

    c.uint.encode(state, flags)

    if (m.channelKey) c.string.encode(state, m.channelKey)
    if (m.videoId) c.string.encode(state, m.videoId)
    if (m.videoKey) c.string.encode(state, m.videoKey)
    if (m.title) c.string.encode(state, m.title)
    if (m.duration) c.uint.encode(state, m.duration)
    if (m.position) c.uint.encode(state, m.position)
    if (m.timestamp) c.uint.encode(state, m.timestamp)
    if (m.identity) encoding133_8.encode(state, m.identity)
    if (m.playbackGeneration) c.uint.encode(state, m.playbackGeneration)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      channelKey: (flags & 1) !== 0 ? c.string.decode(state) : null,
      videoId: (flags & 2) !== 0 ? c.string.decode(state) : null,
      videoKey: (flags & 4) !== 0 ? c.string.decode(state) : null,
      title: (flags & 8) !== 0 ? c.string.decode(state) : null,
      duration: (flags & 16) !== 0 ? c.uint.decode(state) : 0,
      position: (flags & 32) !== 0 ? c.uint.decode(state) : 0,
      completed: (flags & 64) !== 0,
      timestamp: (flags & 128) !== 0 ? c.uint.decode(state) : 0,
      identity: (flags & 256) !== 0 ? encoding133_8.decode(state) : null,
      saved: (flags & 512) !== 0,
      playbackGeneration: (flags & 1024) !== 0 ? c.uint.decode(state) : 0,
      tombstone: (flags & 2048) !== 0
    }
  }
}

// @peartube/log-watch-history-response
const encoding134 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte

    if (m.eventId) c.string.preencode(state, m.eventId)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.eventId ? 2 : 0)

    c.uint.encode(state, flags)

    if (m.eventId) c.string.encode(state, m.eventId)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      eventId: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-watch-history-request
const encoding135 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte

    if (m.limit) c.uint.preencode(state, m.limit)
  },
  encode(state, m) {
    const flags = m.limit ? 1 : 0

    c.uint.encode(state, flags)

    if (m.limit) c.uint.encode(state, m.limit)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      limit: (flags & 1) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/get-watch-history-response.entries
const encoding136_0 = c.array(c.frame(encoding113))

// @peartube/get-watch-history-response
const encoding136 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte

    if (m.entries) encoding136_0.preencode(state, m.entries)
  },
  encode(state, m) {
    const flags = m.entries ? 1 : 0

    c.uint.encode(state, flags)

    if (m.entries) encoding136_0.encode(state, m.entries)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      entries: (flags & 1) !== 0 ? encoding136_0.decode(state) : null
    }
  }
}

// @peartube/get-resume-position-request
const encoding137 = {
  preencode(state, m) {
    c.string.preencode(state, m.videoKey)
  },
  encode(state, m) {
    c.string.encode(state, m.videoKey)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      videoKey: r0
    }
  }
}

// @peartube/get-resume-position-response.resume
const encoding138_1 = c.frame(encoding116)

// @peartube/get-resume-position-response
const encoding138 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte

    if (m.resume) encoding138_1.preencode(state, m.resume)
  },
  encode(state, m) {
    const flags = (m.found ? 1 : 0) | (m.resume ? 2 : 0)

    c.uint.encode(state, flags)

    if (m.resume) encoding138_1.encode(state, m.resume)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      found: (flags & 1) !== 0,
      resume: (flags & 2) !== 0 ? encoding138_1.decode(state) : null
    }
  }
}

// @peartube/list-resume-positions-request
const encoding139 = encoding0

// @peartube/list-resume-positions-response.entries
const encoding140_0 = c.array(c.frame(encoding116))

// @peartube/list-resume-positions-response
const encoding140 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte

    if (m.entries) encoding140_0.preencode(state, m.entries)
  },
  encode(state, m) {
    const flags = m.entries ? 1 : 0

    c.uint.encode(state, flags)

    if (m.entries) encoding140_0.encode(state, m.entries)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      entries: (flags & 1) !== 0 ? encoding140_0.decode(state) : null
    }
  }
}

// @peartube/set-personal-setting-request
const encoding141 = encoding118

// @peartube/set-personal-setting-response
const encoding142 = encoding13

// @peartube/get-personal-settings-request
const encoding143 = encoding0

// @peartube/get-personal-settings-response.settings
const encoding144_0 = c.array(c.frame(encoding118))

// @peartube/get-personal-settings-response
const encoding144 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte

    if (m.settings) encoding144_0.preencode(state, m.settings)
  },
  encode(state, m) {
    const flags = m.settings ? 1 : 0

    c.uint.encode(state, flags)

    if (m.settings) encoding144_0.encode(state, m.settings)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      settings: (flags & 1) !== 0 ? encoding144_0.decode(state) : null
    }
  }
}

// @peartube/provision-personal-encryption-request
const encoding145 = {
  preencode(state, m) {
    c.string.preencode(state, m.secret)
    state.end++ // max flag is 2 so always one byte

    if (m.bootstrapKey) c.string.preencode(state, m.bootstrapKey)
  },
  encode(state, m) {
    const flags = (m.bootstrapKey ? 1 : 0) | (m.deviceLocal ? 2 : 0)

    c.string.encode(state, m.secret)
    c.uint.encode(state, flags)

    if (m.bootstrapKey) c.string.encode(state, m.bootstrapKey)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      secret: r0,
      bootstrapKey: (flags & 1) !== 0 ? c.string.decode(state) : null,
      deviceLocal: (flags & 2) !== 0
    }
  }
}

// @peartube/provision-personal-encryption-response
const encoding146 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (m.bootstrapKey) c.string.preencode(state, m.bootstrapKey)
    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) | (m.bootstrapKey ? 2 : 0) | (m.encrypted ? 4 : 0) | (m.error ? 8 : 0)

    c.uint.encode(state, flags)

    if (m.bootstrapKey) c.string.encode(state, m.bootstrapKey)
    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      bootstrapKey: (flags & 2) !== 0 ? c.string.decode(state) : null,
      encrypted: (flags & 4) !== 0,
      error: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/create-personal-device-invite-request
const encoding147 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte

    if (m.expiresInMs) c.uint.preencode(state, m.expiresInMs)
  },
  encode(state, m) {
    const flags = m.expiresInMs ? 1 : 0

    c.uint.encode(state, flags)

    if (m.expiresInMs) c.uint.encode(state, m.expiresInMs)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      expiresInMs: (flags & 1) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/create-personal-device-invite-response
const encoding148 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (m.inviteCode) c.string.preencode(state, m.inviteCode)
    if (m.expiresAt) c.uint.preencode(state, m.expiresAt)
    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) | (m.inviteCode ? 2 : 0) | (m.expiresAt ? 4 : 0) | (m.error ? 8 : 0)

    c.uint.encode(state, flags)

    if (m.inviteCode) c.string.encode(state, m.inviteCode)
    if (m.expiresAt) c.uint.encode(state, m.expiresAt)
    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      inviteCode: (flags & 2) !== 0 ? c.string.decode(state) : null,
      expiresAt: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      error: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/redeem-personal-device-invite-request
const encoding149 = {
  preencode(state, m) {
    c.string.preencode(state, m.inviteCode)
    state.end++ // max flag is 1 so always one byte

    if (m.deviceName) c.string.preencode(state, m.deviceName)
  },
  encode(state, m) {
    const flags = m.deviceName ? 1 : 0

    c.string.encode(state, m.inviteCode)
    c.uint.encode(state, flags)

    if (m.deviceName) c.string.encode(state, m.deviceName)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      inviteCode: r0,
      deviceName: (flags & 1) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/redeem-personal-device-invite-response
const encoding150 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (m.secret) c.string.preencode(state, m.secret)
    if (m.bootstrapKey) c.string.preencode(state, m.bootstrapKey)
    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) | (m.secret ? 2 : 0) | (m.bootstrapKey ? 4 : 0) | (m.error ? 8 : 0)

    c.uint.encode(state, flags)

    if (m.secret) c.string.encode(state, m.secret)
    if (m.bootstrapKey) c.string.encode(state, m.bootstrapKey)
    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      secret: (flags & 2) !== 0 ? c.string.decode(state) : null,
      bootstrapKey: (flags & 4) !== 0 ? c.string.decode(state) : null,
      error: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/list-personal-devices-request
const encoding151 = encoding0

// @peartube/list-personal-devices-response.devices
const encoding152_1 = c.array(c.frame(encoding117))

// @peartube/list-personal-devices-response
const encoding152 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.devices) encoding152_1.preencode(state, m.devices)
    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.devices ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.devices) encoding152_1.encode(state, m.devices)
    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      devices: (flags & 2) !== 0 ? encoding152_1.decode(state) : null,
      error: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/revoke-personal-device-request
const encoding153 = {
  preencode(state, m) {
    c.string.preencode(state, m.keyHex)
    c.string.preencode(state, m.secret)
    state.end++ // max flag is 1 so always one byte

    if (m.deviceName) c.string.preencode(state, m.deviceName)
  },
  encode(state, m) {
    const flags = m.deviceName ? 1 : 0

    c.string.encode(state, m.keyHex)
    c.string.encode(state, m.secret)
    c.uint.encode(state, flags)

    if (m.deviceName) c.string.encode(state, m.deviceName)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      keyHex: r0,
      secret: r1,
      deviceName: (flags & 1) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/revoke-personal-device-response
const encoding154 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (m.bootstrapKey) c.string.preencode(state, m.bootstrapKey)
    if (m.remainingDeviceCount) c.uint.preencode(state, m.remainingDeviceCount)
    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.bootstrapKey ? 2 : 0) |
      (m.remainingDeviceCount ? 4 : 0) |
      (m.error ? 8 : 0)

    c.uint.encode(state, flags)

    if (m.bootstrapKey) c.string.encode(state, m.bootstrapKey)
    if (m.remainingDeviceCount) c.uint.encode(state, m.remainingDeviceCount)
    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      bootstrapKey: (flags & 2) !== 0 ? c.string.decode(state) : null,
      remainingDeviceCount: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      error: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/join-channel-request
const encoding155 = encoding105

// @peartube/join-channel-response
const encoding156 = encoding13

// @peartube/hide-channel-request
const encoding157 = encoding105

// @peartube/hide-channel-response
const encoding158 = encoding13

// @peartube/get-channel-meta-request
const encoding159 = encoding38

// @peartube/get-channel-meta-response
const encoding160 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.name) c.string.preencode(state, m.name)
    if (m.description) c.string.preencode(state, m.description)
    if (m.videoCount) c.uint.preencode(state, m.videoCount)
  },
  encode(state, m) {
    const flags = (m.name ? 1 : 0) | (m.description ? 2 : 0) | (m.videoCount ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.name) c.string.encode(state, m.name)
    if (m.description) c.string.encode(state, m.description)
    if (m.videoCount) c.uint.encode(state, m.videoCount)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      name: (flags & 1) !== 0 ? c.string.decode(state) : null,
      description: (flags & 2) !== 0 ? c.string.decode(state) : null,
      videoCount: (flags & 4) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/get-swarm-status-request
const encoding161 = encoding0

// @peartube/get-swarm-status-response
const encoding162 = {
  preencode(state, m) {
    const flags =
      (m.connected ? 1 : 0) |
      (m.peerCount ? 2 : 0) |
      (m.swarmConnections ? 4 : 0) |
      (m.swarmPeers ? 8 : 0) |
      (m.channelsLoaded ? 16 : 0) |
      (m.swarmOffline ? 32 : 0) |
      (m.swarmOfflineReason ? 64 : 0) |
      (m.swarmListenResolved ? 128 : 0) |
      (m.peerPoolJoined ? 256 : 0) |
      (m.networkJson ? 512 : 0) |
      (m.startupTimingJson ? 1024 : 0) |
      (m.doctorJson ? 2048 : 0) |
      (m.recommendedBoundary ? 4096 : 0)

    c.uint.preencode(state, flags)

    if (m.peerCount) c.uint.preencode(state, m.peerCount)
    if (m.swarmConnections) c.uint.preencode(state, m.swarmConnections)
    if (m.swarmPeers) c.uint.preencode(state, m.swarmPeers)
    if (m.channelsLoaded) c.uint.preencode(state, m.channelsLoaded)
    if (m.swarmOfflineReason) c.string.preencode(state, m.swarmOfflineReason)
    if (m.networkJson) c.string.preencode(state, m.networkJson)
    if (m.startupTimingJson) c.string.preencode(state, m.startupTimingJson)
    if (m.doctorJson) c.string.preencode(state, m.doctorJson)
    if (m.recommendedBoundary) c.string.preencode(state, m.recommendedBoundary)
  },
  encode(state, m) {
    const flags =
      (m.connected ? 1 : 0) |
      (m.peerCount ? 2 : 0) |
      (m.swarmConnections ? 4 : 0) |
      (m.swarmPeers ? 8 : 0) |
      (m.channelsLoaded ? 16 : 0) |
      (m.swarmOffline ? 32 : 0) |
      (m.swarmOfflineReason ? 64 : 0) |
      (m.swarmListenResolved ? 128 : 0) |
      (m.peerPoolJoined ? 256 : 0) |
      (m.networkJson ? 512 : 0) |
      (m.startupTimingJson ? 1024 : 0) |
      (m.doctorJson ? 2048 : 0) |
      (m.recommendedBoundary ? 4096 : 0)

    c.uint.encode(state, flags)

    if (m.peerCount) c.uint.encode(state, m.peerCount)
    if (m.swarmConnections) c.uint.encode(state, m.swarmConnections)
    if (m.swarmPeers) c.uint.encode(state, m.swarmPeers)
    if (m.channelsLoaded) c.uint.encode(state, m.channelsLoaded)
    if (m.swarmOfflineReason) c.string.encode(state, m.swarmOfflineReason)
    if (m.networkJson) c.string.encode(state, m.networkJson)
    if (m.startupTimingJson) c.string.encode(state, m.startupTimingJson)
    if (m.doctorJson) c.string.encode(state, m.doctorJson)
    if (m.recommendedBoundary) c.string.encode(state, m.recommendedBoundary)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      connected: (flags & 1) !== 0,
      peerCount: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      swarmConnections: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      swarmPeers: (flags & 8) !== 0 ? c.uint.decode(state) : 0,
      channelsLoaded: (flags & 16) !== 0 ? c.uint.decode(state) : 0,
      swarmOffline: (flags & 32) !== 0,
      swarmOfflineReason: (flags & 64) !== 0 ? c.string.decode(state) : null,
      swarmListenResolved: (flags & 128) !== 0,
      peerPoolJoined: (flags & 256) !== 0,
      networkJson: (flags & 512) !== 0 ? c.string.decode(state) : null,
      startupTimingJson: (flags & 1024) !== 0 ? c.string.decode(state) : null,
      doctorJson: (flags & 2048) !== 0 ? c.string.decode(state) : null,
      recommendedBoundary: (flags & 4096) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/prefetch-video-request
const encoding163 = encoding85

// @peartube/prefetch-video-response
const encoding164 = encoding13

// @peartube/video-stats
const encoding165 = {
  preencode(state, m) {
    const flags =
      (m.videoId ? 1 : 0) |
      (m.channelKey ? 2 : 0) |
      (m.status ? 4 : 0) |
      (m.progress ? 8 : 0) |
      (m.totalBlocks ? 16 : 0) |
      (m.downloadedBlocks ? 32 : 0) |
      (m.totalBytes ? 64 : 0) |
      (m.downloadedBytes ? 128 : 0) |
      (m.peerCount ? 256 : 0) |
      (m.blobCoreKey ? 512 : 0) |
      (m.blobPeerIdsJson ? 1024 : 0) |
      (m.blobPeersJson ? 2048 : 0) |
      (m.speedMBps ? 4096 : 0) |
      (m.uploadSpeedMBps ? 8192 : 0) |
      (m.elapsed ? 16384 : 0) |
      (m.isComplete ? 32768 : 0)

    c.uint.preencode(state, flags)

    if (m.videoId) c.string.preencode(state, m.videoId)
    if (m.channelKey) c.string.preencode(state, m.channelKey)
    if (m.status) c.string.preencode(state, m.status)
    if (m.progress) c.uint.preencode(state, m.progress)
    if (m.totalBlocks) c.uint.preencode(state, m.totalBlocks)
    if (m.downloadedBlocks) c.uint.preencode(state, m.downloadedBlocks)
    if (m.totalBytes) c.uint.preencode(state, m.totalBytes)
    if (m.downloadedBytes) c.uint.preencode(state, m.downloadedBytes)
    if (m.peerCount) c.uint.preencode(state, m.peerCount)
    if (m.blobCoreKey) c.string.preencode(state, m.blobCoreKey)
    if (m.blobPeerIdsJson) c.string.preencode(state, m.blobPeerIdsJson)
    if (m.blobPeersJson) c.string.preencode(state, m.blobPeersJson)
    if (m.speedMBps) c.string.preencode(state, m.speedMBps)
    if (m.uploadSpeedMBps) c.string.preencode(state, m.uploadSpeedMBps)
    if (m.elapsed) c.uint.preencode(state, m.elapsed)
  },
  encode(state, m) {
    const flags =
      (m.videoId ? 1 : 0) |
      (m.channelKey ? 2 : 0) |
      (m.status ? 4 : 0) |
      (m.progress ? 8 : 0) |
      (m.totalBlocks ? 16 : 0) |
      (m.downloadedBlocks ? 32 : 0) |
      (m.totalBytes ? 64 : 0) |
      (m.downloadedBytes ? 128 : 0) |
      (m.peerCount ? 256 : 0) |
      (m.blobCoreKey ? 512 : 0) |
      (m.blobPeerIdsJson ? 1024 : 0) |
      (m.blobPeersJson ? 2048 : 0) |
      (m.speedMBps ? 4096 : 0) |
      (m.uploadSpeedMBps ? 8192 : 0) |
      (m.elapsed ? 16384 : 0) |
      (m.isComplete ? 32768 : 0)

    c.uint.encode(state, flags)

    if (m.videoId) c.string.encode(state, m.videoId)
    if (m.channelKey) c.string.encode(state, m.channelKey)
    if (m.status) c.string.encode(state, m.status)
    if (m.progress) c.uint.encode(state, m.progress)
    if (m.totalBlocks) c.uint.encode(state, m.totalBlocks)
    if (m.downloadedBlocks) c.uint.encode(state, m.downloadedBlocks)
    if (m.totalBytes) c.uint.encode(state, m.totalBytes)
    if (m.downloadedBytes) c.uint.encode(state, m.downloadedBytes)
    if (m.peerCount) c.uint.encode(state, m.peerCount)
    if (m.blobCoreKey) c.string.encode(state, m.blobCoreKey)
    if (m.blobPeerIdsJson) c.string.encode(state, m.blobPeerIdsJson)
    if (m.blobPeersJson) c.string.encode(state, m.blobPeersJson)
    if (m.speedMBps) c.string.encode(state, m.speedMBps)
    if (m.uploadSpeedMBps) c.string.encode(state, m.uploadSpeedMBps)
    if (m.elapsed) c.uint.encode(state, m.elapsed)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      videoId: (flags & 1) !== 0 ? c.string.decode(state) : null,
      channelKey: (flags & 2) !== 0 ? c.string.decode(state) : null,
      status: (flags & 4) !== 0 ? c.string.decode(state) : null,
      progress: (flags & 8) !== 0 ? c.uint.decode(state) : 0,
      totalBlocks: (flags & 16) !== 0 ? c.uint.decode(state) : 0,
      downloadedBlocks: (flags & 32) !== 0 ? c.uint.decode(state) : 0,
      totalBytes: (flags & 64) !== 0 ? c.uint.decode(state) : 0,
      downloadedBytes: (flags & 128) !== 0 ? c.uint.decode(state) : 0,
      peerCount: (flags & 256) !== 0 ? c.uint.decode(state) : 0,
      blobCoreKey: (flags & 512) !== 0 ? c.string.decode(state) : null,
      blobPeerIdsJson: (flags & 1024) !== 0 ? c.string.decode(state) : null,
      blobPeersJson: (flags & 2048) !== 0 ? c.string.decode(state) : null,
      speedMBps: (flags & 4096) !== 0 ? c.string.decode(state) : null,
      uploadSpeedMBps: (flags & 8192) !== 0 ? c.string.decode(state) : null,
      elapsed: (flags & 16384) !== 0 ? c.uint.decode(state) : 0,
      isComplete: (flags & 32768) !== 0
    }
  }
}

// @peartube/get-video-stats-request
const encoding166 = {
  preencode(state, m) {
    c.string.preencode(state, m.channelKey)
    c.string.preencode(state, m.videoId)
  },
  encode(state, m) {
    c.string.encode(state, m.channelKey)
    c.string.encode(state, m.videoId)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)

    return {
      channelKey: r0,
      videoId: r1
    }
  }
}

// @peartube/get-video-stats-response
const encoding167 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte

    if (m.stats) encoding167_0.preencode(state, m.stats)
  },
  encode(state, m) {
    const flags = m.stats ? 1 : 0

    c.uint.encode(state, flags)

    if (m.stats) encoding167_0.encode(state, m.stats)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      stats: (flags & 1) !== 0 ? encoding167_0.decode(state) : null
    }
  }
}

// @peartube/seeding-config
const encoding168 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.maxStorage) c.uint.preencode(state, m.maxStorage)
    if (m.maxBandwidth) c.uint.preencode(state, m.maxBandwidth)
  },
  encode(state, m) {
    const flags = (m.enabled ? 1 : 0) | (m.maxStorage ? 2 : 0) | (m.maxBandwidth ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.maxStorage) c.uint.encode(state, m.maxStorage)
    if (m.maxBandwidth) c.uint.encode(state, m.maxBandwidth)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      enabled: (flags & 1) !== 0,
      maxStorage: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      maxBandwidth: (flags & 4) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/seeding-status
const encoding169 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (m.usedStorage) c.uint.preencode(state, m.usedStorage)
    if (m.maxStorage) c.uint.preencode(state, m.maxStorage)
    if (m.seedingCount) c.uint.preencode(state, m.seedingCount)
  },
  encode(state, m) {
    const flags =
      (m.enabled ? 1 : 0) |
      (m.usedStorage ? 2 : 0) |
      (m.maxStorage ? 4 : 0) |
      (m.seedingCount ? 8 : 0)

    c.uint.encode(state, flags)

    if (m.usedStorage) c.uint.encode(state, m.usedStorage)
    if (m.maxStorage) c.uint.encode(state, m.maxStorage)
    if (m.seedingCount) c.uint.encode(state, m.seedingCount)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      enabled: (flags & 1) !== 0,
      usedStorage: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      maxStorage: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      seedingCount: (flags & 8) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/get-seeding-status-request
const encoding170 = encoding0

// @peartube/get-seeding-status-response.status
const encoding171_0 = c.frame(encoding169)

// @peartube/get-seeding-status-response
const encoding171 = {
  preencode(state, m) {
    encoding171_0.preencode(state, m.status)
  },
  encode(state, m) {
    encoding171_0.encode(state, m.status)
  },
  decode(state) {
    const r0 = encoding171_0.decode(state)

    return {
      status: r0
    }
  }
}

// @peartube/set-seeding-config-request.config
const encoding172_0 = c.frame(encoding168)

// @peartube/set-seeding-config-request
const encoding172 = {
  preencode(state, m) {
    encoding172_0.preencode(state, m.config)
  },
  encode(state, m) {
    encoding172_0.encode(state, m.config)
  },
  decode(state) {
    const r0 = encoding172_0.decode(state)

    return {
      config: r0
    }
  }
}

// @peartube/set-seeding-config-response
const encoding173 = encoding13

// @peartube/transcode-settings
const encoding174 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (m.videoToolboxDecodeSource) c.string.preencode(state, m.videoToolboxDecodeSource)
  },
  encode(state, m) {
    const flags =
      (m.videoToolboxDecodeEnabled ? 1 : 0) |
      (m.videoToolboxDecodeLocked ? 2 : 0) |
      (m.videoToolboxDecodeDefault ? 4 : 0) |
      (m.videoToolboxDecodeSource ? 8 : 0)

    c.uint.encode(state, flags)

    if (m.videoToolboxDecodeSource) c.string.encode(state, m.videoToolboxDecodeSource)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      videoToolboxDecodeEnabled: (flags & 1) !== 0,
      videoToolboxDecodeLocked: (flags & 2) !== 0,
      videoToolboxDecodeDefault: (flags & 4) !== 0,
      videoToolboxDecodeSource: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-transcode-settings-request
const encoding175 = encoding0

// @peartube/get-transcode-settings-response.settings
const encoding176_0 = c.frame(encoding174)

// @peartube/get-transcode-settings-response
const encoding176 = {
  preencode(state, m) {
    encoding176_0.preencode(state, m.settings)
  },
  encode(state, m) {
    encoding176_0.encode(state, m.settings)
  },
  decode(state) {
    const r0 = encoding176_0.decode(state)

    return {
      settings: r0
    }
  }
}

// @peartube/set-transcode-settings-request
const encoding177 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte
  },
  encode(state, m) {
    const flags = m.videoToolboxDecodeEnabled ? 1 : 0

    c.uint.encode(state, flags)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      videoToolboxDecodeEnabled: (flags & 1) !== 0
    }
  }
}

// @peartube/set-transcode-settings-response.settings
const encoding178_1 = encoding176_0

// @peartube/set-transcode-settings-response
const encoding178 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte
    encoding178_1.preencode(state, m.settings)

    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.error ? 2 : 0)

    c.uint.encode(state, flags)
    encoding178_1.encode(state, m.settings)

    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      settings: encoding178_1.decode(state),
      error: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/pin-channel-request
const encoding179 = encoding105

// @peartube/pin-channel-response
const encoding180 = encoding13

// @peartube/unpin-channel-request
const encoding181 = encoding105

// @peartube/unpin-channel-response
const encoding182 = encoding13

// @peartube/get-pinned-channels-request
const encoding183 = encoding0

// @peartube/get-pinned-channels-response.channels
const encoding184_0 = encoding44_11

// @peartube/get-pinned-channels-response
const encoding184 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte

    if (m.channels) encoding184_0.preencode(state, m.channels)
  },
  encode(state, m) {
    const flags = m.channels ? 1 : 0

    c.uint.encode(state, flags)

    if (m.channels) encoding184_0.encode(state, m.channels)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      channels: (flags & 1) !== 0 ? encoding184_0.decode(state) : null
    }
  }
}

// @peartube/get-storage-stats-request
const encoding185 = encoding0

// @peartube/get-storage-stats-response
const encoding186 = {
  preencode(state, m) {
    const flags =
      (m.totalStorageBytes ? 1 : 0) |
      (m.totalStorageGB ? 2 : 0) |
      (m.untrackedStorageBytes ? 4 : 0) |
      (m.untrackedStorageGB ? 8 : 0) |
      (m.ownedOriginalBytes ? 16 : 0) |
      (m.immutablePublicationBytes ? 32 : 0) |
      (m.pledgedArchiveBytes ? 64 : 0) |
      (m.localCacheBytes ? 128 : 0) |
      (m.thumbnailBytes ? 256 : 0) |
      (m.indexBytes ? 512 : 0) |
      (m.temporaryTransferBytes ? 1024 : 0) |
      (m.totalCategorizedBytes ? 2048 : 0) |
      (m.evictableBytes ? 4096 : 0) |
      (m.protectedBytes ? 8192 : 0)

    c.uint.preencode(state, m.usedBytes)
    c.uint.preencode(state, m.maxBytes)
    c.string.preencode(state, m.usedGB)
    c.uint.preencode(state, m.maxGB)
    c.uint.preencode(state, m.seedCount)
    c.uint.preencode(state, m.pinnedCount)
    c.uint.preencode(state, flags)

    if (m.totalStorageBytes) c.uint.preencode(state, m.totalStorageBytes)
    if (m.totalStorageGB) c.string.preencode(state, m.totalStorageGB)
    if (m.untrackedStorageBytes) c.uint.preencode(state, m.untrackedStorageBytes)
    if (m.untrackedStorageGB) c.string.preencode(state, m.untrackedStorageGB)
    if (m.ownedOriginalBytes) c.uint.preencode(state, m.ownedOriginalBytes)
    if (m.immutablePublicationBytes) c.uint.preencode(state, m.immutablePublicationBytes)
    if (m.pledgedArchiveBytes) c.uint.preencode(state, m.pledgedArchiveBytes)
    if (m.localCacheBytes) c.uint.preencode(state, m.localCacheBytes)
    if (m.thumbnailBytes) c.uint.preencode(state, m.thumbnailBytes)
    if (m.indexBytes) c.uint.preencode(state, m.indexBytes)
    if (m.temporaryTransferBytes) c.uint.preencode(state, m.temporaryTransferBytes)
    if (m.totalCategorizedBytes) c.uint.preencode(state, m.totalCategorizedBytes)
    if (m.evictableBytes) c.uint.preencode(state, m.evictableBytes)
    if (m.protectedBytes) c.uint.preencode(state, m.protectedBytes)
  },
  encode(state, m) {
    const flags =
      (m.totalStorageBytes ? 1 : 0) |
      (m.totalStorageGB ? 2 : 0) |
      (m.untrackedStorageBytes ? 4 : 0) |
      (m.untrackedStorageGB ? 8 : 0) |
      (m.ownedOriginalBytes ? 16 : 0) |
      (m.immutablePublicationBytes ? 32 : 0) |
      (m.pledgedArchiveBytes ? 64 : 0) |
      (m.localCacheBytes ? 128 : 0) |
      (m.thumbnailBytes ? 256 : 0) |
      (m.indexBytes ? 512 : 0) |
      (m.temporaryTransferBytes ? 1024 : 0) |
      (m.totalCategorizedBytes ? 2048 : 0) |
      (m.evictableBytes ? 4096 : 0) |
      (m.protectedBytes ? 8192 : 0)

    c.uint.encode(state, m.usedBytes)
    c.uint.encode(state, m.maxBytes)
    c.string.encode(state, m.usedGB)
    c.uint.encode(state, m.maxGB)
    c.uint.encode(state, m.seedCount)
    c.uint.encode(state, m.pinnedCount)
    c.uint.encode(state, flags)

    if (m.totalStorageBytes) c.uint.encode(state, m.totalStorageBytes)
    if (m.totalStorageGB) c.string.encode(state, m.totalStorageGB)
    if (m.untrackedStorageBytes) c.uint.encode(state, m.untrackedStorageBytes)
    if (m.untrackedStorageGB) c.string.encode(state, m.untrackedStorageGB)
    if (m.ownedOriginalBytes) c.uint.encode(state, m.ownedOriginalBytes)
    if (m.immutablePublicationBytes) c.uint.encode(state, m.immutablePublicationBytes)
    if (m.pledgedArchiveBytes) c.uint.encode(state, m.pledgedArchiveBytes)
    if (m.localCacheBytes) c.uint.encode(state, m.localCacheBytes)
    if (m.thumbnailBytes) c.uint.encode(state, m.thumbnailBytes)
    if (m.indexBytes) c.uint.encode(state, m.indexBytes)
    if (m.temporaryTransferBytes) c.uint.encode(state, m.temporaryTransferBytes)
    if (m.totalCategorizedBytes) c.uint.encode(state, m.totalCategorizedBytes)
    if (m.evictableBytes) c.uint.encode(state, m.evictableBytes)
    if (m.protectedBytes) c.uint.encode(state, m.protectedBytes)
  },
  decode(state) {
    const r0 = c.uint.decode(state)
    const r1 = c.uint.decode(state)
    const r2 = c.string.decode(state)
    const r3 = c.uint.decode(state)
    const r4 = c.uint.decode(state)
    const r5 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      usedBytes: r0,
      maxBytes: r1,
      usedGB: r2,
      maxGB: r3,
      seedCount: r4,
      pinnedCount: r5,
      totalStorageBytes: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      totalStorageGB: (flags & 2) !== 0 ? c.string.decode(state) : null,
      untrackedStorageBytes: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      untrackedStorageGB: (flags & 8) !== 0 ? c.string.decode(state) : null,
      ownedOriginalBytes: (flags & 16) !== 0 ? c.uint.decode(state) : 0,
      immutablePublicationBytes: (flags & 32) !== 0 ? c.uint.decode(state) : 0,
      pledgedArchiveBytes: (flags & 64) !== 0 ? c.uint.decode(state) : 0,
      localCacheBytes: (flags & 128) !== 0 ? c.uint.decode(state) : 0,
      thumbnailBytes: (flags & 256) !== 0 ? c.uint.decode(state) : 0,
      indexBytes: (flags & 512) !== 0 ? c.uint.decode(state) : 0,
      temporaryTransferBytes: (flags & 1024) !== 0 ? c.uint.decode(state) : 0,
      totalCategorizedBytes: (flags & 2048) !== 0 ? c.uint.decode(state) : 0,
      evictableBytes: (flags & 4096) !== 0 ? c.uint.decode(state) : 0,
      protectedBytes: (flags & 8192) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/set-storage-limit-request
const encoding187 = {
  preencode(state, m) {
    c.uint.preencode(state, m.maxGB)
  },
  encode(state, m) {
    c.uint.encode(state, m.maxGB)
  },
  decode(state) {
    const r0 = c.uint.decode(state)

    return {
      maxGB: r0
    }
  }
}

// @peartube/set-storage-limit-response
const encoding188 = encoding13

// @peartube/get-migration-status-request
const encoding189 = {
  preencode(state, m) {
    c.string.preencode(state, m.migrationId)
  },
  encode(state, m) {
    c.string.encode(state, m.migrationId)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      migrationId: r0
    }
  }
}

// @peartube/get-migration-status-response
const encoding190 = {
  preencode(state, m) {
    state.end++ // max flag is 16 so always one byte
    c.string.preencode(state, m.migrationId)
    c.string.preencode(state, m.state)
    c.uint.preencode(state, m.version)
    c.uint.preencode(state, m.processedCount)
    c.uint.preencode(state, m.importedCount)
    c.uint.preencode(state, m.skippedCount)
    c.uint.preencode(state, m.quarantinedCount)
    c.uint.preencode(state, m.unsupportedCount)
    c.uint.preencode(state, m.remainingCount)
    c.uint.preencode(state, m.updatedAt)

    if (m.errorCode) c.string.preencode(state, m.errorCode)
    if (m.errorMessage) c.string.preencode(state, m.errorMessage)
    if (m.reportDigest) c.string.preencode(state, m.reportDigest)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.retryable ? 2 : 0) |
      (m.errorCode ? 4 : 0) |
      (m.errorMessage ? 8 : 0) |
      (m.reportDigest ? 16 : 0)

    c.uint.encode(state, flags)
    c.string.encode(state, m.migrationId)
    c.string.encode(state, m.state)
    c.uint.encode(state, m.version)
    c.uint.encode(state, m.processedCount)
    c.uint.encode(state, m.importedCount)
    c.uint.encode(state, m.skippedCount)
    c.uint.encode(state, m.quarantinedCount)
    c.uint.encode(state, m.unsupportedCount)
    c.uint.encode(state, m.remainingCount)
    c.uint.encode(state, m.updatedAt)

    if (m.errorCode) c.string.encode(state, m.errorCode)
    if (m.errorMessage) c.string.encode(state, m.errorMessage)
    if (m.reportDigest) c.string.encode(state, m.reportDigest)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      migrationId: c.string.decode(state),
      state: c.string.decode(state),
      version: c.uint.decode(state),
      processedCount: c.uint.decode(state),
      importedCount: c.uint.decode(state),
      skippedCount: c.uint.decode(state),
      quarantinedCount: c.uint.decode(state),
      unsupportedCount: c.uint.decode(state),
      remainingCount: c.uint.decode(state),
      retryable: (flags & 2) !== 0,
      updatedAt: c.uint.decode(state),
      errorCode: (flags & 4) !== 0 ? c.string.decode(state) : null,
      errorMessage: (flags & 8) !== 0 ? c.string.decode(state) : null,
      reportDigest: (flags & 16) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/retry-migration-request
const encoding191 = encoding189

// @peartube/retry-migration-response
const encoding192 = {
  preencode(state, m) {
    state.end++ // max flag is 32 so always one byte
    c.string.preencode(state, m.migrationId)
    c.string.preencode(state, m.state)
    c.uint.preencode(state, m.version)
    c.uint.preencode(state, m.processedCount)
    c.uint.preencode(state, m.importedCount)
    c.uint.preencode(state, m.skippedCount)
    c.uint.preencode(state, m.quarantinedCount)
    c.uint.preencode(state, m.unsupportedCount)
    c.uint.preencode(state, m.remainingCount)
    c.uint.preencode(state, m.updatedAt)

    if (m.errorCode) c.string.preencode(state, m.errorCode)
    if (m.errorMessage) c.string.preencode(state, m.errorMessage)
    if (m.reportDigest) c.string.preencode(state, m.reportDigest)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.retryable ? 2 : 0) |
      (m.joined ? 4 : 0) |
      (m.errorCode ? 8 : 0) |
      (m.errorMessage ? 16 : 0) |
      (m.reportDigest ? 32 : 0)

    c.uint.encode(state, flags)
    c.string.encode(state, m.migrationId)
    c.string.encode(state, m.state)
    c.uint.encode(state, m.version)
    c.uint.encode(state, m.processedCount)
    c.uint.encode(state, m.importedCount)
    c.uint.encode(state, m.skippedCount)
    c.uint.encode(state, m.quarantinedCount)
    c.uint.encode(state, m.unsupportedCount)
    c.uint.encode(state, m.remainingCount)
    c.uint.encode(state, m.updatedAt)

    if (m.errorCode) c.string.encode(state, m.errorCode)
    if (m.errorMessage) c.string.encode(state, m.errorMessage)
    if (m.reportDigest) c.string.encode(state, m.reportDigest)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      migrationId: c.string.decode(state),
      state: c.string.decode(state),
      version: c.uint.decode(state),
      processedCount: c.uint.decode(state),
      importedCount: c.uint.decode(state),
      skippedCount: c.uint.decode(state),
      quarantinedCount: c.uint.decode(state),
      unsupportedCount: c.uint.decode(state),
      remainingCount: c.uint.decode(state),
      retryable: (flags & 2) !== 0,
      updatedAt: c.uint.decode(state),
      joined: (flags & 4) !== 0,
      errorCode: (flags & 8) !== 0 ? c.string.decode(state) : null,
      errorMessage: (flags & 16) !== 0 ? c.string.decode(state) : null,
      reportDigest: (flags & 32) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/export-migration-report-request
const encoding193 = encoding189

// @peartube/export-migration-report-response
const encoding194 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte
    c.string.preencode(state, m.migrationId)

    if (m.reportBytes) c.buffer.preencode(state, m.reportBytes)
    if (m.reportDigest) c.string.preencode(state, m.reportDigest)
    if (m.errorCode) c.string.preencode(state, m.errorCode)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.reportBytes ? 2 : 0) |
      (m.reportDigest ? 4 : 0) |
      (m.errorCode ? 8 : 0)

    c.uint.encode(state, flags)
    c.string.encode(state, m.migrationId)

    if (m.reportBytes) c.buffer.encode(state, m.reportBytes)
    if (m.reportDigest) c.string.encode(state, m.reportDigest)
    if (m.errorCode) c.string.encode(state, m.errorCode)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      migrationId: c.string.decode(state),
      reportBytes: (flags & 2) !== 0 ? c.buffer.decode(state) : null,
      reportDigest: (flags & 4) !== 0 ? c.string.decode(state) : null,
      errorCode: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-publisher-device-status-request
const encoding195 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte

    if (m.publisherId) c.buffer.preencode(state, m.publisherId)
    if (m.devicePublicKey) c.buffer.preencode(state, m.devicePublicKey)
  },
  encode(state, m) {
    const flags = (m.publisherId ? 1 : 0) | (m.devicePublicKey ? 2 : 0)

    c.uint.encode(state, flags)

    if (m.publisherId) c.buffer.encode(state, m.publisherId)
    if (m.devicePublicKey) c.buffer.encode(state, m.devicePublicKey)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      publisherId: (flags & 1) !== 0 ? c.buffer.decode(state) : null,
      devicePublicKey: (flags & 2) !== 0 ? c.buffer.decode(state) : null
    }
  }
}

// @peartube/get-publisher-device-status-response
const encoding196 = {
  preencode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.publisherId ? 2 : 0) |
      (m.devicePublicKey ? 4 : 0) |
      (m.reasonCode ? 8 : 0) |
      (m.canPublish ? 16 : 0) |
      (m.canPlayLocal ? 32 : 0) |
      (m.canExportLocal ? 64 : 0) |
      (m.canDeleteLocal ? 128 : 0) |
      (m.canRootTransition ? 256 : 0) |
      (m.catalogEpoch ? 512 : 0) |
      (m.policyEpoch ? 1024 : 0) |
      (m.admissionExpiresAt ? 2048 : 0) |
      (m.revocationCutoff ? 4096 : 0) |
      (m.legacyImportState ? 8192 : 0)

    c.uint.preencode(state, flags)

    if (m.publisherId) c.buffer.preencode(state, m.publisherId)
    if (m.devicePublicKey) c.buffer.preencode(state, m.devicePublicKey)
    c.string.preencode(state, m.status)
    if (m.reasonCode) c.string.preencode(state, m.reasonCode)
    if (m.catalogEpoch) c.uint.preencode(state, m.catalogEpoch)
    if (m.policyEpoch) c.uint.preencode(state, m.policyEpoch)
    if (m.admissionExpiresAt) c.uint.preencode(state, m.admissionExpiresAt)
    if (m.revocationCutoff) c.uint.preencode(state, m.revocationCutoff)
    if (m.legacyImportState) c.string.preencode(state, m.legacyImportState)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.publisherId ? 2 : 0) |
      (m.devicePublicKey ? 4 : 0) |
      (m.reasonCode ? 8 : 0) |
      (m.canPublish ? 16 : 0) |
      (m.canPlayLocal ? 32 : 0) |
      (m.canExportLocal ? 64 : 0) |
      (m.canDeleteLocal ? 128 : 0) |
      (m.canRootTransition ? 256 : 0) |
      (m.catalogEpoch ? 512 : 0) |
      (m.policyEpoch ? 1024 : 0) |
      (m.admissionExpiresAt ? 2048 : 0) |
      (m.revocationCutoff ? 4096 : 0) |
      (m.legacyImportState ? 8192 : 0)

    c.uint.encode(state, flags)

    if (m.publisherId) c.buffer.encode(state, m.publisherId)
    if (m.devicePublicKey) c.buffer.encode(state, m.devicePublicKey)
    c.string.encode(state, m.status)
    if (m.reasonCode) c.string.encode(state, m.reasonCode)
    if (m.catalogEpoch) c.uint.encode(state, m.catalogEpoch)
    if (m.policyEpoch) c.uint.encode(state, m.policyEpoch)
    if (m.admissionExpiresAt) c.uint.encode(state, m.admissionExpiresAt)
    if (m.revocationCutoff) c.uint.encode(state, m.revocationCutoff)
    if (m.legacyImportState) c.string.encode(state, m.legacyImportState)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      publisherId: (flags & 2) !== 0 ? c.buffer.decode(state) : null,
      devicePublicKey: (flags & 4) !== 0 ? c.buffer.decode(state) : null,
      status: c.string.decode(state),
      reasonCode: (flags & 8) !== 0 ? c.string.decode(state) : null,
      canPublish: (flags & 16) !== 0,
      canPlayLocal: (flags & 32) !== 0,
      canExportLocal: (flags & 64) !== 0,
      canDeleteLocal: (flags & 128) !== 0,
      canRootTransition: (flags & 256) !== 0,
      catalogEpoch: (flags & 512) !== 0 ? c.uint.decode(state) : 0,
      policyEpoch: (flags & 1024) !== 0 ? c.uint.decode(state) : 0,
      admissionExpiresAt: (flags & 2048) !== 0 ? c.uint.decode(state) : 0,
      revocationCutoff: (flags & 4096) !== 0 ? c.uint.decode(state) : 0,
      legacyImportState: (flags & 8192) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/export-portable-state-request
const encoding197 = encoding0

// @peartube/export-portable-state-response
const encoding198 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte
    c.uint.preencode(state, m.schemaVersion)

    if (m.manifestBytes) c.buffer.preencode(state, m.manifestBytes)
    if (m.manifestDigest) c.string.preencode(state, m.manifestDigest)
    c.uint.preencode(state, m.itemCount)
    if (m.errorCode) c.string.preencode(state, m.errorCode)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.manifestBytes ? 2 : 0) |
      (m.manifestDigest ? 4 : 0) |
      (m.errorCode ? 8 : 0)

    c.uint.encode(state, flags)
    c.uint.encode(state, m.schemaVersion)

    if (m.manifestBytes) c.buffer.encode(state, m.manifestBytes)
    if (m.manifestDigest) c.string.encode(state, m.manifestDigest)
    c.uint.encode(state, m.itemCount)
    if (m.errorCode) c.string.encode(state, m.errorCode)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      schemaVersion: c.uint.decode(state),
      manifestBytes: (flags & 2) !== 0 ? c.buffer.decode(state) : null,
      manifestDigest: (flags & 4) !== 0 ? c.string.decode(state) : null,
      itemCount: c.uint.decode(state),
      errorCode: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/restore-portable-state-request
const encoding199 = {
  preencode(state, m) {
    c.buffer.preencode(state, m.manifestBytes)
    state.end++ // max flag is 1 so always one byte

    if (m.manifestDigest) c.string.preencode(state, m.manifestDigest)
  },
  encode(state, m) {
    const flags = m.manifestDigest ? 1 : 0

    c.buffer.encode(state, m.manifestBytes)
    c.uint.encode(state, flags)

    if (m.manifestDigest) c.string.encode(state, m.manifestDigest)
  },
  decode(state) {
    const r0 = c.buffer.decode(state)
    const flags = c.uint.decode(state)

    return {
      manifestBytes: r0,
      manifestDigest: (flags & 1) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/restore-portable-state-response
const encoding200 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte
    c.uint.preencode(state, m.schemaVersion)
    c.uint.preencode(state, m.importedCount)
    c.uint.preencode(state, m.skippedCount)

    if (m.errorCode) c.string.preencode(state, m.errorCode)
    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) | (m.idempotent ? 2 : 0) | (m.errorCode ? 4 : 0) | (m.error ? 8 : 0)

    c.uint.encode(state, flags)
    c.uint.encode(state, m.schemaVersion)
    c.uint.encode(state, m.importedCount)
    c.uint.encode(state, m.skippedCount)

    if (m.errorCode) c.string.encode(state, m.errorCode)
    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      schemaVersion: c.uint.decode(state),
      importedCount: c.uint.decode(state),
      skippedCount: c.uint.decode(state),
      idempotent: (flags & 2) !== 0,
      errorCode: (flags & 4) !== 0 ? c.string.decode(state) : null,
      error: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/preview-storage-limit-request
const encoding201 = {
  preencode(state, m) {
    c.uint.preencode(state, m.maxBytes)
  },
  encode(state, m) {
    c.uint.encode(state, m.maxBytes)
  },
  decode(state) {
    const r0 = c.uint.decode(state)

    return {
      maxBytes: r0
    }
  }
}

// @peartube/preview-storage-limit-response.affectedCategories
const encoding202_7 = encoding44_11
// @peartube/preview-storage-limit-response.consequences
const encoding202_8 = encoding44_11

// @peartube/preview-storage-limit-response
const encoding202 = {
  preencode(state, m) {
    state.end++ // max flag is 16 so always one byte
    c.uint.preencode(state, m.requestedMaxBytes)
    c.uint.preencode(state, m.currentUsedBytes)
    c.uint.preencode(state, m.requiredEvictionBytes)
    c.uint.preencode(state, m.evictableBytes)
    c.uint.preencode(state, m.protectedBytes)
    c.uint.preencode(state, m.affectedSeedCount)

    if (m.affectedCategories) encoding202_7.preencode(state, m.affectedCategories)
    if (m.consequences) encoding202_8.preencode(state, m.consequences)
    if (m.errorCode) c.string.preencode(state, m.errorCode)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.affectedCategories ? 2 : 0) |
      (m.consequences ? 4 : 0) |
      (m.feasible ? 8 : 0) |
      (m.errorCode ? 16 : 0)

    c.uint.encode(state, flags)
    c.uint.encode(state, m.requestedMaxBytes)
    c.uint.encode(state, m.currentUsedBytes)
    c.uint.encode(state, m.requiredEvictionBytes)
    c.uint.encode(state, m.evictableBytes)
    c.uint.encode(state, m.protectedBytes)
    c.uint.encode(state, m.affectedSeedCount)

    if (m.affectedCategories) encoding202_7.encode(state, m.affectedCategories)
    if (m.consequences) encoding202_8.encode(state, m.consequences)
    if (m.errorCode) c.string.encode(state, m.errorCode)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      requestedMaxBytes: c.uint.decode(state),
      currentUsedBytes: c.uint.decode(state),
      requiredEvictionBytes: c.uint.decode(state),
      evictableBytes: c.uint.decode(state),
      protectedBytes: c.uint.decode(state),
      affectedSeedCount: c.uint.decode(state),
      affectedCategories: (flags & 2) !== 0 ? encoding202_7.decode(state) : null,
      consequences: (flags & 4) !== 0 ? encoding202_8.decode(state) : null,
      feasible: (flags & 8) !== 0,
      errorCode: (flags & 16) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-archive-operator-status-request
const encoding203 = encoding0

// @peartube/get-archive-operator-status-response.recentFailureCodes
const encoding204_12 = encoding44_11

// @peartube/get-archive-operator-status-response
const encoding204 = {
  preencode(state, m) {
    state.end++ // max flag is 32 so always one byte
    c.string.preencode(state, m.operatorMode)
    c.uint.preencode(state, m.activePledgeCount)
    c.uint.preencode(state, m.healthyPledgeCount)
    c.uint.preencode(state, m.failedPledgeCount)
    c.uint.preencode(state, m.challengeSuccessCount)
    c.uint.preencode(state, m.challengeFailureCount)

    if (m.capacityTotalBytes) c.uint.preencode(state, m.capacityTotalBytes)
    if (m.capacityReservedBytes) c.uint.preencode(state, m.capacityReservedBytes)
    if (m.capacityAvailableBytes) c.uint.preencode(state, m.capacityAvailableBytes)
    c.uint.preencode(state, m.capacityRejectionCount)
    c.uint.preencode(state, m.offloadRejectionCount)
    if (m.recentFailureCodes) encoding204_12.preencode(state, m.recentFailureCodes)
    c.uint.preencode(state, m.updatedAt)
    if (m.errorCode) c.string.preencode(state, m.errorCode)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.capacityTotalBytes ? 2 : 0) |
      (m.capacityReservedBytes ? 4 : 0) |
      (m.capacityAvailableBytes ? 8 : 0) |
      (m.recentFailureCodes ? 16 : 0) |
      (m.errorCode ? 32 : 0)

    c.uint.encode(state, flags)
    c.string.encode(state, m.operatorMode)
    c.uint.encode(state, m.activePledgeCount)
    c.uint.encode(state, m.healthyPledgeCount)
    c.uint.encode(state, m.failedPledgeCount)
    c.uint.encode(state, m.challengeSuccessCount)
    c.uint.encode(state, m.challengeFailureCount)

    if (m.capacityTotalBytes) c.uint.encode(state, m.capacityTotalBytes)
    if (m.capacityReservedBytes) c.uint.encode(state, m.capacityReservedBytes)
    if (m.capacityAvailableBytes) c.uint.encode(state, m.capacityAvailableBytes)
    c.uint.encode(state, m.capacityRejectionCount)
    c.uint.encode(state, m.offloadRejectionCount)
    if (m.recentFailureCodes) encoding204_12.encode(state, m.recentFailureCodes)
    c.uint.encode(state, m.updatedAt)
    if (m.errorCode) c.string.encode(state, m.errorCode)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      operatorMode: c.string.decode(state),
      activePledgeCount: c.uint.decode(state),
      healthyPledgeCount: c.uint.decode(state),
      failedPledgeCount: c.uint.decode(state),
      challengeSuccessCount: c.uint.decode(state),
      challengeFailureCount: c.uint.decode(state),
      capacityTotalBytes: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      capacityReservedBytes: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      capacityAvailableBytes: (flags & 8) !== 0 ? c.uint.decode(state) : 0,
      capacityRejectionCount: c.uint.decode(state),
      offloadRejectionCount: c.uint.decode(state),
      recentFailureCodes: (flags & 16) !== 0 ? encoding204_12.decode(state) : null,
      updatedAt: c.uint.decode(state),
      errorCode: (flags & 32) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-archive-participation-request
const encoding205 = encoding0

// @peartube/get-archive-participation-response
const encoding206 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte
    c.uint.preencode(state, m.capacityBytes)
    c.uint.preencode(state, m.maxRequestBytes)
    c.uint.preencode(state, m.reservedBytes)
    c.uint.preencode(state, m.availableBytes)
    c.uint.preencode(state, m.acceptedRequests)
    c.uint.preencode(state, m.knownRequests)
    c.uint.preencode(state, m.receivedPledges)
    c.uint.preencode(state, m.randomRejections)
    c.uint.preencode(state, m.capacityRejections)
    c.uint.preencode(state, m.authorizationRejections)
    c.uint.preencode(state, m.acceptancePermille)

    if (m.errorCode) c.string.preencode(state, m.errorCode)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.enabled ? 2 : 0) | (m.errorCode ? 4 : 0)

    c.uint.encode(state, flags)
    c.uint.encode(state, m.capacityBytes)
    c.uint.encode(state, m.maxRequestBytes)
    c.uint.encode(state, m.reservedBytes)
    c.uint.encode(state, m.availableBytes)
    c.uint.encode(state, m.acceptedRequests)
    c.uint.encode(state, m.knownRequests)
    c.uint.encode(state, m.receivedPledges)
    c.uint.encode(state, m.randomRejections)
    c.uint.encode(state, m.capacityRejections)
    c.uint.encode(state, m.authorizationRejections)
    c.uint.encode(state, m.acceptancePermille)

    if (m.errorCode) c.string.encode(state, m.errorCode)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      enabled: (flags & 2) !== 0,
      capacityBytes: c.uint.decode(state),
      maxRequestBytes: c.uint.decode(state),
      reservedBytes: c.uint.decode(state),
      availableBytes: c.uint.decode(state),
      acceptedRequests: c.uint.decode(state),
      knownRequests: c.uint.decode(state),
      receivedPledges: c.uint.decode(state),
      randomRejections: c.uint.decode(state),
      capacityRejections: c.uint.decode(state),
      authorizationRejections: c.uint.decode(state),
      acceptancePermille: c.uint.decode(state),
      errorCode: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/set-archive-participation-request
const encoding207 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte
    c.uint.preencode(state, m.capacityBytes)
    c.uint.preencode(state, m.maxRequestBytes)
    c.uint.preencode(state, m.acceptancePermille)
  },
  encode(state, m) {
    const flags = m.enabled ? 1 : 0

    c.uint.encode(state, flags)
    c.uint.encode(state, m.capacityBytes)
    c.uint.encode(state, m.maxRequestBytes)
    c.uint.encode(state, m.acceptancePermille)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      enabled: (flags & 1) !== 0,
      capacityBytes: c.uint.decode(state),
      maxRequestBytes: c.uint.decode(state),
      acceptancePermille: c.uint.decode(state)
    }
  }
}

// @peartube/set-archive-participation-response
const encoding208 = encoding206

// @peartube/request-archive-publication-request
const encoding209 = {
  preencode(state, m) {
    c.string.preencode(state, m.publicationId)
    c.string.preencode(state, m.renditionId)
    state.end++ // max flag is 1 so always one byte

    if (m.retentionUntil) c.uint.preencode(state, m.retentionUntil)
  },
  encode(state, m) {
    const flags = m.retentionUntil ? 1 : 0

    c.string.encode(state, m.publicationId)
    c.string.encode(state, m.renditionId)
    c.uint.encode(state, flags)

    if (m.retentionUntil) c.uint.encode(state, m.retentionUntil)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      publicationId: r0,
      renditionId: r1,
      retentionUntil: (flags & 1) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/request-archive-publication-response
const encoding210 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte
    c.string.preencode(state, m.status)
    c.string.preencode(state, m.requestId)

    if (m.errorCode) c.string.preencode(state, m.errorCode)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.errorCode ? 2 : 0)

    c.uint.encode(state, flags)
    c.string.encode(state, m.status)
    c.string.encode(state, m.requestId)

    if (m.errorCode) c.string.encode(state, m.errorCode)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      status: c.string.decode(state),
      requestId: c.string.decode(state),
      errorCode: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-network-policy-request
const encoding211 = encoding0

// @peartube/get-network-policy-response
const encoding212 = {
  preencode(state, m) {
    const flags =
      (m.uploadPermission ? 1 : 0) |
      (m.meteredNetwork ? 2 : 0) |
      (m.backgroundMode ? 4 : 0) |
      (m.diskCeilingBytes ? 8 : 0) |
      (m.uploadCeilingBytes ? 16 : 0) |
      (m.retentionMode ? 32 : 0) |
      (m.followedPublishersJson ? 64 : 0) |
      (m.followedIndexesJson ? 128 : 0) |
      (m.trustedModerationFeedsJson ? 256 : 0) |
      (m.aiAnalysis ? 512 : 0) |
      (m.participationMode ? 1024 : 0)

    c.uint.preencode(state, flags)

    if (m.uploadPermission) c.string.preencode(state, m.uploadPermission)
    if (m.meteredNetwork) c.string.preencode(state, m.meteredNetwork)
    if (m.backgroundMode) c.string.preencode(state, m.backgroundMode)
    if (m.diskCeilingBytes) c.uint.preencode(state, m.diskCeilingBytes)
    if (m.uploadCeilingBytes) c.uint.preencode(state, m.uploadCeilingBytes)
    if (m.retentionMode) c.string.preencode(state, m.retentionMode)
    if (m.followedPublishersJson) c.string.preencode(state, m.followedPublishersJson)
    if (m.followedIndexesJson) c.string.preencode(state, m.followedIndexesJson)
    if (m.trustedModerationFeedsJson) c.string.preencode(state, m.trustedModerationFeedsJson)
    if (m.aiAnalysis) c.string.preencode(state, m.aiAnalysis)
    if (m.participationMode) c.string.preencode(state, m.participationMode)
  },
  encode(state, m) {
    const flags =
      (m.uploadPermission ? 1 : 0) |
      (m.meteredNetwork ? 2 : 0) |
      (m.backgroundMode ? 4 : 0) |
      (m.diskCeilingBytes ? 8 : 0) |
      (m.uploadCeilingBytes ? 16 : 0) |
      (m.retentionMode ? 32 : 0) |
      (m.followedPublishersJson ? 64 : 0) |
      (m.followedIndexesJson ? 128 : 0) |
      (m.trustedModerationFeedsJson ? 256 : 0) |
      (m.aiAnalysis ? 512 : 0) |
      (m.participationMode ? 1024 : 0)

    c.uint.encode(state, flags)

    if (m.uploadPermission) c.string.encode(state, m.uploadPermission)
    if (m.meteredNetwork) c.string.encode(state, m.meteredNetwork)
    if (m.backgroundMode) c.string.encode(state, m.backgroundMode)
    if (m.diskCeilingBytes) c.uint.encode(state, m.diskCeilingBytes)
    if (m.uploadCeilingBytes) c.uint.encode(state, m.uploadCeilingBytes)
    if (m.retentionMode) c.string.encode(state, m.retentionMode)
    if (m.followedPublishersJson) c.string.encode(state, m.followedPublishersJson)
    if (m.followedIndexesJson) c.string.encode(state, m.followedIndexesJson)
    if (m.trustedModerationFeedsJson) c.string.encode(state, m.trustedModerationFeedsJson)
    if (m.aiAnalysis) c.string.encode(state, m.aiAnalysis)
    if (m.participationMode) c.string.encode(state, m.participationMode)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      uploadPermission: (flags & 1) !== 0 ? c.string.decode(state) : null,
      meteredNetwork: (flags & 2) !== 0 ? c.string.decode(state) : null,
      backgroundMode: (flags & 4) !== 0 ? c.string.decode(state) : null,
      diskCeilingBytes: (flags & 8) !== 0 ? c.uint.decode(state) : 0,
      uploadCeilingBytes: (flags & 16) !== 0 ? c.uint.decode(state) : 0,
      retentionMode: (flags & 32) !== 0 ? c.string.decode(state) : null,
      followedPublishersJson: (flags & 64) !== 0 ? c.string.decode(state) : null,
      followedIndexesJson: (flags & 128) !== 0 ? c.string.decode(state) : null,
      trustedModerationFeedsJson: (flags & 256) !== 0 ? c.string.decode(state) : null,
      aiAnalysis: (flags & 512) !== 0 ? c.string.decode(state) : null,
      participationMode: (flags & 1024) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/set-network-policy-request
const encoding213 = {
  preencode(state, m) {
    const flags =
      (m.uploadPermission ? 1 : 0) |
      (m.meteredNetwork ? 2 : 0) |
      (m.backgroundMode ? 4 : 0) |
      (m.diskCeilingBytes ? 8 : 0) |
      (m.uploadCeilingBytes ? 16 : 0) |
      (m.diskCeilingBytesPresent ? 32 : 0) |
      (m.uploadCeilingBytesPresent ? 64 : 0) |
      (m.retentionMode ? 128 : 0) |
      (m.followedPublishersJson ? 256 : 0) |
      (m.followedIndexesJson ? 512 : 0) |
      (m.trustedModerationFeedsJson ? 1024 : 0) |
      (m.aiAnalysis ? 2048 : 0) |
      (m.participationMode ? 4096 : 0)

    c.uint.preencode(state, flags)

    if (m.uploadPermission) c.string.preencode(state, m.uploadPermission)
    if (m.meteredNetwork) c.string.preencode(state, m.meteredNetwork)
    if (m.backgroundMode) c.string.preencode(state, m.backgroundMode)
    if (m.diskCeilingBytes) c.uint.preencode(state, m.diskCeilingBytes)
    if (m.uploadCeilingBytes) c.uint.preencode(state, m.uploadCeilingBytes)
    if (m.retentionMode) c.string.preencode(state, m.retentionMode)
    if (m.followedPublishersJson) c.string.preencode(state, m.followedPublishersJson)
    if (m.followedIndexesJson) c.string.preencode(state, m.followedIndexesJson)
    if (m.trustedModerationFeedsJson) c.string.preencode(state, m.trustedModerationFeedsJson)
    if (m.aiAnalysis) c.string.preencode(state, m.aiAnalysis)
    if (m.participationMode) c.string.preencode(state, m.participationMode)
  },
  encode(state, m) {
    const flags =
      (m.uploadPermission ? 1 : 0) |
      (m.meteredNetwork ? 2 : 0) |
      (m.backgroundMode ? 4 : 0) |
      (m.diskCeilingBytes ? 8 : 0) |
      (m.uploadCeilingBytes ? 16 : 0) |
      (m.diskCeilingBytesPresent ? 32 : 0) |
      (m.uploadCeilingBytesPresent ? 64 : 0) |
      (m.retentionMode ? 128 : 0) |
      (m.followedPublishersJson ? 256 : 0) |
      (m.followedIndexesJson ? 512 : 0) |
      (m.trustedModerationFeedsJson ? 1024 : 0) |
      (m.aiAnalysis ? 2048 : 0) |
      (m.participationMode ? 4096 : 0)

    c.uint.encode(state, flags)

    if (m.uploadPermission) c.string.encode(state, m.uploadPermission)
    if (m.meteredNetwork) c.string.encode(state, m.meteredNetwork)
    if (m.backgroundMode) c.string.encode(state, m.backgroundMode)
    if (m.diskCeilingBytes) c.uint.encode(state, m.diskCeilingBytes)
    if (m.uploadCeilingBytes) c.uint.encode(state, m.uploadCeilingBytes)
    if (m.retentionMode) c.string.encode(state, m.retentionMode)
    if (m.followedPublishersJson) c.string.encode(state, m.followedPublishersJson)
    if (m.followedIndexesJson) c.string.encode(state, m.followedIndexesJson)
    if (m.trustedModerationFeedsJson) c.string.encode(state, m.trustedModerationFeedsJson)
    if (m.aiAnalysis) c.string.encode(state, m.aiAnalysis)
    if (m.participationMode) c.string.encode(state, m.participationMode)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      uploadPermission: (flags & 1) !== 0 ? c.string.decode(state) : null,
      meteredNetwork: (flags & 2) !== 0 ? c.string.decode(state) : null,
      backgroundMode: (flags & 4) !== 0 ? c.string.decode(state) : null,
      diskCeilingBytes: (flags & 8) !== 0 ? c.uint.decode(state) : 0,
      uploadCeilingBytes: (flags & 16) !== 0 ? c.uint.decode(state) : 0,
      diskCeilingBytesPresent: (flags & 32) !== 0,
      uploadCeilingBytesPresent: (flags & 64) !== 0,
      retentionMode: (flags & 128) !== 0 ? c.string.decode(state) : null,
      followedPublishersJson: (flags & 256) !== 0 ? c.string.decode(state) : null,
      followedIndexesJson: (flags & 512) !== 0 ? c.string.decode(state) : null,
      trustedModerationFeedsJson: (flags & 1024) !== 0 ? c.string.decode(state) : null,
      aiAnalysis: (flags & 2048) !== 0 ? c.string.decode(state) : null,
      participationMode: (flags & 4096) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/set-network-policy-response
const encoding214 = encoding73

// @peartube/clear-cache-request
const encoding215 = encoding0

// @peartube/clear-cache-response
const encoding216 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte

    if (m.clearedBytes) c.uint.preencode(state, m.clearedBytes)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.clearedBytes ? 2 : 0)

    c.uint.encode(state, flags)

    if (m.clearedBytes) c.uint.encode(state, m.clearedBytes)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      clearedBytes: (flags & 2) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/assess-source-offload-request
const encoding217 = {
  preencode(state, m) {
    c.string.preencode(state, m.publicationId)
  },
  encode(state, m) {
    c.string.encode(state, m.publicationId)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      publicationId: r0
    }
  }
}

// @peartube/assess-source-offload-response.limitations
const encoding218_9 = encoding44_11

// @peartube/assess-source-offload-response
const encoding218 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte
    c.string.preencode(state, m.publicationId)
    c.string.preencode(state, m.assessmentId)
    c.string.preencode(state, m.evidenceDigest)
    c.string.preencode(state, m.confirmationNonce)
    c.uint.preencode(state, m.expiresAt)
    c.uint.preencode(state, m.policyVersion)
    c.uint.preencode(state, m.byteLength)

    if (m.limitations) encoding218_9.preencode(state, m.limitations)
    if (m.errorCode) c.string.preencode(state, m.errorCode)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) | (m.eligible ? 2 : 0) | (m.limitations ? 4 : 0) | (m.errorCode ? 8 : 0)

    c.uint.encode(state, flags)
    c.string.encode(state, m.publicationId)
    c.string.encode(state, m.assessmentId)
    c.string.encode(state, m.evidenceDigest)
    c.string.encode(state, m.confirmationNonce)
    c.uint.encode(state, m.expiresAt)
    c.uint.encode(state, m.policyVersion)
    c.uint.encode(state, m.byteLength)

    if (m.limitations) encoding218_9.encode(state, m.limitations)
    if (m.errorCode) c.string.encode(state, m.errorCode)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      eligible: (flags & 2) !== 0,
      publicationId: c.string.decode(state),
      assessmentId: c.string.decode(state),
      evidenceDigest: c.string.decode(state),
      confirmationNonce: c.string.decode(state),
      expiresAt: c.uint.decode(state),
      policyVersion: c.uint.decode(state),
      byteLength: c.uint.decode(state),
      limitations: (flags & 4) !== 0 ? encoding218_9.decode(state) : null,
      errorCode: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/confirm-source-offload-request
const encoding219 = {
  preencode(state, m) {
    c.string.preencode(state, m.publicationId)
    c.string.preencode(state, m.assessmentId)
    c.string.preencode(state, m.evidenceDigest)
    c.string.preencode(state, m.confirmationNonce)
    c.uint.preencode(state, m.policyVersion)
    state.end++ // max flag is 1 so always one byte
  },
  encode(state, m) {
    const flags = m.confirmIrrecoverableRisk ? 1 : 0

    c.string.encode(state, m.publicationId)
    c.string.encode(state, m.assessmentId)
    c.string.encode(state, m.evidenceDigest)
    c.string.encode(state, m.confirmationNonce)
    c.uint.encode(state, m.policyVersion)
    c.uint.encode(state, flags)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const r3 = c.string.decode(state)
    const r4 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      publicationId: r0,
      assessmentId: r1,
      evidenceDigest: r2,
      confirmationNonce: r3,
      policyVersion: r4,
      confirmIrrecoverableRisk: (flags & 1) !== 0
    }
  }
}

// @peartube/confirm-source-offload-response
const encoding220 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte
    c.string.preencode(state, m.publicationId)
    c.string.preencode(state, m.assessmentId)
    c.uint.preencode(state, m.freedBytes)

    if (m.auditId) c.string.preencode(state, m.auditId)
    if (m.reason) c.string.preencode(state, m.reason)
    if (m.errorCode) c.string.preencode(state, m.errorCode)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) | (m.auditId ? 2 : 0) | (m.reason ? 4 : 0) | (m.errorCode ? 8 : 0)

    c.uint.encode(state, flags)
    c.string.encode(state, m.publicationId)
    c.string.encode(state, m.assessmentId)
    c.uint.encode(state, m.freedBytes)

    if (m.auditId) c.string.encode(state, m.auditId)
    if (m.reason) c.string.encode(state, m.reason)
    if (m.errorCode) c.string.encode(state, m.errorCode)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      publicationId: c.string.decode(state),
      assessmentId: c.string.decode(state),
      freedBytes: c.uint.decode(state),
      auditId: (flags & 2) !== 0 ? c.string.decode(state) : null,
      reason: (flags & 4) !== 0 ? c.string.decode(state) : null,
      errorCode: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-video-thumbnail-request
const encoding221 = {
  preencode(state, m) {
    c.string.preencode(state, m.channelKey)
    c.string.preencode(state, m.videoId)
    state.end++ // max flag is 4 so always one byte

    if (m.thumbnailBlobId) c.string.preencode(state, m.thumbnailBlobId)
    if (m.thumbnailBlobsCoreKey) c.string.preencode(state, m.thumbnailBlobsCoreKey)
    if (m.thumbnailMimeType) c.string.preencode(state, m.thumbnailMimeType)
  },
  encode(state, m) {
    const flags =
      (m.thumbnailBlobId ? 1 : 0) |
      (m.thumbnailBlobsCoreKey ? 2 : 0) |
      (m.thumbnailMimeType ? 4 : 0)

    c.string.encode(state, m.channelKey)
    c.string.encode(state, m.videoId)
    c.uint.encode(state, flags)

    if (m.thumbnailBlobId) c.string.encode(state, m.thumbnailBlobId)
    if (m.thumbnailBlobsCoreKey) c.string.encode(state, m.thumbnailBlobsCoreKey)
    if (m.thumbnailMimeType) c.string.encode(state, m.thumbnailMimeType)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      channelKey: r0,
      videoId: r1,
      thumbnailBlobId: (flags & 1) !== 0 ? c.string.decode(state) : null,
      thumbnailBlobsCoreKey: (flags & 2) !== 0 ? c.string.decode(state) : null,
      thumbnailMimeType: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/get-video-thumbnail-response
const encoding222 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.url) c.string.preencode(state, m.url)
    if (m.dataUrl) c.string.preencode(state, m.dataUrl)
  },
  encode(state, m) {
    const flags = (m.url ? 1 : 0) | (m.dataUrl ? 2 : 0) | (m.exists ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.url) c.string.encode(state, m.url)
    if (m.dataUrl) c.string.encode(state, m.dataUrl)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      url: (flags & 1) !== 0 ? c.string.decode(state) : null,
      dataUrl: (flags & 2) !== 0 ? c.string.decode(state) : null,
      exists: (flags & 4) !== 0
    }
  }
}

// @peartube/get-video-metadata-request
const encoding223 = encoding166

// @peartube/get-video-metadata-response.video
const encoding224_0 = encoding86_0

// @peartube/get-video-metadata-response
const encoding224 = {
  preencode(state, m) {
    encoding224_0.preencode(state, m.video)
  },
  encode(state, m) {
    encoding224_0.encode(state, m.video)
  },
  decode(state) {
    const r0 = encoding224_0.decode(state)

    return {
      video: r0
    }
  }
}

// @peartube/set-video-thumbnail-request
const encoding225 = {
  preencode(state, m) {
    c.string.preencode(state, m.videoId)
    c.string.preencode(state, m.imageData)
    state.end++ // max flag is 1 so always one byte

    if (m.mimeType) c.string.preencode(state, m.mimeType)
  },
  encode(state, m) {
    const flags = m.mimeType ? 1 : 0

    c.string.encode(state, m.videoId)
    c.string.encode(state, m.imageData)
    c.uint.encode(state, flags)

    if (m.mimeType) c.string.encode(state, m.mimeType)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      videoId: r0,
      imageData: r1,
      mimeType: (flags & 1) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/set-video-thumbnail-from-file-request
const encoding226 = {
  preencode(state, m) {
    c.string.preencode(state, m.videoId)
    c.string.preencode(state, m.filePath)
  },
  encode(state, m) {
    c.string.encode(state, m.videoId)
    c.string.encode(state, m.filePath)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)

    return {
      videoId: r0,
      filePath: r1
    }
  }
}

// @peartube/set-video-thumbnail-from-file-response
const encoding227 = encoding13

// @peartube/set-video-thumbnail-response
const encoding228 = encoding13

// @peartube/desktop-browse-video.tags
const encoding229_8 = encoding44_11
// @peartube/desktop-browse-video.sections
const encoding229_10 = encoding44_11

// @peartube/desktop-browse-video
const encoding229 = {
  preencode(state, m) {
    const flags =
      (m.publicBeeKey ? 1 : 0) |
      (m.durationText ? 2 : 0) |
      (m.summary ? 4 : 0) |
      (m.tags ? 8 : 0) |
      (m.accentHex ? 16 : 0) |
      (m.sections ? 32 : 0) |
      (m.thumbnailUrl ? 64 : 0) |
      (m.path ? 128 : 0) |
      (m.blobId ? 256 : 0) |
      (m.blobsCoreKey ? 512 : 0) |
      (m.mimeType ? 1024 : 0) |
      (m.width ? 2048 : 0) |
      (m.height ? 4096 : 0)

    c.string.preencode(state, m.id)
    c.string.preencode(state, m.backendVideoId)
    c.string.preencode(state, m.channelKey)
    c.uint.preencode(state, flags)

    if (m.publicBeeKey) c.string.preencode(state, m.publicBeeKey)
    c.string.preencode(state, m.title)
    c.string.preencode(state, m.channelName)
    if (m.durationText) c.string.preencode(state, m.durationText)
    if (m.summary) c.string.preencode(state, m.summary)
    if (m.tags) encoding229_8.preencode(state, m.tags)
    if (m.accentHex) c.string.preencode(state, m.accentHex)
    if (m.sections) encoding229_10.preencode(state, m.sections)
    if (m.thumbnailUrl) c.string.preencode(state, m.thumbnailUrl)
    if (m.path) c.string.preencode(state, m.path)
    if (m.blobId) c.string.preencode(state, m.blobId)
    if (m.blobsCoreKey) c.string.preencode(state, m.blobsCoreKey)
    if (m.mimeType) c.string.preencode(state, m.mimeType)
    if (m.width) c.uint.preencode(state, m.width)
    if (m.height) c.uint.preencode(state, m.height)
  },
  encode(state, m) {
    const flags =
      (m.publicBeeKey ? 1 : 0) |
      (m.durationText ? 2 : 0) |
      (m.summary ? 4 : 0) |
      (m.tags ? 8 : 0) |
      (m.accentHex ? 16 : 0) |
      (m.sections ? 32 : 0) |
      (m.thumbnailUrl ? 64 : 0) |
      (m.path ? 128 : 0) |
      (m.blobId ? 256 : 0) |
      (m.blobsCoreKey ? 512 : 0) |
      (m.mimeType ? 1024 : 0) |
      (m.width ? 2048 : 0) |
      (m.height ? 4096 : 0)

    c.string.encode(state, m.id)
    c.string.encode(state, m.backendVideoId)
    c.string.encode(state, m.channelKey)
    c.uint.encode(state, flags)

    if (m.publicBeeKey) c.string.encode(state, m.publicBeeKey)
    c.string.encode(state, m.title)
    c.string.encode(state, m.channelName)
    if (m.durationText) c.string.encode(state, m.durationText)
    if (m.summary) c.string.encode(state, m.summary)
    if (m.tags) encoding229_8.encode(state, m.tags)
    if (m.accentHex) c.string.encode(state, m.accentHex)
    if (m.sections) encoding229_10.encode(state, m.sections)
    if (m.thumbnailUrl) c.string.encode(state, m.thumbnailUrl)
    if (m.path) c.string.encode(state, m.path)
    if (m.blobId) c.string.encode(state, m.blobId)
    if (m.blobsCoreKey) c.string.encode(state, m.blobsCoreKey)
    if (m.mimeType) c.string.encode(state, m.mimeType)
    if (m.width) c.uint.encode(state, m.width)
    if (m.height) c.uint.encode(state, m.height)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      id: r0,
      backendVideoId: r1,
      channelKey: r2,
      publicBeeKey: (flags & 1) !== 0 ? c.string.decode(state) : null,
      title: c.string.decode(state),
      channelName: c.string.decode(state),
      durationText: (flags & 2) !== 0 ? c.string.decode(state) : null,
      summary: (flags & 4) !== 0 ? c.string.decode(state) : null,
      tags: (flags & 8) !== 0 ? encoding229_8.decode(state) : null,
      accentHex: (flags & 16) !== 0 ? c.string.decode(state) : null,
      sections: (flags & 32) !== 0 ? encoding229_10.decode(state) : null,
      thumbnailUrl: (flags & 64) !== 0 ? c.string.decode(state) : null,
      path: (flags & 128) !== 0 ? c.string.decode(state) : null,
      blobId: (flags & 256) !== 0 ? c.string.decode(state) : null,
      blobsCoreKey: (flags & 512) !== 0 ? c.string.decode(state) : null,
      mimeType: (flags & 1024) !== 0 ? c.string.decode(state) : null,
      width: (flags & 2048) !== 0 ? c.uint.decode(state) : 0,
      height: (flags & 4096) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/desktop-browse-sections.home
const encoding230_0 = c.array(c.frame(encoding229))
// @peartube/desktop-browse-sections.subscriptions
const encoding230_1 = encoding230_0
// @peartube/desktop-browse-sections.library
const encoding230_2 = encoding230_0
// @peartube/desktop-browse-sections.studio
const encoding230_3 = encoding230_0
// @peartube/desktop-browse-sections.diagnostics
const encoding230_4 = encoding230_0

// @peartube/desktop-browse-sections
const encoding230 = {
  preencode(state, m) {
    state.end++ // max flag is 16 so always one byte

    if (m.home) encoding230_0.preencode(state, m.home)
    if (m.subscriptions) encoding230_1.preencode(state, m.subscriptions)
    if (m.library) encoding230_2.preencode(state, m.library)
    if (m.studio) encoding230_3.preencode(state, m.studio)
    if (m.diagnostics) encoding230_4.preencode(state, m.diagnostics)
  },
  encode(state, m) {
    const flags =
      (m.home ? 1 : 0) |
      (m.subscriptions ? 2 : 0) |
      (m.library ? 4 : 0) |
      (m.studio ? 8 : 0) |
      (m.diagnostics ? 16 : 0)

    c.uint.encode(state, flags)

    if (m.home) encoding230_0.encode(state, m.home)
    if (m.subscriptions) encoding230_1.encode(state, m.subscriptions)
    if (m.library) encoding230_2.encode(state, m.library)
    if (m.studio) encoding230_3.encode(state, m.studio)
    if (m.diagnostics) encoding230_4.encode(state, m.diagnostics)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      home: (flags & 1) !== 0 ? encoding230_0.decode(state) : null,
      subscriptions: (flags & 2) !== 0 ? encoding230_1.decode(state) : null,
      library: (flags & 4) !== 0 ? encoding230_2.decode(state) : null,
      studio: (flags & 8) !== 0 ? encoding230_3.decode(state) : null,
      diagnostics: (flags & 16) !== 0 ? encoding230_4.decode(state) : null
    }
  }
}

// @peartube/desktop-browse-stats
const encoding231 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (m.homeCount) c.uint.preencode(state, m.homeCount)
    if (m.subscriptionCount) c.uint.preencode(state, m.subscriptionCount)
    if (m.libraryCount) c.uint.preencode(state, m.libraryCount)
    if (m.channelCount) c.uint.preencode(state, m.channelCount)
  },
  encode(state, m) {
    const flags =
      (m.homeCount ? 1 : 0) |
      (m.subscriptionCount ? 2 : 0) |
      (m.libraryCount ? 4 : 0) |
      (m.channelCount ? 8 : 0)

    c.uint.encode(state, flags)

    if (m.homeCount) c.uint.encode(state, m.homeCount)
    if (m.subscriptionCount) c.uint.encode(state, m.subscriptionCount)
    if (m.libraryCount) c.uint.encode(state, m.libraryCount)
    if (m.channelCount) c.uint.encode(state, m.channelCount)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      homeCount: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      subscriptionCount: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      libraryCount: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      channelCount: (flags & 8) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/desktop-browse-state.subscriptionChannelKeys
const encoding232_0 = encoding44_11
// @peartube/desktop-browse-state.identityChannelKeys
const encoding232_1 = encoding44_11

// @peartube/desktop-browse-state
const encoding232 = {
  preencode(state, m) {
    state.end++ // max flag is 16 so always one byte

    if (m.subscriptionChannelKeys) encoding232_0.preencode(state, m.subscriptionChannelKeys)
    if (m.identityChannelKeys) encoding232_1.preencode(state, m.identityChannelKeys)
    if (m.activeIdentityName) c.string.preencode(state, m.activeIdentityName)
    if (m.activeIdentityChannelKey) c.string.preencode(state, m.activeIdentityChannelKey)
  },
  encode(state, m) {
    const flags =
      (m.subscriptionChannelKeys ? 1 : 0) |
      (m.identityChannelKeys ? 2 : 0) |
      (m.activeIdentityName ? 4 : 0) |
      (m.activeIdentityChannelKey ? 8 : 0) |
      (m.activeChannelPublished ? 16 : 0)

    c.uint.encode(state, flags)

    if (m.subscriptionChannelKeys) encoding232_0.encode(state, m.subscriptionChannelKeys)
    if (m.identityChannelKeys) encoding232_1.encode(state, m.identityChannelKeys)
    if (m.activeIdentityName) c.string.encode(state, m.activeIdentityName)
    if (m.activeIdentityChannelKey) c.string.encode(state, m.activeIdentityChannelKey)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      subscriptionChannelKeys: (flags & 1) !== 0 ? encoding232_0.decode(state) : null,
      identityChannelKeys: (flags & 2) !== 0 ? encoding232_1.decode(state) : null,
      activeIdentityName: (flags & 4) !== 0 ? c.string.decode(state) : null,
      activeIdentityChannelKey: (flags & 8) !== 0 ? c.string.decode(state) : null,
      activeChannelPublished: (flags & 16) !== 0
    }
  }
}

// @peartube/desktop-browse-snapshot.sections
const encoding233_1 = c.frame(encoding230)
// @peartube/desktop-browse-snapshot.stats
const encoding233_2 = c.frame(encoding231)
// @peartube/desktop-browse-snapshot.state
const encoding233_3 = c.frame(encoding232)

// @peartube/desktop-browse-snapshot
const encoding233 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte

    if (m.generatedAt) c.uint.preencode(state, m.generatedAt)
    encoding233_1.preencode(state, m.sections)
    encoding233_2.preencode(state, m.stats)
    encoding233_3.preencode(state, m.state)
  },
  encode(state, m) {
    const flags = m.generatedAt ? 1 : 0

    c.uint.encode(state, flags)

    if (m.generatedAt) c.uint.encode(state, m.generatedAt)
    encoding233_1.encode(state, m.sections)
    encoding233_2.encode(state, m.stats)
    encoding233_3.encode(state, m.state)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      generatedAt: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      sections: encoding233_1.decode(state),
      stats: encoding233_2.decode(state),
      state: encoding233_3.decode(state)
    }
  }
}

// @peartube/desktop-bootstrap-request
const encoding234 = {
  preencode(state, m) {
    c.string.preencode(state, m.storagePath)
  },
  encode(state, m) {
    c.string.encode(state, m.storagePath)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      storagePath: r0
    }
  }
}

// @peartube/desktop-bootstrap-response.snapshot
const encoding235_5 = c.frame(encoding233)

// @peartube/desktop-bootstrap-response
const encoding235 = {
  preencode(state, m) {
    state.end++ // max flag is 16 so always one byte

    if (m.blobServerPort) c.uint.preencode(state, m.blobServerPort)
    if (m.blobServerError) c.string.preencode(state, m.blobServerError)
    if (m.protocolVersion) c.uint.preencode(state, m.protocolVersion)
    if (m.storagePath) c.string.preencode(state, m.storagePath)
    encoding235_5.preencode(state, m.snapshot)
  },
  encode(state, m) {
    const flags =
      (m.blobServerPort ? 1 : 0) |
      (m.blobServerReady ? 2 : 0) |
      (m.blobServerError ? 4 : 0) |
      (m.protocolVersion ? 8 : 0) |
      (m.storagePath ? 16 : 0)

    c.uint.encode(state, flags)

    if (m.blobServerPort) c.uint.encode(state, m.blobServerPort)
    if (m.blobServerError) c.string.encode(state, m.blobServerError)
    if (m.protocolVersion) c.uint.encode(state, m.protocolVersion)
    if (m.storagePath) c.string.encode(state, m.storagePath)
    encoding235_5.encode(state, m.snapshot)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      blobServerPort: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      blobServerReady: (flags & 2) !== 0,
      blobServerError: (flags & 4) !== 0 ? c.string.decode(state) : null,
      protocolVersion: (flags & 8) !== 0 ? c.uint.decode(state) : 0,
      storagePath: (flags & 16) !== 0 ? c.string.decode(state) : null,
      snapshot: encoding235_5.decode(state)
    }
  }
}

// @peartube/desktop-shutdown-request
const encoding236 = encoding0

// @peartube/desktop-shutdown-response
const encoding237 = encoding13

// @peartube/desktop-refresh-browse-request
const encoding238 = encoding0

// @peartube/desktop-refresh-browse-response.snapshot
const encoding239_0 = encoding235_5

// @peartube/desktop-refresh-browse-response
const encoding239 = {
  preencode(state, m) {
    encoding239_0.preencode(state, m.snapshot)
  },
  encode(state, m) {
    encoding239_0.encode(state, m.snapshot)
  },
  decode(state) {
    const r0 = encoding239_0.decode(state)

    return {
      snapshot: r0
    }
  }
}

// @peartube/ffmpeg-decode-available-request
const encoding240 = encoding0

// @peartube/ffmpeg-decode-available-response
const encoding241 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte

    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.available ? 1 : 0) | (m.error ? 2 : 0)

    c.uint.encode(state, flags)

    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      available: (flags & 1) !== 0,
      error: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/update-channel-avatar-request
const encoding242 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.filePath) c.string.preencode(state, m.filePath)
    if (m.imageData) c.string.preencode(state, m.imageData)
    if (m.mimeType) c.string.preencode(state, m.mimeType)
  },
  encode(state, m) {
    const flags = (m.filePath ? 1 : 0) | (m.imageData ? 2 : 0) | (m.mimeType ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.filePath) c.string.encode(state, m.filePath)
    if (m.imageData) c.string.encode(state, m.imageData)
    if (m.mimeType) c.string.encode(state, m.mimeType)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      filePath: (flags & 1) !== 0 ? c.string.decode(state) : null,
      imageData: (flags & 2) !== 0 ? c.string.decode(state) : null,
      mimeType: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/update-channel-avatar-response
const encoding243 = encoding92

// @peartube/transcode-start-request
const encoding244 = {
  preencode(state, m) {
    c.string.preencode(state, m.sourceUrl)
    state.end++ // max flag is 2 so always one byte

    if (m.duration) c.uint.preencode(state, m.duration)
    if (m.title) c.string.preencode(state, m.title)
  },
  encode(state, m) {
    const flags = (m.duration ? 1 : 0) | (m.title ? 2 : 0)

    c.string.encode(state, m.sourceUrl)
    c.uint.encode(state, flags)

    if (m.duration) c.uint.encode(state, m.duration)
    if (m.title) c.string.encode(state, m.title)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      sourceUrl: r0,
      duration: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      title: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/transcode-start-response
const encoding245 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (m.sessionId) c.string.preencode(state, m.sessionId)
    if (m.transcodeUrl) c.string.preencode(state, m.transcodeUrl)
    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) | (m.sessionId ? 2 : 0) | (m.transcodeUrl ? 4 : 0) | (m.error ? 8 : 0)

    c.uint.encode(state, flags)

    if (m.sessionId) c.string.encode(state, m.sessionId)
    if (m.transcodeUrl) c.string.encode(state, m.transcodeUrl)
    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      sessionId: (flags & 2) !== 0 ? c.string.decode(state) : null,
      transcodeUrl: (flags & 4) !== 0 ? c.string.decode(state) : null,
      error: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/transcode-stop-request
const encoding246 = {
  preencode(state, m) {
    c.string.preencode(state, m.sessionId)
  },
  encode(state, m) {
    c.string.encode(state, m.sessionId)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      sessionId: r0
    }
  }
}

// @peartube/transcode-stop-response
const encoding247 = encoding92

// @peartube/transcode-status-request
const encoding248 = encoding246

// @peartube/transcode-status-response
const encoding249 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (m.status) c.string.preencode(state, m.status)
    if (m.progress) c.uint.preencode(state, m.progress)
    if (m.bytesWritten) c.uint.preencode(state, m.bytesWritten)
    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags =
      (m.status ? 1 : 0) | (m.progress ? 2 : 0) | (m.bytesWritten ? 4 : 0) | (m.error ? 8 : 0)

    c.uint.encode(state, flags)

    if (m.status) c.string.encode(state, m.status)
    if (m.progress) c.uint.encode(state, m.progress)
    if (m.bytesWritten) c.uint.encode(state, m.bytesWritten)
    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      status: (flags & 1) !== 0 ? c.string.decode(state) : null,
      progress: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      bytesWritten: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      error: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/event-transcode-progress
const encoding250 = {
  preencode(state, m) {
    c.string.preencode(state, m.sessionId)
    state.end++ // max flag is 2 so always one byte

    if (m.percent) c.uint.preencode(state, m.percent)
    if (m.bytesWritten) c.uint.preencode(state, m.bytesWritten)
  },
  encode(state, m) {
    const flags = (m.percent ? 1 : 0) | (m.bytesWritten ? 2 : 0)

    c.string.encode(state, m.sessionId)
    c.uint.encode(state, flags)

    if (m.percent) c.uint.encode(state, m.percent)
    if (m.bytesWritten) c.uint.encode(state, m.bytesWritten)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      sessionId: r0,
      percent: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      bytesWritten: (flags & 2) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/status
const encoding251 = {
  preencode(state, m) {
    state.end++ // max flag is 32 so always one byte

    if (m.blobServerPort) c.uint.preencode(state, m.blobServerPort)
    if (m.blobServerError) c.string.preencode(state, m.blobServerError)
    if (m.protocolVersion) c.uint.preencode(state, m.protocolVersion)
  },
  encode(state, m) {
    const flags =
      (m.ready ? 1 : 0) |
      (m.hasIdentity ? 2 : 0) |
      (m.blobServerPort ? 4 : 0) |
      (m.blobServerReady ? 8 : 0) |
      (m.blobServerError ? 16 : 0) |
      (m.protocolVersion ? 32 : 0)

    c.uint.encode(state, flags)

    if (m.blobServerPort) c.uint.encode(state, m.blobServerPort)
    if (m.blobServerError) c.string.encode(state, m.blobServerError)
    if (m.protocolVersion) c.uint.encode(state, m.protocolVersion)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      ready: (flags & 1) !== 0,
      hasIdentity: (flags & 2) !== 0,
      blobServerPort: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      blobServerReady: (flags & 8) !== 0,
      blobServerError: (flags & 16) !== 0 ? c.string.decode(state) : null,
      protocolVersion: (flags & 32) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/get-status-request
const encoding252 = encoding0

// @peartube/get-status-response.status
const encoding253_0 = c.frame(encoding251)

// @peartube/get-status-response
const encoding253 = {
  preencode(state, m) {
    encoding253_0.preencode(state, m.status)
  },
  encode(state, m) {
    encoding253_0.encode(state, m.status)
  },
  decode(state) {
    const r0 = encoding253_0.decode(state)

    return {
      status: r0
    }
  }
}

// @peartube/pick-video-file-request
const encoding254 = encoding0

// @peartube/pick-video-file-response
const encoding255 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (m.filePath) c.string.preencode(state, m.filePath)
    if (m.name) c.string.preencode(state, m.name)
    if (m.size) c.uint.preencode(state, m.size)
  },
  encode(state, m) {
    const flags = (m.filePath ? 1 : 0) | (m.name ? 2 : 0) | (m.size ? 4 : 0) | (m.cancelled ? 8 : 0)

    c.uint.encode(state, flags)

    if (m.filePath) c.string.encode(state, m.filePath)
    if (m.name) c.string.encode(state, m.name)
    if (m.size) c.uint.encode(state, m.size)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      filePath: (flags & 1) !== 0 ? c.string.decode(state) : null,
      name: (flags & 2) !== 0 ? c.string.decode(state) : null,
      size: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      cancelled: (flags & 8) !== 0
    }
  }
}

// @peartube/pick-image-file-request
const encoding256 = encoding0

// @peartube/pick-image-file-response
const encoding257 = {
  preencode(state, m) {
    state.end++ // max flag is 16 so always one byte

    if (m.filePath) c.string.preencode(state, m.filePath)
    if (m.name) c.string.preencode(state, m.name)
    if (m.size) c.uint.preencode(state, m.size)
    if (m.dataUrl) c.string.preencode(state, m.dataUrl)
  },
  encode(state, m) {
    const flags =
      (m.filePath ? 1 : 0) |
      (m.name ? 2 : 0) |
      (m.size ? 4 : 0) |
      (m.dataUrl ? 8 : 0) |
      (m.cancelled ? 16 : 0)

    c.uint.encode(state, flags)

    if (m.filePath) c.string.encode(state, m.filePath)
    if (m.name) c.string.encode(state, m.name)
    if (m.size) c.uint.encode(state, m.size)
    if (m.dataUrl) c.string.encode(state, m.dataUrl)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      filePath: (flags & 1) !== 0 ? c.string.decode(state) : null,
      name: (flags & 2) !== 0 ? c.string.decode(state) : null,
      size: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      dataUrl: (flags & 8) !== 0 ? c.string.decode(state) : null,
      cancelled: (flags & 16) !== 0
    }
  }
}

// @peartube/get-blob-server-port-request
const encoding258 = encoding0

// @peartube/get-blob-server-port-response
const encoding259 = {
  preencode(state, m) {
    c.uint.preencode(state, m.port)
  },
  encode(state, m) {
    c.uint.encode(state, m.port)
  },
  decode(state) {
    const r0 = c.uint.decode(state)

    return {
      port: r0
    }
  }
}

// @peartube/search-result
const encoding260 = {
  preencode(state, m) {
    c.string.preencode(state, m.id)
    state.end++ // max flag is 2 so always one byte

    if (m.score) c.string.preencode(state, m.score)
    if (m.metadata) c.string.preencode(state, m.metadata)
  },
  encode(state, m) {
    const flags = (m.score ? 1 : 0) | (m.metadata ? 2 : 0)

    c.string.encode(state, m.id)
    c.uint.encode(state, flags)

    if (m.score) c.string.encode(state, m.score)
    if (m.metadata) c.string.encode(state, m.metadata)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      id: r0,
      score: (flags & 1) !== 0 ? c.string.decode(state) : null,
      metadata: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/global-search-videos-request
const encoding261 = {
  preencode(state, m) {
    c.string.preencode(state, m.query)
    state.end++ // max flag is 1 so always one byte

    if (m.topK) c.uint.preencode(state, m.topK)
  },
  encode(state, m) {
    const flags = m.topK ? 1 : 0

    c.string.encode(state, m.query)
    c.uint.encode(state, flags)

    if (m.topK) c.uint.encode(state, m.topK)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      query: r0,
      topK: (flags & 1) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/global-search-videos-response.results
const encoding262_0 = c.array(c.frame(encoding260))

// @peartube/global-search-videos-response
const encoding262 = {
  preencode(state, m) {
    encoding262_0.preencode(state, m.results)
  },
  encode(state, m) {
    encoding262_0.encode(state, m.results)
  },
  decode(state) {
    const r0 = encoding262_0.decode(state)

    return {
      results: r0
    }
  }
}

// @peartube/index-search-selector
const encoding263 = {
  preencode(state, m) {
    c.string.preencode(state, m.namespace)
    c.string.preencode(state, m.identifier)
    c.string.preencode(state, m.kind)
  },
  encode(state, m) {
    c.string.encode(state, m.namespace)
    c.string.encode(state, m.identifier)
    c.string.encode(state, m.kind)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)

    return {
      namespace: r0,
      identifier: r1,
      kind: r2
    }
  }
}

// @peartube/index-external-reference
const encoding264 = {
  preencode(state, m) {
    c.string.preencode(state, m.namespace)
    c.string.preencode(state, m.identifier)
  },
  encode(state, m) {
    c.string.encode(state, m.namespace)
    c.string.encode(state, m.identifier)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)

    return {
      namespace: r0,
      identifier: r1
    }
  }
}

// @peartube/index-episode
const encoding265 = {
  preencode(state, m) {
    c.string.preencode(state, m.seriesEntityId)
    c.uint.preencode(state, m.seasonNumber)
    c.uint.preencode(state, m.episodeNumber)
  },
  encode(state, m) {
    c.string.encode(state, m.seriesEntityId)
    c.uint.encode(state, m.seasonNumber)
    c.uint.encode(state, m.episodeNumber)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.uint.decode(state)
    const r2 = c.uint.decode(state)

    return {
      seriesEntityId: r0,
      seasonNumber: r1,
      episodeNumber: r2
    }
  }
}

// @peartube/index-work.externalRefs
const encoding266_4 = c.array(c.frame(encoding264))
// @peartube/index-work.episode
const encoding266_5 = c.frame(encoding265)

// @peartube/index-work
const encoding266 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (m.entityId) c.string.preencode(state, m.entityId)
    if (m.title) c.string.preencode(state, m.title)
    c.uint.preencode(state, m.releaseYear)
    encoding266_4.preencode(state, m.externalRefs)
    if (m.episode) encoding266_5.preencode(state, m.episode)
  },
  encode(state, m) {
    const flags =
      (m.entityId ? 1 : 0) |
      (m.title ? 2 : 0) |
      (m.releaseYearPresent ? 4 : 0) |
      (m.episode ? 8 : 0)

    c.uint.encode(state, flags)

    if (m.entityId) c.string.encode(state, m.entityId)
    if (m.title) c.string.encode(state, m.title)
    c.uint.encode(state, m.releaseYear)
    encoding266_4.encode(state, m.externalRefs)
    if (m.episode) encoding266_5.encode(state, m.episode)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      entityId: (flags & 1) !== 0 ? c.string.decode(state) : null,
      title: (flags & 2) !== 0 ? c.string.decode(state) : null,
      releaseYear: c.uint.decode(state),
      releaseYearPresent: (flags & 4) !== 0,
      externalRefs: encoding266_4.decode(state),
      episode: (flags & 8) !== 0 ? encoding266_5.decode(state) : null
    }
  }
}

// @peartube/index-edition
const encoding267 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.entityId) c.string.preencode(state, m.entityId)
    if (m.label) c.string.preencode(state, m.label)
    if (m.kind) c.string.preencode(state, m.kind)
  },
  encode(state, m) {
    const flags = (m.entityId ? 1 : 0) | (m.label ? 2 : 0) | (m.kind ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.entityId) c.string.encode(state, m.entityId)
    if (m.label) c.string.encode(state, m.label)
    if (m.kind) c.string.encode(state, m.kind)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      entityId: (flags & 1) !== 0 ? c.string.decode(state) : null,
      label: (flags & 2) !== 0 ? c.string.decode(state) : null,
      kind: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/index-publication
const encoding268 = {
  preencode(state, m) {
    c.string.preencode(state, m.publicationId)
    c.string.preencode(state, m.publisherId)
    c.string.preencode(state, m.manifestId)
    c.uint.preencode(state, m.catalogEpoch)
    state.end++ // max flag is 4 so always one byte

    if (m.catalogHead) c.string.preencode(state, m.catalogHead)
    if (m.title) c.string.preencode(state, m.title)
  },
  encode(state, m) {
    const flags = (m.catalogEpochPresent ? 1 : 0) | (m.catalogHead ? 2 : 0) | (m.title ? 4 : 0)

    c.string.encode(state, m.publicationId)
    c.string.encode(state, m.publisherId)
    c.string.encode(state, m.manifestId)
    c.uint.encode(state, m.catalogEpoch)
    c.uint.encode(state, flags)

    if (m.catalogHead) c.string.encode(state, m.catalogHead)
    if (m.title) c.string.encode(state, m.title)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const r3 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      publicationId: r0,
      publisherId: r1,
      manifestId: r2,
      catalogEpoch: r3,
      catalogEpochPresent: (flags & 1) !== 0,
      catalogHead: (flags & 2) !== 0 ? c.string.decode(state) : null,
      title: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/index-audio-track.languages
const encoding269_3 = encoding44_11

// @peartube/index-audio-track
const encoding269 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte

    if (m.codec) c.string.preencode(state, m.codec)
    c.uint.preencode(state, m.channels)
    encoding269_3.preencode(state, m.languages)
  },
  encode(state, m) {
    const flags = (m.codec ? 1 : 0) | (m.channelsPresent ? 2 : 0)

    c.uint.encode(state, flags)

    if (m.codec) c.string.encode(state, m.codec)
    c.uint.encode(state, m.channels)
    encoding269_3.encode(state, m.languages)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      codec: (flags & 1) !== 0 ? c.string.decode(state) : null,
      channels: c.uint.decode(state),
      channelsPresent: (flags & 2) !== 0,
      languages: encoding269_3.decode(state)
    }
  }
}

// @peartube/index-subtitle-track
const encoding270 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte

    if (m.format) c.string.preencode(state, m.format)
    if (m.language) c.string.preencode(state, m.language)
  },
  encode(state, m) {
    const flags = (m.format ? 1 : 0) | (m.language ? 2 : 0)

    c.uint.encode(state, flags)

    if (m.format) c.string.encode(state, m.format)
    if (m.language) c.string.encode(state, m.language)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      format: (flags & 1) !== 0 ? c.string.decode(state) : null,
      language: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/index-rendition.hdrFormats
const encoding271_8 = encoding44_11
// @peartube/index-rendition.audioTracks
const encoding271_9 = c.array(c.frame(encoding269))
// @peartube/index-rendition.subtitleTracks
const encoding271_10 = c.array(c.frame(encoding270))

// @peartube/index-rendition
const encoding271 = {
  preencode(state, m) {
    c.string.preencode(state, m.renditionId)
    state.end++ // max flag is 64 so always one byte

    if (m.container) c.string.preencode(state, m.container)
    if (m.videoCodec) c.string.preencode(state, m.videoCodec)
    c.uint.preencode(state, m.width)
    c.uint.preencode(state, m.height)
    if (m.resolutionLabel) c.string.preencode(state, m.resolutionLabel)
    encoding271_8.preencode(state, m.hdrFormats)
    encoding271_9.preencode(state, m.audioTracks)
    encoding271_10.preencode(state, m.subtitleTracks)
    if (m.purpose) c.string.preencode(state, m.purpose)
    c.uint.preencode(state, m.byteLength)
  },
  encode(state, m) {
    const flags =
      (m.container ? 1 : 0) |
      (m.videoCodec ? 2 : 0) |
      (m.widthPresent ? 4 : 0) |
      (m.heightPresent ? 8 : 0) |
      (m.resolutionLabel ? 16 : 0) |
      (m.purpose ? 32 : 0) |
      (m.byteLengthPresent ? 64 : 0)

    c.string.encode(state, m.renditionId)
    c.uint.encode(state, flags)

    if (m.container) c.string.encode(state, m.container)
    if (m.videoCodec) c.string.encode(state, m.videoCodec)
    c.uint.encode(state, m.width)
    c.uint.encode(state, m.height)
    if (m.resolutionLabel) c.string.encode(state, m.resolutionLabel)
    encoding271_8.encode(state, m.hdrFormats)
    encoding271_9.encode(state, m.audioTracks)
    encoding271_10.encode(state, m.subtitleTracks)
    if (m.purpose) c.string.encode(state, m.purpose)
    c.uint.encode(state, m.byteLength)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      renditionId: r0,
      container: (flags & 1) !== 0 ? c.string.decode(state) : null,
      videoCodec: (flags & 2) !== 0 ? c.string.decode(state) : null,
      width: c.uint.decode(state),
      widthPresent: (flags & 4) !== 0,
      height: c.uint.decode(state),
      heightPresent: (flags & 8) !== 0,
      resolutionLabel: (flags & 16) !== 0 ? c.string.decode(state) : null,
      hdrFormats: encoding271_8.decode(state),
      audioTracks: encoding271_9.decode(state),
      subtitleTracks: encoding271_10.decode(state),
      purpose: (flags & 32) !== 0 ? c.string.decode(state) : null,
      byteLength: c.uint.decode(state),
      byteLengthPresent: (flags & 64) !== 0
    }
  }
}

// @peartube/index-asset
const encoding272 = {
  preencode(state, m) {
    state.end++ // max flag is 32 so always one byte

    if (m.assetId) c.string.preencode(state, m.assetId)
    if (m.coreKey) c.string.preencode(state, m.coreKey)
    if (m.treeHash) c.string.preencode(state, m.treeHash)
    c.uint.preencode(state, m.blockLength)
    c.uint.preencode(state, m.blockSize)
    c.uint.preencode(state, m.byteLength)
  },
  encode(state, m) {
    const flags =
      (m.assetId ? 1 : 0) |
      (m.coreKey ? 2 : 0) |
      (m.treeHash ? 4 : 0) |
      (m.blockLengthPresent ? 8 : 0) |
      (m.blockSizePresent ? 16 : 0) |
      (m.byteLengthPresent ? 32 : 0)

    c.uint.encode(state, flags)

    if (m.assetId) c.string.encode(state, m.assetId)
    if (m.coreKey) c.string.encode(state, m.coreKey)
    if (m.treeHash) c.string.encode(state, m.treeHash)
    c.uint.encode(state, m.blockLength)
    c.uint.encode(state, m.blockSize)
    c.uint.encode(state, m.byteLength)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      assetId: (flags & 1) !== 0 ? c.string.decode(state) : null,
      coreKey: (flags & 2) !== 0 ? c.string.decode(state) : null,
      treeHash: (flags & 4) !== 0 ? c.string.decode(state) : null,
      blockLength: c.uint.decode(state),
      blockLengthPresent: (flags & 8) !== 0,
      blockSize: c.uint.decode(state),
      blockSizePresent: (flags & 16) !== 0,
      byteLength: c.uint.decode(state),
      byteLengthPresent: (flags & 32) !== 0
    }
  }
}

// @peartube/index-provenance
const encoding273 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.sourceKind) c.string.preencode(state, m.sourceKind)
    if (m.releaseName) c.string.preencode(state, m.releaseName)
    if (m.publicInfohash) c.string.preencode(state, m.publicInfohash)
  },
  encode(state, m) {
    const flags = (m.sourceKind ? 1 : 0) | (m.releaseName ? 2 : 0) | (m.publicInfohash ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.sourceKind) c.string.encode(state, m.sourceKind)
    if (m.releaseName) c.string.encode(state, m.releaseName)
    if (m.publicInfohash) c.string.encode(state, m.publicInfohash)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      sourceKind: (flags & 1) !== 0 ? c.string.decode(state) : null,
      releaseName: (flags & 2) !== 0 ? c.string.decode(state) : null,
      publicInfohash: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/index-availability
const encoding274 = {
  preencode(state, m) {
    c.uint.preencode(state, m.peers)
    state.end++ // max flag is 8 so always one byte
    c.uint.preencode(state, m.completeSeeders)
    c.uint.preencode(state, m.observedAtMs)
    c.uint.preencode(state, m.expiresAtMs)
  },
  encode(state, m) {
    const flags =
      (m.peersPresent ? 1 : 0) |
      (m.completeSeedersPresent ? 2 : 0) |
      (m.observedAtMsPresent ? 4 : 0) |
      (m.expiresAtMsPresent ? 8 : 0)

    c.uint.encode(state, m.peers)
    c.uint.encode(state, flags)
    c.uint.encode(state, m.completeSeeders)
    c.uint.encode(state, m.observedAtMs)
    c.uint.encode(state, m.expiresAtMs)
  },
  decode(state) {
    const r0 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      peers: r0,
      peersPresent: (flags & 1) !== 0,
      completeSeeders: c.uint.decode(state),
      completeSeedersPresent: (flags & 2) !== 0,
      observedAtMs: c.uint.decode(state),
      observedAtMsPresent: (flags & 4) !== 0,
      expiresAtMs: c.uint.decode(state),
      expiresAtMsPresent: (flags & 8) !== 0
    }
  }
}

// @peartube/index-source-indexer
const encoding275 = {
  preencode(state, m) {
    c.string.preencode(state, m.indexerId)
    c.uint.preencode(state, m.observedAtMs)
  },
  encode(state, m) {
    c.string.encode(state, m.indexerId)
    c.uint.encode(state, m.observedAtMs)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.uint.decode(state)

    return {
      indexerId: r0,
      observedAtMs: r1
    }
  }
}

// @peartube/index-publisher-descriptor
const encoding276 = {
  preencode(state, m) {
    c.string.preencode(state, m.publisherId)
    c.string.preencode(state, m.publisherRootKey)
    c.string.preencode(state, m.catalogBootstrapKey)
    c.uint.preencode(state, m.catalogEpoch)
    c.uint.preencode(state, m.policySequence)
  },
  encode(state, m) {
    c.string.encode(state, m.publisherId)
    c.string.encode(state, m.publisherRootKey)
    c.string.encode(state, m.catalogBootstrapKey)
    c.uint.encode(state, m.catalogEpoch)
    c.uint.encode(state, m.policySequence)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const r3 = c.uint.decode(state)
    const r4 = c.uint.decode(state)

    return {
      publisherId: r0,
      publisherRootKey: r1,
      catalogBootstrapKey: r2,
      catalogEpoch: r3,
      policySequence: r4
    }
  }
}

// @peartube/index-catalog-head
const encoding277 = {
  preencode(state, m) {
    c.string.preencode(state, m.viewKey)
    c.uint.preencode(state, m.length)
    c.string.preencode(state, m.digest)
    state.end++ // max flag is 1 so always one byte

    if (m.authorizationStateDigest) c.string.preencode(state, m.authorizationStateDigest)
  },
  encode(state, m) {
    const flags = m.authorizationStateDigest ? 1 : 0

    c.string.encode(state, m.viewKey)
    c.uint.encode(state, m.length)
    c.string.encode(state, m.digest)
    c.uint.encode(state, flags)

    if (m.authorizationStateDigest) c.string.encode(state, m.authorizationStateDigest)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.uint.decode(state)
    const r2 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      viewKey: r0,
      length: r1,
      digest: r2,
      authorizationStateDigest: (flags & 1) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/index-verification.publisherDescriptor
const encoding278_1 = c.frame(encoding276)
// @peartube/index-verification.catalogHead
const encoding278_2 = c.frame(encoding277)

// @peartube/index-verification
const encoding278 = {
  preencode(state, m) {
    c.string.preencode(state, m.state)
    state.end++ // max flag is 2 so always one byte

    if (m.publisherDescriptor) encoding278_1.preencode(state, m.publisherDescriptor)
    if (m.catalogHead) encoding278_2.preencode(state, m.catalogHead)
  },
  encode(state, m) {
    const flags = (m.publisherDescriptor ? 1 : 0) | (m.catalogHead ? 2 : 0)

    c.string.encode(state, m.state)
    c.uint.encode(state, flags)

    if (m.publisherDescriptor) encoding278_1.encode(state, m.publisherDescriptor)
    if (m.catalogHead) encoding278_2.encode(state, m.catalogHead)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      state: r0,
      publisherDescriptor: (flags & 1) !== 0 ? encoding278_1.decode(state) : null,
      catalogHead: (flags & 2) !== 0 ? encoding278_2.decode(state) : null
    }
  }
}

// @peartube/index-candidate-v2.work
const encoding279_2 = c.frame(encoding266)
// @peartube/index-candidate-v2.edition
const encoding279_3 = c.frame(encoding267)
// @peartube/index-candidate-v2.publication
const encoding279_4 = c.frame(encoding268)
// @peartube/index-candidate-v2.rendition
const encoding279_5 = c.frame(encoding271)
// @peartube/index-candidate-v2.asset
const encoding279_6 = c.frame(encoding272)
// @peartube/index-candidate-v2.provenance
const encoding279_7 = c.frame(encoding273)
// @peartube/index-candidate-v2.availability
const encoding279_8 = c.frame(encoding274)
// @peartube/index-candidate-v2.verification
const encoding279_9 = c.frame(encoding278)
// @peartube/index-candidate-v2.sourceIndexers
const encoding279_10 = c.array(c.frame(encoding275))

// @peartube/index-candidate-v2
const encoding279 = {
  preencode(state, m) {
    c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.candidateRef)
    encoding279_2.preencode(state, m.work)
    state.end++ // max flag is 1 so always one byte

    if (m.edition) encoding279_3.preencode(state, m.edition)
    encoding279_4.preencode(state, m.publication)
    encoding279_5.preencode(state, m.rendition)
    encoding279_6.preencode(state, m.asset)
    encoding279_7.preencode(state, m.provenance)
    encoding279_8.preencode(state, m.availability)
    encoding279_9.preencode(state, m.verification)
    encoding279_10.preencode(state, m.sourceIndexers)
  },
  encode(state, m) {
    const flags = m.edition ? 1 : 0

    c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.candidateRef)
    encoding279_2.encode(state, m.work)
    c.uint.encode(state, flags)

    if (m.edition) encoding279_3.encode(state, m.edition)
    encoding279_4.encode(state, m.publication)
    encoding279_5.encode(state, m.rendition)
    encoding279_6.encode(state, m.asset)
    encoding279_7.encode(state, m.provenance)
    encoding279_8.encode(state, m.availability)
    encoding279_9.encode(state, m.verification)
    encoding279_10.encode(state, m.sourceIndexers)
  },
  decode(state) {
    const r0 = c.uint.decode(state)
    const r1 = c.string.decode(state)
    const r2 = encoding279_2.decode(state)
    const flags = c.uint.decode(state)

    return {
      schemaVersion: r0,
      candidateRef: r1,
      work: r2,
      edition: (flags & 1) !== 0 ? encoding279_3.decode(state) : null,
      publication: encoding279_4.decode(state),
      rendition: encoding279_5.decode(state),
      asset: encoding279_6.decode(state),
      provenance: encoding279_7.decode(state),
      availability: encoding279_8.decode(state),
      verification: encoding279_9.decode(state),
      sourceIndexers: encoding279_10.decode(state)
    }
  }
}

// @peartube/search-index-candidates-request.selector
const encoding280_0 = c.frame(encoding263)

// @peartube/search-index-candidates-request
const encoding280 = {
  preencode(state, m) {
    encoding280_0.preencode(state, m.selector)
  },
  encode(state, m) {
    encoding280_0.encode(state, m.selector)
  },
  decode(state) {
    const r0 = encoding280_0.decode(state)

    return {
      selector: r0
    }
  }
}

// @peartube/search-index-candidates-response.candidates
const encoding281_1 = c.array(c.frame(encoding279))

// @peartube/search-index-candidates-response
const encoding281 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte
    encoding281_1.preencode(state, m.candidates)

    if (m.errorCode) c.string.preencode(state, m.errorCode)
    if (m.errorMessage) c.string.preencode(state, m.errorMessage)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.errorCode ? 2 : 0) | (m.errorMessage ? 4 : 0)

    c.uint.encode(state, flags)
    encoding281_1.encode(state, m.candidates)

    if (m.errorCode) c.string.encode(state, m.errorCode)
    if (m.errorMessage) c.string.encode(state, m.errorMessage)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      candidates: encoding281_1.decode(state),
      errorCode: (flags & 2) !== 0 ? c.string.decode(state) : null,
      errorMessage: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/verify-index-candidate-request
const encoding282 = {
  preencode(state, m) {
    c.string.preencode(state, m.candidateRef)
  },
  encode(state, m) {
    c.string.encode(state, m.candidateRef)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      candidateRef: r0
    }
  }
}

// @peartube/verify-index-candidate-response.candidate
const encoding283_1 = c.frame(encoding279)

// @peartube/verify-index-candidate-response
const encoding283 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (m.candidate) encoding283_1.preencode(state, m.candidate)
    if (m.errorCode) c.string.preencode(state, m.errorCode)
    if (m.errorMessage) c.string.preencode(state, m.errorMessage)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) | (m.candidate ? 2 : 0) | (m.errorCode ? 4 : 0) | (m.errorMessage ? 8 : 0)

    c.uint.encode(state, flags)

    if (m.candidate) encoding283_1.encode(state, m.candidate)
    if (m.errorCode) c.string.encode(state, m.errorCode)
    if (m.errorMessage) c.string.encode(state, m.errorMessage)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      candidate: (flags & 2) !== 0 ? encoding283_1.decode(state) : null,
      errorCode: (flags & 4) !== 0 ? c.string.decode(state) : null,
      errorMessage: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/device
const encoding284 = {
  preencode(state, m) {
    c.string.preencode(state, m.keyHex)
    state.end++ // max flag is 8 so always one byte

    if (m.role) c.string.preencode(state, m.role)
    if (m.deviceName) c.string.preencode(state, m.deviceName)
    if (m.addedAt) c.uint.preencode(state, m.addedAt)
    if (m.blobDriveKey) c.string.preencode(state, m.blobDriveKey)
  },
  encode(state, m) {
    const flags =
      (m.role ? 1 : 0) | (m.deviceName ? 2 : 0) | (m.addedAt ? 4 : 0) | (m.blobDriveKey ? 8 : 0)

    c.string.encode(state, m.keyHex)
    c.uint.encode(state, flags)

    if (m.role) c.string.encode(state, m.role)
    if (m.deviceName) c.string.encode(state, m.deviceName)
    if (m.addedAt) c.uint.encode(state, m.addedAt)
    if (m.blobDriveKey) c.string.encode(state, m.blobDriveKey)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      keyHex: r0,
      role: (flags & 1) !== 0 ? c.string.decode(state) : null,
      deviceName: (flags & 2) !== 0 ? c.string.decode(state) : null,
      addedAt: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      blobDriveKey: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/create-device-invite-request
const encoding285 = encoding105

// @peartube/create-device-invite-response
const encoding286 = {
  preencode(state, m) {
    c.string.preencode(state, m.inviteCode)
  },
  encode(state, m) {
    c.string.encode(state, m.inviteCode)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      inviteCode: r0
    }
  }
}

// @peartube/pair-device-request
const encoding287 = encoding149

// @peartube/pair-device-response
const encoding288 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte
    c.string.preencode(state, m.channelKey)

    if (m.syncState) c.string.preencode(state, m.syncState)
    if (m.videoCount) c.uint.preencode(state, m.videoCount)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.syncState ? 2 : 0) | (m.videoCount ? 4 : 0)

    c.uint.encode(state, flags)
    c.string.encode(state, m.channelKey)

    if (m.syncState) c.string.encode(state, m.syncState)
    if (m.videoCount) c.uint.encode(state, m.videoCount)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      channelKey: c.string.decode(state),
      syncState: (flags & 2) !== 0 ? c.string.decode(state) : null,
      videoCount: (flags & 4) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/retry-sync-channel-request
const encoding289 = encoding105

// @peartube/retry-sync-channel-response
const encoding290 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.state) c.string.preencode(state, m.state)
    if (m.videoCount) c.uint.preencode(state, m.videoCount)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.state ? 2 : 0) | (m.videoCount ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.state) c.string.encode(state, m.state)
    if (m.videoCount) c.uint.encode(state, m.videoCount)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      state: (flags & 2) !== 0 ? c.string.decode(state) : null,
      videoCount: (flags & 4) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/list-devices-request
const encoding291 = encoding105

// @peartube/list-devices-response.devices
const encoding292_0 = c.array(c.frame(encoding284))

// @peartube/list-devices-response
const encoding292 = {
  preencode(state, m) {
    encoding292_0.preencode(state, m.devices)
  },
  encode(state, m) {
    encoding292_0.encode(state, m.devices)
  },
  decode(state) {
    const r0 = encoding292_0.decode(state)

    return {
      devices: r0
    }
  }
}

// @peartube/event-ready
const encoding293 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (m.blobServerPort) c.uint.preencode(state, m.blobServerPort)
    if (m.protocolVersion) c.uint.preencode(state, m.protocolVersion)
    if (m.blobServerError) c.string.preencode(state, m.blobServerError)
  },
  encode(state, m) {
    const flags =
      (m.blobServerPort ? 1 : 0) |
      (m.protocolVersion ? 2 : 0) |
      (m.blobServerReady ? 4 : 0) |
      (m.blobServerError ? 8 : 0)

    c.uint.encode(state, flags)

    if (m.blobServerPort) c.uint.encode(state, m.blobServerPort)
    if (m.protocolVersion) c.uint.encode(state, m.protocolVersion)
    if (m.blobServerError) c.string.encode(state, m.blobServerError)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      blobServerPort: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      protocolVersion: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      blobServerReady: (flags & 4) !== 0,
      blobServerError: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/event-error
const encoding294 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte

    if (m.code) c.string.preencode(state, m.code)
    c.string.preencode(state, m.message)
  },
  encode(state, m) {
    const flags = (m.code ? 1 : 0) | (m.retryable ? 2 : 0)

    c.uint.encode(state, flags)

    if (m.code) c.string.encode(state, m.code)
    c.string.encode(state, m.message)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      code: (flags & 1) !== 0 ? c.string.decode(state) : null,
      message: c.string.decode(state),
      retryable: (flags & 2) !== 0
    }
  }
}

// @peartube/event-upload-progress
const encoding295 = {
  preencode(state, m) {
    c.string.preencode(state, m.videoId)
    c.uint.preencode(state, m.progress)
    state.end++ // max flag is 8 so always one byte

    if (m.bytesUploaded) c.uint.preencode(state, m.bytesUploaded)
    if (m.totalBytes) c.uint.preencode(state, m.totalBytes)
    if (m.speed) c.uint.preencode(state, m.speed)
    if (m.eta) c.uint.preencode(state, m.eta)
  },
  encode(state, m) {
    const flags =
      (m.bytesUploaded ? 1 : 0) | (m.totalBytes ? 2 : 0) | (m.speed ? 4 : 0) | (m.eta ? 8 : 0)

    c.string.encode(state, m.videoId)
    c.uint.encode(state, m.progress)
    c.uint.encode(state, flags)

    if (m.bytesUploaded) c.uint.encode(state, m.bytesUploaded)
    if (m.totalBytes) c.uint.encode(state, m.totalBytes)
    if (m.speed) c.uint.encode(state, m.speed)
    if (m.eta) c.uint.encode(state, m.eta)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      videoId: r0,
      progress: r1,
      bytesUploaded: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      totalBytes: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      speed: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      eta: (flags & 8) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/event-download-progress
const encoding296 = {
  preencode(state, m) {
    c.string.preencode(state, m.id)
    c.uint.preencode(state, m.progress)
    state.end++ // max flag is 2 so always one byte

    if (m.bytesDownloaded) c.uint.preencode(state, m.bytesDownloaded)
    if (m.totalBytes) c.uint.preencode(state, m.totalBytes)
  },
  encode(state, m) {
    const flags = (m.bytesDownloaded ? 1 : 0) | (m.totalBytes ? 2 : 0)

    c.string.encode(state, m.id)
    c.uint.encode(state, m.progress)
    c.uint.encode(state, flags)

    if (m.bytesDownloaded) c.uint.encode(state, m.bytesDownloaded)
    if (m.totalBytes) c.uint.encode(state, m.totalBytes)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      id: r0,
      progress: r1,
      bytesDownloaded: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      totalBytes: (flags & 2) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/event-media-graph-update
const encoding297 = {
  preencode(state, m) {
    c.string.preencode(state, m.revision)
    c.uint.preencode(state, m.changedCount)
  },
  encode(state, m) {
    c.string.encode(state, m.revision)
    c.uint.encode(state, m.changedCount)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.uint.decode(state)

    return {
      revision: r0,
      changedCount: r1
    }
  }
}

// @peartube/event-log
const encoding298 = {
  preencode(state, m) {
    c.string.preencode(state, m.level)
    c.string.preencode(state, m.message)
    state.end++ // max flag is 1 so always one byte

    if (m.timestamp) c.uint.preencode(state, m.timestamp)
  },
  encode(state, m) {
    const flags = m.timestamp ? 1 : 0

    c.string.encode(state, m.level)
    c.string.encode(state, m.message)
    c.uint.encode(state, flags)

    if (m.timestamp) c.uint.encode(state, m.timestamp)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      level: r0,
      message: r1,
      timestamp: (flags & 1) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/event-video-stats
const encoding299 = {
  preencode(state, m) {
    encoding299_0.preencode(state, m.stats)
  },
  encode(state, m) {
    encoding299_0.encode(state, m.stats)
  },
  decode(state) {
    const r0 = encoding299_0.decode(state)

    return {
      stats: r0
    }
  }
}

// @peartube/channel-op-base
const encoding300 = {
  preencode(state, m) {
    c.string.preencode(state, m.type)
    state.end++ // max flag is 1 so always one byte

    if (m.schemaVersion) c.uint.preencode(state, m.schemaVersion)
  },
  encode(state, m) {
    const flags = m.schemaVersion ? 1 : 0

    c.string.encode(state, m.type)
    c.uint.encode(state, flags)

    if (m.schemaVersion) c.uint.encode(state, m.schemaVersion)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      type: r0,
      schemaVersion: (flags & 1) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/channel-op-update-channel
const encoding301 = {
  preencode(state, m) {
    const flags =
      (m.schemaVersion ? 1 : 0) |
      (m.key ? 2 : 0) |
      (m.name ? 4 : 0) |
      (m.description ? 8 : 0) |
      (m.avatar ? 16 : 0) |
      (m.updatedAt ? 32 : 0) |
      (m.updatedBy ? 64 : 0) |
      (m.createdAt ? 128 : 0) |
      (m.createdBy ? 256 : 0)

    c.string.preencode(state, m.type)
    c.uint.preencode(state, flags)

    if (m.schemaVersion) c.uint.preencode(state, m.schemaVersion)
    if (m.key) c.string.preencode(state, m.key)
    if (m.name) c.string.preencode(state, m.name)
    if (m.description) c.string.preencode(state, m.description)
    if (m.avatar) c.string.preencode(state, m.avatar)
    if (m.updatedAt) c.uint.preencode(state, m.updatedAt)
    if (m.updatedBy) c.string.preencode(state, m.updatedBy)
    if (m.createdAt) c.uint.preencode(state, m.createdAt)
    if (m.createdBy) c.string.preencode(state, m.createdBy)
  },
  encode(state, m) {
    const flags =
      (m.schemaVersion ? 1 : 0) |
      (m.key ? 2 : 0) |
      (m.name ? 4 : 0) |
      (m.description ? 8 : 0) |
      (m.avatar ? 16 : 0) |
      (m.updatedAt ? 32 : 0) |
      (m.updatedBy ? 64 : 0) |
      (m.createdAt ? 128 : 0) |
      (m.createdBy ? 256 : 0)

    c.string.encode(state, m.type)
    c.uint.encode(state, flags)

    if (m.schemaVersion) c.uint.encode(state, m.schemaVersion)
    if (m.key) c.string.encode(state, m.key)
    if (m.name) c.string.encode(state, m.name)
    if (m.description) c.string.encode(state, m.description)
    if (m.avatar) c.string.encode(state, m.avatar)
    if (m.updatedAt) c.uint.encode(state, m.updatedAt)
    if (m.updatedBy) c.string.encode(state, m.updatedBy)
    if (m.createdAt) c.uint.encode(state, m.createdAt)
    if (m.createdBy) c.string.encode(state, m.createdBy)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      type: r0,
      schemaVersion: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      key: (flags & 2) !== 0 ? c.string.decode(state) : null,
      name: (flags & 4) !== 0 ? c.string.decode(state) : null,
      description: (flags & 8) !== 0 ? c.string.decode(state) : null,
      avatar: (flags & 16) !== 0 ? c.string.decode(state) : null,
      updatedAt: (flags & 32) !== 0 ? c.uint.decode(state) : 0,
      updatedBy: (flags & 64) !== 0 ? c.string.decode(state) : null,
      createdAt: (flags & 128) !== 0 ? c.uint.decode(state) : 0,
      createdBy: (flags & 256) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/channel-op-add-video
const encoding302 = {
  preencode(state, m) {
    const flags =
      (m.schemaVersion ? 1 : 0) |
      (m.description ? 2 : 0) |
      (m.path ? 4 : 0) |
      (m.duration ? 8 : 0) |
      (m.thumbnail ? 16 : 0) |
      (m.blobDriveKey ? 32 : 0) |
      (m.mimeType ? 64 : 0) |
      (m.size ? 128 : 0) |
      (m.uploadedAt ? 256 : 0) |
      (m.uploadedBy ? 512 : 0) |
      (m.category ? 1024 : 0) |
      (m.views ? 2048 : 0)

    c.string.preencode(state, m.type)
    c.uint.preencode(state, flags)

    if (m.schemaVersion) c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.id)
    c.string.preencode(state, m.title)
    if (m.description) c.string.preencode(state, m.description)
    if (m.path) c.string.preencode(state, m.path)
    if (m.duration) c.uint.preencode(state, m.duration)
    if (m.thumbnail) c.string.preencode(state, m.thumbnail)
    if (m.blobDriveKey) c.string.preencode(state, m.blobDriveKey)
    if (m.mimeType) c.string.preencode(state, m.mimeType)
    if (m.size) c.uint.preencode(state, m.size)
    if (m.uploadedAt) c.uint.preencode(state, m.uploadedAt)
    if (m.uploadedBy) c.string.preencode(state, m.uploadedBy)
    if (m.category) c.string.preencode(state, m.category)
    if (m.views) c.uint.preencode(state, m.views)
  },
  encode(state, m) {
    const flags =
      (m.schemaVersion ? 1 : 0) |
      (m.description ? 2 : 0) |
      (m.path ? 4 : 0) |
      (m.duration ? 8 : 0) |
      (m.thumbnail ? 16 : 0) |
      (m.blobDriveKey ? 32 : 0) |
      (m.mimeType ? 64 : 0) |
      (m.size ? 128 : 0) |
      (m.uploadedAt ? 256 : 0) |
      (m.uploadedBy ? 512 : 0) |
      (m.category ? 1024 : 0) |
      (m.views ? 2048 : 0)

    c.string.encode(state, m.type)
    c.uint.encode(state, flags)

    if (m.schemaVersion) c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.id)
    c.string.encode(state, m.title)
    if (m.description) c.string.encode(state, m.description)
    if (m.path) c.string.encode(state, m.path)
    if (m.duration) c.uint.encode(state, m.duration)
    if (m.thumbnail) c.string.encode(state, m.thumbnail)
    if (m.blobDriveKey) c.string.encode(state, m.blobDriveKey)
    if (m.mimeType) c.string.encode(state, m.mimeType)
    if (m.size) c.uint.encode(state, m.size)
    if (m.uploadedAt) c.uint.encode(state, m.uploadedAt)
    if (m.uploadedBy) c.string.encode(state, m.uploadedBy)
    if (m.category) c.string.encode(state, m.category)
    if (m.views) c.uint.encode(state, m.views)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      type: r0,
      schemaVersion: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      id: c.string.decode(state),
      title: c.string.decode(state),
      description: (flags & 2) !== 0 ? c.string.decode(state) : null,
      path: (flags & 4) !== 0 ? c.string.decode(state) : null,
      duration: (flags & 8) !== 0 ? c.uint.decode(state) : 0,
      thumbnail: (flags & 16) !== 0 ? c.string.decode(state) : null,
      blobDriveKey: (flags & 32) !== 0 ? c.string.decode(state) : null,
      mimeType: (flags & 64) !== 0 ? c.string.decode(state) : null,
      size: (flags & 128) !== 0 ? c.uint.decode(state) : 0,
      uploadedAt: (flags & 256) !== 0 ? c.uint.decode(state) : 0,
      uploadedBy: (flags & 512) !== 0 ? c.string.decode(state) : null,
      category: (flags & 1024) !== 0 ? c.string.decode(state) : null,
      views: (flags & 2048) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/channel-op-update-video
const encoding303 = {
  preencode(state, m) {
    c.string.preencode(state, m.type)
    state.end++ // max flag is 64 so always one byte

    if (m.schemaVersion) c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.id)
    if (m.title) c.string.preencode(state, m.title)
    if (m.description) c.string.preencode(state, m.description)
    if (m.thumbnail) c.string.preencode(state, m.thumbnail)
    if (m.category) c.string.preencode(state, m.category)
    if (m.updatedAt) c.uint.preencode(state, m.updatedAt)
    if (m.updatedBy) c.string.preencode(state, m.updatedBy)
  },
  encode(state, m) {
    const flags =
      (m.schemaVersion ? 1 : 0) |
      (m.title ? 2 : 0) |
      (m.description ? 4 : 0) |
      (m.thumbnail ? 8 : 0) |
      (m.category ? 16 : 0) |
      (m.updatedAt ? 32 : 0) |
      (m.updatedBy ? 64 : 0)

    c.string.encode(state, m.type)
    c.uint.encode(state, flags)

    if (m.schemaVersion) c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.id)
    if (m.title) c.string.encode(state, m.title)
    if (m.description) c.string.encode(state, m.description)
    if (m.thumbnail) c.string.encode(state, m.thumbnail)
    if (m.category) c.string.encode(state, m.category)
    if (m.updatedAt) c.uint.encode(state, m.updatedAt)
    if (m.updatedBy) c.string.encode(state, m.updatedBy)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      type: r0,
      schemaVersion: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      id: c.string.decode(state),
      title: (flags & 2) !== 0 ? c.string.decode(state) : null,
      description: (flags & 4) !== 0 ? c.string.decode(state) : null,
      thumbnail: (flags & 8) !== 0 ? c.string.decode(state) : null,
      category: (flags & 16) !== 0 ? c.string.decode(state) : null,
      updatedAt: (flags & 32) !== 0 ? c.uint.decode(state) : 0,
      updatedBy: (flags & 64) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/channel-op-delete-video
const encoding304 = {
  preencode(state, m) {
    c.string.preencode(state, m.type)
    state.end++ // max flag is 1 so always one byte

    if (m.schemaVersion) c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.id)
  },
  encode(state, m) {
    const flags = m.schemaVersion ? 1 : 0

    c.string.encode(state, m.type)
    c.uint.encode(state, flags)

    if (m.schemaVersion) c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.id)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      type: r0,
      schemaVersion: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      id: c.string.decode(state)
    }
  }
}

// @peartube/channel-op-add-writer
const encoding305 = {
  preencode(state, m) {
    c.string.preencode(state, m.type)
    state.end++ // max flag is 16 so always one byte

    if (m.schemaVersion) c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.keyHex)
    if (m.role) c.string.preencode(state, m.role)
    if (m.deviceName) c.string.preencode(state, m.deviceName)
    if (m.addedAt) c.uint.preencode(state, m.addedAt)
    if (m.blobDriveKey) c.string.preencode(state, m.blobDriveKey)
  },
  encode(state, m) {
    const flags =
      (m.schemaVersion ? 1 : 0) |
      (m.role ? 2 : 0) |
      (m.deviceName ? 4 : 0) |
      (m.addedAt ? 8 : 0) |
      (m.blobDriveKey ? 16 : 0)

    c.string.encode(state, m.type)
    c.uint.encode(state, flags)

    if (m.schemaVersion) c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.keyHex)
    if (m.role) c.string.encode(state, m.role)
    if (m.deviceName) c.string.encode(state, m.deviceName)
    if (m.addedAt) c.uint.encode(state, m.addedAt)
    if (m.blobDriveKey) c.string.encode(state, m.blobDriveKey)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      type: r0,
      schemaVersion: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      keyHex: c.string.decode(state),
      role: (flags & 2) !== 0 ? c.string.decode(state) : null,
      deviceName: (flags & 4) !== 0 ? c.string.decode(state) : null,
      addedAt: (flags & 8) !== 0 ? c.uint.decode(state) : 0,
      blobDriveKey: (flags & 16) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/channel-op-upsert-writer
const encoding306 = encoding305

// @peartube/channel-op-remove-writer
const encoding307 = {
  preencode(state, m) {
    c.string.preencode(state, m.type)
    state.end++ // max flag is 2 so always one byte

    if (m.schemaVersion) c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.keyHex)
  },
  encode(state, m) {
    const flags = (m.schemaVersion ? 1 : 0) | (m.ban ? 2 : 0)

    c.string.encode(state, m.type)
    c.uint.encode(state, flags)

    if (m.schemaVersion) c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.keyHex)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      type: r0,
      schemaVersion: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      keyHex: c.string.decode(state),
      ban: (flags & 2) !== 0
    }
  }
}

// @peartube/channel-op-add-invite
const encoding308 = {
  preencode(state, m) {
    c.string.preencode(state, m.type)
    state.end++ // max flag is 8 so always one byte

    if (m.schemaVersion) c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.idHex)
    c.string.preencode(state, m.inviteZ32)
    if (m.publicKeyHex) c.string.preencode(state, m.publicKeyHex)
    if (m.expires) c.uint.preencode(state, m.expires)
    if (m.createdAt) c.uint.preencode(state, m.createdAt)
  },
  encode(state, m) {
    const flags =
      (m.schemaVersion ? 1 : 0) |
      (m.publicKeyHex ? 2 : 0) |
      (m.expires ? 4 : 0) |
      (m.createdAt ? 8 : 0)

    c.string.encode(state, m.type)
    c.uint.encode(state, flags)

    if (m.schemaVersion) c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.idHex)
    c.string.encode(state, m.inviteZ32)
    if (m.publicKeyHex) c.string.encode(state, m.publicKeyHex)
    if (m.expires) c.uint.encode(state, m.expires)
    if (m.createdAt) c.uint.encode(state, m.createdAt)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      type: r0,
      schemaVersion: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      idHex: c.string.decode(state),
      inviteZ32: c.string.decode(state),
      publicKeyHex: (flags & 2) !== 0 ? c.string.decode(state) : null,
      expires: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      createdAt: (flags & 8) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/channel-op-delete-invite
const encoding309 = {
  preencode(state, m) {
    c.string.preencode(state, m.type)
    state.end++ // max flag is 1 so always one byte

    if (m.schemaVersion) c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.idHex)
  },
  encode(state, m) {
    const flags = m.schemaVersion ? 1 : 0

    c.string.encode(state, m.type)
    c.uint.encode(state, flags)

    if (m.schemaVersion) c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.idHex)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      type: r0,
      schemaVersion: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      idHex: c.string.decode(state)
    }
  }
}

// @peartube/channel-op-add-comment
const encoding310 = {
  preencode(state, m) {
    c.string.preencode(state, m.type)
    state.end++ // max flag is 4 so always one byte

    if (m.schemaVersion) c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.videoId)
    c.string.preencode(state, m.commentId)
    c.string.preencode(state, m.text)
    c.string.preencode(state, m.authorKeyHex)
    if (m.timestamp) c.uint.preencode(state, m.timestamp)
    if (m.parentId) c.string.preencode(state, m.parentId)
  },
  encode(state, m) {
    const flags = (m.schemaVersion ? 1 : 0) | (m.timestamp ? 2 : 0) | (m.parentId ? 4 : 0)

    c.string.encode(state, m.type)
    c.uint.encode(state, flags)

    if (m.schemaVersion) c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.videoId)
    c.string.encode(state, m.commentId)
    c.string.encode(state, m.text)
    c.string.encode(state, m.authorKeyHex)
    if (m.timestamp) c.uint.encode(state, m.timestamp)
    if (m.parentId) c.string.encode(state, m.parentId)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      type: r0,
      schemaVersion: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      videoId: c.string.decode(state),
      commentId: c.string.decode(state),
      text: c.string.decode(state),
      authorKeyHex: c.string.decode(state),
      timestamp: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      parentId: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/channel-op-add-reaction
const encoding311 = {
  preencode(state, m) {
    c.string.preencode(state, m.type)
    state.end++ // max flag is 2 so always one byte

    if (m.schemaVersion) c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.videoId)
    c.string.preencode(state, m.reactionType)
    c.string.preencode(state, m.authorKeyHex)
    if (m.timestamp) c.uint.preencode(state, m.timestamp)
  },
  encode(state, m) {
    const flags = (m.schemaVersion ? 1 : 0) | (m.timestamp ? 2 : 0)

    c.string.encode(state, m.type)
    c.uint.encode(state, flags)

    if (m.schemaVersion) c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.videoId)
    c.string.encode(state, m.reactionType)
    c.string.encode(state, m.authorKeyHex)
    if (m.timestamp) c.uint.encode(state, m.timestamp)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      type: r0,
      schemaVersion: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      videoId: c.string.decode(state),
      reactionType: c.string.decode(state),
      authorKeyHex: c.string.decode(state),
      timestamp: (flags & 2) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/channel-op-remove-reaction
const encoding312 = {
  preencode(state, m) {
    c.string.preencode(state, m.type)
    state.end++ // max flag is 1 so always one byte

    if (m.schemaVersion) c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.videoId)
    c.string.preencode(state, m.authorKeyHex)
  },
  encode(state, m) {
    const flags = m.schemaVersion ? 1 : 0

    c.string.encode(state, m.type)
    c.uint.encode(state, flags)

    if (m.schemaVersion) c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.videoId)
    c.string.encode(state, m.authorKeyHex)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      type: r0,
      schemaVersion: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      videoId: c.string.decode(state),
      authorKeyHex: c.string.decode(state)
    }
  }
}

// @peartube/channel-op-hide-comment
const encoding313 = {
  preencode(state, m) {
    c.string.preencode(state, m.type)
    state.end++ // max flag is 1 so always one byte

    if (m.schemaVersion) c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.videoId)
    c.string.preencode(state, m.commentId)
    c.string.preencode(state, m.moderatorKeyHex)
  },
  encode(state, m) {
    const flags = m.schemaVersion ? 1 : 0

    c.string.encode(state, m.type)
    c.uint.encode(state, flags)

    if (m.schemaVersion) c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.videoId)
    c.string.encode(state, m.commentId)
    c.string.encode(state, m.moderatorKeyHex)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      type: r0,
      schemaVersion: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      videoId: c.string.decode(state),
      commentId: c.string.decode(state),
      moderatorKeyHex: c.string.decode(state)
    }
  }
}

// @peartube/channel-op-remove-comment
const encoding314 = {
  preencode(state, m) {
    c.string.preencode(state, m.type)
    state.end++ // max flag is 4 so always one byte

    if (m.schemaVersion) c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.videoId)
    c.string.preencode(state, m.commentId)
    if (m.moderatorKeyHex) c.string.preencode(state, m.moderatorKeyHex)
    if (m.authorKeyHex) c.string.preencode(state, m.authorKeyHex)
  },
  encode(state, m) {
    const flags = (m.schemaVersion ? 1 : 0) | (m.moderatorKeyHex ? 2 : 0) | (m.authorKeyHex ? 4 : 0)

    c.string.encode(state, m.type)
    c.uint.encode(state, flags)

    if (m.schemaVersion) c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.videoId)
    c.string.encode(state, m.commentId)
    if (m.moderatorKeyHex) c.string.encode(state, m.moderatorKeyHex)
    if (m.authorKeyHex) c.string.encode(state, m.authorKeyHex)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      type: r0,
      schemaVersion: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      videoId: c.string.decode(state),
      commentId: c.string.decode(state),
      moderatorKeyHex: (flags & 2) !== 0 ? c.string.decode(state) : null,
      authorKeyHex: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/channel-op-add-vector-index
const encoding315 = {
  preencode(state, m) {
    c.string.preencode(state, m.type)
    state.end++ // max flag is 8 so always one byte

    if (m.schemaVersion) c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.videoId)
    if (m.vector) c.string.preencode(state, m.vector)
    if (m.text) c.string.preencode(state, m.text)
    if (m.metadata) c.string.preencode(state, m.metadata)
  },
  encode(state, m) {
    const flags =
      (m.schemaVersion ? 1 : 0) | (m.vector ? 2 : 0) | (m.text ? 4 : 0) | (m.metadata ? 8 : 0)

    c.string.encode(state, m.type)
    c.uint.encode(state, flags)

    if (m.schemaVersion) c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.videoId)
    if (m.vector) c.string.encode(state, m.vector)
    if (m.text) c.string.encode(state, m.text)
    if (m.metadata) c.string.encode(state, m.metadata)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      type: r0,
      schemaVersion: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      videoId: c.string.decode(state),
      vector: (flags & 2) !== 0 ? c.string.decode(state) : null,
      text: (flags & 4) !== 0 ? c.string.decode(state) : null,
      metadata: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/channel-op-log-watch-event
const encoding316 = {
  preencode(state, m) {
    c.string.preencode(state, m.type)
    state.end++ // max flag is 32 so always one byte

    if (m.schemaVersion) c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.videoId)
    if (m.channelKey) c.string.preencode(state, m.channelKey)
    if (m.watcherKeyHex) c.string.preencode(state, m.watcherKeyHex)
    if (m.duration) c.uint.preencode(state, m.duration)
    if (m.timestamp) c.uint.preencode(state, m.timestamp)
  },
  encode(state, m) {
    const flags =
      (m.schemaVersion ? 1 : 0) |
      (m.channelKey ? 2 : 0) |
      (m.watcherKeyHex ? 4 : 0) |
      (m.duration ? 8 : 0) |
      (m.completed ? 16 : 0) |
      (m.timestamp ? 32 : 0)

    c.string.encode(state, m.type)
    c.uint.encode(state, flags)

    if (m.schemaVersion) c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.videoId)
    if (m.channelKey) c.string.encode(state, m.channelKey)
    if (m.watcherKeyHex) c.string.encode(state, m.watcherKeyHex)
    if (m.duration) c.uint.encode(state, m.duration)
    if (m.timestamp) c.uint.encode(state, m.timestamp)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      type: r0,
      schemaVersion: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      videoId: c.string.decode(state),
      channelKey: (flags & 2) !== 0 ? c.string.decode(state) : null,
      watcherKeyHex: (flags & 4) !== 0 ? c.string.decode(state) : null,
      duration: (flags & 8) !== 0 ? c.uint.decode(state) : 0,
      completed: (flags & 16) !== 0,
      timestamp: (flags & 32) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/channel-op-migrate-schema
const encoding317 = {
  preencode(state, m) {
    c.string.preencode(state, m.type)
    c.uint.preencode(state, m.schemaVersion)
    c.uint.preencode(state, m.fromVersion)
    c.uint.preencode(state, m.toVersion)
    state.end++ // max flag is 1 so always one byte

    if (m.migratedAt) c.uint.preencode(state, m.migratedAt)
  },
  encode(state, m) {
    const flags = m.migratedAt ? 1 : 0

    c.string.encode(state, m.type)
    c.uint.encode(state, m.schemaVersion)
    c.uint.encode(state, m.fromVersion)
    c.uint.encode(state, m.toVersion)
    c.uint.encode(state, flags)

    if (m.migratedAt) c.uint.encode(state, m.migratedAt)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.uint.decode(state)
    const r2 = c.uint.decode(state)
    const r3 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      type: r0,
      schemaVersion: r1,
      fromVersion: r2,
      toVersion: r3,
      migratedAt: (flags & 1) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/add-comment-request
const encoding318 = {
  preencode(state, m) {
    c.string.preencode(state, m.channelKey)
    c.string.preencode(state, m.videoId)
    c.string.preencode(state, m.text)
    state.end++ // max flag is 4 so always one byte

    if (m.parentId) c.string.preencode(state, m.parentId)
    if (m.authorChannelKey) c.string.preencode(state, m.authorChannelKey)
    if (m.publicBeeKey) c.string.preencode(state, m.publicBeeKey)
  },
  encode(state, m) {
    const flags = (m.parentId ? 1 : 0) | (m.authorChannelKey ? 2 : 0) | (m.publicBeeKey ? 4 : 0)

    c.string.encode(state, m.channelKey)
    c.string.encode(state, m.videoId)
    c.string.encode(state, m.text)
    c.uint.encode(state, flags)

    if (m.parentId) c.string.encode(state, m.parentId)
    if (m.authorChannelKey) c.string.encode(state, m.authorChannelKey)
    if (m.publicBeeKey) c.string.encode(state, m.publicBeeKey)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      channelKey: r0,
      videoId: r1,
      text: r2,
      parentId: (flags & 1) !== 0 ? c.string.decode(state) : null,
      authorChannelKey: (flags & 2) !== 0 ? c.string.decode(state) : null,
      publicBeeKey: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/add-comment-response
const encoding319 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (m.commentId) c.string.preencode(state, m.commentId)
    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) | (m.commentId ? 2 : 0) | (m.queued ? 4 : 0) | (m.error ? 8 : 0)

    c.uint.encode(state, flags)

    if (m.commentId) c.string.encode(state, m.commentId)
    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      commentId: (flags & 2) !== 0 ? c.string.decode(state) : null,
      queued: (flags & 4) !== 0,
      error: (flags & 8) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/list-comments-request
const encoding320 = {
  preencode(state, m) {
    c.string.preencode(state, m.channelKey)
    c.string.preencode(state, m.videoId)
    state.end++ // max flag is 4 so always one byte

    if (m.page) c.uint.preencode(state, m.page)
    if (m.limit) c.uint.preencode(state, m.limit)
    if (m.publicBeeKey) c.string.preencode(state, m.publicBeeKey)
  },
  encode(state, m) {
    const flags = (m.page ? 1 : 0) | (m.limit ? 2 : 0) | (m.publicBeeKey ? 4 : 0)

    c.string.encode(state, m.channelKey)
    c.string.encode(state, m.videoId)
    c.uint.encode(state, flags)

    if (m.page) c.uint.encode(state, m.page)
    if (m.limit) c.uint.encode(state, m.limit)
    if (m.publicBeeKey) c.string.encode(state, m.publicBeeKey)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      channelKey: r0,
      videoId: r1,
      page: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      limit: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      publicBeeKey: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/comment
const encoding321 = {
  preencode(state, m) {
    c.string.preencode(state, m.videoId)
    c.string.preencode(state, m.commentId)
    c.string.preencode(state, m.text)
    c.string.preencode(state, m.authorKeyHex)
    state.end++ // max flag is 4 so always one byte

    if (m.timestamp) c.uint.preencode(state, m.timestamp)
    if (m.parentId) c.string.preencode(state, m.parentId)
  },
  encode(state, m) {
    const flags = (m.timestamp ? 1 : 0) | (m.parentId ? 2 : 0) | (m.isAdmin ? 4 : 0)

    c.string.encode(state, m.videoId)
    c.string.encode(state, m.commentId)
    c.string.encode(state, m.text)
    c.string.encode(state, m.authorKeyHex)
    c.uint.encode(state, flags)

    if (m.timestamp) c.uint.encode(state, m.timestamp)
    if (m.parentId) c.string.encode(state, m.parentId)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const r3 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      videoId: r0,
      commentId: r1,
      text: r2,
      authorKeyHex: r3,
      timestamp: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      parentId: (flags & 2) !== 0 ? c.string.decode(state) : null,
      isAdmin: (flags & 4) !== 0
    }
  }
}

// @peartube/list-comments-response.comments
const encoding322_1 = c.array(c.frame(encoding321))

// @peartube/list-comments-response
const encoding322 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte
    encoding322_1.preencode(state, m.comments)

    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.error ? 2 : 0)

    c.uint.encode(state, flags)
    encoding322_1.encode(state, m.comments)

    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      comments: encoding322_1.decode(state),
      error: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/hide-comment-request
const encoding323 = {
  preencode(state, m) {
    c.string.preencode(state, m.channelKey)
    c.string.preencode(state, m.videoId)
    c.string.preencode(state, m.commentId)
    state.end++ // max flag is 1 so always one byte

    if (m.publicBeeKey) c.string.preencode(state, m.publicBeeKey)
  },
  encode(state, m) {
    const flags = m.publicBeeKey ? 1 : 0

    c.string.encode(state, m.channelKey)
    c.string.encode(state, m.videoId)
    c.string.encode(state, m.commentId)
    c.uint.encode(state, flags)

    if (m.publicBeeKey) c.string.encode(state, m.publicBeeKey)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      channelKey: r0,
      videoId: r1,
      commentId: r2,
      publicBeeKey: (flags & 1) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/hide-comment-response
const encoding324 = encoding92

// @peartube/remove-comment-request
const encoding325 = {
  preencode(state, m) {
    c.string.preencode(state, m.channelKey)
    c.string.preencode(state, m.videoId)
    c.string.preencode(state, m.commentId)
    state.end++ // max flag is 2 so always one byte

    if (m.authorChannelKey) c.string.preencode(state, m.authorChannelKey)
    if (m.publicBeeKey) c.string.preencode(state, m.publicBeeKey)
  },
  encode(state, m) {
    const flags = (m.authorChannelKey ? 1 : 0) | (m.publicBeeKey ? 2 : 0)

    c.string.encode(state, m.channelKey)
    c.string.encode(state, m.videoId)
    c.string.encode(state, m.commentId)
    c.uint.encode(state, flags)

    if (m.authorChannelKey) c.string.encode(state, m.authorChannelKey)
    if (m.publicBeeKey) c.string.encode(state, m.publicBeeKey)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      channelKey: r0,
      videoId: r1,
      commentId: r2,
      authorChannelKey: (flags & 1) !== 0 ? c.string.decode(state) : null,
      publicBeeKey: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/remove-comment-response
const encoding326 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.queued ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      queued: (flags & 2) !== 0,
      error: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/reaction-count
const encoding327 = {
  preencode(state, m) {
    c.string.preencode(state, m.reactionType)
    c.uint.preencode(state, m.count)
  },
  encode(state, m) {
    c.string.encode(state, m.reactionType)
    c.uint.encode(state, m.count)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.uint.decode(state)

    return {
      reactionType: r0,
      count: r1
    }
  }
}

// @peartube/add-reaction-request
const encoding328 = {
  preencode(state, m) {
    c.string.preencode(state, m.channelKey)
    c.string.preencode(state, m.videoId)
    c.string.preencode(state, m.reactionType)
    state.end++ // max flag is 2 so always one byte

    if (m.authorChannelKey) c.string.preencode(state, m.authorChannelKey)
    if (m.publicBeeKey) c.string.preencode(state, m.publicBeeKey)
  },
  encode(state, m) {
    const flags = (m.authorChannelKey ? 1 : 0) | (m.publicBeeKey ? 2 : 0)

    c.string.encode(state, m.channelKey)
    c.string.encode(state, m.videoId)
    c.string.encode(state, m.reactionType)
    c.uint.encode(state, flags)

    if (m.authorChannelKey) c.string.encode(state, m.authorChannelKey)
    if (m.publicBeeKey) c.string.encode(state, m.publicBeeKey)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      channelKey: r0,
      videoId: r1,
      reactionType: r2,
      authorChannelKey: (flags & 1) !== 0 ? c.string.decode(state) : null,
      publicBeeKey: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/add-reaction-response
const encoding329 = encoding326

// @peartube/remove-reaction-request
const encoding330 = {
  preencode(state, m) {
    c.string.preencode(state, m.channelKey)
    c.string.preencode(state, m.videoId)
    state.end++ // max flag is 2 so always one byte

    if (m.authorChannelKey) c.string.preencode(state, m.authorChannelKey)
    if (m.publicBeeKey) c.string.preencode(state, m.publicBeeKey)
  },
  encode(state, m) {
    const flags = (m.authorChannelKey ? 1 : 0) | (m.publicBeeKey ? 2 : 0)

    c.string.encode(state, m.channelKey)
    c.string.encode(state, m.videoId)
    c.uint.encode(state, flags)

    if (m.authorChannelKey) c.string.encode(state, m.authorChannelKey)
    if (m.publicBeeKey) c.string.encode(state, m.publicBeeKey)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      channelKey: r0,
      videoId: r1,
      authorChannelKey: (flags & 1) !== 0 ? c.string.decode(state) : null,
      publicBeeKey: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/remove-reaction-response
const encoding331 = encoding326

// @peartube/get-reactions-request
const encoding332 = encoding330

// @peartube/get-reactions-response.counts
const encoding333_1 = c.array(c.frame(encoding327))

// @peartube/get-reactions-response
const encoding333 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte
    encoding333_1.preencode(state, m.counts)

    if (m.userReaction) c.string.preencode(state, m.userReaction)
    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.userReaction ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)
    encoding333_1.encode(state, m.counts)

    if (m.userReaction) c.string.encode(state, m.userReaction)
    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      counts: encoding333_1.decode(state),
      userReaction: (flags & 2) !== 0 ? c.string.decode(state) : null,
      error: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/cast-device
const encoding334 = {
  preencode(state, m) {
    c.string.preencode(state, m.id)
    c.string.preencode(state, m.name)
    c.string.preencode(state, m.host)
    c.uint.preencode(state, m.port)
    c.string.preencode(state, m.castProtocol)
  },
  encode(state, m) {
    c.string.encode(state, m.id)
    c.string.encode(state, m.name)
    c.string.encode(state, m.host)
    c.uint.encode(state, m.port)
    c.string.encode(state, m.castProtocol)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const r3 = c.uint.decode(state)
    const r4 = c.string.decode(state)

    return {
      id: r0,
      name: r1,
      host: r2,
      port: r3,
      castProtocol: r4
    }
  }
}

// @peartube/cast-available-request
const encoding335 = encoding0

// @peartube/cast-available-response
const encoding336 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte
  },
  encode(state, m) {
    const flags = m.available ? 1 : 0

    c.uint.encode(state, flags)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      available: (flags & 1) !== 0
    }
  }
}

// @peartube/cast-start-discovery-request
const encoding337 = encoding0

// @peartube/cast-start-discovery-response
const encoding338 = encoding13

// @peartube/cast-stop-discovery-request
const encoding339 = encoding0

// @peartube/cast-stop-discovery-response
const encoding340 = encoding13

// @peartube/cast-get-devices-request
const encoding341 = encoding0

// @peartube/cast-get-devices-response.devices
const encoding342_0 = c.array(c.frame(encoding334))

// @peartube/cast-get-devices-response
const encoding342 = {
  preencode(state, m) {
    encoding342_0.preencode(state, m.devices)
  },
  encode(state, m) {
    encoding342_0.encode(state, m.devices)
  },
  decode(state) {
    const r0 = encoding342_0.decode(state)

    return {
      devices: r0
    }
  }
}

// @peartube/cast-add-manual-device-request
const encoding343 = {
  preencode(state, m) {
    c.string.preencode(state, m.name)
    c.string.preencode(state, m.host)
    state.end++ // max flag is 2 so always one byte

    if (m.port) c.uint.preencode(state, m.port)
    if (m.castProtocol) c.string.preencode(state, m.castProtocol)
  },
  encode(state, m) {
    const flags = (m.port ? 1 : 0) | (m.castProtocol ? 2 : 0)

    c.string.encode(state, m.name)
    c.string.encode(state, m.host)
    c.uint.encode(state, flags)

    if (m.port) c.uint.encode(state, m.port)
    if (m.castProtocol) c.string.encode(state, m.castProtocol)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      name: r0,
      host: r1,
      port: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      castProtocol: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/cast-add-manual-device-response.device
const encoding344_1 = c.frame(encoding334)

// @peartube/cast-add-manual-device-response
const encoding344 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.device) encoding344_1.preencode(state, m.device)
    if (m.error) c.string.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.device ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.device) encoding344_1.encode(state, m.device)
    if (m.error) c.string.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      device: (flags & 2) !== 0 ? encoding344_1.decode(state) : null,
      error: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/cast-connect-request
const encoding345 = {
  preencode(state, m) {
    c.string.preencode(state, m.deviceId)
  },
  encode(state, m) {
    c.string.encode(state, m.deviceId)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      deviceId: r0
    }
  }
}

// @peartube/cast-connect-response
const encoding346 = encoding92

// @peartube/cast-disconnect-request
const encoding347 = encoding0

// @peartube/cast-disconnect-response
const encoding348 = encoding92

// @peartube/cast-play-request
const encoding349 = {
  preencode(state, m) {
    c.string.preencode(state, m.url)
    c.string.preencode(state, m.contentType)
    state.end++ // max flag is 8 so always one byte

    if (m.title) c.string.preencode(state, m.title)
    if (m.thumbnail) c.string.preencode(state, m.thumbnail)
    if (m.time) c.uint.preencode(state, m.time)
    if (m.volume) c.uint.preencode(state, m.volume)
  },
  encode(state, m) {
    const flags = (m.title ? 1 : 0) | (m.thumbnail ? 2 : 0) | (m.time ? 4 : 0) | (m.volume ? 8 : 0)

    c.string.encode(state, m.url)
    c.string.encode(state, m.contentType)
    c.uint.encode(state, flags)

    if (m.title) c.string.encode(state, m.title)
    if (m.thumbnail) c.string.encode(state, m.thumbnail)
    if (m.time) c.uint.encode(state, m.time)
    if (m.volume) c.uint.encode(state, m.volume)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      url: r0,
      contentType: r1,
      title: (flags & 1) !== 0 ? c.string.decode(state) : null,
      thumbnail: (flags & 2) !== 0 ? c.string.decode(state) : null,
      time: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      volume: (flags & 8) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/cast-play-response
const encoding350 = encoding92

// @peartube/cast-pause-request
const encoding351 = encoding0

// @peartube/cast-pause-response
const encoding352 = encoding92

// @peartube/cast-resume-request
const encoding353 = encoding0

// @peartube/cast-resume-response
const encoding354 = encoding92

// @peartube/cast-stop-request
const encoding355 = encoding0

// @peartube/cast-stop-response
const encoding356 = encoding92

// @peartube/cast-seek-request
const encoding357 = {
  preencode(state, m) {
    c.uint.preencode(state, m.time)
  },
  encode(state, m) {
    c.uint.encode(state, m.time)
  },
  decode(state) {
    const r0 = c.uint.decode(state)

    return {
      time: r0
    }
  }
}

// @peartube/cast-seek-response
const encoding358 = encoding92

// @peartube/cast-set-volume-request
const encoding359 = {
  preencode(state, m) {
    c.uint.preencode(state, m.volume)
  },
  encode(state, m) {
    c.uint.encode(state, m.volume)
  },
  decode(state) {
    const r0 = c.uint.decode(state)

    return {
      volume: r0
    }
  }
}

// @peartube/cast-set-volume-response
const encoding360 = encoding92

// @peartube/cast-get-state-request
const encoding361 = encoding0

// @peartube/cast-get-state-response
const encoding362 = {
  preencode(state, m) {
    c.string.preencode(state, m.state)
    state.end++ // max flag is 4 so always one byte

    if (m.currentTime) c.uint.preencode(state, m.currentTime)
    if (m.duration) c.uint.preencode(state, m.duration)
    if (m.volume) c.uint.preencode(state, m.volume)
  },
  encode(state, m) {
    const flags = (m.currentTime ? 1 : 0) | (m.duration ? 2 : 0) | (m.volume ? 4 : 0)

    c.string.encode(state, m.state)
    c.uint.encode(state, flags)

    if (m.currentTime) c.uint.encode(state, m.currentTime)
    if (m.duration) c.uint.encode(state, m.duration)
    if (m.volume) c.uint.encode(state, m.volume)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      state: r0,
      currentTime: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      duration: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      volume: (flags & 4) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/cast-is-connected-request
const encoding363 = encoding0

// @peartube/cast-is-connected-response
const encoding364 = {
  preencode(state, m) {
    state.end++ // max flag is 1 so always one byte
  },
  encode(state, m) {
    const flags = m.connected ? 1 : 0

    c.uint.encode(state, flags)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      connected: (flags & 1) !== 0
    }
  }
}

// @peartube/event-cast-device-found.device
const encoding365_0 = encoding344_1

// @peartube/event-cast-device-found
const encoding365 = {
  preencode(state, m) {
    encoding365_0.preencode(state, m.device)
  },
  encode(state, m) {
    encoding365_0.encode(state, m.device)
  },
  decode(state) {
    const r0 = encoding365_0.decode(state)

    return {
      device: r0
    }
  }
}

// @peartube/event-cast-device-lost
const encoding366 = encoding345

// @peartube/event-cast-playback-state
const encoding367 = {
  preencode(state, m) {
    c.string.preencode(state, m.state)
  },
  encode(state, m) {
    c.string.encode(state, m.state)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      state: r0
    }
  }
}

// @peartube/event-cast-time-update
const encoding368 = {
  preencode(state, m) {
    c.uint.preencode(state, m.currentTime)
  },
  encode(state, m) {
    c.uint.encode(state, m.currentTime)
  },
  decode(state) {
    const r0 = c.uint.decode(state)

    return {
      currentTime: r0
    }
  }
}

// @peartube/search-videos-request
const encoding369 = {
  preencode(state, m) {
    c.string.preencode(state, m.channelKey)
    c.string.preencode(state, m.query)
    state.end++ // max flag is 2 so always one byte

    if (m.topK) c.uint.preencode(state, m.topK)
  },
  encode(state, m) {
    const flags = (m.topK ? 1 : 0) | (m.federated ? 2 : 0)

    c.string.encode(state, m.channelKey)
    c.string.encode(state, m.query)
    c.uint.encode(state, flags)

    if (m.topK) c.uint.encode(state, m.topK)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      channelKey: r0,
      query: r1,
      topK: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      federated: (flags & 2) !== 0
    }
  }
}

// @peartube/search-videos-response.results
const encoding370_0 = encoding262_0

// @peartube/search-videos-response
const encoding370 = {
  preencode(state, m) {
    encoding370_0.preencode(state, m.results)
  },
  encode(state, m) {
    encoding370_0.encode(state, m.results)
  },
  decode(state) {
    const r0 = encoding370_0.decode(state)

    return {
      results: r0
    }
  }
}

// @peartube/index-video-vectors-request
const encoding371 = encoding166

// @peartube/index-video-vectors-response
const encoding372 = encoding92

// @peartube/get-participation-status-request
const encoding373 = encoding0

// @peartube/get-participation-status-response.reasonCodes
const encoding374_13 = encoding44_11

// @peartube/get-participation-status-response
const encoding374 = {
  preencode(state, m) {
    state.end++ // max flag is 16 so always one byte
    c.string.preencode(state, m.mode)
    c.string.preencode(state, m.state)
    c.uint.preencode(state, m.cacheCeilingBytes)
    c.uint.preencode(state, m.uploadCeilingBytesPer24h)
    c.uint.preencode(state, m.uploadedBytesLast24h)
    c.uint.preencode(state, m.outboundBytesPerSecond)
    c.uint.preencode(state, m.postPlaybackGraceMs)
    c.uint.preencode(state, m.backgroundRemainingSessionMs)
    c.uint.preencode(state, m.backgroundRemainingDailyMs)
    encoding374_13.preencode(state, m.reasonCodes)

    if (m.errorCode) c.string.preencode(state, m.errorCode)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) |
      (m.uploadEligible ? 2 : 0) |
      (m.uploading ? 4 : 0) |
      (m.backgroundEligible ? 8 : 0) |
      (m.errorCode ? 16 : 0)

    c.uint.encode(state, flags)
    c.string.encode(state, m.mode)
    c.string.encode(state, m.state)
    c.uint.encode(state, m.cacheCeilingBytes)
    c.uint.encode(state, m.uploadCeilingBytesPer24h)
    c.uint.encode(state, m.uploadedBytesLast24h)
    c.uint.encode(state, m.outboundBytesPerSecond)
    c.uint.encode(state, m.postPlaybackGraceMs)
    c.uint.encode(state, m.backgroundRemainingSessionMs)
    c.uint.encode(state, m.backgroundRemainingDailyMs)
    encoding374_13.encode(state, m.reasonCodes)

    if (m.errorCode) c.string.encode(state, m.errorCode)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      mode: c.string.decode(state),
      state: c.string.decode(state),
      uploadEligible: (flags & 2) !== 0,
      uploading: (flags & 4) !== 0,
      backgroundEligible: (flags & 8) !== 0,
      cacheCeilingBytes: c.uint.decode(state),
      uploadCeilingBytesPer24h: c.uint.decode(state),
      uploadedBytesLast24h: c.uint.decode(state),
      outboundBytesPerSecond: c.uint.decode(state),
      postPlaybackGraceMs: c.uint.decode(state),
      backgroundRemainingSessionMs: c.uint.decode(state),
      backgroundRemainingDailyMs: c.uint.decode(state),
      reasonCodes: encoding374_13.decode(state),
      errorCode: (flags & 16) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/set-device-conditions-request
const encoding375 = {
  preencode(state, m) {
    const flags =
      (m.metered ? 1 : 0) |
      (m.meteredProvided ? 2 : 0) |
      (m.thermalState ? 4 : 0) |
      (m.batteryPercent ? 8 : 0) |
      (m.batteryPercentProvided ? 16 : 0) |
      (m.charging ? 32 : 0) |
      (m.chargingProvided ? 64 : 0) |
      (m.backgroundPermitted ? 128 : 0) |
      (m.backgroundPermittedProvided ? 256 : 0) |
      (m.freeDiskBytes ? 512 : 0) |
      (m.freeDiskBytesProvided ? 1024 : 0) |
      (m.totalDiskBytes ? 2048 : 0) |
      (m.totalDiskBytesProvided ? 4096 : 0)

    c.uint.preencode(state, flags)

    if (m.thermalState) c.string.preencode(state, m.thermalState)
    if (m.batteryPercent) c.uint.preencode(state, m.batteryPercent)
    if (m.freeDiskBytes) c.uint.preencode(state, m.freeDiskBytes)
    if (m.totalDiskBytes) c.uint.preencode(state, m.totalDiskBytes)
  },
  encode(state, m) {
    const flags =
      (m.metered ? 1 : 0) |
      (m.meteredProvided ? 2 : 0) |
      (m.thermalState ? 4 : 0) |
      (m.batteryPercent ? 8 : 0) |
      (m.batteryPercentProvided ? 16 : 0) |
      (m.charging ? 32 : 0) |
      (m.chargingProvided ? 64 : 0) |
      (m.backgroundPermitted ? 128 : 0) |
      (m.backgroundPermittedProvided ? 256 : 0) |
      (m.freeDiskBytes ? 512 : 0) |
      (m.freeDiskBytesProvided ? 1024 : 0) |
      (m.totalDiskBytes ? 2048 : 0) |
      (m.totalDiskBytesProvided ? 4096 : 0)

    c.uint.encode(state, flags)

    if (m.thermalState) c.string.encode(state, m.thermalState)
    if (m.batteryPercent) c.uint.encode(state, m.batteryPercent)
    if (m.freeDiskBytes) c.uint.encode(state, m.freeDiskBytes)
    if (m.totalDiskBytes) c.uint.encode(state, m.totalDiskBytes)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      metered: (flags & 1) !== 0,
      meteredProvided: (flags & 2) !== 0,
      thermalState: (flags & 4) !== 0 ? c.string.decode(state) : null,
      batteryPercent: (flags & 8) !== 0 ? c.uint.decode(state) : 0,
      batteryPercentProvided: (flags & 16) !== 0,
      charging: (flags & 32) !== 0,
      chargingProvided: (flags & 64) !== 0,
      backgroundPermitted: (flags & 128) !== 0,
      backgroundPermittedProvided: (flags & 256) !== 0,
      freeDiskBytes: (flags & 512) !== 0 ? c.uint.decode(state) : 0,
      freeDiskBytesProvided: (flags & 1024) !== 0,
      totalDiskBytes: (flags & 2048) !== 0 ? c.uint.decode(state) : 0,
      totalDiskBytesProvided: (flags & 4096) !== 0
    }
  }
}

// @peartube/set-device-conditions-response
const encoding376 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte

    if (m.errorCode) c.string.preencode(state, m.errorCode)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.errorCode ? 2 : 0)

    c.uint.encode(state, flags)

    if (m.errorCode) c.string.encode(state, m.errorCode)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      errorCode: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/suspend-network-request
const encoding377 = encoding0

// @peartube/suspend-network-response
const encoding378 = encoding92

// @peartube/resume-network-request
const encoding379 = encoding0

// @peartube/resume-network-response
const encoding380 = encoding92

// @peartube/set-playback-active-request
const encoding381 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte

    if (m.ttlMs) c.uint.preencode(state, m.ttlMs)
  },
  encode(state, m) {
    const flags = (m.active ? 1 : 0) | (m.ttlMs ? 2 : 0)

    c.uint.encode(state, flags)

    if (m.ttlMs) c.uint.encode(state, m.ttlMs)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      active: (flags & 1) !== 0,
      ttlMs: (flags & 2) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/set-playback-active-response
const encoding382 = {
  preencode(state, m) {
    state.end++ // max flag is 8 so always one byte

    if (m.updatedAt) c.uint.preencode(state, m.updatedAt)
    if (m.expiresAt) c.uint.preencode(state, m.expiresAt)
  },
  encode(state, m) {
    const flags =
      (m.success ? 1 : 0) | (m.active ? 2 : 0) | (m.updatedAt ? 4 : 0) | (m.expiresAt ? 8 : 0)

    c.uint.encode(state, flags)

    if (m.updatedAt) c.uint.encode(state, m.updatedAt)
    if (m.expiresAt) c.uint.encode(state, m.expiresAt)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      active: (flags & 2) !== 0,
      updatedAt: (flags & 4) !== 0 ? c.uint.decode(state) : 0,
      expiresAt: (flags & 8) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/provider-error
const encoding383 = {
  preencode(state, m) {
    c.string.preencode(state, m.code)
    c.string.preencode(state, m.message)
    state.end++ // max flag is 2 so always one byte

    if (m.field) c.string.preencode(state, m.field)
  },
  encode(state, m) {
    const flags = (m.field ? 1 : 0) | (m.retryable ? 2 : 0)

    c.string.encode(state, m.code)
    c.string.encode(state, m.message)
    c.uint.encode(state, flags)

    if (m.field) c.string.encode(state, m.field)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      code: r0,
      message: r1,
      field: (flags & 1) !== 0 ? c.string.decode(state) : null,
      retryable: (flags & 2) !== 0
    }
  }
}

// @peartube/provider-search-hit-v1
const encoding384 = {
  preencode(state, m) {
    c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.resolutionRef)
    c.string.preencode(state, m.title)
    c.string.preencode(state, m.mediaKind)
    state.end++ // max flag is 32 so always one byte

    if (m.subtitle) c.string.preencode(state, m.subtitle)
    if (m.entityId) c.string.preencode(state, m.entityId)
    if (m.publicationId) c.string.preencode(state, m.publicationId)
    if (m.expectedBytes) c.uint.preencode(state, m.expectedBytes)
  },
  encode(state, m) {
    const flags =
      (m.subtitle ? 1 : 0) |
      (m.published ? 2 : 0) |
      (m.acquirable ? 4 : 0) |
      (m.entityId ? 8 : 0) |
      (m.publicationId ? 16 : 0) |
      (m.expectedBytes ? 32 : 0)

    c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.resolutionRef)
    c.string.encode(state, m.title)
    c.string.encode(state, m.mediaKind)
    c.uint.encode(state, flags)

    if (m.subtitle) c.string.encode(state, m.subtitle)
    if (m.entityId) c.string.encode(state, m.entityId)
    if (m.publicationId) c.string.encode(state, m.publicationId)
    if (m.expectedBytes) c.uint.encode(state, m.expectedBytes)
  },
  decode(state) {
    const r0 = c.uint.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const r3 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      schemaVersion: r0,
      resolutionRef: r1,
      title: r2,
      mediaKind: r3,
      subtitle: (flags & 1) !== 0 ? c.string.decode(state) : null,
      published: (flags & 2) !== 0,
      acquirable: (flags & 4) !== 0,
      entityId: (flags & 8) !== 0 ? c.string.decode(state) : null,
      publicationId: (flags & 16) !== 0 ? c.string.decode(state) : null,
      expectedBytes: (flags & 32) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/provider-search-request
const encoding385 = {
  preencode(state, m) {
    c.string.preencode(state, m.query)
    state.end++ // max flag is 2 so always one byte

    if (m.cursor) c.string.preencode(state, m.cursor)
    if (m.limit) c.uint.preencode(state, m.limit)
  },
  encode(state, m) {
    const flags = (m.cursor ? 1 : 0) | (m.limit ? 2 : 0)

    c.string.encode(state, m.query)
    c.uint.encode(state, flags)

    if (m.cursor) c.string.encode(state, m.cursor)
    if (m.limit) c.uint.encode(state, m.limit)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      query: r0,
      cursor: (flags & 1) !== 0 ? c.string.decode(state) : null,
      limit: (flags & 2) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/provider-search-response.hits
const encoding386_1 = c.array(c.frame(encoding384))
// @peartube/provider-search-response.error
const encoding386_3 = c.frame(encoding383)

// @peartube/provider-search-response
const encoding386 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte
    encoding386_1.preencode(state, m.hits)

    if (m.nextCursor) c.string.preencode(state, m.nextCursor)
    if (m.error) encoding386_3.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.nextCursor ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)
    encoding386_1.encode(state, m.hits)

    if (m.nextCursor) c.string.encode(state, m.nextCursor)
    if (m.error) encoding386_3.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      hits: encoding386_1.decode(state),
      nextCursor: (flags & 2) !== 0 ? c.string.decode(state) : null,
      error: (flags & 4) !== 0 ? encoding386_3.decode(state) : null
    }
  }
}

// @peartube/provider-resolution-v1
const encoding387 = {
  preencode(state, m) {
    c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.resolutionRef)
    c.string.preencode(state, m.publisherId)
    c.string.preencode(state, m.title)
    c.string.preencode(state, m.mediaKind)
    state.end++ // max flag is 32 so always one byte

    if (m.subtitle) c.string.preencode(state, m.subtitle)
    if (m.entityId) c.string.preencode(state, m.entityId)
    if (m.publicationId) c.string.preencode(state, m.publicationId)
    if (m.expectedBytes) c.uint.preencode(state, m.expectedBytes)
  },
  encode(state, m) {
    const flags =
      (m.subtitle ? 1 : 0) |
      (m.published ? 2 : 0) |
      (m.acquirable ? 4 : 0) |
      (m.entityId ? 8 : 0) |
      (m.publicationId ? 16 : 0) |
      (m.expectedBytes ? 32 : 0)

    c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.resolutionRef)
    c.string.encode(state, m.publisherId)
    c.string.encode(state, m.title)
    c.string.encode(state, m.mediaKind)
    c.uint.encode(state, flags)

    if (m.subtitle) c.string.encode(state, m.subtitle)
    if (m.entityId) c.string.encode(state, m.entityId)
    if (m.publicationId) c.string.encode(state, m.publicationId)
    if (m.expectedBytes) c.uint.encode(state, m.expectedBytes)
  },
  decode(state) {
    const r0 = c.uint.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const r3 = c.string.decode(state)
    const r4 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      schemaVersion: r0,
      resolutionRef: r1,
      publisherId: r2,
      title: r3,
      mediaKind: r4,
      subtitle: (flags & 1) !== 0 ? c.string.decode(state) : null,
      published: (flags & 2) !== 0,
      acquirable: (flags & 4) !== 0,
      entityId: (flags & 8) !== 0 ? c.string.decode(state) : null,
      publicationId: (flags & 16) !== 0 ? c.string.decode(state) : null,
      expectedBytes: (flags & 32) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

// @peartube/resolve-provider-ref-request
const encoding388 = {
  preencode(state, m) {
    c.string.preencode(state, m.resolutionRef)
  },
  encode(state, m) {
    c.string.encode(state, m.resolutionRef)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      resolutionRef: r0
    }
  }
}

// @peartube/resolve-provider-ref-response.resolution
const encoding389_1 = c.frame(encoding387)
// @peartube/resolve-provider-ref-response.error
const encoding389_2 = encoding386_3

// @peartube/resolve-provider-ref-response
const encoding389 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.resolution) encoding389_1.preencode(state, m.resolution)
    if (m.error) encoding389_2.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.resolution ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.resolution) encoding389_1.encode(state, m.resolution)
    if (m.error) encoding389_2.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      resolution: (flags & 2) !== 0 ? encoding389_1.decode(state) : null,
      error: (flags & 4) !== 0 ? encoding389_2.decode(state) : null
    }
  }
}

// @peartube/acquisition-request-v1
const encoding390 = {
  preencode(state, m) {
    c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.resolutionRef)
    c.string.preencode(state, m.publisherId)
    c.string.preencode(state, m.retentionClass)
    state.end++ // max flag is 2 so always one byte

    if (m.retentionUntil) c.uint.preencode(state, m.retentionUntil)
  },
  encode(state, m) {
    const flags = (m.retentionUntil ? 1 : 0) | (m.retentionUntilPresent ? 2 : 0)

    c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.resolutionRef)
    c.string.encode(state, m.publisherId)
    c.string.encode(state, m.retentionClass)
    c.uint.encode(state, flags)

    if (m.retentionUntil) c.uint.encode(state, m.retentionUntil)
  },
  decode(state) {
    const r0 = c.uint.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const r3 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      schemaVersion: r0,
      resolutionRef: r1,
      publisherId: r2,
      retentionClass: r3,
      retentionUntil: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      retentionUntilPresent: (flags & 2) !== 0
    }
  }
}

// @peartube/acquisition-v1
const encoding391 = {
  preencode(state, m) {
    c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.acquisitionId)
    c.string.preencode(state, m.state)
    c.string.preencode(state, m.retentionClass)
    c.uint.preencode(state, m.bytesAcquired)
    state.end++ // max flag is 64 so always one byte

    if (m.expectedBytes) c.uint.preencode(state, m.expectedBytes)
    if (m.publicationId) c.string.preencode(state, m.publicationId)
    if (m.manifestId) c.string.preencode(state, m.manifestId)
    if (m.renditionId) c.string.preencode(state, m.renditionId)
    if (m.assetId) c.string.preencode(state, m.assetId)
    if (m.errorCode) c.string.preencode(state, m.errorCode)
    c.uint.preencode(state, m.createdAt)
    c.uint.preencode(state, m.updatedAt)
  },
  encode(state, m) {
    const flags =
      (m.expectedBytes ? 1 : 0) |
      (m.publicationId ? 2 : 0) |
      (m.manifestId ? 4 : 0) |
      (m.renditionId ? 8 : 0) |
      (m.assetId ? 16 : 0) |
      (m.errorCode ? 32 : 0) |
      (m.recoverable ? 64 : 0)

    c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.acquisitionId)
    c.string.encode(state, m.state)
    c.string.encode(state, m.retentionClass)
    c.uint.encode(state, m.bytesAcquired)
    c.uint.encode(state, flags)

    if (m.expectedBytes) c.uint.encode(state, m.expectedBytes)
    if (m.publicationId) c.string.encode(state, m.publicationId)
    if (m.manifestId) c.string.encode(state, m.manifestId)
    if (m.renditionId) c.string.encode(state, m.renditionId)
    if (m.assetId) c.string.encode(state, m.assetId)
    if (m.errorCode) c.string.encode(state, m.errorCode)
    c.uint.encode(state, m.createdAt)
    c.uint.encode(state, m.updatedAt)
  },
  decode(state) {
    const r0 = c.uint.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const r3 = c.string.decode(state)
    const r4 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      schemaVersion: r0,
      acquisitionId: r1,
      state: r2,
      retentionClass: r3,
      bytesAcquired: r4,
      expectedBytes: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      publicationId: (flags & 2) !== 0 ? c.string.decode(state) : null,
      manifestId: (flags & 4) !== 0 ? c.string.decode(state) : null,
      renditionId: (flags & 8) !== 0 ? c.string.decode(state) : null,
      assetId: (flags & 16) !== 0 ? c.string.decode(state) : null,
      errorCode: (flags & 32) !== 0 ? c.string.decode(state) : null,
      recoverable: (flags & 64) !== 0,
      createdAt: c.uint.decode(state),
      updatedAt: c.uint.decode(state)
    }
  }
}

// @peartube/request-acquisition-request.request
const encoding392_1 = c.frame(encoding390)

// @peartube/request-acquisition-request
const encoding392 = {
  preencode(state, m) {
    c.string.preencode(state, m.idempotencyKey)
    encoding392_1.preencode(state, m.request)
  },
  encode(state, m) {
    c.string.encode(state, m.idempotencyKey)
    encoding392_1.encode(state, m.request)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = encoding392_1.decode(state)

    return {
      idempotencyKey: r0,
      request: r1
    }
  }
}

// @peartube/request-acquisition-response.acquisition
const encoding393_1 = c.frame(encoding391)
// @peartube/request-acquisition-response.error
const encoding393_2 = encoding386_3

// @peartube/request-acquisition-response
const encoding393 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.acquisition) encoding393_1.preencode(state, m.acquisition)
    if (m.error) encoding393_2.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.acquisition ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.acquisition) encoding393_1.encode(state, m.acquisition)
    if (m.error) encoding393_2.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      acquisition: (flags & 2) !== 0 ? encoding393_1.decode(state) : null,
      error: (flags & 4) !== 0 ? encoding393_2.decode(state) : null
    }
  }
}

// @peartube/attach-source-grant-request
const encoding394 = {
  preencode(state, m) {
    c.string.preencode(state, m.acquisitionId)
    c.buffer.preencode(state, m.grant)
  },
  encode(state, m) {
    c.string.encode(state, m.acquisitionId)
    c.buffer.encode(state, m.grant)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.buffer.decode(state)

    return {
      acquisitionId: r0,
      grant: r1
    }
  }
}

// @peartube/attach-source-grant-response.acquisition
const encoding395_1 = encoding393_1
// @peartube/attach-source-grant-response.error
const encoding395_2 = encoding386_3

// @peartube/attach-source-grant-response
const encoding395 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.acquisition) encoding395_1.preencode(state, m.acquisition)
    if (m.error) encoding395_2.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.acquisition ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.acquisition) encoding395_1.encode(state, m.acquisition)
    if (m.error) encoding395_2.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      acquisition: (flags & 2) !== 0 ? encoding395_1.decode(state) : null,
      error: (flags & 4) !== 0 ? encoding395_2.decode(state) : null
    }
  }
}

// @peartube/get-acquisition-request
const encoding396 = {
  preencode(state, m) {
    c.string.preencode(state, m.acquisitionId)
  },
  encode(state, m) {
    c.string.encode(state, m.acquisitionId)
  },
  decode(state) {
    const r0 = c.string.decode(state)

    return {
      acquisitionId: r0
    }
  }
}

// @peartube/get-acquisition-response.acquisition
const encoding397_1 = encoding393_1
// @peartube/get-acquisition-response.error
const encoding397_2 = encoding386_3

// @peartube/get-acquisition-response
const encoding397 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.acquisition) encoding397_1.preencode(state, m.acquisition)
    if (m.error) encoding397_2.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.acquisition ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.acquisition) encoding397_1.encode(state, m.acquisition)
    if (m.error) encoding397_2.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      acquisition: (flags & 2) !== 0 ? encoding397_1.decode(state) : null,
      error: (flags & 4) !== 0 ? encoding397_2.decode(state) : null
    }
  }
}

// @peartube/list-acquisitions-request.states
const encoding398_2 = encoding44_11

// @peartube/list-acquisitions-request
const encoding398 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.cursor) c.string.preencode(state, m.cursor)
    if (m.limit) c.uint.preencode(state, m.limit)
    if (m.states) encoding398_2.preencode(state, m.states)
  },
  encode(state, m) {
    const flags = (m.cursor ? 1 : 0) | (m.limit ? 2 : 0) | (m.states ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.cursor) c.string.encode(state, m.cursor)
    if (m.limit) c.uint.encode(state, m.limit)
    if (m.states) encoding398_2.encode(state, m.states)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      cursor: (flags & 1) !== 0 ? c.string.decode(state) : null,
      limit: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      states: (flags & 4) !== 0 ? encoding398_2.decode(state) : null
    }
  }
}

// @peartube/list-acquisitions-response.acquisitions
const encoding399_1 = c.array(c.frame(encoding391))
// @peartube/list-acquisitions-response.error
const encoding399_3 = encoding386_3

// @peartube/list-acquisitions-response
const encoding399 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte
    encoding399_1.preencode(state, m.acquisitions)

    if (m.nextCursor) c.string.preencode(state, m.nextCursor)
    if (m.error) encoding399_3.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.nextCursor ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)
    encoding399_1.encode(state, m.acquisitions)

    if (m.nextCursor) c.string.encode(state, m.nextCursor)
    if (m.error) encoding399_3.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      acquisitions: encoding399_1.decode(state),
      nextCursor: (flags & 2) !== 0 ? c.string.decode(state) : null,
      error: (flags & 4) !== 0 ? encoding399_3.decode(state) : null
    }
  }
}

// @peartube/cancel-acquisition-request
const encoding400 = encoding396

// @peartube/cancel-acquisition-response.acquisition
const encoding401_1 = encoding393_1
// @peartube/cancel-acquisition-response.error
const encoding401_2 = encoding386_3

// @peartube/cancel-acquisition-response
const encoding401 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.acquisition) encoding401_1.preencode(state, m.acquisition)
    if (m.error) encoding401_2.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.acquisition ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.acquisition) encoding401_1.encode(state, m.acquisition)
    if (m.error) encoding401_2.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      acquisition: (flags & 2) !== 0 ? encoding401_1.decode(state) : null,
      error: (flags & 4) !== 0 ? encoding401_2.decode(state) : null
    }
  }
}

// @peartube/retry-acquisition-request
const encoding402 = encoding396

// @peartube/retry-acquisition-response.acquisition
const encoding403_1 = encoding393_1
// @peartube/retry-acquisition-response.error
const encoding403_2 = encoding386_3

// @peartube/retry-acquisition-response
const encoding403 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.acquisition) encoding403_1.preencode(state, m.acquisition)
    if (m.error) encoding403_2.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.acquisition ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.acquisition) encoding403_1.encode(state, m.acquisition)
    if (m.error) encoding403_2.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      acquisition: (flags & 2) !== 0 ? encoding403_1.decode(state) : null,
      error: (flags & 4) !== 0 ? encoding403_2.decode(state) : null
    }
  }
}

// @peartube/provider-publication-v1
const encoding404 = {
  preencode(state, m) {
    c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.publicationId)
    c.string.preencode(state, m.entityId)
    c.string.preencode(state, m.manifestId)
    c.string.preencode(state, m.renditionId)
    c.string.preencode(state, m.assetId)
    c.string.preencode(state, m.title)
  },
  encode(state, m) {
    c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.publicationId)
    c.string.encode(state, m.entityId)
    c.string.encode(state, m.manifestId)
    c.string.encode(state, m.renditionId)
    c.string.encode(state, m.assetId)
    c.string.encode(state, m.title)
  },
  decode(state) {
    const r0 = c.uint.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const r3 = c.string.decode(state)
    const r4 = c.string.decode(state)
    const r5 = c.string.decode(state)
    const r6 = c.string.decode(state)

    return {
      schemaVersion: r0,
      publicationId: r1,
      entityId: r2,
      manifestId: r3,
      renditionId: r4,
      assetId: r5,
      title: r6
    }
  }
}

// @peartube/get-provider-publication-request
const encoding405 = encoding217

// @peartube/get-provider-publication-response.publication
const encoding406_1 = c.frame(encoding404)
// @peartube/get-provider-publication-response.error
const encoding406_2 = encoding386_3

// @peartube/get-provider-publication-response
const encoding406 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.publication) encoding406_1.preencode(state, m.publication)
    if (m.error) encoding406_2.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.publication ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.publication) encoding406_1.encode(state, m.publication)
    if (m.error) encoding406_2.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      publication: (flags & 2) !== 0 ? encoding406_1.decode(state) : null,
      error: (flags & 4) !== 0 ? encoding406_2.decode(state) : null
    }
  }
}

// @peartube/provider-stream-v1
const encoding407 = {
  preencode(state, m) {
    c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.publicationId)
    c.string.preencode(state, m.renditionId)
    c.string.preencode(state, m.assetId)
    c.string.preencode(state, m.url)
    c.string.preencode(state, m.mimeType)
    c.uint.preencode(state, m.byteLength)
    state.end++ // max flag is 1 so always one byte

    if (m.etag) c.string.preencode(state, m.etag)
  },
  encode(state, m) {
    const flags = m.etag ? 1 : 0

    c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.publicationId)
    c.string.encode(state, m.renditionId)
    c.string.encode(state, m.assetId)
    c.string.encode(state, m.url)
    c.string.encode(state, m.mimeType)
    c.uint.encode(state, m.byteLength)
    c.uint.encode(state, flags)

    if (m.etag) c.string.encode(state, m.etag)
  },
  decode(state) {
    const r0 = c.uint.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const r3 = c.string.decode(state)
    const r4 = c.string.decode(state)
    const r5 = c.string.decode(state)
    const r6 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      schemaVersion: r0,
      publicationId: r1,
      renditionId: r2,
      assetId: r3,
      url: r4,
      mimeType: r5,
      byteLength: r6,
      etag: (flags & 1) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/open-provider-stream-request
const encoding408 = {
  preencode(state, m) {
    c.string.preencode(state, m.publicationId)
    state.end++ // max flag is 1 so always one byte

    if (m.renditionId) c.string.preencode(state, m.renditionId)
  },
  encode(state, m) {
    const flags = m.renditionId ? 1 : 0

    c.string.encode(state, m.publicationId)
    c.uint.encode(state, flags)

    if (m.renditionId) c.string.encode(state, m.renditionId)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      publicationId: r0,
      renditionId: (flags & 1) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/open-provider-stream-response.stream
const encoding409_1 = c.frame(encoding407)
// @peartube/open-provider-stream-response.error
const encoding409_2 = encoding386_3

// @peartube/open-provider-stream-response
const encoding409 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.stream) encoding409_1.preencode(state, m.stream)
    if (m.error) encoding409_2.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.stream ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.stream) encoding409_1.encode(state, m.stream)
    if (m.error) encoding409_2.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      stream: (flags & 2) !== 0 ? encoding409_1.decode(state) : null,
      error: (flags & 4) !== 0 ? encoding409_2.decode(state) : null
    }
  }
}

// @peartube/provider-status-v1
const encoding410 = {
  preencode(state, m) {
    c.uint.preencode(state, m.schemaVersion)
    state.end++ // max flag is 4 so always one byte
    c.uint.preencode(state, m.queuedAcquisitions)
    c.uint.preencode(state, m.activeAcquisitions)
  },
  encode(state, m) {
    const flags = (m.ready ? 1 : 0) | (m.searchEnabled ? 2 : 0) | (m.acquisitionEnabled ? 4 : 0)

    c.uint.encode(state, m.schemaVersion)
    c.uint.encode(state, flags)
    c.uint.encode(state, m.queuedAcquisitions)
    c.uint.encode(state, m.activeAcquisitions)
  },
  decode(state) {
    const r0 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      schemaVersion: r0,
      ready: (flags & 1) !== 0,
      searchEnabled: (flags & 2) !== 0,
      acquisitionEnabled: (flags & 4) !== 0,
      queuedAcquisitions: c.uint.decode(state),
      activeAcquisitions: c.uint.decode(state)
    }
  }
}

// @peartube/get-provider-status-request
const encoding411 = encoding0

// @peartube/get-provider-status-response.status
const encoding412_1 = c.frame(encoding410)
// @peartube/get-provider-status-response.error
const encoding412_2 = encoding386_3

// @peartube/get-provider-status-response
const encoding412 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.status) encoding412_1.preencode(state, m.status)
    if (m.error) encoding412_2.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.status ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.status) encoding412_1.encode(state, m.status)
    if (m.error) encoding412_2.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      status: (flags & 2) !== 0 ? encoding412_1.decode(state) : null,
      error: (flags & 4) !== 0 ? encoding412_2.decode(state) : null
    }
  }
}

// @peartube/provider-policy-v1
const encoding413 = {
  preencode(state, m) {
    c.uint.preencode(state, m.schemaVersion)
    c.uint.preencode(state, m.revision)
    state.end++ // max flag is 4 so always one byte
  },
  encode(state, m) {
    const flags =
      (m.searchEnabled ? 1 : 0) | (m.resolveEnabled ? 2 : 0) | (m.acquisitionEnabled ? 4 : 0)

    c.uint.encode(state, m.schemaVersion)
    c.uint.encode(state, m.revision)
    c.uint.encode(state, flags)
  },
  decode(state) {
    const r0 = c.uint.decode(state)
    const r1 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      schemaVersion: r0,
      revision: r1,
      searchEnabled: (flags & 1) !== 0,
      resolveEnabled: (flags & 2) !== 0,
      acquisitionEnabled: (flags & 4) !== 0
    }
  }
}

// @peartube/get-provider-policy-request
const encoding414 = encoding0

// @peartube/get-provider-policy-response.policy
const encoding415_1 = c.frame(encoding413)
// @peartube/get-provider-policy-response.error
const encoding415_2 = encoding386_3

// @peartube/get-provider-policy-response
const encoding415 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.policy) encoding415_1.preencode(state, m.policy)
    if (m.error) encoding415_2.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.policy ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.policy) encoding415_1.encode(state, m.policy)
    if (m.error) encoding415_2.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      policy: (flags & 2) !== 0 ? encoding415_1.decode(state) : null,
      error: (flags & 4) !== 0 ? encoding415_2.decode(state) : null
    }
  }
}

// @peartube/set-provider-policy-request.policy
const encoding416_0 = encoding415_1

// @peartube/set-provider-policy-request
const encoding416 = {
  preencode(state, m) {
    encoding416_0.preencode(state, m.policy)
    c.uint.preencode(state, m.expectedRevision)
  },
  encode(state, m) {
    encoding416_0.encode(state, m.policy)
    c.uint.encode(state, m.expectedRevision)
  },
  decode(state) {
    const r0 = encoding416_0.decode(state)
    const r1 = c.uint.decode(state)

    return {
      policy: r0,
      expectedRevision: r1
    }
  }
}

// @peartube/set-provider-policy-response.policy
const encoding417_1 = encoding415_1
// @peartube/set-provider-policy-response.error
const encoding417_2 = encoding386_3

// @peartube/set-provider-policy-response
const encoding417 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.policy) encoding417_1.preencode(state, m.policy)
    if (m.error) encoding417_2.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.policy ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.policy) encoding417_1.encode(state, m.policy)
    if (m.error) encoding417_2.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      policy: (flags & 2) !== 0 ? encoding417_1.decode(state) : null,
      error: (flags & 4) !== 0 ? encoding417_2.decode(state) : null
    }
  }
}

// @peartube/acquisition-policy-v1.allowedPublisherIds
const encoding418_7 = encoding44_11
// @peartube/acquisition-policy-v1.allowedAdapterIds
const encoding418_8 = encoding44_11

// @peartube/acquisition-policy-v1
const encoding418 = {
  preencode(state, m) {
    c.uint.preencode(state, m.policyVersion)
    c.uint.preencode(state, m.revision)
    c.uint.preencode(state, m.consentVersion)
    state.end++ // max flag is 4 so always one byte
    c.string.preencode(state, m.requesterMode)
    encoding418_7.preencode(state, m.allowedPublisherIds)
    encoding418_8.preencode(state, m.allowedAdapterIds)
    c.uint.preencode(state, m.maxQueuedJobs)
    c.uint.preencode(state, m.maxConcurrentJobs)
    c.uint.preencode(state, m.maxConcurrentPerRequester)
    c.uint.preencode(state, m.maxRequestBytes)
    c.uint.preencode(state, m.maxAcquireBytesPer24h)
    c.uint.preencode(state, m.maxAcquireBytesPerSecond)
    c.uint.preencode(state, m.maxStagingBytes)
    c.uint.preencode(state, m.minFreeDiskBytes)
    c.uint.preencode(state, m.maxJobRuntimeMs)
    c.uint.preencode(state, m.sourceGrantTtlMs)
    c.uint.preencode(state, m.publicRequestsPerMinute)
    c.uint.preencode(state, m.maxAttempts)
    c.uint.preencode(state, m.retryBaseMs)
    c.uint.preencode(state, m.retryMaxMs)
  },
  encode(state, m) {
    const flags =
      (m.migrationRequired ? 1 : 0) | (m.enabled ? 2 : 0) | (m.acceptPublicRequests ? 4 : 0)

    c.uint.encode(state, m.policyVersion)
    c.uint.encode(state, m.revision)
    c.uint.encode(state, m.consentVersion)
    c.uint.encode(state, flags)
    c.string.encode(state, m.requesterMode)
    encoding418_7.encode(state, m.allowedPublisherIds)
    encoding418_8.encode(state, m.allowedAdapterIds)
    c.uint.encode(state, m.maxQueuedJobs)
    c.uint.encode(state, m.maxConcurrentJobs)
    c.uint.encode(state, m.maxConcurrentPerRequester)
    c.uint.encode(state, m.maxRequestBytes)
    c.uint.encode(state, m.maxAcquireBytesPer24h)
    c.uint.encode(state, m.maxAcquireBytesPerSecond)
    c.uint.encode(state, m.maxStagingBytes)
    c.uint.encode(state, m.minFreeDiskBytes)
    c.uint.encode(state, m.maxJobRuntimeMs)
    c.uint.encode(state, m.sourceGrantTtlMs)
    c.uint.encode(state, m.publicRequestsPerMinute)
    c.uint.encode(state, m.maxAttempts)
    c.uint.encode(state, m.retryBaseMs)
    c.uint.encode(state, m.retryMaxMs)
  },
  decode(state) {
    const r0 = c.uint.decode(state)
    const r1 = c.uint.decode(state)
    const r2 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      policyVersion: r0,
      revision: r1,
      consentVersion: r2,
      migrationRequired: (flags & 1) !== 0,
      enabled: (flags & 2) !== 0,
      acceptPublicRequests: (flags & 4) !== 0,
      requesterMode: c.string.decode(state),
      allowedPublisherIds: encoding418_7.decode(state),
      allowedAdapterIds: encoding418_8.decode(state),
      maxQueuedJobs: c.uint.decode(state),
      maxConcurrentJobs: c.uint.decode(state),
      maxConcurrentPerRequester: c.uint.decode(state),
      maxRequestBytes: c.uint.decode(state),
      maxAcquireBytesPer24h: c.uint.decode(state),
      maxAcquireBytesPerSecond: c.uint.decode(state),
      maxStagingBytes: c.uint.decode(state),
      minFreeDiskBytes: c.uint.decode(state),
      maxJobRuntimeMs: c.uint.decode(state),
      sourceGrantTtlMs: c.uint.decode(state),
      publicRequestsPerMinute: c.uint.decode(state),
      maxAttempts: c.uint.decode(state),
      retryBaseMs: c.uint.decode(state),
      retryMaxMs: c.uint.decode(state)
    }
  }
}

// @peartube/get-acquisition-policy-request
const encoding419 = encoding0

// @peartube/get-acquisition-policy-response.policy
const encoding420_1 = c.frame(encoding418)
// @peartube/get-acquisition-policy-response.error
const encoding420_2 = encoding386_3

// @peartube/get-acquisition-policy-response
const encoding420 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.policy) encoding420_1.preencode(state, m.policy)
    if (m.error) encoding420_2.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.policy ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.policy) encoding420_1.encode(state, m.policy)
    if (m.error) encoding420_2.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      policy: (flags & 2) !== 0 ? encoding420_1.decode(state) : null,
      error: (flags & 4) !== 0 ? encoding420_2.decode(state) : null
    }
  }
}

// @peartube/acquisition-consent-v1
const encoding421 = {
  preencode(state, m) {
    c.uint.preencode(state, m.version)
    state.end++ // max flag is 1 so always one byte
  },
  encode(state, m) {
    const flags = m.granted ? 1 : 0

    c.uint.encode(state, m.version)
    c.uint.encode(state, flags)
  },
  decode(state) {
    const r0 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      version: r0,
      granted: (flags & 1) !== 0
    }
  }
}

// @peartube/set-acquisition-policy-request.policy
const encoding422_0 = encoding420_1
// @peartube/set-acquisition-policy-request.consent
const encoding422_2 = c.frame(encoding421)

// @peartube/set-acquisition-policy-request
const encoding422 = {
  preencode(state, m) {
    encoding422_0.preencode(state, m.policy)
    c.uint.preencode(state, m.expectedRevision)
    encoding422_2.preencode(state, m.consent)
  },
  encode(state, m) {
    encoding422_0.encode(state, m.policy)
    c.uint.encode(state, m.expectedRevision)
    encoding422_2.encode(state, m.consent)
  },
  decode(state) {
    const r0 = encoding422_0.decode(state)
    const r1 = c.uint.decode(state)
    const r2 = encoding422_2.decode(state)

    return {
      policy: r0,
      expectedRevision: r1,
      consent: r2
    }
  }
}

// @peartube/set-acquisition-policy-response.policy
const encoding423_1 = encoding420_1
// @peartube/set-acquisition-policy-response.error
const encoding423_2 = encoding386_3

// @peartube/set-acquisition-policy-response
const encoding423 = {
  preencode(state, m) {
    state.end++ // max flag is 4 so always one byte

    if (m.policy) encoding423_1.preencode(state, m.policy)
    if (m.error) encoding423_2.preencode(state, m.error)
  },
  encode(state, m) {
    const flags = (m.success ? 1 : 0) | (m.policy ? 2 : 0) | (m.error ? 4 : 0)

    c.uint.encode(state, flags)

    if (m.policy) encoding423_1.encode(state, m.policy)
    if (m.error) encoding423_2.encode(state, m.error)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      success: (flags & 1) !== 0,
      policy: (flags & 2) !== 0 ? encoding423_1.decode(state) : null,
      error: (flags & 4) !== 0 ? encoding423_2.decode(state) : null
    }
  }
}

// @peartube/acquisition-lifecycle-event-v1
const encoding424 = {
  preencode(state, m) {
    c.uint.preencode(state, m.schemaVersion)
    c.string.preencode(state, m.eventId)
    c.string.preencode(state, m.acquisitionId)
    c.string.preencode(state, m.type)
    c.string.preencode(state, m.state)
    c.uint.preencode(state, m.sequence)
    c.uint.preencode(state, m.at)
    c.uint.preencode(state, m.bytesAcquired)
    state.end++ // max flag is 4 so always one byte

    if (m.expectedBytes) c.uint.preencode(state, m.expectedBytes)
    if (m.errorCode) c.string.preencode(state, m.errorCode)
    if (m.publicationId) c.string.preencode(state, m.publicationId)
  },
  encode(state, m) {
    const flags = (m.expectedBytes ? 1 : 0) | (m.errorCode ? 2 : 0) | (m.publicationId ? 4 : 0)

    c.uint.encode(state, m.schemaVersion)
    c.string.encode(state, m.eventId)
    c.string.encode(state, m.acquisitionId)
    c.string.encode(state, m.type)
    c.string.encode(state, m.state)
    c.uint.encode(state, m.sequence)
    c.uint.encode(state, m.at)
    c.uint.encode(state, m.bytesAcquired)
    c.uint.encode(state, flags)

    if (m.expectedBytes) c.uint.encode(state, m.expectedBytes)
    if (m.errorCode) c.string.encode(state, m.errorCode)
    if (m.publicationId) c.string.encode(state, m.publicationId)
  },
  decode(state) {
    const r0 = c.uint.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const r3 = c.string.decode(state)
    const r4 = c.string.decode(state)
    const r5 = c.uint.decode(state)
    const r6 = c.uint.decode(state)
    const r7 = c.uint.decode(state)
    const flags = c.uint.decode(state)

    return {
      schemaVersion: r0,
      eventId: r1,
      acquisitionId: r2,
      type: r3,
      state: r4,
      sequence: r5,
      at: r6,
      bytesAcquired: r7,
      expectedBytes: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      errorCode: (flags & 2) !== 0 ? c.string.decode(state) : null,
      publicationId: (flags & 4) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartube/prepare-playback-response.stats, deferred due to recusive use
const encoding83_1 = c.frame(encoding165)
// @peartube/web-prepare-playback-response.stats, deferred due to recusive use
const encoding84_5 = encoding83_1
// @peartube/history-entry.identity, deferred due to recusive use
const encoding113_9 = c.frame(encoding114)
// @peartube/history-entry.order, deferred due to recusive use
const encoding113_11 = c.frame(encoding115)
// @peartube/resume-entry.identity, deferred due to recusive use
const encoding116_7 = encoding113_9
// @peartube/resume-entry.order, deferred due to recusive use
const encoding116_9 = encoding113_11
// @peartube/log-watch-history-request.identity, deferred due to recusive use
const encoding133_8 = encoding113_9
// @peartube/get-video-stats-response.stats, deferred due to recusive use
const encoding167_0 = encoding83_1
// @peartube/event-video-stats.stats, deferred due to recusive use
const encoding299_0 = encoding83_1

function setVersion(v) {
  version = v
}

function encode(name, value, v = VERSION) {
  version = v
  return c.encode(getEncoding(name), value)
}

function decode(name, buffer, v = VERSION) {
  version = v
  return c.decode(getEncoding(name), buffer)
}

function getEnum(name) {
  switch (name) {
    default:
      throw new Error('Enum not found ' + name)
  }
}

function getEncoding(name) {
  switch (name) {
    case '@peartube/empty':
      return encoding0
    case '@peartube/error':
      return encoding1
    case '@peartube/peer-performance-metric':
      return encoding2
    case '@peartube/resource-budget-state':
      return encoding3
    case '@peartube/resource-allocation-state':
      return encoding4
    case '@peartube/identity':
      return encoding5
    case '@peartube/create-identity-request':
      return encoding6
    case '@peartube/create-identity-response':
      return encoding7
    case '@peartube/get-identity-request':
      return encoding8
    case '@peartube/get-identity-response':
      return encoding9
    case '@peartube/get-identities-request':
      return encoding10
    case '@peartube/get-identities-response':
      return encoding11
    case '@peartube/set-active-identity-request':
      return encoding12
    case '@peartube/set-active-identity-response':
      return encoding13
    case '@peartube/recover-identity-request':
      return encoding14
    case '@peartube/recover-identity-response':
      return encoding15
    case '@peartube/bootstrap-device-request':
      return encoding16
    case '@peartube/bootstrap-device-response':
      return encoding17
    case '@peartube/attest-device-request':
      return encoding18
    case '@peartube/attest-device-response':
      return encoding19
    case '@peartube/verify-attestation-request':
      return encoding20
    case '@peartube/verify-attestation-response':
      return encoding21
    case '@peartube/provision-publisher-catalog-request':
      return encoding22
    case '@peartube/provision-publisher-catalog-response':
      return encoding23
    case '@peartube/prepare-publisher-root-operation-request':
      return encoding24
    case '@peartube/prepare-publisher-root-operation-response':
      return encoding25
    case '@peartube/submit-publisher-root-operation-request':
      return encoding26
    case '@peartube/submit-publisher-root-operation-response':
      return encoding27
    case '@peartube/channel':
      return encoding28
    case '@peartube/get-channel-request':
      return encoding29
    case '@peartube/get-channel-response':
      return encoding30
    case '@peartube/update-channel-request':
      return encoding31
    case '@peartube/update-channel-response':
      return encoding32
    case '@peartube/content-artwork':
      return encoding33
    case '@peartube/channel-source':
      return encoding34
    case '@peartube/channel-catalog-profile':
      return encoding35
    case '@peartube/channel-catalog-group-summary':
      return encoding36
    case '@peartube/channel-catalog-item':
      return encoding37
    case '@peartube/get-content-catalog-request':
      return encoding38
    case '@peartube/get-content-catalog-response':
      return encoding39
    case '@peartube/get-content-items-request':
      return encoding40
    case '@peartube/get-content-items-response':
      return encoding41
    case '@peartube/media-page-request':
      return encoding42
    case '@peartube/media-rendition-descriptor':
      return encoding43
    case '@peartube/media-availability':
      return encoding44
    case '@peartube/media-source-coordinates':
      return encoding45
    case '@peartube/media-publication-source':
      return encoding46
    case '@peartube/media-claim-provenance':
      return encoding47
    case '@peartube/media-conflict-summary':
      return encoding48
    case '@peartube/media-agent-summary':
      return encoding49
    case '@peartube/media-contribution-summary':
      return encoding50
    case '@peartube/media-entity-summary':
      return encoding51
    case '@peartube/get-media-catalog-response':
      return encoding52
    case '@peartube/get-media-entity-request':
      return encoding53
    case '@peartube/get-media-entity-response':
      return encoding54
    case '@peartube/get-media-collection-request':
      return encoding55
    case '@peartube/get-media-collection-response':
      return encoding56
    case '@peartube/get-media-agent-request':
      return encoding57
    case '@peartube/get-media-agent-response':
      return encoding58
    case '@peartube/get-media-collection-items-request':
      return encoding59
    case '@peartube/get-media-collection-items-response':
      return encoding60
    case '@peartube/get-agent-contributions-request':
      return encoding61
    case '@peartube/get-agent-contributions-response':
      return encoding62
    case '@peartube/get-publication-sources-request':
      return encoding63
    case '@peartube/get-publication-sources-response':
      return encoding64
    case '@peartube/get-entity-artwork-request':
      return encoding65
    case '@peartube/get-entity-artwork-response':
      return encoding66
    case '@peartube/media-playback-attempt':
      return encoding67
    case '@peartube/prepare-media-playback-request':
      return encoding68
    case '@peartube/prepare-media-playback-response':
      return encoding69
    case '@peartube/get-claim-provenance-request':
      return encoding70
    case '@peartube/get-claim-provenance-response':
      return encoding71
    case '@peartube/set-source-preference-request':
      return encoding72
    case '@peartube/set-source-preference-response':
      return encoding73
    case '@peartube/video-immutable-publication':
      return encoding74
    case '@peartube/video':
      return encoding75
    case '@peartube/list-videos-request':
      return encoding76
    case '@peartube/list-videos-response':
      return encoding77
    case '@peartube/get-video-url-request':
      return encoding78
    case '@peartube/get-video-url-response':
      return encoding79
    case '@peartube/prepare-playback-request':
      return encoding80
    case '@peartube/selected-blob-warmup':
      return encoding81
    case '@peartube/peer-warmup':
      return encoding82
    case '@peartube/prepare-playback-response':
      return encoding83
    case '@peartube/web-prepare-playback-response':
      return encoding84
    case '@peartube/get-video-data-request':
      return encoding85
    case '@peartube/get-video-data-response':
      return encoding86
    case '@peartube/upload-video-request':
      return encoding87
    case '@peartube/upload-video-response':
      return encoding88
    case '@peartube/download-video-request':
      return encoding89
    case '@peartube/download-video-response':
      return encoding90
    case '@peartube/delete-video-request':
      return encoding91
    case '@peartube/delete-video-response':
      return encoding92
    case '@peartube/start-livestream-request':
      return encoding93
    case '@peartube/start-livestream-response':
      return encoding94
    case '@peartube/stop-livestream-request':
      return encoding95
    case '@peartube/livestream-status':
      return encoding96
    case '@peartube/stop-livestream-response':
      return encoding97
    case '@peartube/get-livestream-status-request':
      return encoding98
    case '@peartube/get-livestream-status-response':
      return encoding99
    case '@peartube/prepare-live-playback-request':
      return encoding100
    case '@peartube/prepare-live-playback-response':
      return encoding101
    case '@peartube/update-video-metadata-request':
      return encoding102
    case '@peartube/update-video-metadata-response':
      return encoding103
    case '@peartube/subscription':
      return encoding104
    case '@peartube/subscribe-channel-request':
      return encoding105
    case '@peartube/subscribe-channel-response':
      return encoding106
    case '@peartube/unsubscribe-channel-request':
      return encoding107
    case '@peartube/unsubscribe-channel-response':
      return encoding108
    case '@peartube/get-subscriptions-request':
      return encoding109
    case '@peartube/get-subscriptions-response':
      return encoding110
    case '@peartube/playlist':
      return encoding111
    case '@peartube/playlist-item':
      return encoding112
    case '@peartube/history-entry':
      return encoding113
    case '@peartube/personal-progress-identity':
      return encoding114
    case '@peartube/personal-progress-order':
      return encoding115
    case '@peartube/resume-entry':
      return encoding116
    case '@peartube/personal-device':
      return encoding117
    case '@peartube/personal-setting':
      return encoding118
    case '@peartube/get-playlists-request':
      return encoding119
    case '@peartube/get-playlists-response':
      return encoding120
    case '@peartube/get-playlist-items-request':
      return encoding121
    case '@peartube/get-playlist-items-response':
      return encoding122
    case '@peartube/create-playlist-request':
      return encoding123
    case '@peartube/create-playlist-response':
      return encoding124
    case '@peartube/update-playlist-request':
      return encoding125
    case '@peartube/update-playlist-response':
      return encoding126
    case '@peartube/delete-playlist-request':
      return encoding127
    case '@peartube/delete-playlist-response':
      return encoding128
    case '@peartube/add-to-playlist-request':
      return encoding129
    case '@peartube/add-to-playlist-response':
      return encoding130
    case '@peartube/remove-from-playlist-request':
      return encoding131
    case '@peartube/remove-from-playlist-response':
      return encoding132
    case '@peartube/log-watch-history-request':
      return encoding133
    case '@peartube/log-watch-history-response':
      return encoding134
    case '@peartube/get-watch-history-request':
      return encoding135
    case '@peartube/get-watch-history-response':
      return encoding136
    case '@peartube/get-resume-position-request':
      return encoding137
    case '@peartube/get-resume-position-response':
      return encoding138
    case '@peartube/list-resume-positions-request':
      return encoding139
    case '@peartube/list-resume-positions-response':
      return encoding140
    case '@peartube/set-personal-setting-request':
      return encoding141
    case '@peartube/set-personal-setting-response':
      return encoding142
    case '@peartube/get-personal-settings-request':
      return encoding143
    case '@peartube/get-personal-settings-response':
      return encoding144
    case '@peartube/provision-personal-encryption-request':
      return encoding145
    case '@peartube/provision-personal-encryption-response':
      return encoding146
    case '@peartube/create-personal-device-invite-request':
      return encoding147
    case '@peartube/create-personal-device-invite-response':
      return encoding148
    case '@peartube/redeem-personal-device-invite-request':
      return encoding149
    case '@peartube/redeem-personal-device-invite-response':
      return encoding150
    case '@peartube/list-personal-devices-request':
      return encoding151
    case '@peartube/list-personal-devices-response':
      return encoding152
    case '@peartube/revoke-personal-device-request':
      return encoding153
    case '@peartube/revoke-personal-device-response':
      return encoding154
    case '@peartube/join-channel-request':
      return encoding155
    case '@peartube/join-channel-response':
      return encoding156
    case '@peartube/hide-channel-request':
      return encoding157
    case '@peartube/hide-channel-response':
      return encoding158
    case '@peartube/get-channel-meta-request':
      return encoding159
    case '@peartube/get-channel-meta-response':
      return encoding160
    case '@peartube/get-swarm-status-request':
      return encoding161
    case '@peartube/get-swarm-status-response':
      return encoding162
    case '@peartube/prefetch-video-request':
      return encoding163
    case '@peartube/prefetch-video-response':
      return encoding164
    case '@peartube/video-stats':
      return encoding165
    case '@peartube/get-video-stats-request':
      return encoding166
    case '@peartube/get-video-stats-response':
      return encoding167
    case '@peartube/seeding-config':
      return encoding168
    case '@peartube/seeding-status':
      return encoding169
    case '@peartube/get-seeding-status-request':
      return encoding170
    case '@peartube/get-seeding-status-response':
      return encoding171
    case '@peartube/set-seeding-config-request':
      return encoding172
    case '@peartube/set-seeding-config-response':
      return encoding173
    case '@peartube/transcode-settings':
      return encoding174
    case '@peartube/get-transcode-settings-request':
      return encoding175
    case '@peartube/get-transcode-settings-response':
      return encoding176
    case '@peartube/set-transcode-settings-request':
      return encoding177
    case '@peartube/set-transcode-settings-response':
      return encoding178
    case '@peartube/pin-channel-request':
      return encoding179
    case '@peartube/pin-channel-response':
      return encoding180
    case '@peartube/unpin-channel-request':
      return encoding181
    case '@peartube/unpin-channel-response':
      return encoding182
    case '@peartube/get-pinned-channels-request':
      return encoding183
    case '@peartube/get-pinned-channels-response':
      return encoding184
    case '@peartube/get-storage-stats-request':
      return encoding185
    case '@peartube/get-storage-stats-response':
      return encoding186
    case '@peartube/set-storage-limit-request':
      return encoding187
    case '@peartube/set-storage-limit-response':
      return encoding188
    case '@peartube/get-migration-status-request':
      return encoding189
    case '@peartube/get-migration-status-response':
      return encoding190
    case '@peartube/retry-migration-request':
      return encoding191
    case '@peartube/retry-migration-response':
      return encoding192
    case '@peartube/export-migration-report-request':
      return encoding193
    case '@peartube/export-migration-report-response':
      return encoding194
    case '@peartube/get-publisher-device-status-request':
      return encoding195
    case '@peartube/get-publisher-device-status-response':
      return encoding196
    case '@peartube/export-portable-state-request':
      return encoding197
    case '@peartube/export-portable-state-response':
      return encoding198
    case '@peartube/restore-portable-state-request':
      return encoding199
    case '@peartube/restore-portable-state-response':
      return encoding200
    case '@peartube/preview-storage-limit-request':
      return encoding201
    case '@peartube/preview-storage-limit-response':
      return encoding202
    case '@peartube/get-archive-operator-status-request':
      return encoding203
    case '@peartube/get-archive-operator-status-response':
      return encoding204
    case '@peartube/get-archive-participation-request':
      return encoding205
    case '@peartube/get-archive-participation-response':
      return encoding206
    case '@peartube/set-archive-participation-request':
      return encoding207
    case '@peartube/set-archive-participation-response':
      return encoding208
    case '@peartube/request-archive-publication-request':
      return encoding209
    case '@peartube/request-archive-publication-response':
      return encoding210
    case '@peartube/get-network-policy-request':
      return encoding211
    case '@peartube/get-network-policy-response':
      return encoding212
    case '@peartube/set-network-policy-request':
      return encoding213
    case '@peartube/set-network-policy-response':
      return encoding214
    case '@peartube/clear-cache-request':
      return encoding215
    case '@peartube/clear-cache-response':
      return encoding216
    case '@peartube/assess-source-offload-request':
      return encoding217
    case '@peartube/assess-source-offload-response':
      return encoding218
    case '@peartube/confirm-source-offload-request':
      return encoding219
    case '@peartube/confirm-source-offload-response':
      return encoding220
    case '@peartube/get-video-thumbnail-request':
      return encoding221
    case '@peartube/get-video-thumbnail-response':
      return encoding222
    case '@peartube/get-video-metadata-request':
      return encoding223
    case '@peartube/get-video-metadata-response':
      return encoding224
    case '@peartube/set-video-thumbnail-request':
      return encoding225
    case '@peartube/set-video-thumbnail-from-file-request':
      return encoding226
    case '@peartube/set-video-thumbnail-from-file-response':
      return encoding227
    case '@peartube/set-video-thumbnail-response':
      return encoding228
    case '@peartube/desktop-browse-video':
      return encoding229
    case '@peartube/desktop-browse-sections':
      return encoding230
    case '@peartube/desktop-browse-stats':
      return encoding231
    case '@peartube/desktop-browse-state':
      return encoding232
    case '@peartube/desktop-browse-snapshot':
      return encoding233
    case '@peartube/desktop-bootstrap-request':
      return encoding234
    case '@peartube/desktop-bootstrap-response':
      return encoding235
    case '@peartube/desktop-shutdown-request':
      return encoding236
    case '@peartube/desktop-shutdown-response':
      return encoding237
    case '@peartube/desktop-refresh-browse-request':
      return encoding238
    case '@peartube/desktop-refresh-browse-response':
      return encoding239
    case '@peartube/ffmpeg-decode-available-request':
      return encoding240
    case '@peartube/ffmpeg-decode-available-response':
      return encoding241
    case '@peartube/update-channel-avatar-request':
      return encoding242
    case '@peartube/update-channel-avatar-response':
      return encoding243
    case '@peartube/transcode-start-request':
      return encoding244
    case '@peartube/transcode-start-response':
      return encoding245
    case '@peartube/transcode-stop-request':
      return encoding246
    case '@peartube/transcode-stop-response':
      return encoding247
    case '@peartube/transcode-status-request':
      return encoding248
    case '@peartube/transcode-status-response':
      return encoding249
    case '@peartube/event-transcode-progress':
      return encoding250
    case '@peartube/status':
      return encoding251
    case '@peartube/get-status-request':
      return encoding252
    case '@peartube/get-status-response':
      return encoding253
    case '@peartube/pick-video-file-request':
      return encoding254
    case '@peartube/pick-video-file-response':
      return encoding255
    case '@peartube/pick-image-file-request':
      return encoding256
    case '@peartube/pick-image-file-response':
      return encoding257
    case '@peartube/get-blob-server-port-request':
      return encoding258
    case '@peartube/get-blob-server-port-response':
      return encoding259
    case '@peartube/search-result':
      return encoding260
    case '@peartube/global-search-videos-request':
      return encoding261
    case '@peartube/global-search-videos-response':
      return encoding262
    case '@peartube/index-search-selector':
      return encoding263
    case '@peartube/index-external-reference':
      return encoding264
    case '@peartube/index-episode':
      return encoding265
    case '@peartube/index-work':
      return encoding266
    case '@peartube/index-edition':
      return encoding267
    case '@peartube/index-publication':
      return encoding268
    case '@peartube/index-audio-track':
      return encoding269
    case '@peartube/index-subtitle-track':
      return encoding270
    case '@peartube/index-rendition':
      return encoding271
    case '@peartube/index-asset':
      return encoding272
    case '@peartube/index-provenance':
      return encoding273
    case '@peartube/index-availability':
      return encoding274
    case '@peartube/index-source-indexer':
      return encoding275
    case '@peartube/index-publisher-descriptor':
      return encoding276
    case '@peartube/index-catalog-head':
      return encoding277
    case '@peartube/index-verification':
      return encoding278
    case '@peartube/index-candidate-v2':
      return encoding279
    case '@peartube/search-index-candidates-request':
      return encoding280
    case '@peartube/search-index-candidates-response':
      return encoding281
    case '@peartube/verify-index-candidate-request':
      return encoding282
    case '@peartube/verify-index-candidate-response':
      return encoding283
    case '@peartube/device':
      return encoding284
    case '@peartube/create-device-invite-request':
      return encoding285
    case '@peartube/create-device-invite-response':
      return encoding286
    case '@peartube/pair-device-request':
      return encoding287
    case '@peartube/pair-device-response':
      return encoding288
    case '@peartube/retry-sync-channel-request':
      return encoding289
    case '@peartube/retry-sync-channel-response':
      return encoding290
    case '@peartube/list-devices-request':
      return encoding291
    case '@peartube/list-devices-response':
      return encoding292
    case '@peartube/event-ready':
      return encoding293
    case '@peartube/event-error':
      return encoding294
    case '@peartube/event-upload-progress':
      return encoding295
    case '@peartube/event-download-progress':
      return encoding296
    case '@peartube/event-media-graph-update':
      return encoding297
    case '@peartube/event-log':
      return encoding298
    case '@peartube/event-video-stats':
      return encoding299
    case '@peartube/channel-op-base':
      return encoding300
    case '@peartube/channel-op-update-channel':
      return encoding301
    case '@peartube/channel-op-add-video':
      return encoding302
    case '@peartube/channel-op-update-video':
      return encoding303
    case '@peartube/channel-op-delete-video':
      return encoding304
    case '@peartube/channel-op-add-writer':
      return encoding305
    case '@peartube/channel-op-upsert-writer':
      return encoding306
    case '@peartube/channel-op-remove-writer':
      return encoding307
    case '@peartube/channel-op-add-invite':
      return encoding308
    case '@peartube/channel-op-delete-invite':
      return encoding309
    case '@peartube/channel-op-add-comment':
      return encoding310
    case '@peartube/channel-op-add-reaction':
      return encoding311
    case '@peartube/channel-op-remove-reaction':
      return encoding312
    case '@peartube/channel-op-hide-comment':
      return encoding313
    case '@peartube/channel-op-remove-comment':
      return encoding314
    case '@peartube/channel-op-add-vector-index':
      return encoding315
    case '@peartube/channel-op-log-watch-event':
      return encoding316
    case '@peartube/channel-op-migrate-schema':
      return encoding317
    case '@peartube/add-comment-request':
      return encoding318
    case '@peartube/add-comment-response':
      return encoding319
    case '@peartube/list-comments-request':
      return encoding320
    case '@peartube/comment':
      return encoding321
    case '@peartube/list-comments-response':
      return encoding322
    case '@peartube/hide-comment-request':
      return encoding323
    case '@peartube/hide-comment-response':
      return encoding324
    case '@peartube/remove-comment-request':
      return encoding325
    case '@peartube/remove-comment-response':
      return encoding326
    case '@peartube/reaction-count':
      return encoding327
    case '@peartube/add-reaction-request':
      return encoding328
    case '@peartube/add-reaction-response':
      return encoding329
    case '@peartube/remove-reaction-request':
      return encoding330
    case '@peartube/remove-reaction-response':
      return encoding331
    case '@peartube/get-reactions-request':
      return encoding332
    case '@peartube/get-reactions-response':
      return encoding333
    case '@peartube/cast-device':
      return encoding334
    case '@peartube/cast-available-request':
      return encoding335
    case '@peartube/cast-available-response':
      return encoding336
    case '@peartube/cast-start-discovery-request':
      return encoding337
    case '@peartube/cast-start-discovery-response':
      return encoding338
    case '@peartube/cast-stop-discovery-request':
      return encoding339
    case '@peartube/cast-stop-discovery-response':
      return encoding340
    case '@peartube/cast-get-devices-request':
      return encoding341
    case '@peartube/cast-get-devices-response':
      return encoding342
    case '@peartube/cast-add-manual-device-request':
      return encoding343
    case '@peartube/cast-add-manual-device-response':
      return encoding344
    case '@peartube/cast-connect-request':
      return encoding345
    case '@peartube/cast-connect-response':
      return encoding346
    case '@peartube/cast-disconnect-request':
      return encoding347
    case '@peartube/cast-disconnect-response':
      return encoding348
    case '@peartube/cast-play-request':
      return encoding349
    case '@peartube/cast-play-response':
      return encoding350
    case '@peartube/cast-pause-request':
      return encoding351
    case '@peartube/cast-pause-response':
      return encoding352
    case '@peartube/cast-resume-request':
      return encoding353
    case '@peartube/cast-resume-response':
      return encoding354
    case '@peartube/cast-stop-request':
      return encoding355
    case '@peartube/cast-stop-response':
      return encoding356
    case '@peartube/cast-seek-request':
      return encoding357
    case '@peartube/cast-seek-response':
      return encoding358
    case '@peartube/cast-set-volume-request':
      return encoding359
    case '@peartube/cast-set-volume-response':
      return encoding360
    case '@peartube/cast-get-state-request':
      return encoding361
    case '@peartube/cast-get-state-response':
      return encoding362
    case '@peartube/cast-is-connected-request':
      return encoding363
    case '@peartube/cast-is-connected-response':
      return encoding364
    case '@peartube/event-cast-device-found':
      return encoding365
    case '@peartube/event-cast-device-lost':
      return encoding366
    case '@peartube/event-cast-playback-state':
      return encoding367
    case '@peartube/event-cast-time-update':
      return encoding368
    case '@peartube/search-videos-request':
      return encoding369
    case '@peartube/search-videos-response':
      return encoding370
    case '@peartube/index-video-vectors-request':
      return encoding371
    case '@peartube/index-video-vectors-response':
      return encoding372
    case '@peartube/get-participation-status-request':
      return encoding373
    case '@peartube/get-participation-status-response':
      return encoding374
    case '@peartube/set-device-conditions-request':
      return encoding375
    case '@peartube/set-device-conditions-response':
      return encoding376
    case '@peartube/suspend-network-request':
      return encoding377
    case '@peartube/suspend-network-response':
      return encoding378
    case '@peartube/resume-network-request':
      return encoding379
    case '@peartube/resume-network-response':
      return encoding380
    case '@peartube/set-playback-active-request':
      return encoding381
    case '@peartube/set-playback-active-response':
      return encoding382
    case '@peartube/provider-error':
      return encoding383
    case '@peartube/provider-search-hit-v1':
      return encoding384
    case '@peartube/provider-search-request':
      return encoding385
    case '@peartube/provider-search-response':
      return encoding386
    case '@peartube/provider-resolution-v1':
      return encoding387
    case '@peartube/resolve-provider-ref-request':
      return encoding388
    case '@peartube/resolve-provider-ref-response':
      return encoding389
    case '@peartube/acquisition-request-v1':
      return encoding390
    case '@peartube/acquisition-v1':
      return encoding391
    case '@peartube/request-acquisition-request':
      return encoding392
    case '@peartube/request-acquisition-response':
      return encoding393
    case '@peartube/attach-source-grant-request':
      return encoding394
    case '@peartube/attach-source-grant-response':
      return encoding395
    case '@peartube/get-acquisition-request':
      return encoding396
    case '@peartube/get-acquisition-response':
      return encoding397
    case '@peartube/list-acquisitions-request':
      return encoding398
    case '@peartube/list-acquisitions-response':
      return encoding399
    case '@peartube/cancel-acquisition-request':
      return encoding400
    case '@peartube/cancel-acquisition-response':
      return encoding401
    case '@peartube/retry-acquisition-request':
      return encoding402
    case '@peartube/retry-acquisition-response':
      return encoding403
    case '@peartube/provider-publication-v1':
      return encoding404
    case '@peartube/get-provider-publication-request':
      return encoding405
    case '@peartube/get-provider-publication-response':
      return encoding406
    case '@peartube/provider-stream-v1':
      return encoding407
    case '@peartube/open-provider-stream-request':
      return encoding408
    case '@peartube/open-provider-stream-response':
      return encoding409
    case '@peartube/provider-status-v1':
      return encoding410
    case '@peartube/get-provider-status-request':
      return encoding411
    case '@peartube/get-provider-status-response':
      return encoding412
    case '@peartube/provider-policy-v1':
      return encoding413
    case '@peartube/get-provider-policy-request':
      return encoding414
    case '@peartube/get-provider-policy-response':
      return encoding415
    case '@peartube/set-provider-policy-request':
      return encoding416
    case '@peartube/set-provider-policy-response':
      return encoding417
    case '@peartube/acquisition-policy-v1':
      return encoding418
    case '@peartube/get-acquisition-policy-request':
      return encoding419
    case '@peartube/get-acquisition-policy-response':
      return encoding420
    case '@peartube/acquisition-consent-v1':
      return encoding421
    case '@peartube/set-acquisition-policy-request':
      return encoding422
    case '@peartube/set-acquisition-policy-response':
      return encoding423
    case '@peartube/acquisition-lifecycle-event-v1':
      return encoding424
    default:
      throw new Error('Encoder not found ' + name)
  }
}

function getStruct(name, v = VERSION) {
  const enc = getEncoding(name)
  return {
    preencode(state, m) {
      version = v
      enc.preencode(state, m)
    },
    encode(state, m) {
      version = v
      enc.encode(state, m)
    },
    decode(state) {
      version = v
      return enc.decode(state)
    }
  }
}

const resolveStruct = getStruct // compat

module.exports = {
  resolveStruct,
  getStruct,
  getEnum,
  getEncoding,
  encode,
  decode,
  setVersion,
  version
}
