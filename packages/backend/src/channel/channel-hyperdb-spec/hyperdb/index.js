// This file is autogenerated by the hyperdb compiler
/* eslint-disable camelcase */

import { IndexEncoder, c, b4a } from 'hyperdb/runtime'
import { version, getEncoding, setVersion } from './messages.js'

const versions = { schema: version, db: 2 }

// '@peartubeChannel/metadata' collection key
const collection0_key = new IndexEncoder([
  IndexEncoder.STRING
], { prefix: 0 })

function collection0_indexify (record) {
  const a = record.key
  return a === undefined ? [] : [a]
}

// '@peartubeChannel/metadata' value encoding
const collection0_enc = getEncoding('@peartubeChannel/metadata/hyperdb#0')

// '@peartubeChannel/metadata' reconstruction function
function collection0_reconstruct (schemaVersion, keyBuf, valueBuf) {
  const key = collection0_key.decode(keyBuf)
  setVersion(schemaVersion)
  const state = { start: 0, end: valueBuf.byteLength, buffer: valueBuf }
  const type = c.uint.decode(state)
  if (type !== 0) throw new Error('Unknown collection type: ' + type)
  collection0.decodedVersion = c.uint.decode(state)
  const record = collection0_enc.decode(state)
  record.key = key[0]
  return record
}
// '@peartubeChannel/metadata' key reconstruction function
function collection0_reconstruct_key (keyBuf) {
  const key = collection0_key.decode(keyBuf)
  return {
    key: key[0]
  }
}

// '@peartubeChannel/metadata'
const collection0 = {
  name: '@peartubeChannel/metadata',
  id: 0,
  version: 1,
  encodeKey (record) {
    const key = [record.key]
    return collection0_key.encode(key)
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return collection0_key.encodeRange({
      gt: gt ? collection0_indexify(gt) : null,
      lt: lt ? collection0_indexify(lt) : null,
      gte: gte ? collection0_indexify(gte) : null,
      lte: lte ? collection0_indexify(lte) : null
    })
  },
  encodeValue (schemaVersion, collectionVersion, record) {
    setVersion(schemaVersion)
    const state = { start: 0, end: 2, buffer: null }
    collection0_enc.preencode(state, record)
    state.buffer = b4a.allocUnsafe(state.end)
    state.buffer[state.start++] = 0
    state.buffer[state.start++] = collectionVersion
    collection0_enc.encode(state, record)
    return state.buffer
  },
  trigger: null,
  reconstruct: collection0_reconstruct,
  reconstructKey: collection0_reconstruct_key,
  indexes: [],
  decodedVersion: 0
}

// '@peartubeChannel/writers' collection key
const collection1_key = new IndexEncoder([
  IndexEncoder.STRING
], { prefix: 1 })

function collection1_indexify (record) {
  const a = record.keyHex
  return a === undefined ? [] : [a]
}

// '@peartubeChannel/writers' value encoding
const collection1_enc = getEncoding('@peartubeChannel/writer/hyperdb#1')

// '@peartubeChannel/writers' reconstruction function
function collection1_reconstruct (schemaVersion, keyBuf, valueBuf) {
  const key = collection1_key.decode(keyBuf)
  setVersion(schemaVersion)
  const state = { start: 0, end: valueBuf.byteLength, buffer: valueBuf }
  const type = c.uint.decode(state)
  if (type !== 0) throw new Error('Unknown collection type: ' + type)
  collection1.decodedVersion = c.uint.decode(state)
  const record = collection1_enc.decode(state)
  record.keyHex = key[0]
  return record
}
// '@peartubeChannel/writers' key reconstruction function
function collection1_reconstruct_key (keyBuf) {
  const key = collection1_key.decode(keyBuf)
  return {
    keyHex: key[0]
  }
}

// '@peartubeChannel/writers'
const collection1 = {
  name: '@peartubeChannel/writers',
  id: 1,
  version: 1,
  encodeKey (record) {
    const key = [record.keyHex]
    return collection1_key.encode(key)
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return collection1_key.encodeRange({
      gt: gt ? collection1_indexify(gt) : null,
      lt: lt ? collection1_indexify(lt) : null,
      gte: gte ? collection1_indexify(gte) : null,
      lte: lte ? collection1_indexify(lte) : null
    })
  },
  encodeValue (schemaVersion, collectionVersion, record) {
    setVersion(schemaVersion)
    const state = { start: 0, end: 2, buffer: null }
    collection1_enc.preencode(state, record)
    state.buffer = b4a.allocUnsafe(state.end)
    state.buffer[state.start++] = 0
    state.buffer[state.start++] = collectionVersion
    collection1_enc.encode(state, record)
    return state.buffer
  },
  trigger: null,
  reconstruct: collection1_reconstruct,
  reconstructKey: collection1_reconstruct_key,
  indexes: [],
  decodedVersion: 0
}

// '@peartubeChannel/videos' collection key
const collection2_key = new IndexEncoder([
  IndexEncoder.STRING
], { prefix: 2 })

function collection2_indexify (record) {
  const a = record.id
  return a === undefined ? [] : [a]
}

// '@peartubeChannel/videos' value encoding
const collection2_enc = getEncoding('@peartubeChannel/video/hyperdb#2')

// '@peartubeChannel/videos' reconstruction function
function collection2_reconstruct (schemaVersion, keyBuf, valueBuf) {
  const key = collection2_key.decode(keyBuf)
  setVersion(schemaVersion)
  const state = { start: 0, end: valueBuf.byteLength, buffer: valueBuf }
  const type = c.uint.decode(state)
  if (type !== 0) throw new Error('Unknown collection type: ' + type)
  collection2.decodedVersion = c.uint.decode(state)
  const record = collection2_enc.decode(state)
  record.id = key[0]
  return record
}
// '@peartubeChannel/videos' key reconstruction function
function collection2_reconstruct_key (keyBuf) {
  const key = collection2_key.decode(keyBuf)
  return {
    id: key[0]
  }
}

// '@peartubeChannel/videos'
const collection2 = {
  name: '@peartubeChannel/videos',
  id: 2,
  version: 1,
  encodeKey (record) {
    const key = [record.id]
    return collection2_key.encode(key)
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return collection2_key.encodeRange({
      gt: gt ? collection2_indexify(gt) : null,
      lt: lt ? collection2_indexify(lt) : null,
      gte: gte ? collection2_indexify(gte) : null,
      lte: lte ? collection2_indexify(lte) : null
    })
  },
  encodeValue (schemaVersion, collectionVersion, record) {
    setVersion(schemaVersion)
    const state = { start: 0, end: 2, buffer: null }
    collection2_enc.preencode(state, record)
    state.buffer = b4a.allocUnsafe(state.end)
    state.buffer[state.start++] = 0
    state.buffer[state.start++] = collectionVersion
    collection2_enc.encode(state, record)
    return state.buffer
  },
  trigger: null,
  reconstruct: collection2_reconstruct,
  reconstructKey: collection2_reconstruct_key,
  indexes: [],
  decodedVersion: 0
}

// '@peartubeChannel/comments' collection key
const collection3_key = new IndexEncoder([
  IndexEncoder.STRING,
  IndexEncoder.STRING
], { prefix: 3 })

function collection3_indexify (record) {
  const arr = []

  const a0 = record.videoId
  if (a0 === undefined) return arr
  arr.push(a0)

  const a1 = record.commentId
  if (a1 === undefined) return arr
  arr.push(a1)

  return arr
}

// '@peartubeChannel/comments' value encoding
const collection3_enc = getEncoding('@peartubeChannel/comment/hyperdb#3')

// '@peartubeChannel/comments' reconstruction function
function collection3_reconstruct (schemaVersion, keyBuf, valueBuf) {
  const key = collection3_key.decode(keyBuf)
  setVersion(schemaVersion)
  const state = { start: 0, end: valueBuf.byteLength, buffer: valueBuf }
  const type = c.uint.decode(state)
  if (type !== 0) throw new Error('Unknown collection type: ' + type)
  collection3.decodedVersion = c.uint.decode(state)
  const record = collection3_enc.decode(state)
  record.videoId = key[0]
  record.commentId = key[1]
  return record
}
// '@peartubeChannel/comments' key reconstruction function
function collection3_reconstruct_key (keyBuf) {
  const key = collection3_key.decode(keyBuf)
  return {
    videoId: key[0],
    commentId: key[1]
  }
}

// '@peartubeChannel/comments'
const collection3 = {
  name: '@peartubeChannel/comments',
  id: 3,
  version: 1,
  encodeKey (record) {
    const key = [record.videoId, record.commentId]
    return collection3_key.encode(key)
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return collection3_key.encodeRange({
      gt: gt ? collection3_indexify(gt) : null,
      lt: lt ? collection3_indexify(lt) : null,
      gte: gte ? collection3_indexify(gte) : null,
      lte: lte ? collection3_indexify(lte) : null
    })
  },
  encodeValue (schemaVersion, collectionVersion, record) {
    setVersion(schemaVersion)
    const state = { start: 0, end: 2, buffer: null }
    collection3_enc.preencode(state, record)
    state.buffer = b4a.allocUnsafe(state.end)
    state.buffer[state.start++] = 0
    state.buffer[state.start++] = collectionVersion
    collection3_enc.encode(state, record)
    return state.buffer
  },
  trigger: null,
  reconstruct: collection3_reconstruct,
  reconstructKey: collection3_reconstruct_key,
  indexes: [],
  decodedVersion: 0
}

// '@peartubeChannel/reactions' collection key
const collection4_key = new IndexEncoder([
  IndexEncoder.STRING,
  IndexEncoder.STRING
], { prefix: 4 })

function collection4_indexify (record) {
  const arr = []

  const a0 = record.videoId
  if (a0 === undefined) return arr
  arr.push(a0)

  const a1 = record.authorKeyHex
  if (a1 === undefined) return arr
  arr.push(a1)

  return arr
}

// '@peartubeChannel/reactions' value encoding
const collection4_enc = getEncoding('@peartubeChannel/reaction/hyperdb#4')

// '@peartubeChannel/reactions' reconstruction function
function collection4_reconstruct (schemaVersion, keyBuf, valueBuf) {
  const key = collection4_key.decode(keyBuf)
  setVersion(schemaVersion)
  const state = { start: 0, end: valueBuf.byteLength, buffer: valueBuf }
  const type = c.uint.decode(state)
  if (type !== 0) throw new Error('Unknown collection type: ' + type)
  collection4.decodedVersion = c.uint.decode(state)
  const record = collection4_enc.decode(state)
  record.videoId = key[0]
  record.authorKeyHex = key[1]
  return record
}
// '@peartubeChannel/reactions' key reconstruction function
function collection4_reconstruct_key (keyBuf) {
  const key = collection4_key.decode(keyBuf)
  return {
    videoId: key[0],
    authorKeyHex: key[1]
  }
}

// '@peartubeChannel/reactions'
const collection4 = {
  name: '@peartubeChannel/reactions',
  id: 4,
  version: 1,
  encodeKey (record) {
    const key = [record.videoId, record.authorKeyHex]
    return collection4_key.encode(key)
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return collection4_key.encodeRange({
      gt: gt ? collection4_indexify(gt) : null,
      lt: lt ? collection4_indexify(lt) : null,
      gte: gte ? collection4_indexify(gte) : null,
      lte: lte ? collection4_indexify(lte) : null
    })
  },
  encodeValue (schemaVersion, collectionVersion, record) {
    setVersion(schemaVersion)
    const state = { start: 0, end: 2, buffer: null }
    collection4_enc.preencode(state, record)
    state.buffer = b4a.allocUnsafe(state.end)
    state.buffer[state.start++] = 0
    state.buffer[state.start++] = collectionVersion
    collection4_enc.encode(state, record)
    return state.buffer
  },
  trigger: null,
  reconstruct: collection4_reconstruct,
  reconstructKey: collection4_reconstruct_key,
  indexes: [],
  decodedVersion: 0
}

// '@peartubeChannel/invites' collection key
const collection5_key = new IndexEncoder([
  IndexEncoder.STRING
], { prefix: 5 })

function collection5_indexify (record) {
  const a = record.idHex
  return a === undefined ? [] : [a]
}

// '@peartubeChannel/invites' value encoding
const collection5_enc = getEncoding('@peartubeChannel/invite/hyperdb#5')

// '@peartubeChannel/invites' reconstruction function
function collection5_reconstruct (schemaVersion, keyBuf, valueBuf) {
  const key = collection5_key.decode(keyBuf)
  setVersion(schemaVersion)
  const state = { start: 0, end: valueBuf.byteLength, buffer: valueBuf }
  const type = c.uint.decode(state)
  if (type !== 0) throw new Error('Unknown collection type: ' + type)
  collection5.decodedVersion = c.uint.decode(state)
  const record = collection5_enc.decode(state)
  record.idHex = key[0]
  return record
}
// '@peartubeChannel/invites' key reconstruction function
function collection5_reconstruct_key (keyBuf) {
  const key = collection5_key.decode(keyBuf)
  return {
    idHex: key[0]
  }
}

// '@peartubeChannel/invites'
const collection5 = {
  name: '@peartubeChannel/invites',
  id: 5,
  version: 1,
  encodeKey (record) {
    const key = [record.idHex]
    return collection5_key.encode(key)
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return collection5_key.encodeRange({
      gt: gt ? collection5_indexify(gt) : null,
      lt: lt ? collection5_indexify(lt) : null,
      gte: gte ? collection5_indexify(gte) : null,
      lte: lte ? collection5_indexify(lte) : null
    })
  },
  encodeValue (schemaVersion, collectionVersion, record) {
    setVersion(schemaVersion)
    const state = { start: 0, end: 2, buffer: null }
    collection5_enc.preencode(state, record)
    state.buffer = b4a.allocUnsafe(state.end)
    state.buffer[state.start++] = 0
    state.buffer[state.start++] = collectionVersion
    collection5_enc.encode(state, record)
    return state.buffer
  },
  trigger: null,
  reconstruct: collection5_reconstruct,
  reconstructKey: collection5_reconstruct_key,
  indexes: [],
  decodedVersion: 0
}

// '@peartubeChannel/watchEvents' collection key
const collection6_key = new IndexEncoder([
  IndexEncoder.STRING,
  IndexEncoder.STRING
], { prefix: 6 })

function collection6_indexify (record) {
  const arr = []

  const a0 = record.videoId
  if (a0 === undefined) return arr
  arr.push(a0)

  const a1 = record.eventId
  if (a1 === undefined) return arr
  arr.push(a1)

  return arr
}

// '@peartubeChannel/watchEvents' value encoding
const collection6_enc = getEncoding('@peartubeChannel/watchEvent/hyperdb#6')

// '@peartubeChannel/watchEvents' reconstruction function
function collection6_reconstruct (schemaVersion, keyBuf, valueBuf) {
  const key = collection6_key.decode(keyBuf)
  setVersion(schemaVersion)
  const state = { start: 0, end: valueBuf.byteLength, buffer: valueBuf }
  const type = c.uint.decode(state)
  if (type !== 0) throw new Error('Unknown collection type: ' + type)
  collection6.decodedVersion = c.uint.decode(state)
  const record = collection6_enc.decode(state)
  record.videoId = key[0]
  record.eventId = key[1]
  return record
}
// '@peartubeChannel/watchEvents' key reconstruction function
function collection6_reconstruct_key (keyBuf) {
  const key = collection6_key.decode(keyBuf)
  return {
    videoId: key[0],
    eventId: key[1]
  }
}

// '@peartubeChannel/watchEvents'
const collection6 = {
  name: '@peartubeChannel/watchEvents',
  id: 6,
  version: 1,
  encodeKey (record) {
    const key = [record.videoId, record.eventId]
    return collection6_key.encode(key)
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return collection6_key.encodeRange({
      gt: gt ? collection6_indexify(gt) : null,
      lt: lt ? collection6_indexify(lt) : null,
      gte: gte ? collection6_indexify(gte) : null,
      lte: lte ? collection6_indexify(lte) : null
    })
  },
  encodeValue (schemaVersion, collectionVersion, record) {
    setVersion(schemaVersion)
    const state = { start: 0, end: 2, buffer: null }
    collection6_enc.preencode(state, record)
    state.buffer = b4a.allocUnsafe(state.end)
    state.buffer[state.start++] = 0
    state.buffer[state.start++] = collectionVersion
    collection6_enc.encode(state, record)
    return state.buffer
  },
  trigger: null,
  reconstruct: collection6_reconstruct,
  reconstructKey: collection6_reconstruct_key,
  indexes: [],
  decodedVersion: 0
}

// '@peartubeChannel/vectorIndexes' collection key
const collection7_key = new IndexEncoder([
  IndexEncoder.STRING
], { prefix: 7 })

function collection7_indexify (record) {
  const a = record.videoId
  return a === undefined ? [] : [a]
}

// '@peartubeChannel/vectorIndexes' value encoding
const collection7_enc = getEncoding('@peartubeChannel/vectorIndex/hyperdb#7')

// '@peartubeChannel/vectorIndexes' reconstruction function
function collection7_reconstruct (schemaVersion, keyBuf, valueBuf) {
  const key = collection7_key.decode(keyBuf)
  setVersion(schemaVersion)
  const state = { start: 0, end: valueBuf.byteLength, buffer: valueBuf }
  const type = c.uint.decode(state)
  if (type !== 0) throw new Error('Unknown collection type: ' + type)
  collection7.decodedVersion = c.uint.decode(state)
  const record = collection7_enc.decode(state)
  record.videoId = key[0]
  return record
}
// '@peartubeChannel/vectorIndexes' key reconstruction function
function collection7_reconstruct_key (keyBuf) {
  const key = collection7_key.decode(keyBuf)
  return {
    videoId: key[0]
  }
}

// '@peartubeChannel/vectorIndexes'
const collection7 = {
  name: '@peartubeChannel/vectorIndexes',
  id: 7,
  version: 1,
  encodeKey (record) {
    const key = [record.videoId]
    return collection7_key.encode(key)
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return collection7_key.encodeRange({
      gt: gt ? collection7_indexify(gt) : null,
      lt: lt ? collection7_indexify(lt) : null,
      gte: gte ? collection7_indexify(gte) : null,
      lte: lte ? collection7_indexify(lte) : null
    })
  },
  encodeValue (schemaVersion, collectionVersion, record) {
    setVersion(schemaVersion)
    const state = { start: 0, end: 2, buffer: null }
    collection7_enc.preencode(state, record)
    state.buffer = b4a.allocUnsafe(state.end)
    state.buffer[state.start++] = 0
    state.buffer[state.start++] = collectionVersion
    collection7_enc.encode(state, record)
    return state.buffer
  },
  trigger: null,
  reconstruct: collection7_reconstruct,
  reconstructKey: collection7_reconstruct_key,
  indexes: [],
  decodedVersion: 0
}

// '@peartubeChannel/writers-by-role' collection key
const index8_key = new IndexEncoder([
  IndexEncoder.STRING,
  IndexEncoder.STRING
], { prefix: 8 })

function index8_indexify (record) {
  const arr = []

  const a0 = record.role
  if (a0 === undefined) return arr
  arr.push(a0)

  const a1 = record.keyHex
  if (a1 === undefined) return arr
  arr.push(a1)

  return arr
}

// '@peartubeChannel/writers-by-role'
const index8 = {
  name: '@peartubeChannel/writers-by-role',
  version: 1,
  id: 8,
  encodeKey (record) {
    return index8_key.encode(index8_indexify(record))
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return index8_key.encodeRange({
      gt: gt ? index8_indexify(gt) : null,
      lt: lt ? index8_indexify(lt) : null,
      gte: gte ? index8_indexify(gte) : null,
      lte: lte ? index8_indexify(lte) : null
    })
  },
  encodeValue: (record) => index8.collection.encodeKey(record),
  encodeIndexKeys (record, context) {
    return [index8_key.encode([record.role, record.keyHex])]
  },
  reconstruct: (keyBuf, valueBuf) => valueBuf,
  offset: collection1.indexes.length,
  collection: collection1
}
collection1.indexes.push(index8)

// '@peartubeChannel/videos-by-uploaded-at' collection key
const index9_key = new IndexEncoder([
  IndexEncoder.UINT,
  IndexEncoder.STRING
], { prefix: 9 })

function index9_indexify (record) {
  const arr = []

  const a0 = record.uploadedAt
  if (a0 === undefined) return arr
  arr.push(a0)

  const a1 = record.id
  if (a1 === undefined) return arr
  arr.push(a1)

  return arr
}

// '@peartubeChannel/videos-by-uploaded-at'
const index9 = {
  name: '@peartubeChannel/videos-by-uploaded-at',
  version: 1,
  id: 9,
  encodeKey (record) {
    return index9_key.encode(index9_indexify(record))
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return index9_key.encodeRange({
      gt: gt ? index9_indexify(gt) : null,
      lt: lt ? index9_indexify(lt) : null,
      gte: gte ? index9_indexify(gte) : null,
      lte: lte ? index9_indexify(lte) : null
    })
  },
  encodeValue: (record) => index9.collection.encodeKey(record),
  encodeIndexKeys (record, context) {
    return [index9_key.encode([record.uploadedAt, record.id])]
  },
  reconstruct: (keyBuf, valueBuf) => valueBuf,
  offset: collection2.indexes.length,
  collection: collection2
}
collection2.indexes.push(index9)

// '@peartubeChannel/comments-by-video-timestamp' collection key
const index10_key = new IndexEncoder([
  IndexEncoder.STRING,
  IndexEncoder.UINT,
  IndexEncoder.STRING,
  IndexEncoder.STRING
], { prefix: 10 })

function index10_indexify (record) {
  const arr = []

  const a0 = record.videoId
  if (a0 === undefined) return arr
  arr.push(a0)

  const a1 = record.timestamp
  if (a1 === undefined) return arr
  arr.push(a1)

  const a2 = record.videoId
  if (a2 === undefined) return arr
  arr.push(a2)

  const a3 = record.commentId
  if (a3 === undefined) return arr
  arr.push(a3)

  return arr
}

// '@peartubeChannel/comments-by-video-timestamp'
const index10 = {
  name: '@peartubeChannel/comments-by-video-timestamp',
  version: 1,
  id: 10,
  encodeKey (record) {
    return index10_key.encode(index10_indexify(record))
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return index10_key.encodeRange({
      gt: gt ? index10_indexify(gt) : null,
      lt: lt ? index10_indexify(lt) : null,
      gte: gte ? index10_indexify(gte) : null,
      lte: lte ? index10_indexify(lte) : null
    })
  },
  encodeValue: (record) => index10.collection.encodeKey(record),
  encodeIndexKeys (record, context) {
    return [index10_key.encode([record.videoId, record.timestamp, record.videoId, record.commentId])]
  },
  reconstruct: (keyBuf, valueBuf) => valueBuf,
  offset: collection3.indexes.length,
  collection: collection3
}
collection3.indexes.push(index10)

// '@peartubeChannel/reactions-by-video-type' collection key
const index11_key = new IndexEncoder([
  IndexEncoder.STRING,
  IndexEncoder.STRING,
  IndexEncoder.STRING,
  IndexEncoder.STRING
], { prefix: 11 })

function index11_indexify (record) {
  const arr = []

  const a0 = record.videoId
  if (a0 === undefined) return arr
  arr.push(a0)

  const a1 = record.reactionType
  if (a1 === undefined) return arr
  arr.push(a1)

  const a2 = record.videoId
  if (a2 === undefined) return arr
  arr.push(a2)

  const a3 = record.authorKeyHex
  if (a3 === undefined) return arr
  arr.push(a3)

  return arr
}

// '@peartubeChannel/reactions-by-video-type'
const index11 = {
  name: '@peartubeChannel/reactions-by-video-type',
  version: 1,
  id: 11,
  encodeKey (record) {
    return index11_key.encode(index11_indexify(record))
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return index11_key.encodeRange({
      gt: gt ? index11_indexify(gt) : null,
      lt: lt ? index11_indexify(lt) : null,
      gte: gte ? index11_indexify(gte) : null,
      lte: lte ? index11_indexify(lte) : null
    })
  },
  encodeValue: (record) => index11.collection.encodeKey(record),
  encodeIndexKeys (record, context) {
    return [index11_key.encode([record.videoId, record.reactionType, record.videoId, record.authorKeyHex])]
  },
  reconstruct: (keyBuf, valueBuf) => valueBuf,
  offset: collection4.indexes.length,
  collection: collection4
}
collection4.indexes.push(index11)

// '@peartubeChannel/watch-events-by-video-timestamp' collection key
const index12_key = new IndexEncoder([
  IndexEncoder.STRING,
  IndexEncoder.UINT,
  IndexEncoder.STRING,
  IndexEncoder.STRING
], { prefix: 12 })

function index12_indexify (record) {
  const arr = []

  const a0 = record.videoId
  if (a0 === undefined) return arr
  arr.push(a0)

  const a1 = record.timestamp
  if (a1 === undefined) return arr
  arr.push(a1)

  const a2 = record.videoId
  if (a2 === undefined) return arr
  arr.push(a2)

  const a3 = record.eventId
  if (a3 === undefined) return arr
  arr.push(a3)

  return arr
}

// '@peartubeChannel/watch-events-by-video-timestamp'
const index12 = {
  name: '@peartubeChannel/watch-events-by-video-timestamp',
  version: 1,
  id: 12,
  encodeKey (record) {
    return index12_key.encode(index12_indexify(record))
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return index12_key.encodeRange({
      gt: gt ? index12_indexify(gt) : null,
      lt: lt ? index12_indexify(lt) : null,
      gte: gte ? index12_indexify(gte) : null,
      lte: lte ? index12_indexify(lte) : null
    })
  },
  encodeValue: (record) => index12.collection.encodeKey(record),
  encodeIndexKeys (record, context) {
    return [index12_key.encode([record.videoId, record.timestamp, record.videoId, record.eventId])]
  },
  reconstruct: (keyBuf, valueBuf) => valueBuf,
  offset: collection6.indexes.length,
  collection: collection6
}
collection6.indexes.push(index12)

// '@peartubeChannel/channelProfiles' collection key
const collection13_key = new IndexEncoder([
  IndexEncoder.STRING
], { prefix: 13 })

function collection13_indexify (record) {
  const a = record.id
  return a === undefined ? [] : [a]
}

// '@peartubeChannel/channelProfiles' value encoding
const collection13_enc = getEncoding('@peartubeChannel/channelProfile/hyperdb#13')

// '@peartubeChannel/channelProfiles' reconstruction function
function collection13_reconstruct (schemaVersion, keyBuf, valueBuf) {
  const key = collection13_key.decode(keyBuf)
  setVersion(schemaVersion)
  const state = { start: 0, end: valueBuf.byteLength, buffer: valueBuf }
  const type = c.uint.decode(state)
  if (type !== 0) throw new Error('Unknown collection type: ' + type)
  collection13.decodedVersion = c.uint.decode(state)
  const record = collection13_enc.decode(state)
  record.id = key[0]
  return record
}
// '@peartubeChannel/channelProfiles' key reconstruction function
function collection13_reconstruct_key (keyBuf) {
  const key = collection13_key.decode(keyBuf)
  return {
    id: key[0]
  }
}

// '@peartubeChannel/channelProfiles'
const collection13 = {
  name: '@peartubeChannel/channelProfiles',
  id: 13,
  version: 2,
  encodeKey (record) {
    const key = [record.id]
    return collection13_key.encode(key)
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return collection13_key.encodeRange({
      gt: gt ? collection13_indexify(gt) : null,
      lt: lt ? collection13_indexify(lt) : null,
      gte: gte ? collection13_indexify(gte) : null,
      lte: lte ? collection13_indexify(lte) : null
    })
  },
  encodeValue (schemaVersion, collectionVersion, record) {
    setVersion(schemaVersion)
    const state = { start: 0, end: 2, buffer: null }
    collection13_enc.preencode(state, record)
    state.buffer = b4a.allocUnsafe(state.end)
    state.buffer[state.start++] = 0
    state.buffer[state.start++] = collectionVersion
    collection13_enc.encode(state, record)
    return state.buffer
  },
  trigger: null,
  reconstruct: collection13_reconstruct,
  reconstructKey: collection13_reconstruct_key,
  indexes: [],
  decodedVersion: 0
}

// '@peartubeChannel/contentDetails' collection key
const collection14_key = new IndexEncoder([
  IndexEncoder.STRING
], { prefix: 14 })

function collection14_indexify (record) {
  const a = record.id
  return a === undefined ? [] : [a]
}

// '@peartubeChannel/contentDetails' value encoding
const collection14_enc = getEncoding('@peartubeChannel/contentDetails/hyperdb#14')

// '@peartubeChannel/contentDetails' reconstruction function
function collection14_reconstruct (schemaVersion, keyBuf, valueBuf) {
  const key = collection14_key.decode(keyBuf)
  setVersion(schemaVersion)
  const state = { start: 0, end: valueBuf.byteLength, buffer: valueBuf }
  const type = c.uint.decode(state)
  if (type !== 0) throw new Error('Unknown collection type: ' + type)
  collection14.decodedVersion = c.uint.decode(state)
  const record = collection14_enc.decode(state)
  record.id = key[0]
  return record
}
// '@peartubeChannel/contentDetails' key reconstruction function
function collection14_reconstruct_key (keyBuf) {
  const key = collection14_key.decode(keyBuf)
  return {
    id: key[0]
  }
}

// '@peartubeChannel/contentDetails'
const collection14 = {
  name: '@peartubeChannel/contentDetails',
  id: 14,
  version: 2,
  encodeKey (record) {
    const key = [record.id]
    return collection14_key.encode(key)
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return collection14_key.encodeRange({
      gt: gt ? collection14_indexify(gt) : null,
      lt: lt ? collection14_indexify(lt) : null,
      gte: gte ? collection14_indexify(gte) : null,
      lte: lte ? collection14_indexify(lte) : null
    })
  },
  encodeValue (schemaVersion, collectionVersion, record) {
    setVersion(schemaVersion)
    const state = { start: 0, end: 2, buffer: null }
    collection14_enc.preencode(state, record)
    state.buffer = b4a.allocUnsafe(state.end)
    state.buffer[state.start++] = 0
    state.buffer[state.start++] = collectionVersion
    collection14_enc.encode(state, record)
    return state.buffer
  },
  trigger: null,
  reconstruct: collection14_reconstruct,
  reconstructKey: collection14_reconstruct_key,
  indexes: [],
  decodedVersion: 0
}

// '@peartubeChannel/channelSources' collection key
const collection15_key = new IndexEncoder([
  IndexEncoder.STRING,
  IndexEncoder.STRING
], { prefix: 15 })

function collection15_indexify (record) {
  const arr = []

  const a0 = record.provider
  if (a0 === undefined) return arr
  arr.push(a0)

  const a1 = record.identityKey
  if (a1 === undefined) return arr
  arr.push(a1)

  return arr
}

// '@peartubeChannel/channelSources' value encoding
const collection15_enc = getEncoding('@peartubeChannel/channelSource/hyperdb#15')

// '@peartubeChannel/channelSources' reconstruction function
function collection15_reconstruct (schemaVersion, keyBuf, valueBuf) {
  const key = collection15_key.decode(keyBuf)
  setVersion(schemaVersion)
  const state = { start: 0, end: valueBuf.byteLength, buffer: valueBuf }
  const type = c.uint.decode(state)
  if (type !== 0) throw new Error('Unknown collection type: ' + type)
  collection15.decodedVersion = c.uint.decode(state)
  const record = collection15_enc.decode(state)
  record.provider = key[0]
  record.identityKey = key[1]
  return record
}
// '@peartubeChannel/channelSources' key reconstruction function
function collection15_reconstruct_key (keyBuf) {
  const key = collection15_key.decode(keyBuf)
  return {
    provider: key[0],
    identityKey: key[1]
  }
}

// '@peartubeChannel/channelSources'
const collection15 = {
  name: '@peartubeChannel/channelSources',
  id: 15,
  version: 2,
  encodeKey (record) {
    const key = [record.provider, record.identityKey]
    return collection15_key.encode(key)
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return collection15_key.encodeRange({
      gt: gt ? collection15_indexify(gt) : null,
      lt: lt ? collection15_indexify(lt) : null,
      gte: gte ? collection15_indexify(gte) : null,
      lte: lte ? collection15_indexify(lte) : null
    })
  },
  encodeValue (schemaVersion, collectionVersion, record) {
    setVersion(schemaVersion)
    const state = { start: 0, end: 2, buffer: null }
    collection15_enc.preencode(state, record)
    state.buffer = b4a.allocUnsafe(state.end)
    state.buffer[state.start++] = 0
    state.buffer[state.start++] = collectionVersion
    collection15_enc.encode(state, record)
    return state.buffer
  },
  trigger: null,
  reconstruct: collection15_reconstruct,
  reconstructKey: collection15_reconstruct_key,
  indexes: [],
  decodedVersion: 0
}

// '@peartubeChannel/channelArtwork' collection key
const collection16_key = new IndexEncoder([
  IndexEncoder.STRING
], { prefix: 16 })

function collection16_indexify (record) {
  const a = record.role
  return a === undefined ? [] : [a]
}

// '@peartubeChannel/channelArtwork' value encoding
const collection16_enc = getEncoding('@peartubeChannel/channelArtwork/hyperdb#16')

// '@peartubeChannel/channelArtwork' reconstruction function
function collection16_reconstruct (schemaVersion, keyBuf, valueBuf) {
  const key = collection16_key.decode(keyBuf)
  setVersion(schemaVersion)
  const state = { start: 0, end: valueBuf.byteLength, buffer: valueBuf }
  const type = c.uint.decode(state)
  if (type !== 0) throw new Error('Unknown collection type: ' + type)
  collection16.decodedVersion = c.uint.decode(state)
  const record = collection16_enc.decode(state)
  record.role = key[0]
  return record
}
// '@peartubeChannel/channelArtwork' key reconstruction function
function collection16_reconstruct_key (keyBuf) {
  const key = collection16_key.decode(keyBuf)
  return {
    role: key[0]
  }
}

// '@peartubeChannel/channelArtwork'
const collection16 = {
  name: '@peartubeChannel/channelArtwork',
  id: 16,
  version: 2,
  encodeKey (record) {
    const key = [record.role]
    return collection16_key.encode(key)
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return collection16_key.encodeRange({
      gt: gt ? collection16_indexify(gt) : null,
      lt: lt ? collection16_indexify(lt) : null,
      gte: gte ? collection16_indexify(gte) : null,
      lte: lte ? collection16_indexify(lte) : null
    })
  },
  encodeValue (schemaVersion, collectionVersion, record) {
    setVersion(schemaVersion)
    const state = { start: 0, end: 2, buffer: null }
    collection16_enc.preencode(state, record)
    state.buffer = b4a.allocUnsafe(state.end)
    state.buffer[state.start++] = 0
    state.buffer[state.start++] = collectionVersion
    collection16_enc.encode(state, record)
    return state.buffer
  },
  trigger: null,
  reconstruct: collection16_reconstruct,
  reconstructKey: collection16_reconstruct_key,
  indexes: [],
  decodedVersion: 0
}

// '@peartubeChannel/importClaims' collection key
const collection17_key = new IndexEncoder([
  IndexEncoder.STRING,
  IndexEncoder.STRING
], { prefix: 17 })

function collection17_indexify (record) {
  const arr = []

  const a0 = record.identityKey
  if (a0 === undefined) return arr
  arr.push(a0)

  const a1 = record.claimantId
  if (a1 === undefined) return arr
  arr.push(a1)

  return arr
}

// '@peartubeChannel/importClaims' value encoding
const collection17_enc = getEncoding('@peartubeChannel/importClaim/hyperdb#17')

// '@peartubeChannel/importClaims' reconstruction function
function collection17_reconstruct (schemaVersion, keyBuf, valueBuf) {
  const key = collection17_key.decode(keyBuf)
  setVersion(schemaVersion)
  const state = { start: 0, end: valueBuf.byteLength, buffer: valueBuf }
  const type = c.uint.decode(state)
  if (type !== 0) throw new Error('Unknown collection type: ' + type)
  collection17.decodedVersion = c.uint.decode(state)
  const record = collection17_enc.decode(state)
  record.identityKey = key[0]
  record.claimantId = key[1]
  return record
}
// '@peartubeChannel/importClaims' key reconstruction function
function collection17_reconstruct_key (keyBuf) {
  const key = collection17_key.decode(keyBuf)
  return {
    identityKey: key[0],
    claimantId: key[1]
  }
}

// '@peartubeChannel/importClaims'
const collection17 = {
  name: '@peartubeChannel/importClaims',
  id: 17,
  version: 2,
  encodeKey (record) {
    const key = [record.identityKey, record.claimantId]
    return collection17_key.encode(key)
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return collection17_key.encodeRange({
      gt: gt ? collection17_indexify(gt) : null,
      lt: lt ? collection17_indexify(lt) : null,
      gte: gte ? collection17_indexify(gte) : null,
      lte: lte ? collection17_indexify(lte) : null
    })
  },
  encodeValue (schemaVersion, collectionVersion, record) {
    setVersion(schemaVersion)
    const state = { start: 0, end: 2, buffer: null }
    collection17_enc.preencode(state, record)
    state.buffer = b4a.allocUnsafe(state.end)
    state.buffer[state.start++] = 0
    state.buffer[state.start++] = collectionVersion
    collection17_enc.encode(state, record)
    return state.buffer
  },
  trigger: null,
  reconstruct: collection17_reconstruct,
  reconstructKey: collection17_reconstruct_key,
  indexes: [],
  decodedVersion: 0
}

// '@peartubeChannel/claims-by-writer' collection key
const index18_key = new IndexEncoder([
  IndexEncoder.STRING,
  IndexEncoder.STRING,
  IndexEncoder.STRING,
  IndexEncoder.STRING,
  IndexEncoder.STRING
], { prefix: 18 })

function index18_indexify (record) {
  const arr = []

  const a0 = record.identityKey
  if (a0 === undefined) return arr
  arr.push(a0)

  const a1 = record.writerKey
  if (a1 === undefined) return arr
  arr.push(a1)

  const a2 = record.claimantId
  if (a2 === undefined) return arr
  arr.push(a2)

  const a3 = record.identityKey
  if (a3 === undefined) return arr
  arr.push(a3)

  const a4 = record.claimantId
  if (a4 === undefined) return arr
  arr.push(a4)

  return arr
}

// '@peartubeChannel/claims-by-writer'
const index18 = {
  name: '@peartubeChannel/claims-by-writer',
  version: 2,
  id: 18,
  encodeKey (record) {
    return index18_key.encode(index18_indexify(record))
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return index18_key.encodeRange({
      gt: gt ? index18_indexify(gt) : null,
      lt: lt ? index18_indexify(lt) : null,
      gte: gte ? index18_indexify(gte) : null,
      lte: lte ? index18_indexify(lte) : null
    })
  },
  encodeValue: (record) => index18.collection.encodeKey(record),
  encodeIndexKeys (record, context) {
    return [index18_key.encode([record.identityKey, record.writerKey, record.claimantId, record.identityKey, record.claimantId])]
  },
  reconstruct: (keyBuf, valueBuf) => valueBuf,
  offset: collection17.indexes.length,
  collection: collection17
}
collection17.indexes.push(index18)

// '@peartubeChannel/claims-by-identity' collection key
const index19_key = new IndexEncoder([
  IndexEncoder.STRING,
  IndexEncoder.STRING,
  IndexEncoder.STRING,
  IndexEncoder.STRING
], { prefix: 19 })

function index19_indexify (record) {
  const arr = []

  const a0 = record.identityKey
  if (a0 === undefined) return arr
  arr.push(a0)

  const a1 = record.claimantId
  if (a1 === undefined) return arr
  arr.push(a1)

  const a2 = record.identityKey
  if (a2 === undefined) return arr
  arr.push(a2)

  const a3 = record.claimantId
  if (a3 === undefined) return arr
  arr.push(a3)

  return arr
}

// '@peartubeChannel/claims-by-identity'
const index19 = {
  name: '@peartubeChannel/claims-by-identity',
  version: 2,
  id: 19,
  encodeKey (record) {
    return index19_key.encode(index19_indexify(record))
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return index19_key.encodeRange({
      gt: gt ? index19_indexify(gt) : null,
      lt: lt ? index19_indexify(lt) : null,
      gte: gte ? index19_indexify(gte) : null,
      lte: lte ? index19_indexify(lte) : null
    })
  },
  encodeValue: (record) => index19.collection.encodeKey(record),
  encodeIndexKeys (record, context) {
    return [index19_key.encode([record.identityKey, record.claimantId, record.identityKey, record.claimantId])]
  },
  reconstruct: (keyBuf, valueBuf) => valueBuf,
  offset: collection17.indexes.length,
  collection: collection17
}
collection17.indexes.push(index19)

// '@peartubeChannel/videos-by-season-episode' collection key
const index20_key = new IndexEncoder([
  IndexEncoder.UINT,
  IndexEncoder.UINT,
  IndexEncoder.STRING,
  IndexEncoder.STRING
], { prefix: 20 })

function index20_indexify (record) {
  const arr = []

  const a0 = record.seasonNumber
  if (a0 === undefined) return arr
  arr.push(a0)

  const a1 = record.episodeNumber
  if (a1 === undefined) return arr
  arr.push(a1)

  const a2 = record.id
  if (a2 === undefined) return arr
  arr.push(a2)

  const a3 = record.id
  if (a3 === undefined) return arr
  arr.push(a3)

  return arr
}

// '@peartubeChannel/videos-by-season-episode'
const index20 = {
  name: '@peartubeChannel/videos-by-season-episode',
  version: 2,
  id: 20,
  encodeKey (record) {
    return index20_key.encode(index20_indexify(record))
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return index20_key.encodeRange({
      gt: gt ? index20_indexify(gt) : null,
      lt: lt ? index20_indexify(lt) : null,
      gte: gte ? index20_indexify(gte) : null,
      lte: lte ? index20_indexify(lte) : null
    })
  },
  encodeValue: (record) => index20.collection.encodeKey(record),
  encodeIndexKeys (record, context) {
    return [index20_key.encode([record.seasonNumber, record.episodeNumber, record.id, record.id])]
  },
  reconstruct: (keyBuf, valueBuf) => valueBuf,
  offset: collection14.indexes.length,
  collection: collection14
}
collection14.indexes.push(index20)

// '@peartubeChannel/videos-by-source' collection key
const index21_key = new IndexEncoder([
  IndexEncoder.STRING,
  IndexEncoder.STRING,
  IndexEncoder.STRING,
  IndexEncoder.STRING
], { prefix: 21 })

function index21_indexify (record) {
  const arr = []

  const a0 = record.sourceProvider
  if (a0 === undefined) return arr
  arr.push(a0)

  const a1 = record.sourceVideoId
  if (a1 === undefined) return arr
  arr.push(a1)

  const a2 = record.id
  if (a2 === undefined) return arr
  arr.push(a2)

  const a3 = record.id
  if (a3 === undefined) return arr
  arr.push(a3)

  return arr
}

// '@peartubeChannel/videos-by-source'
const index21 = {
  name: '@peartubeChannel/videos-by-source',
  version: 2,
  id: 21,
  encodeKey (record) {
    return index21_key.encode(index21_indexify(record))
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return index21_key.encodeRange({
      gt: gt ? index21_indexify(gt) : null,
      lt: lt ? index21_indexify(lt) : null,
      gte: gte ? index21_indexify(gte) : null,
      lte: lte ? index21_indexify(lte) : null
    })
  },
  encodeValue: (record) => index21.collection.encodeKey(record),
  encodeIndexKeys (record, context) {
    return [index21_key.encode([record.sourceProvider, record.sourceVideoId, record.id, record.id])]
  },
  reconstruct: (keyBuf, valueBuf) => valueBuf,
  offset: collection14.indexes.length,
  collection: collection14
}
collection14.indexes.push(index21)

// '@peartubeChannel/videos-by-kind-published' collection key
const index22_key = new IndexEncoder([
  IndexEncoder.STRING,
  IndexEncoder.UINT,
  IndexEncoder.STRING,
  IndexEncoder.STRING
], { prefix: 22 })

function index22_indexify (record) {
  const arr = []

  const a0 = record.contentKind
  if (a0 === undefined) return arr
  arr.push(a0)

  const a1 = record.sourcePublishedAt
  if (a1 === undefined) return arr
  arr.push(a1)

  const a2 = record.id
  if (a2 === undefined) return arr
  arr.push(a2)

  const a3 = record.id
  if (a3 === undefined) return arr
  arr.push(a3)

  return arr
}

// '@peartubeChannel/videos-by-kind-published'
const index22 = {
  name: '@peartubeChannel/videos-by-kind-published',
  version: 2,
  id: 22,
  encodeKey (record) {
    return index22_key.encode(index22_indexify(record))
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return index22_key.encodeRange({
      gt: gt ? index22_indexify(gt) : null,
      lt: lt ? index22_indexify(lt) : null,
      gte: gte ? index22_indexify(gte) : null,
      lte: lte ? index22_indexify(lte) : null
    })
  },
  encodeValue: (record) => index22.collection.encodeKey(record),
  encodeIndexKeys (record, context) {
    return [index22_key.encode([record.contentKind, record.sourcePublishedAt, record.id, record.id])]
  },
  reconstruct: (keyBuf, valueBuf) => valueBuf,
  offset: collection14.indexes.length,
  collection: collection14
}
collection14.indexes.push(index22)

const collections = [
  collection0,
  collection1,
  collection2,
  collection3,
  collection4,
  collection5,
  collection6,
  collection7,
  collection13,
  collection14,
  collection15,
  collection16,
  collection17
]

const indexes = [
  index8,
  index9,
  index10,
  index11,
  index12,
  index18,
  index19,
  index20,
  index21,
  index22
]

export default { versions, collections, indexes, resolveCollection, resolveIndex }

function resolveCollection (name) {
  switch (name) {
    case '@peartubeChannel/metadata': return collection0
    case '@peartubeChannel/writers': return collection1
    case '@peartubeChannel/videos': return collection2
    case '@peartubeChannel/comments': return collection3
    case '@peartubeChannel/reactions': return collection4
    case '@peartubeChannel/invites': return collection5
    case '@peartubeChannel/watchEvents': return collection6
    case '@peartubeChannel/vectorIndexes': return collection7
    case '@peartubeChannel/channelProfiles': return collection13
    case '@peartubeChannel/contentDetails': return collection14
    case '@peartubeChannel/channelSources': return collection15
    case '@peartubeChannel/channelArtwork': return collection16
    case '@peartubeChannel/importClaims': return collection17
    default: return null
  }
}

function resolveIndex (name) {
  switch (name) {
    case '@peartubeChannel/writers-by-role': return index8
    case '@peartubeChannel/videos-by-uploaded-at': return index9
    case '@peartubeChannel/comments-by-video-timestamp': return index10
    case '@peartubeChannel/reactions-by-video-type': return index11
    case '@peartubeChannel/watch-events-by-video-timestamp': return index12
    case '@peartubeChannel/claims-by-writer': return index18
    case '@peartubeChannel/claims-by-identity': return index19
    case '@peartubeChannel/videos-by-season-episode': return index20
    case '@peartubeChannel/videos-by-source': return index21
    case '@peartubeChannel/videos-by-kind-published': return index22
    default: return null
  }
}
