// This file is autogenerated by the hrpc compiler

import BareRPC
import CompactEncoding
import Foundation

public class HRPC: RPCDelegate, @unchecked Sendable {
  private let _rpc: RPC
  private var _handlers: [String: Any] = [:]
  private weak var _outerDelegate: RPCDelegate?

  private let addCommentRequest = AddCommentRequestCodec()
  private let addCommentResponse = AddCommentResponseCodec()
  private let addReactionRequest = AddReactionRequestCodec()
  private let addReactionResponse = AddReactionResponseCodec()
  private let addToPlaylistRequest = AddToPlaylistRequestCodec()
  private let addToPlaylistResponse = AddToPlaylistResponseCodec()
  private let assessSourceOffloadRequest = AssessSourceOffloadRequestCodec()
  private let assessSourceOffloadResponse = AssessSourceOffloadResponseCodec()
  private let attestDeviceRequest = AttestDeviceRequestCodec()
  private let attestDeviceResponse = AttestDeviceResponseCodec()
  private let bootstrapDeviceRequest = BootstrapDeviceRequestCodec()
  private let bootstrapDeviceResponse = BootstrapDeviceResponseCodec()
  private let castAddManualDeviceRequest = CastAddManualDeviceRequestCodec()
  private let castAddManualDeviceResponse = CastAddManualDeviceResponseCodec()
  private let castAvailableRequest = CastAvailableRequestCodec()
  private let castAvailableResponse = CastAvailableResponseCodec()
  private let castConnectRequest = CastConnectRequestCodec()
  private let castConnectResponse = CastConnectResponseCodec()
  private let castDisconnectRequest = CastDisconnectRequestCodec()
  private let castDisconnectResponse = CastDisconnectResponseCodec()
  private let castGetDevicesRequest = CastGetDevicesRequestCodec()
  private let castGetDevicesResponse = CastGetDevicesResponseCodec()
  private let castGetStateRequest = CastGetStateRequestCodec()
  private let castGetStateResponse = CastGetStateResponseCodec()
  private let castIsConnectedRequest = CastIsConnectedRequestCodec()
  private let castIsConnectedResponse = CastIsConnectedResponseCodec()
  private let castPauseRequest = CastPauseRequestCodec()
  private let castPauseResponse = CastPauseResponseCodec()
  private let castPlayRequest = CastPlayRequestCodec()
  private let castPlayResponse = CastPlayResponseCodec()
  private let castResumeRequest = CastResumeRequestCodec()
  private let castResumeResponse = CastResumeResponseCodec()
  private let castSeekRequest = CastSeekRequestCodec()
  private let castSeekResponse = CastSeekResponseCodec()
  private let castSetVolumeRequest = CastSetVolumeRequestCodec()
  private let castSetVolumeResponse = CastSetVolumeResponseCodec()
  private let castStartDiscoveryRequest = CastStartDiscoveryRequestCodec()
  private let castStartDiscoveryResponse = CastStartDiscoveryResponseCodec()
  private let castStopDiscoveryRequest = CastStopDiscoveryRequestCodec()
  private let castStopDiscoveryResponse = CastStopDiscoveryResponseCodec()
  private let castStopRequest = CastStopRequestCodec()
  private let castStopResponse = CastStopResponseCodec()
  private let clearCacheRequest = ClearCacheRequestCodec()
  private let clearCacheResponse = ClearCacheResponseCodec()
  private let confirmSourceOffloadRequest = ConfirmSourceOffloadRequestCodec()
  private let confirmSourceOffloadResponse = ConfirmSourceOffloadResponseCodec()
  private let createDeviceInviteRequest = CreateDeviceInviteRequestCodec()
  private let createDeviceInviteResponse = CreateDeviceInviteResponseCodec()
  private let createIdentityRequest = CreateIdentityRequestCodec()
  private let createIdentityResponse = CreateIdentityResponseCodec()
  private let createPlaylistRequest = CreatePlaylistRequestCodec()
  private let createPlaylistResponse = CreatePlaylistResponseCodec()
  private let deletePlaylistRequest = DeletePlaylistRequestCodec()
  private let deletePlaylistResponse = DeletePlaylistResponseCodec()
  private let deleteVideoRequest = DeleteVideoRequestCodec()
  private let deleteVideoResponse = DeleteVideoResponseCodec()
  private let desktopBootstrapRequest = DesktopBootstrapRequestCodec()
  private let desktopBootstrapResponse = DesktopBootstrapResponseCodec()
  private let desktopRefreshBrowseRequest = DesktopRefreshBrowseRequestCodec()
  private let desktopRefreshBrowseResponse = DesktopRefreshBrowseResponseCodec()
  private let desktopShutdownRequest = DesktopShutdownRequestCodec()
  private let desktopShutdownResponse = DesktopShutdownResponseCodec()
  private let downloadVideoRequest = DownloadVideoRequestCodec()
  private let downloadVideoResponse = DownloadVideoResponseCodec()
  private let eventCastDeviceFound = EventCastDeviceFoundCodec()
  private let eventCastDeviceLost = EventCastDeviceLostCodec()
  private let eventCastPlaybackState = EventCastPlaybackStateCodec()
  private let eventCastTimeUpdate = EventCastTimeUpdateCodec()
  private let eventDownloadProgress = EventDownloadProgressCodec()
  private let eventError = EventErrorCodec()
  private let eventLog = EventLogCodec()
  private let eventMediaGraphUpdate = EventMediaGraphUpdateCodec()
  private let eventReady = EventReadyCodec()
  private let eventTranscodeProgress = EventTranscodeProgressCodec()
  private let eventUploadProgress = EventUploadProgressCodec()
  private let eventVideoStats = EventVideoStatsCodec()
  private let exportMigrationReportRequest = ExportMigrationReportRequestCodec()
  private let exportMigrationReportResponse = ExportMigrationReportResponseCodec()
  private let exportPortableStateRequest = ExportPortableStateRequestCodec()
  private let exportPortableStateResponse = ExportPortableStateResponseCodec()
  private let ffmpegDecodeAvailableRequest = FfmpegDecodeAvailableRequestCodec()
  private let ffmpegDecodeAvailableResponse = FfmpegDecodeAvailableResponseCodec()
  private let getAgentContributionsRequest = GetAgentContributionsRequestCodec()
  private let getAgentContributionsResponse = GetAgentContributionsResponseCodec()
  private let getArchiveOperatorStatusRequest = GetArchiveOperatorStatusRequestCodec()
  private let getArchiveOperatorStatusResponse = GetArchiveOperatorStatusResponseCodec()
  private let getArchiveParticipationRequest = GetArchiveParticipationRequestCodec()
  private let getArchiveParticipationResponse = GetArchiveParticipationResponseCodec()
  private let getBlobServerPortRequest = GetBlobServerPortRequestCodec()
  private let getBlobServerPortResponse = GetBlobServerPortResponseCodec()
  private let getChannelMetaRequest = GetChannelMetaRequestCodec()
  private let getChannelMetaResponse = GetChannelMetaResponseCodec()
  private let getChannelRequest = GetChannelRequestCodec()
  private let getChannelResponse = GetChannelResponseCodec()
  private let getClaimProvenanceRequest = GetClaimProvenanceRequestCodec()
  private let getClaimProvenanceResponse = GetClaimProvenanceResponseCodec()
  private let getContentCatalogRequest = GetContentCatalogRequestCodec()
  private let getContentCatalogResponse = GetContentCatalogResponseCodec()
  private let getContentItemsRequest = GetContentItemsRequestCodec()
  private let getContentItemsResponse = GetContentItemsResponseCodec()
  private let getIdentitiesRequest = GetIdentitiesRequestCodec()
  private let getIdentitiesResponse = GetIdentitiesResponseCodec()
  private let getIdentityRequest = GetIdentityRequestCodec()
  private let getIdentityResponse = GetIdentityResponseCodec()
  private let getLivestreamStatusRequest = GetLivestreamStatusRequestCodec()
  private let getLivestreamStatusResponse = GetLivestreamStatusResponseCodec()
  private let getMediaAgentRequest = GetMediaAgentRequestCodec()
  private let getMediaAgentResponse = GetMediaAgentResponseCodec()
  private let getMediaCatalogResponse = GetMediaCatalogResponseCodec()
  private let getMediaCollectionItemsRequest = GetMediaCollectionItemsRequestCodec()
  private let getMediaCollectionItemsResponse = GetMediaCollectionItemsResponseCodec()
  private let getMediaCollectionRequest = GetMediaCollectionRequestCodec()
  private let getMediaCollectionResponse = GetMediaCollectionResponseCodec()
  private let getMediaEntityRequest = GetMediaEntityRequestCodec()
  private let getMediaEntityResponse = GetMediaEntityResponseCodec()
  private let getMigrationStatusRequest = GetMigrationStatusRequestCodec()
  private let getMigrationStatusResponse = GetMigrationStatusResponseCodec()
  private let getNetworkPolicyRequest = GetNetworkPolicyRequestCodec()
  private let getNetworkPolicyResponse = GetNetworkPolicyResponseCodec()
  private let getPersonalSettingsRequest = GetPersonalSettingsRequestCodec()
  private let getPersonalSettingsResponse = GetPersonalSettingsResponseCodec()
  private let getPinnedChannelsRequest = GetPinnedChannelsRequestCodec()
  private let getPinnedChannelsResponse = GetPinnedChannelsResponseCodec()
  private let getPlaylistItemsRequest = GetPlaylistItemsRequestCodec()
  private let getPlaylistItemsResponse = GetPlaylistItemsResponseCodec()
  private let getPlaylistsRequest = GetPlaylistsRequestCodec()
  private let getPlaylistsResponse = GetPlaylistsResponseCodec()
  private let getPublicationSourcesRequest = GetPublicationSourcesRequestCodec()
  private let getPublicationSourcesResponse = GetPublicationSourcesResponseCodec()
  private let getPublisherDeviceStatusRequest = GetPublisherDeviceStatusRequestCodec()
  private let getPublisherDeviceStatusResponse = GetPublisherDeviceStatusResponseCodec()
  private let getReactionsRequest = GetReactionsRequestCodec()
  private let getReactionsResponse = GetReactionsResponseCodec()
  private let getResumePositionRequest = GetResumePositionRequestCodec()
  private let getResumePositionResponse = GetResumePositionResponseCodec()
  private let getSeedingStatusRequest = GetSeedingStatusRequestCodec()
  private let getSeedingStatusResponse = GetSeedingStatusResponseCodec()
  private let getStatusRequest = GetStatusRequestCodec()
  private let getStatusResponse = GetStatusResponseCodec()
  private let getStorageStatsRequest = GetStorageStatsRequestCodec()
  private let getStorageStatsResponse = GetStorageStatsResponseCodec()
  private let getSubscriptionsRequest = GetSubscriptionsRequestCodec()
  private let getSubscriptionsResponse = GetSubscriptionsResponseCodec()
  private let getSwarmStatusRequest = GetSwarmStatusRequestCodec()
  private let getSwarmStatusResponse = GetSwarmStatusResponseCodec()
  private let getTranscodeSettingsRequest = GetTranscodeSettingsRequestCodec()
  private let getTranscodeSettingsResponse = GetTranscodeSettingsResponseCodec()
  private let getVideoDataRequest = GetVideoDataRequestCodec()
  private let getVideoDataResponse = GetVideoDataResponseCodec()
  private let getVideoMetadataRequest = GetVideoMetadataRequestCodec()
  private let getVideoMetadataResponse = GetVideoMetadataResponseCodec()
  private let getVideoStatsRequest = GetVideoStatsRequestCodec()
  private let getVideoStatsResponse = GetVideoStatsResponseCodec()
  private let getVideoThumbnailRequest = GetVideoThumbnailRequestCodec()
  private let getVideoThumbnailResponse = GetVideoThumbnailResponseCodec()
  private let getVideoUrlRequest = GetVideoUrlRequestCodec()
  private let getVideoUrlResponse = GetVideoUrlResponseCodec()
  private let getWatchHistoryRequest = GetWatchHistoryRequestCodec()
  private let getWatchHistoryResponse = GetWatchHistoryResponseCodec()
  private let globalSearchVideosRequest = GlobalSearchVideosRequestCodec()
  private let globalSearchVideosResponse = GlobalSearchVideosResponseCodec()
  private let hideChannelRequest = HideChannelRequestCodec()
  private let hideChannelResponse = HideChannelResponseCodec()
  private let hideCommentRequest = HideCommentRequestCodec()
  private let hideCommentResponse = HideCommentResponseCodec()
  private let indexVideoVectorsRequest = IndexVideoVectorsRequestCodec()
  private let indexVideoVectorsResponse = IndexVideoVectorsResponseCodec()
  private let joinChannelRequest = JoinChannelRequestCodec()
  private let joinChannelResponse = JoinChannelResponseCodec()
  private let listCommentsRequest = ListCommentsRequestCodec()
  private let listCommentsResponse = ListCommentsResponseCodec()
  private let listDevicesRequest = ListDevicesRequestCodec()
  private let listDevicesResponse = ListDevicesResponseCodec()
  private let listResumePositionsRequest = ListResumePositionsRequestCodec()
  private let listResumePositionsResponse = ListResumePositionsResponseCodec()
  private let listVideosRequest = ListVideosRequestCodec()
  private let listVideosResponse = ListVideosResponseCodec()
  private let logWatchHistoryRequest = LogWatchHistoryRequestCodec()
  private let logWatchHistoryResponse = LogWatchHistoryResponseCodec()
  private let mediaPageRequest = MediaPageRequestCodec()
  private let pairDeviceRequest = PairDeviceRequestCodec()
  private let pairDeviceResponse = PairDeviceResponseCodec()
  private let pickImageFileRequest = PickImageFileRequestCodec()
  private let pickImageFileResponse = PickImageFileResponseCodec()
  private let pickVideoFileRequest = PickVideoFileRequestCodec()
  private let pickVideoFileResponse = PickVideoFileResponseCodec()
  private let pinChannelRequest = PinChannelRequestCodec()
  private let pinChannelResponse = PinChannelResponseCodec()
  private let prefetchVideoRequest = PrefetchVideoRequestCodec()
  private let prefetchVideoResponse = PrefetchVideoResponseCodec()
  private let prepareLivePlaybackRequest = PrepareLivePlaybackRequestCodec()
  private let prepareLivePlaybackResponse = PrepareLivePlaybackResponseCodec()
  private let prepareMediaPlaybackRequest = PrepareMediaPlaybackRequestCodec()
  private let prepareMediaPlaybackResponse = PrepareMediaPlaybackResponseCodec()
  private let preparePlaybackRequest = PreparePlaybackRequestCodec()
  private let preparePlaybackResponse = PreparePlaybackResponseCodec()
  private let preparePublisherRootOperationRequest = PreparePublisherRootOperationRequestCodec()
  private let preparePublisherRootOperationResponse = PreparePublisherRootOperationResponseCodec()
  private let previewStorageLimitRequest = PreviewStorageLimitRequestCodec()
  private let previewStorageLimitResponse = PreviewStorageLimitResponseCodec()
  private let createPersonalDeviceInviteRequest = CreatePersonalDeviceInviteRequestCodec()
  private let createPersonalDeviceInviteResponse = CreatePersonalDeviceInviteResponseCodec()
  private let redeemPersonalDeviceInviteRequest = RedeemPersonalDeviceInviteRequestCodec()
  private let redeemPersonalDeviceInviteResponse = RedeemPersonalDeviceInviteResponseCodec()
  private let listPersonalDevicesRequest = ListPersonalDevicesRequestCodec()
  private let listPersonalDevicesResponse = ListPersonalDevicesResponseCodec()
  private let revokePersonalDeviceRequest = RevokePersonalDeviceRequestCodec()
  private let revokePersonalDeviceResponse = RevokePersonalDeviceResponseCodec()
  private let provisionPersonalEncryptionRequest = ProvisionPersonalEncryptionRequestCodec()
  private let provisionPersonalEncryptionResponse = ProvisionPersonalEncryptionResponseCodec()
  private let provisionPublisherCatalogRequest = ProvisionPublisherCatalogRequestCodec()
  private let provisionPublisherCatalogResponse = ProvisionPublisherCatalogResponseCodec()
  private let recoverIdentityRequest = RecoverIdentityRequestCodec()
  private let recoverIdentityResponse = RecoverIdentityResponseCodec()
  private let removeCommentRequest = RemoveCommentRequestCodec()
  private let removeCommentResponse = RemoveCommentResponseCodec()
  private let removeFromPlaylistRequest = RemoveFromPlaylistRequestCodec()
  private let removeFromPlaylistResponse = RemoveFromPlaylistResponseCodec()
  private let removeReactionRequest = RemoveReactionRequestCodec()
  private let removeReactionResponse = RemoveReactionResponseCodec()
  private let requestArchivePublicationRequest = RequestArchivePublicationRequestCodec()
  private let requestArchivePublicationResponse = RequestArchivePublicationResponseCodec()
  private let restorePortableStateRequest = RestorePortableStateRequestCodec()
  private let restorePortableStateResponse = RestorePortableStateResponseCodec()
  private let retryMigrationRequest = RetryMigrationRequestCodec()
  private let retryMigrationResponse = RetryMigrationResponseCodec()
  private let retrySyncChannelRequest = RetrySyncChannelRequestCodec()
  private let retrySyncChannelResponse = RetrySyncChannelResponseCodec()
  private let searchVideosRequest = SearchVideosRequestCodec()
  private let searchVideosResponse = SearchVideosResponseCodec()
  private let setActiveIdentityRequest = SetActiveIdentityRequestCodec()
  private let setActiveIdentityResponse = SetActiveIdentityResponseCodec()
  private let setArchiveParticipationRequest = SetArchiveParticipationRequestCodec()
  private let setArchiveParticipationResponse = SetArchiveParticipationResponseCodec()
  private let setNetworkPolicyRequest = SetNetworkPolicyRequestCodec()
  private let setNetworkPolicyResponse = SetNetworkPolicyResponseCodec()
  private let setPersonalSettingRequest = SetPersonalSettingRequestCodec()
  private let setPersonalSettingResponse = SetPersonalSettingResponseCodec()
  private let setSeedingConfigRequest = SetSeedingConfigRequestCodec()
  private let setSeedingConfigResponse = SetSeedingConfigResponseCodec()
  private let setSourcePreferenceRequest = SetSourcePreferenceRequestCodec()
  private let setSourcePreferenceResponse = SetSourcePreferenceResponseCodec()
  private let setStorageLimitRequest = SetStorageLimitRequestCodec()
  private let setStorageLimitResponse = SetStorageLimitResponseCodec()
  private let setTranscodeSettingsRequest = SetTranscodeSettingsRequestCodec()
  private let setTranscodeSettingsResponse = SetTranscodeSettingsResponseCodec()
  private let setVideoThumbnailFromFileRequest = SetVideoThumbnailFromFileRequestCodec()
  private let setVideoThumbnailFromFileResponse = SetVideoThumbnailFromFileResponseCodec()
  private let setVideoThumbnailRequest = SetVideoThumbnailRequestCodec()
  private let setVideoThumbnailResponse = SetVideoThumbnailResponseCodec()
  private let startLivestreamRequest = StartLivestreamRequestCodec()
  private let startLivestreamResponse = StartLivestreamResponseCodec()
  private let stopLivestreamRequest = StopLivestreamRequestCodec()
  private let stopLivestreamResponse = StopLivestreamResponseCodec()
  private let submitPublisherRootOperationRequest = SubmitPublisherRootOperationRequestCodec()
  private let submitPublisherRootOperationResponse = SubmitPublisherRootOperationResponseCodec()
  private let subscribeChannelRequest = SubscribeChannelRequestCodec()
  private let subscribeChannelResponse = SubscribeChannelResponseCodec()
  private let transcodeStartRequest = TranscodeStartRequestCodec()
  private let transcodeStartResponse = TranscodeStartResponseCodec()
  private let transcodeStatusRequest = TranscodeStatusRequestCodec()
  private let transcodeStatusResponse = TranscodeStatusResponseCodec()
  private let transcodeStopRequest = TranscodeStopRequestCodec()
  private let transcodeStopResponse = TranscodeStopResponseCodec()
  private let unpinChannelRequest = UnpinChannelRequestCodec()
  private let unpinChannelResponse = UnpinChannelResponseCodec()
  private let unsubscribeChannelRequest = UnsubscribeChannelRequestCodec()
  private let unsubscribeChannelResponse = UnsubscribeChannelResponseCodec()
  private let updateChannelAvatarRequest = UpdateChannelAvatarRequestCodec()
  private let updateChannelAvatarResponse = UpdateChannelAvatarResponseCodec()
  private let updateChannelRequest = UpdateChannelRequestCodec()
  private let updateChannelResponse = UpdateChannelResponseCodec()
  private let updatePlaylistRequest = UpdatePlaylistRequestCodec()
  private let updatePlaylistResponse = UpdatePlaylistResponseCodec()
  private let updateVideoMetadataRequest = UpdateVideoMetadataRequestCodec()
  private let updateVideoMetadataResponse = UpdateVideoMetadataResponseCodec()
  private let uploadVideoRequest = UploadVideoRequestCodec()
  private let uploadVideoResponse = UploadVideoResponseCodec()
  private let verifyAttestationRequest = VerifyAttestationRequestCodec()
  private let verifyAttestationResponse = VerifyAttestationResponseCodec()
  private let webPreparePlaybackResponse = WebPreparePlaybackResponseCodec()

  public init(delegate: RPCDelegate) {
    self._outerDelegate = delegate
    self._rpc = RPC()
    // RPC is an actor: delegate binding goes through its isolated setter.
    // Safe against early request()/receive() because actor FIFO ordering
    // ensures setDelegate runs before any later call enqueues behind it.
    let rpc = self._rpc
    Task { await rpc.setDelegate(self) }
  }

  // RPCDelegate — forward send/error to outer delegate, dispatch request/event internally
  public func rpc(_ rpc: RPC, send data: Data) {
    _outerDelegate?.rpc(rpc, send: data)
  }

  public func rpc(_ rpc: RPC, didReceiveRequest request: IncomingRequest) async throws {
    await _dispatchRequest(request)
  }

  public func rpc(_ rpc: RPC, didReceiveEvent event: IncomingEvent) async {
    await _dispatchEvent(event)
  }

  public func rpc(_ rpc: RPC, didFailWith error: Swift.Error) {
    _outerDelegate?.rpc(rpc, didFailWith: error)
  }

  public func receive(_ data: Data) {
    let rpc = _rpc
    Task { await rpc.receive(data) }
  }

  // Request/response — client
  public func createIdentity(_ args: CreateIdentityRequest) async throws -> CreateIdentityResponse {
    let encoded = try _encode(createIdentityRequest, args)
    guard let raw = try await _rpc.request(0, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(createIdentityResponse, raw)
  }

  public func onCreateIdentity(_ handler: @escaping (CreateIdentityRequest) async throws -> CreateIdentityResponse) {
    _handlers["@peartube/create-identity"] = handler
  }

  // Request/response — client
  public func getIdentity(_ args: GetIdentityRequest) async throws -> GetIdentityResponse {
    let encoded = try _encode(getIdentityRequest, args)
    guard let raw = try await _rpc.request(1, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getIdentityResponse, raw)
  }

  public func onGetIdentity(_ handler: @escaping (GetIdentityRequest) async throws -> GetIdentityResponse) {
    _handlers["@peartube/get-identity"] = handler
  }

  // Request/response — client
  public func getIdentities(_ args: GetIdentitiesRequest) async throws -> GetIdentitiesResponse {
    let encoded = try _encode(getIdentitiesRequest, args)
    guard let raw = try await _rpc.request(2, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getIdentitiesResponse, raw)
  }

  public func onGetIdentities(_ handler: @escaping (GetIdentitiesRequest) async throws -> GetIdentitiesResponse) {
    _handlers["@peartube/get-identities"] = handler
  }

  // Request/response — client
  public func setActiveIdentity(_ args: SetActiveIdentityRequest) async throws -> SetActiveIdentityResponse {
    let encoded = try _encode(setActiveIdentityRequest, args)
    guard let raw = try await _rpc.request(3, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(setActiveIdentityResponse, raw)
  }

  public func onSetActiveIdentity(_ handler: @escaping (SetActiveIdentityRequest) async throws -> SetActiveIdentityResponse) {
    _handlers["@peartube/set-active-identity"] = handler
  }

  // Request/response — client
  public func recoverIdentity(_ args: RecoverIdentityRequest) async throws -> RecoverIdentityResponse {
    let encoded = try _encode(recoverIdentityRequest, args)
    guard let raw = try await _rpc.request(4, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(recoverIdentityResponse, raw)
  }

  public func onRecoverIdentity(_ handler: @escaping (RecoverIdentityRequest) async throws -> RecoverIdentityResponse) {
    _handlers["@peartube/recover-identity"] = handler
  }

  // Request/response — client
  public func bootstrapDevice(_ args: BootstrapDeviceRequest) async throws -> BootstrapDeviceResponse {
    let encoded = try _encode(bootstrapDeviceRequest, args)
    guard let raw = try await _rpc.request(5, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(bootstrapDeviceResponse, raw)
  }

  public func onBootstrapDevice(_ handler: @escaping (BootstrapDeviceRequest) async throws -> BootstrapDeviceResponse) {
    _handlers["@peartube/bootstrap-device"] = handler
  }

  // Request/response — client
  public func attestDevice(_ args: AttestDeviceRequest) async throws -> AttestDeviceResponse {
    let encoded = try _encode(attestDeviceRequest, args)
    guard let raw = try await _rpc.request(6, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(attestDeviceResponse, raw)
  }

  public func onAttestDevice(_ handler: @escaping (AttestDeviceRequest) async throws -> AttestDeviceResponse) {
    _handlers["@peartube/attest-device"] = handler
  }

  // Request/response — client
  public func provisionPublisherCatalog(_ args: ProvisionPublisherCatalogRequest) async throws -> ProvisionPublisherCatalogResponse {
    let encoded = try _encode(provisionPublisherCatalogRequest, args)
    guard let raw = try await _rpc.request(7, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(provisionPublisherCatalogResponse, raw)
  }

  public func onProvisionPublisherCatalog(_ handler: @escaping (ProvisionPublisherCatalogRequest) async throws -> ProvisionPublisherCatalogResponse) {
    _handlers["@peartube/provision-publisher-catalog"] = handler
  }

  // Request/response — client
  public func verifyAttestation(_ args: VerifyAttestationRequest) async throws -> VerifyAttestationResponse {
    let encoded = try _encode(verifyAttestationRequest, args)
    guard let raw = try await _rpc.request(8, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(verifyAttestationResponse, raw)
  }

  public func onVerifyAttestation(_ handler: @escaping (VerifyAttestationRequest) async throws -> VerifyAttestationResponse) {
    _handlers["@peartube/verify-attestation"] = handler
  }

  // Request/response — client
  public func preparePublisherRootOperation(_ args: PreparePublisherRootOperationRequest) async throws -> PreparePublisherRootOperationResponse {
    let encoded = try _encode(preparePublisherRootOperationRequest, args)
    guard let raw = try await _rpc.request(9, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(preparePublisherRootOperationResponse, raw)
  }

  public func onPreparePublisherRootOperation(_ handler: @escaping (PreparePublisherRootOperationRequest) async throws -> PreparePublisherRootOperationResponse) {
    _handlers["@peartube/prepare-publisher-root-operation"] = handler
  }

  // Request/response — client
  public func submitPublisherRootOperation(_ args: SubmitPublisherRootOperationRequest) async throws -> SubmitPublisherRootOperationResponse {
    let encoded = try _encode(submitPublisherRootOperationRequest, args)
    guard let raw = try await _rpc.request(10, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(submitPublisherRootOperationResponse, raw)
  }

  public func onSubmitPublisherRootOperation(_ handler: @escaping (SubmitPublisherRootOperationRequest) async throws -> SubmitPublisherRootOperationResponse) {
    _handlers["@peartube/submit-publisher-root-operation"] = handler
  }

  // Request/response — client
  public func getChannel(_ args: GetChannelRequest) async throws -> GetChannelResponse {
    let encoded = try _encode(getChannelRequest, args)
    guard let raw = try await _rpc.request(11, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getChannelResponse, raw)
  }

  public func onGetChannel(_ handler: @escaping (GetChannelRequest) async throws -> GetChannelResponse) {
    _handlers["@peartube/get-channel"] = handler
  }

  // Request/response — client
  public func updateChannel(_ args: UpdateChannelRequest) async throws -> UpdateChannelResponse {
    let encoded = try _encode(updateChannelRequest, args)
    guard let raw = try await _rpc.request(12, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(updateChannelResponse, raw)
  }

  public func onUpdateChannel(_ handler: @escaping (UpdateChannelRequest) async throws -> UpdateChannelResponse) {
    _handlers["@peartube/update-channel"] = handler
  }

  // Request/response — client
  public func getContentCatalog(_ args: GetContentCatalogRequest) async throws -> GetContentCatalogResponse {
    let encoded = try _encode(getContentCatalogRequest, args)
    guard let raw = try await _rpc.request(13, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getContentCatalogResponse, raw)
  }

  public func onGetContentCatalog(_ handler: @escaping (GetContentCatalogRequest) async throws -> GetContentCatalogResponse) {
    _handlers["@peartube/get-content-catalog"] = handler
  }

  // Request/response — client
  public func getContentItems(_ args: GetContentItemsRequest) async throws -> GetContentItemsResponse {
    let encoded = try _encode(getContentItemsRequest, args)
    guard let raw = try await _rpc.request(14, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getContentItemsResponse, raw)
  }

  public func onGetContentItems(_ handler: @escaping (GetContentItemsRequest) async throws -> GetContentItemsResponse) {
    _handlers["@peartube/get-content-items"] = handler
  }

  // Request/response — client
  public func listVideos(_ args: ListVideosRequest) async throws -> ListVideosResponse {
    let encoded = try _encode(listVideosRequest, args)
    guard let raw = try await _rpc.request(15, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(listVideosResponse, raw)
  }

  public func onListVideos(_ handler: @escaping (ListVideosRequest) async throws -> ListVideosResponse) {
    _handlers["@peartube/list-videos"] = handler
  }

  // Request/response — client
  public func getVideoUrl(_ args: GetVideoUrlRequest) async throws -> GetVideoUrlResponse {
    let encoded = try _encode(getVideoUrlRequest, args)
    guard let raw = try await _rpc.request(16, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getVideoUrlResponse, raw)
  }

  public func onGetVideoUrl(_ handler: @escaping (GetVideoUrlRequest) async throws -> GetVideoUrlResponse) {
    _handlers["@peartube/get-video-url"] = handler
  }

  // Request/response — client
  public func preparePlayback(_ args: PreparePlaybackRequest) async throws -> PreparePlaybackResponse {
    let encoded = try _encode(preparePlaybackRequest, args)
    guard let raw = try await _rpc.request(17, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(preparePlaybackResponse, raw)
  }

  public func onPreparePlayback(_ handler: @escaping (PreparePlaybackRequest) async throws -> PreparePlaybackResponse) {
    _handlers["@peartube/prepare-playback"] = handler
  }

  // Request/response — client
  public func webPreparePlayback(_ args: PreparePlaybackRequest) async throws -> WebPreparePlaybackResponse {
    let encoded = try _encode(preparePlaybackRequest, args)
    guard let raw = try await _rpc.request(18, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(webPreparePlaybackResponse, raw)
  }

  public func onWebPreparePlayback(_ handler: @escaping (PreparePlaybackRequest) async throws -> WebPreparePlaybackResponse) {
    _handlers["@peartube/web-prepare-playback"] = handler
  }

  // Request/response — client
  public func getVideoData(_ args: GetVideoDataRequest) async throws -> GetVideoDataResponse {
    let encoded = try _encode(getVideoDataRequest, args)
    guard let raw = try await _rpc.request(19, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getVideoDataResponse, raw)
  }

  public func onGetVideoData(_ handler: @escaping (GetVideoDataRequest) async throws -> GetVideoDataResponse) {
    _handlers["@peartube/get-video-data"] = handler
  }

  // Request/response — client
  public func uploadVideo(_ args: UploadVideoRequest) async throws -> UploadVideoResponse {
    let encoded = try _encode(uploadVideoRequest, args)
    guard let raw = try await _rpc.request(20, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(uploadVideoResponse, raw)
  }

  public func onUploadVideo(_ handler: @escaping (UploadVideoRequest) async throws -> UploadVideoResponse) {
    _handlers["@peartube/upload-video"] = handler
  }

  // Request/response — client
  public func downloadVideo(_ args: DownloadVideoRequest) async throws -> DownloadVideoResponse {
    let encoded = try _encode(downloadVideoRequest, args)
    guard let raw = try await _rpc.request(21, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(downloadVideoResponse, raw)
  }

  public func onDownloadVideo(_ handler: @escaping (DownloadVideoRequest) async throws -> DownloadVideoResponse) {
    _handlers["@peartube/download-video"] = handler
  }

  // Request/response — client
  public func deleteVideo(_ args: DeleteVideoRequest) async throws -> DeleteVideoResponse {
    let encoded = try _encode(deleteVideoRequest, args)
    guard let raw = try await _rpc.request(22, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(deleteVideoResponse, raw)
  }

  public func onDeleteVideo(_ handler: @escaping (DeleteVideoRequest) async throws -> DeleteVideoResponse) {
    _handlers["@peartube/delete-video"] = handler
  }

  // Request/response — client
  public func startLivestream(_ args: StartLivestreamRequest) async throws -> StartLivestreamResponse {
    let encoded = try _encode(startLivestreamRequest, args)
    guard let raw = try await _rpc.request(23, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(startLivestreamResponse, raw)
  }

  public func onStartLivestream(_ handler: @escaping (StartLivestreamRequest) async throws -> StartLivestreamResponse) {
    _handlers["@peartube/start-livestream"] = handler
  }

  // Request/response — client
  public func stopLivestream(_ args: StopLivestreamRequest) async throws -> StopLivestreamResponse {
    let encoded = try _encode(stopLivestreamRequest, args)
    guard let raw = try await _rpc.request(24, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(stopLivestreamResponse, raw)
  }

  public func onStopLivestream(_ handler: @escaping (StopLivestreamRequest) async throws -> StopLivestreamResponse) {
    _handlers["@peartube/stop-livestream"] = handler
  }

  // Request/response — client
  public func getLivestreamStatus(_ args: GetLivestreamStatusRequest) async throws -> GetLivestreamStatusResponse {
    let encoded = try _encode(getLivestreamStatusRequest, args)
    guard let raw = try await _rpc.request(25, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getLivestreamStatusResponse, raw)
  }

  public func onGetLivestreamStatus(_ handler: @escaping (GetLivestreamStatusRequest) async throws -> GetLivestreamStatusResponse) {
    _handlers["@peartube/get-livestream-status"] = handler
  }

  // Request/response — client
  public func prepareLivePlayback(_ args: PrepareLivePlaybackRequest) async throws -> PrepareLivePlaybackResponse {
    let encoded = try _encode(prepareLivePlaybackRequest, args)
    guard let raw = try await _rpc.request(26, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(prepareLivePlaybackResponse, raw)
  }

  public func onPrepareLivePlayback(_ handler: @escaping (PrepareLivePlaybackRequest) async throws -> PrepareLivePlaybackResponse) {
    _handlers["@peartube/prepare-live-playback"] = handler
  }

  // Request/response — client
  public func subscribeChannel(_ args: SubscribeChannelRequest) async throws -> SubscribeChannelResponse {
    let encoded = try _encode(subscribeChannelRequest, args)
    guard let raw = try await _rpc.request(27, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(subscribeChannelResponse, raw)
  }

  public func onSubscribeChannel(_ handler: @escaping (SubscribeChannelRequest) async throws -> SubscribeChannelResponse) {
    _handlers["@peartube/subscribe-channel"] = handler
  }

  // Request/response — client
  public func unsubscribeChannel(_ args: UnsubscribeChannelRequest) async throws -> UnsubscribeChannelResponse {
    let encoded = try _encode(unsubscribeChannelRequest, args)
    guard let raw = try await _rpc.request(28, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(unsubscribeChannelResponse, raw)
  }

  public func onUnsubscribeChannel(_ handler: @escaping (UnsubscribeChannelRequest) async throws -> UnsubscribeChannelResponse) {
    _handlers["@peartube/unsubscribe-channel"] = handler
  }

  // Request/response — client
  public func getSubscriptions(_ args: GetSubscriptionsRequest) async throws -> GetSubscriptionsResponse {
    let encoded = try _encode(getSubscriptionsRequest, args)
    guard let raw = try await _rpc.request(29, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getSubscriptionsResponse, raw)
  }

  public func onGetSubscriptions(_ handler: @escaping (GetSubscriptionsRequest) async throws -> GetSubscriptionsResponse) {
    _handlers["@peartube/get-subscriptions"] = handler
  }

  // Request/response — client
  public func getPlaylists(_ args: GetPlaylistsRequest) async throws -> GetPlaylistsResponse {
    let encoded = try _encode(getPlaylistsRequest, args)
    guard let raw = try await _rpc.request(30, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getPlaylistsResponse, raw)
  }

  public func onGetPlaylists(_ handler: @escaping (GetPlaylistsRequest) async throws -> GetPlaylistsResponse) {
    _handlers["@peartube/get-playlists"] = handler
  }

  // Request/response — client
  public func getPlaylistItems(_ args: GetPlaylistItemsRequest) async throws -> GetPlaylistItemsResponse {
    let encoded = try _encode(getPlaylistItemsRequest, args)
    guard let raw = try await _rpc.request(31, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getPlaylistItemsResponse, raw)
  }

  public func onGetPlaylistItems(_ handler: @escaping (GetPlaylistItemsRequest) async throws -> GetPlaylistItemsResponse) {
    _handlers["@peartube/get-playlist-items"] = handler
  }

  // Request/response — client
  public func createPlaylist(_ args: CreatePlaylistRequest) async throws -> CreatePlaylistResponse {
    let encoded = try _encode(createPlaylistRequest, args)
    guard let raw = try await _rpc.request(32, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(createPlaylistResponse, raw)
  }

  public func onCreatePlaylist(_ handler: @escaping (CreatePlaylistRequest) async throws -> CreatePlaylistResponse) {
    _handlers["@peartube/create-playlist"] = handler
  }

  // Request/response — client
  public func updatePlaylist(_ args: UpdatePlaylistRequest) async throws -> UpdatePlaylistResponse {
    let encoded = try _encode(updatePlaylistRequest, args)
    guard let raw = try await _rpc.request(33, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(updatePlaylistResponse, raw)
  }

  public func onUpdatePlaylist(_ handler: @escaping (UpdatePlaylistRequest) async throws -> UpdatePlaylistResponse) {
    _handlers["@peartube/update-playlist"] = handler
  }

  // Request/response — client
  public func deletePlaylist(_ args: DeletePlaylistRequest) async throws -> DeletePlaylistResponse {
    let encoded = try _encode(deletePlaylistRequest, args)
    guard let raw = try await _rpc.request(34, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(deletePlaylistResponse, raw)
  }

  public func onDeletePlaylist(_ handler: @escaping (DeletePlaylistRequest) async throws -> DeletePlaylistResponse) {
    _handlers["@peartube/delete-playlist"] = handler
  }

  // Request/response — client
  public func addToPlaylist(_ args: AddToPlaylistRequest) async throws -> AddToPlaylistResponse {
    let encoded = try _encode(addToPlaylistRequest, args)
    guard let raw = try await _rpc.request(35, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(addToPlaylistResponse, raw)
  }

  public func onAddToPlaylist(_ handler: @escaping (AddToPlaylistRequest) async throws -> AddToPlaylistResponse) {
    _handlers["@peartube/add-to-playlist"] = handler
  }

  // Request/response — client
  public func removeFromPlaylist(_ args: RemoveFromPlaylistRequest) async throws -> RemoveFromPlaylistResponse {
    let encoded = try _encode(removeFromPlaylistRequest, args)
    guard let raw = try await _rpc.request(36, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(removeFromPlaylistResponse, raw)
  }

  public func onRemoveFromPlaylist(_ handler: @escaping (RemoveFromPlaylistRequest) async throws -> RemoveFromPlaylistResponse) {
    _handlers["@peartube/remove-from-playlist"] = handler
  }

  // Request/response — client
  public func logWatchHistory(_ args: LogWatchHistoryRequest) async throws -> LogWatchHistoryResponse {
    let encoded = try _encode(logWatchHistoryRequest, args)
    guard let raw = try await _rpc.request(37, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(logWatchHistoryResponse, raw)
  }

  public func onLogWatchHistory(_ handler: @escaping (LogWatchHistoryRequest) async throws -> LogWatchHistoryResponse) {
    _handlers["@peartube/log-watch-history"] = handler
  }

  // Request/response — client
  public func getWatchHistory(_ args: GetWatchHistoryRequest) async throws -> GetWatchHistoryResponse {
    let encoded = try _encode(getWatchHistoryRequest, args)
    guard let raw = try await _rpc.request(38, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getWatchHistoryResponse, raw)
  }

  public func onGetWatchHistory(_ handler: @escaping (GetWatchHistoryRequest) async throws -> GetWatchHistoryResponse) {
    _handlers["@peartube/get-watch-history"] = handler
  }

  // Request/response — client
  public func getResumePosition(_ args: GetResumePositionRequest) async throws -> GetResumePositionResponse {
    let encoded = try _encode(getResumePositionRequest, args)
    guard let raw = try await _rpc.request(39, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getResumePositionResponse, raw)
  }

  public func onGetResumePosition(_ handler: @escaping (GetResumePositionRequest) async throws -> GetResumePositionResponse) {
    _handlers["@peartube/get-resume-position"] = handler
  }

  // Request/response — client
  public func listResumePositions(_ args: ListResumePositionsRequest) async throws -> ListResumePositionsResponse {
    let encoded = try _encode(listResumePositionsRequest, args)
    guard let raw = try await _rpc.request(40, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(listResumePositionsResponse, raw)
  }

  public func onListResumePositions(_ handler: @escaping (ListResumePositionsRequest) async throws -> ListResumePositionsResponse) {
    _handlers["@peartube/list-resume-positions"] = handler
  }

  // Request/response — client
  public func setPersonalSetting(_ args: SetPersonalSettingRequest) async throws -> SetPersonalSettingResponse {
    let encoded = try _encode(setPersonalSettingRequest, args)
    guard let raw = try await _rpc.request(41, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(setPersonalSettingResponse, raw)
  }

  public func onSetPersonalSetting(_ handler: @escaping (SetPersonalSettingRequest) async throws -> SetPersonalSettingResponse) {
    _handlers["@peartube/set-personal-setting"] = handler
  }

  // Request/response — client
  public func getPersonalSettings(_ args: GetPersonalSettingsRequest) async throws -> GetPersonalSettingsResponse {
    let encoded = try _encode(getPersonalSettingsRequest, args)
    guard let raw = try await _rpc.request(42, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getPersonalSettingsResponse, raw)
  }

  public func onGetPersonalSettings(_ handler: @escaping (GetPersonalSettingsRequest) async throws -> GetPersonalSettingsResponse) {
    _handlers["@peartube/get-personal-settings"] = handler
  }

  // Request/response — client
  public func provisionPersonalEncryption(_ args: ProvisionPersonalEncryptionRequest) async throws -> ProvisionPersonalEncryptionResponse {
    let encoded = try _encode(provisionPersonalEncryptionRequest, args)
    guard let raw = try await _rpc.request(43, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(provisionPersonalEncryptionResponse, raw)
  }

  public func onProvisionPersonalEncryption(_ handler: @escaping (ProvisionPersonalEncryptionRequest) async throws -> ProvisionPersonalEncryptionResponse) {
    _handlers["@peartube/provision-personal-encryption"] = handler
  }

  // Request/response — client
  public func createPersonalDeviceInvite(_ args: CreatePersonalDeviceInviteRequest) async throws -> CreatePersonalDeviceInviteResponse {
    let encoded = try _encode(createPersonalDeviceInviteRequest, args)
    guard let raw = try await _rpc.request(44, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(createPersonalDeviceInviteResponse, raw)
  }

  public func onCreatePersonalDeviceInvite(_ handler: @escaping (CreatePersonalDeviceInviteRequest) async throws -> CreatePersonalDeviceInviteResponse) {
    _handlers["@peartube/create-personal-device-invite"] = handler
  }

  // Request/response — client
  public func redeemPersonalDeviceInvite(_ args: RedeemPersonalDeviceInviteRequest) async throws -> RedeemPersonalDeviceInviteResponse {
    let encoded = try _encode(redeemPersonalDeviceInviteRequest, args)
    guard let raw = try await _rpc.request(45, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(redeemPersonalDeviceInviteResponse, raw)
  }

  public func onRedeemPersonalDeviceInvite(_ handler: @escaping (RedeemPersonalDeviceInviteRequest) async throws -> RedeemPersonalDeviceInviteResponse) {
    _handlers["@peartube/redeem-personal-device-invite"] = handler
  }

  // Request/response — client
  public func listPersonalDevices(_ args: ListPersonalDevicesRequest) async throws -> ListPersonalDevicesResponse {
    let encoded = try _encode(listPersonalDevicesRequest, args)
    guard let raw = try await _rpc.request(46, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(listPersonalDevicesResponse, raw)
  }

  public func onListPersonalDevices(_ handler: @escaping (ListPersonalDevicesRequest) async throws -> ListPersonalDevicesResponse) {
    _handlers["@peartube/list-personal-devices"] = handler
  }

  // Request/response — client
  public func revokePersonalDevice(_ args: RevokePersonalDeviceRequest) async throws -> RevokePersonalDeviceResponse {
    let encoded = try _encode(revokePersonalDeviceRequest, args)
    guard let raw = try await _rpc.request(47, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(revokePersonalDeviceResponse, raw)
  }

  public func onRevokePersonalDevice(_ handler: @escaping (RevokePersonalDeviceRequest) async throws -> RevokePersonalDeviceResponse) {
    _handlers["@peartube/revoke-personal-device"] = handler
  }

  // Request/response — client
  public func joinChannel(_ args: JoinChannelRequest) async throws -> JoinChannelResponse {
    let encoded = try _encode(joinChannelRequest, args)
    guard let raw = try await _rpc.request(48, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(joinChannelResponse, raw)
  }

  public func onJoinChannel(_ handler: @escaping (JoinChannelRequest) async throws -> JoinChannelResponse) {
    _handlers["@peartube/join-channel"] = handler
  }

  // Request/response — client
  public func hideChannel(_ args: HideChannelRequest) async throws -> HideChannelResponse {
    let encoded = try _encode(hideChannelRequest, args)
    guard let raw = try await _rpc.request(49, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(hideChannelResponse, raw)
  }

  public func onHideChannel(_ handler: @escaping (HideChannelRequest) async throws -> HideChannelResponse) {
    _handlers["@peartube/hide-channel"] = handler
  }

  // Request/response — client
  public func getChannelMeta(_ args: GetChannelMetaRequest) async throws -> GetChannelMetaResponse {
    let encoded = try _encode(getChannelMetaRequest, args)
    guard let raw = try await _rpc.request(50, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getChannelMetaResponse, raw)
  }

  public func onGetChannelMeta(_ handler: @escaping (GetChannelMetaRequest) async throws -> GetChannelMetaResponse) {
    _handlers["@peartube/get-channel-meta"] = handler
  }

  // Request/response — client
  public func getSwarmStatus(_ args: GetSwarmStatusRequest) async throws -> GetSwarmStatusResponse {
    let encoded = try _encode(getSwarmStatusRequest, args)
    guard let raw = try await _rpc.request(51, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getSwarmStatusResponse, raw)
  }

  public func onGetSwarmStatus(_ handler: @escaping (GetSwarmStatusRequest) async throws -> GetSwarmStatusResponse) {
    _handlers["@peartube/get-swarm-status"] = handler
  }

  // Request/response — client
  public func createDeviceInvite(_ args: CreateDeviceInviteRequest) async throws -> CreateDeviceInviteResponse {
    let encoded = try _encode(createDeviceInviteRequest, args)
    guard let raw = try await _rpc.request(52, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(createDeviceInviteResponse, raw)
  }

  public func onCreateDeviceInvite(_ handler: @escaping (CreateDeviceInviteRequest) async throws -> CreateDeviceInviteResponse) {
    _handlers["@peartube/create-device-invite"] = handler
  }

  // Request/response — client
  public func pairDevice(_ args: PairDeviceRequest) async throws -> PairDeviceResponse {
    let encoded = try _encode(pairDeviceRequest, args)
    guard let raw = try await _rpc.request(53, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(pairDeviceResponse, raw)
  }

  public func onPairDevice(_ handler: @escaping (PairDeviceRequest) async throws -> PairDeviceResponse) {
    _handlers["@peartube/pair-device"] = handler
  }

  // Request/response — client
  public func listDevices(_ args: ListDevicesRequest) async throws -> ListDevicesResponse {
    let encoded = try _encode(listDevicesRequest, args)
    guard let raw = try await _rpc.request(54, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(listDevicesResponse, raw)
  }

  public func onListDevices(_ handler: @escaping (ListDevicesRequest) async throws -> ListDevicesResponse) {
    _handlers["@peartube/list-devices"] = handler
  }

  // Request/response — client
  public func retrySyncChannel(_ args: RetrySyncChannelRequest) async throws -> RetrySyncChannelResponse {
    let encoded = try _encode(retrySyncChannelRequest, args)
    guard let raw = try await _rpc.request(55, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(retrySyncChannelResponse, raw)
  }

  public func onRetrySyncChannel(_ handler: @escaping (RetrySyncChannelRequest) async throws -> RetrySyncChannelResponse) {
    _handlers["@peartube/retry-sync-channel"] = handler
  }

  // Request/response — client
  public func prefetchVideo(_ args: PrefetchVideoRequest) async throws -> PrefetchVideoResponse {
    let encoded = try _encode(prefetchVideoRequest, args)
    guard let raw = try await _rpc.request(56, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(prefetchVideoResponse, raw)
  }

  public func onPrefetchVideo(_ handler: @escaping (PrefetchVideoRequest) async throws -> PrefetchVideoResponse) {
    _handlers["@peartube/prefetch-video"] = handler
  }

  // Request/response — client
  public func getVideoStats(_ args: GetVideoStatsRequest) async throws -> GetVideoStatsResponse {
    let encoded = try _encode(getVideoStatsRequest, args)
    guard let raw = try await _rpc.request(57, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getVideoStatsResponse, raw)
  }

  public func onGetVideoStats(_ handler: @escaping (GetVideoStatsRequest) async throws -> GetVideoStatsResponse) {
    _handlers["@peartube/get-video-stats"] = handler
  }

  // Request/response — client
  public func getSeedingStatus(_ args: GetSeedingStatusRequest) async throws -> GetSeedingStatusResponse {
    let encoded = try _encode(getSeedingStatusRequest, args)
    guard let raw = try await _rpc.request(58, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getSeedingStatusResponse, raw)
  }

  public func onGetSeedingStatus(_ handler: @escaping (GetSeedingStatusRequest) async throws -> GetSeedingStatusResponse) {
    _handlers["@peartube/get-seeding-status"] = handler
  }

  // Request/response — client
  public func setSeedingConfig(_ args: SetSeedingConfigRequest) async throws -> SetSeedingConfigResponse {
    let encoded = try _encode(setSeedingConfigRequest, args)
    guard let raw = try await _rpc.request(59, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(setSeedingConfigResponse, raw)
  }

  public func onSetSeedingConfig(_ handler: @escaping (SetSeedingConfigRequest) async throws -> SetSeedingConfigResponse) {
    _handlers["@peartube/set-seeding-config"] = handler
  }

  // Request/response — client
  public func getTranscodeSettings(_ args: GetTranscodeSettingsRequest) async throws -> GetTranscodeSettingsResponse {
    let encoded = try _encode(getTranscodeSettingsRequest, args)
    guard let raw = try await _rpc.request(60, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getTranscodeSettingsResponse, raw)
  }

  public func onGetTranscodeSettings(_ handler: @escaping (GetTranscodeSettingsRequest) async throws -> GetTranscodeSettingsResponse) {
    _handlers["@peartube/get-transcode-settings"] = handler
  }

  // Request/response — client
  public func setTranscodeSettings(_ args: SetTranscodeSettingsRequest) async throws -> SetTranscodeSettingsResponse {
    let encoded = try _encode(setTranscodeSettingsRequest, args)
    guard let raw = try await _rpc.request(61, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(setTranscodeSettingsResponse, raw)
  }

  public func onSetTranscodeSettings(_ handler: @escaping (SetTranscodeSettingsRequest) async throws -> SetTranscodeSettingsResponse) {
    _handlers["@peartube/set-transcode-settings"] = handler
  }

  // Request/response — client
  public func pinChannel(_ args: PinChannelRequest) async throws -> PinChannelResponse {
    let encoded = try _encode(pinChannelRequest, args)
    guard let raw = try await _rpc.request(62, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(pinChannelResponse, raw)
  }

  public func onPinChannel(_ handler: @escaping (PinChannelRequest) async throws -> PinChannelResponse) {
    _handlers["@peartube/pin-channel"] = handler
  }

  // Request/response — client
  public func unpinChannel(_ args: UnpinChannelRequest) async throws -> UnpinChannelResponse {
    let encoded = try _encode(unpinChannelRequest, args)
    guard let raw = try await _rpc.request(63, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(unpinChannelResponse, raw)
  }

  public func onUnpinChannel(_ handler: @escaping (UnpinChannelRequest) async throws -> UnpinChannelResponse) {
    _handlers["@peartube/unpin-channel"] = handler
  }

  // Request/response — client
  public func getPinnedChannels(_ args: GetPinnedChannelsRequest) async throws -> GetPinnedChannelsResponse {
    let encoded = try _encode(getPinnedChannelsRequest, args)
    guard let raw = try await _rpc.request(64, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getPinnedChannelsResponse, raw)
  }

  public func onGetPinnedChannels(_ handler: @escaping (GetPinnedChannelsRequest) async throws -> GetPinnedChannelsResponse) {
    _handlers["@peartube/get-pinned-channels"] = handler
  }

  // Request/response — client
  public func getStorageStats(_ args: GetStorageStatsRequest) async throws -> GetStorageStatsResponse {
    let encoded = try _encode(getStorageStatsRequest, args)
    guard let raw = try await _rpc.request(65, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getStorageStatsResponse, raw)
  }

  public func onGetStorageStats(_ handler: @escaping (GetStorageStatsRequest) async throws -> GetStorageStatsResponse) {
    _handlers["@peartube/get-storage-stats"] = handler
  }

  // Request/response — client
  public func setStorageLimit(_ args: SetStorageLimitRequest) async throws -> SetStorageLimitResponse {
    let encoded = try _encode(setStorageLimitRequest, args)
    guard let raw = try await _rpc.request(66, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(setStorageLimitResponse, raw)
  }

  public func onSetStorageLimit(_ handler: @escaping (SetStorageLimitRequest) async throws -> SetStorageLimitResponse) {
    _handlers["@peartube/set-storage-limit"] = handler
  }

  // Request/response — client
  public func getMigrationStatus(_ args: GetMigrationStatusRequest) async throws -> GetMigrationStatusResponse {
    let encoded = try _encode(getMigrationStatusRequest, args)
    guard let raw = try await _rpc.request(67, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getMigrationStatusResponse, raw)
  }

  public func onGetMigrationStatus(_ handler: @escaping (GetMigrationStatusRequest) async throws -> GetMigrationStatusResponse) {
    _handlers["@peartube/get-migration-status"] = handler
  }

  // Request/response — client
  public func retryMigration(_ args: RetryMigrationRequest) async throws -> RetryMigrationResponse {
    let encoded = try _encode(retryMigrationRequest, args)
    guard let raw = try await _rpc.request(68, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(retryMigrationResponse, raw)
  }

  public func onRetryMigration(_ handler: @escaping (RetryMigrationRequest) async throws -> RetryMigrationResponse) {
    _handlers["@peartube/retry-migration"] = handler
  }

  // Request/response — client
  public func exportMigrationReport(_ args: ExportMigrationReportRequest) async throws -> ExportMigrationReportResponse {
    let encoded = try _encode(exportMigrationReportRequest, args)
    guard let raw = try await _rpc.request(69, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(exportMigrationReportResponse, raw)
  }

  public func onExportMigrationReport(_ handler: @escaping (ExportMigrationReportRequest) async throws -> ExportMigrationReportResponse) {
    _handlers["@peartube/export-migration-report"] = handler
  }

  // Request/response — client
  public func getPublisherDeviceStatus(_ args: GetPublisherDeviceStatusRequest) async throws -> GetPublisherDeviceStatusResponse {
    let encoded = try _encode(getPublisherDeviceStatusRequest, args)
    guard let raw = try await _rpc.request(70, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getPublisherDeviceStatusResponse, raw)
  }

  public func onGetPublisherDeviceStatus(_ handler: @escaping (GetPublisherDeviceStatusRequest) async throws -> GetPublisherDeviceStatusResponse) {
    _handlers["@peartube/get-publisher-device-status"] = handler
  }

  // Request/response — client
  public func exportPortableState(_ args: ExportPortableStateRequest) async throws -> ExportPortableStateResponse {
    let encoded = try _encode(exportPortableStateRequest, args)
    guard let raw = try await _rpc.request(71, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(exportPortableStateResponse, raw)
  }

  public func onExportPortableState(_ handler: @escaping (ExportPortableStateRequest) async throws -> ExportPortableStateResponse) {
    _handlers["@peartube/export-portable-state"] = handler
  }

  // Request/response — client
  public func restorePortableState(_ args: RestorePortableStateRequest) async throws -> RestorePortableStateResponse {
    let encoded = try _encode(restorePortableStateRequest, args)
    guard let raw = try await _rpc.request(72, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(restorePortableStateResponse, raw)
  }

  public func onRestorePortableState(_ handler: @escaping (RestorePortableStateRequest) async throws -> RestorePortableStateResponse) {
    _handlers["@peartube/restore-portable-state"] = handler
  }

  // Request/response — client
  public func previewStorageLimit(_ args: PreviewStorageLimitRequest) async throws -> PreviewStorageLimitResponse {
    let encoded = try _encode(previewStorageLimitRequest, args)
    guard let raw = try await _rpc.request(73, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(previewStorageLimitResponse, raw)
  }

  public func onPreviewStorageLimit(_ handler: @escaping (PreviewStorageLimitRequest) async throws -> PreviewStorageLimitResponse) {
    _handlers["@peartube/preview-storage-limit"] = handler
  }

  // Request/response — client
  public func getArchiveOperatorStatus(_ args: GetArchiveOperatorStatusRequest) async throws -> GetArchiveOperatorStatusResponse {
    let encoded = try _encode(getArchiveOperatorStatusRequest, args)
    guard let raw = try await _rpc.request(74, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getArchiveOperatorStatusResponse, raw)
  }

  public func onGetArchiveOperatorStatus(_ handler: @escaping (GetArchiveOperatorStatusRequest) async throws -> GetArchiveOperatorStatusResponse) {
    _handlers["@peartube/get-archive-operator-status"] = handler
  }

  // Request/response — client
  public func getArchiveParticipation(_ args: GetArchiveParticipationRequest) async throws -> GetArchiveParticipationResponse {
    let encoded = try _encode(getArchiveParticipationRequest, args)
    guard let raw = try await _rpc.request(75, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getArchiveParticipationResponse, raw)
  }

  public func onGetArchiveParticipation(_ handler: @escaping (GetArchiveParticipationRequest) async throws -> GetArchiveParticipationResponse) {
    _handlers["@peartube/get-archive-participation"] = handler
  }

  // Request/response — client
  public func setArchiveParticipation(_ args: SetArchiveParticipationRequest) async throws -> SetArchiveParticipationResponse {
    let encoded = try _encode(setArchiveParticipationRequest, args)
    guard let raw = try await _rpc.request(76, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(setArchiveParticipationResponse, raw)
  }

  public func onSetArchiveParticipation(_ handler: @escaping (SetArchiveParticipationRequest) async throws -> SetArchiveParticipationResponse) {
    _handlers["@peartube/set-archive-participation"] = handler
  }

  // Request/response — client
  public func requestArchivePublication(_ args: RequestArchivePublicationRequest) async throws -> RequestArchivePublicationResponse {
    let encoded = try _encode(requestArchivePublicationRequest, args)
    guard let raw = try await _rpc.request(77, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(requestArchivePublicationResponse, raw)
  }

  public func onRequestArchivePublication(_ handler: @escaping (RequestArchivePublicationRequest) async throws -> RequestArchivePublicationResponse) {
    _handlers["@peartube/request-archive-publication"] = handler
  }

  // Request/response — client
  public func getNetworkPolicy(_ args: GetNetworkPolicyRequest) async throws -> GetNetworkPolicyResponse {
    let encoded = try _encode(getNetworkPolicyRequest, args)
    guard let raw = try await _rpc.request(78, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getNetworkPolicyResponse, raw)
  }

  public func onGetNetworkPolicy(_ handler: @escaping (GetNetworkPolicyRequest) async throws -> GetNetworkPolicyResponse) {
    _handlers["@peartube/get-network-policy"] = handler
  }

  // Request/response — client
  public func setNetworkPolicy(_ args: SetNetworkPolicyRequest) async throws -> SetNetworkPolicyResponse {
    let encoded = try _encode(setNetworkPolicyRequest, args)
    guard let raw = try await _rpc.request(79, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(setNetworkPolicyResponse, raw)
  }

  public func onSetNetworkPolicy(_ handler: @escaping (SetNetworkPolicyRequest) async throws -> SetNetworkPolicyResponse) {
    _handlers["@peartube/set-network-policy"] = handler
  }

  // Request/response — client
  public func clearCache(_ args: ClearCacheRequest) async throws -> ClearCacheResponse {
    let encoded = try _encode(clearCacheRequest, args)
    guard let raw = try await _rpc.request(80, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(clearCacheResponse, raw)
  }

  public func onClearCache(_ handler: @escaping (ClearCacheRequest) async throws -> ClearCacheResponse) {
    _handlers["@peartube/clear-cache"] = handler
  }

  // Request/response — client
  public func assessSourceOffload(_ args: AssessSourceOffloadRequest) async throws -> AssessSourceOffloadResponse {
    let encoded = try _encode(assessSourceOffloadRequest, args)
    guard let raw = try await _rpc.request(81, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(assessSourceOffloadResponse, raw)
  }

  public func onAssessSourceOffload(_ handler: @escaping (AssessSourceOffloadRequest) async throws -> AssessSourceOffloadResponse) {
    _handlers["@peartube/assess-source-offload"] = handler
  }

  // Request/response — client
  public func confirmSourceOffload(_ args: ConfirmSourceOffloadRequest) async throws -> ConfirmSourceOffloadResponse {
    let encoded = try _encode(confirmSourceOffloadRequest, args)
    guard let raw = try await _rpc.request(82, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(confirmSourceOffloadResponse, raw)
  }

  public func onConfirmSourceOffload(_ handler: @escaping (ConfirmSourceOffloadRequest) async throws -> ConfirmSourceOffloadResponse) {
    _handlers["@peartube/confirm-source-offload"] = handler
  }

  // Request/response — client
  public func getVideoThumbnail(_ args: GetVideoThumbnailRequest) async throws -> GetVideoThumbnailResponse {
    let encoded = try _encode(getVideoThumbnailRequest, args)
    guard let raw = try await _rpc.request(83, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getVideoThumbnailResponse, raw)
  }

  public func onGetVideoThumbnail(_ handler: @escaping (GetVideoThumbnailRequest) async throws -> GetVideoThumbnailResponse) {
    _handlers["@peartube/get-video-thumbnail"] = handler
  }

  // Request/response — client
  public func getVideoMetadata(_ args: GetVideoMetadataRequest) async throws -> GetVideoMetadataResponse {
    let encoded = try _encode(getVideoMetadataRequest, args)
    guard let raw = try await _rpc.request(84, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getVideoMetadataResponse, raw)
  }

  public func onGetVideoMetadata(_ handler: @escaping (GetVideoMetadataRequest) async throws -> GetVideoMetadataResponse) {
    _handlers["@peartube/get-video-metadata"] = handler
  }

  // Request/response — client
  public func setVideoThumbnail(_ args: SetVideoThumbnailRequest) async throws -> SetVideoThumbnailResponse {
    let encoded = try _encode(setVideoThumbnailRequest, args)
    guard let raw = try await _rpc.request(85, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(setVideoThumbnailResponse, raw)
  }

  public func onSetVideoThumbnail(_ handler: @escaping (SetVideoThumbnailRequest) async throws -> SetVideoThumbnailResponse) {
    _handlers["@peartube/set-video-thumbnail"] = handler
  }

  // Request/response — client
  public func setVideoThumbnailFromFile(_ args: SetVideoThumbnailFromFileRequest) async throws -> SetVideoThumbnailFromFileResponse {
    let encoded = try _encode(setVideoThumbnailFromFileRequest, args)
    guard let raw = try await _rpc.request(86, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(setVideoThumbnailFromFileResponse, raw)
  }

  public func onSetVideoThumbnailFromFile(_ handler: @escaping (SetVideoThumbnailFromFileRequest) async throws -> SetVideoThumbnailFromFileResponse) {
    _handlers["@peartube/set-video-thumbnail-from-file"] = handler
  }

  // Request/response — client
  public func getStatus(_ args: GetStatusRequest) async throws -> GetStatusResponse {
    let encoded = try _encode(getStatusRequest, args)
    guard let raw = try await _rpc.request(87, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getStatusResponse, raw)
  }

  public func onGetStatus(_ handler: @escaping (GetStatusRequest) async throws -> GetStatusResponse) {
    _handlers["@peartube/get-status"] = handler
  }

  // Request/response — client
  public func pickVideoFile(_ args: PickVideoFileRequest) async throws -> PickVideoFileResponse {
    let encoded = try _encode(pickVideoFileRequest, args)
    guard let raw = try await _rpc.request(88, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(pickVideoFileResponse, raw)
  }

  public func onPickVideoFile(_ handler: @escaping (PickVideoFileRequest) async throws -> PickVideoFileResponse) {
    _handlers["@peartube/pick-video-file"] = handler
  }

  // Request/response — client
  public func pickImageFile(_ args: PickImageFileRequest) async throws -> PickImageFileResponse {
    let encoded = try _encode(pickImageFileRequest, args)
    guard let raw = try await _rpc.request(89, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(pickImageFileResponse, raw)
  }

  public func onPickImageFile(_ handler: @escaping (PickImageFileRequest) async throws -> PickImageFileResponse) {
    _handlers["@peartube/pick-image-file"] = handler
  }

  // Request/response — client
  public func getBlobServerPort(_ args: GetBlobServerPortRequest) async throws -> GetBlobServerPortResponse {
    let encoded = try _encode(getBlobServerPortRequest, args)
    guard let raw = try await _rpc.request(90, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getBlobServerPortResponse, raw)
  }

  public func onGetBlobServerPort(_ handler: @escaping (GetBlobServerPortRequest) async throws -> GetBlobServerPortResponse) {
    _handlers["@peartube/get-blob-server-port"] = handler
  }

  // Request/response — client
  public func globalSearchVideos(_ args: GlobalSearchVideosRequest) async throws -> GlobalSearchVideosResponse {
    let encoded = try _encode(globalSearchVideosRequest, args)
    guard let raw = try await _rpc.request(91, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(globalSearchVideosResponse, raw)
  }

  public func onGlobalSearchVideos(_ handler: @escaping (GlobalSearchVideosRequest) async throws -> GlobalSearchVideosResponse) {
    _handlers["@peartube/global-search-videos"] = handler
  }

  // Request/response — client
  public func addComment(_ args: AddCommentRequest) async throws -> AddCommentResponse {
    let encoded = try _encode(addCommentRequest, args)
    guard let raw = try await _rpc.request(92, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(addCommentResponse, raw)
  }

  public func onAddComment(_ handler: @escaping (AddCommentRequest) async throws -> AddCommentResponse) {
    _handlers["@peartube/add-comment"] = handler
  }

  // Request/response — client
  public func listComments(_ args: ListCommentsRequest) async throws -> ListCommentsResponse {
    let encoded = try _encode(listCommentsRequest, args)
    guard let raw = try await _rpc.request(93, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(listCommentsResponse, raw)
  }

  public func onListComments(_ handler: @escaping (ListCommentsRequest) async throws -> ListCommentsResponse) {
    _handlers["@peartube/list-comments"] = handler
  }

  // Request/response — client
  public func hideComment(_ args: HideCommentRequest) async throws -> HideCommentResponse {
    let encoded = try _encode(hideCommentRequest, args)
    guard let raw = try await _rpc.request(94, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(hideCommentResponse, raw)
  }

  public func onHideComment(_ handler: @escaping (HideCommentRequest) async throws -> HideCommentResponse) {
    _handlers["@peartube/hide-comment"] = handler
  }

  // Request/response — client
  public func removeComment(_ args: RemoveCommentRequest) async throws -> RemoveCommentResponse {
    let encoded = try _encode(removeCommentRequest, args)
    guard let raw = try await _rpc.request(95, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(removeCommentResponse, raw)
  }

  public func onRemoveComment(_ handler: @escaping (RemoveCommentRequest) async throws -> RemoveCommentResponse) {
    _handlers["@peartube/remove-comment"] = handler
  }

  // Request/response — client
  public func addReaction(_ args: AddReactionRequest) async throws -> AddReactionResponse {
    let encoded = try _encode(addReactionRequest, args)
    guard let raw = try await _rpc.request(96, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(addReactionResponse, raw)
  }

  public func onAddReaction(_ handler: @escaping (AddReactionRequest) async throws -> AddReactionResponse) {
    _handlers["@peartube/add-reaction"] = handler
  }

  // Request/response — client
  public func removeReaction(_ args: RemoveReactionRequest) async throws -> RemoveReactionResponse {
    let encoded = try _encode(removeReactionRequest, args)
    guard let raw = try await _rpc.request(97, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(removeReactionResponse, raw)
  }

  public func onRemoveReaction(_ handler: @escaping (RemoveReactionRequest) async throws -> RemoveReactionResponse) {
    _handlers["@peartube/remove-reaction"] = handler
  }

  // Request/response — client
  public func getReactions(_ args: GetReactionsRequest) async throws -> GetReactionsResponse {
    let encoded = try _encode(getReactionsRequest, args)
    guard let raw = try await _rpc.request(98, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getReactionsResponse, raw)
  }

  public func onGetReactions(_ handler: @escaping (GetReactionsRequest) async throws -> GetReactionsResponse) {
    _handlers["@peartube/get-reactions"] = handler
  }

  // Send-only — client (fire and forget)
  public func eventReady(_ args: EventReady) throws {
    let encoded = try _encode(eventReady, args)
    _rpc.event(95, data: encoded)
  }

  public func onEventReady(_ handler: @escaping (EventReady) async -> Void) {
    _handlers["@peartube/event-ready"] = handler
  }

  // Send-only — client (fire and forget)
  public func eventError(_ args: EventError) throws {
    let encoded = try _encode(eventError, args)
    _rpc.event(96, data: encoded)
  }

  public func onEventError(_ handler: @escaping (EventError) async -> Void) {
    _handlers["@peartube/event-error"] = handler
  }

  // Send-only — client (fire and forget)
  public func eventUploadProgress(_ args: EventUploadProgress) throws {
    let encoded = try _encode(eventUploadProgress, args)
    _rpc.event(97, data: encoded)
  }

  public func onEventUploadProgress(_ handler: @escaping (EventUploadProgress) async -> Void) {
    _handlers["@peartube/event-upload-progress"] = handler
  }

  // Send-only — client (fire and forget)
  public func eventDownloadProgress(_ args: EventDownloadProgress) throws {
    let encoded = try _encode(eventDownloadProgress, args)
    _rpc.event(98, data: encoded)
  }

  public func onEventDownloadProgress(_ handler: @escaping (EventDownloadProgress) async -> Void) {
    _handlers["@peartube/event-download-progress"] = handler
  }

  // Send-only — client (fire and forget)
  public func eventMediaGraphUpdate(_ args: EventMediaGraphUpdate) throws {
    let encoded = try _encode(eventMediaGraphUpdate, args)
    _rpc.event(99, data: encoded)
  }

  public func onEventMediaGraphUpdate(_ handler: @escaping (EventMediaGraphUpdate) async -> Void) {
    _handlers["@peartube/event-media-graph-update"] = handler
  }

  // Send-only — client (fire and forget)
  public func eventLog(_ args: EventLog) throws {
    let encoded = try _encode(eventLog, args)
    _rpc.event(100, data: encoded)
  }

  public func onEventLog(_ handler: @escaping (EventLog) async -> Void) {
    _handlers["@peartube/event-log"] = handler
  }

  // Send-only — client (fire and forget)
  public func eventVideoStats(_ args: EventVideoStats) throws {
    let encoded = try _encode(eventVideoStats, args)
    _rpc.event(101, data: encoded)
  }

  public func onEventVideoStats(_ handler: @escaping (EventVideoStats) async -> Void) {
    _handlers["@peartube/event-video-stats"] = handler
  }

  // Request/response — client
  public func castAvailable(_ args: CastAvailableRequest) async throws -> CastAvailableResponse {
    let encoded = try _encode(castAvailableRequest, args)
    guard let raw = try await _rpc.request(106, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(castAvailableResponse, raw)
  }

  public func onCastAvailable(_ handler: @escaping (CastAvailableRequest) async throws -> CastAvailableResponse) {
    _handlers["@peartube/cast-available"] = handler
  }

  // Request/response — client
  public func castStartDiscovery(_ args: CastStartDiscoveryRequest) async throws -> CastStartDiscoveryResponse {
    let encoded = try _encode(castStartDiscoveryRequest, args)
    guard let raw = try await _rpc.request(107, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(castStartDiscoveryResponse, raw)
  }

  public func onCastStartDiscovery(_ handler: @escaping (CastStartDiscoveryRequest) async throws -> CastStartDiscoveryResponse) {
    _handlers["@peartube/cast-start-discovery"] = handler
  }

  // Request/response — client
  public func castStopDiscovery(_ args: CastStopDiscoveryRequest) async throws -> CastStopDiscoveryResponse {
    let encoded = try _encode(castStopDiscoveryRequest, args)
    guard let raw = try await _rpc.request(108, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(castStopDiscoveryResponse, raw)
  }

  public func onCastStopDiscovery(_ handler: @escaping (CastStopDiscoveryRequest) async throws -> CastStopDiscoveryResponse) {
    _handlers["@peartube/cast-stop-discovery"] = handler
  }

  // Request/response — client
  public func castGetDevices(_ args: CastGetDevicesRequest) async throws -> CastGetDevicesResponse {
    let encoded = try _encode(castGetDevicesRequest, args)
    guard let raw = try await _rpc.request(109, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(castGetDevicesResponse, raw)
  }

  public func onCastGetDevices(_ handler: @escaping (CastGetDevicesRequest) async throws -> CastGetDevicesResponse) {
    _handlers["@peartube/cast-get-devices"] = handler
  }

  // Request/response — client
  public func castAddManualDevice(_ args: CastAddManualDeviceRequest) async throws -> CastAddManualDeviceResponse {
    let encoded = try _encode(castAddManualDeviceRequest, args)
    guard let raw = try await _rpc.request(110, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(castAddManualDeviceResponse, raw)
  }

  public func onCastAddManualDevice(_ handler: @escaping (CastAddManualDeviceRequest) async throws -> CastAddManualDeviceResponse) {
    _handlers["@peartube/cast-add-manual-device"] = handler
  }

  // Request/response — client
  public func castConnect(_ args: CastConnectRequest) async throws -> CastConnectResponse {
    let encoded = try _encode(castConnectRequest, args)
    guard let raw = try await _rpc.request(111, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(castConnectResponse, raw)
  }

  public func onCastConnect(_ handler: @escaping (CastConnectRequest) async throws -> CastConnectResponse) {
    _handlers["@peartube/cast-connect"] = handler
  }

  // Request/response — client
  public func castDisconnect(_ args: CastDisconnectRequest) async throws -> CastDisconnectResponse {
    let encoded = try _encode(castDisconnectRequest, args)
    guard let raw = try await _rpc.request(112, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(castDisconnectResponse, raw)
  }

  public func onCastDisconnect(_ handler: @escaping (CastDisconnectRequest) async throws -> CastDisconnectResponse) {
    _handlers["@peartube/cast-disconnect"] = handler
  }

  // Request/response — client
  public func castPlay(_ args: CastPlayRequest) async throws -> CastPlayResponse {
    let encoded = try _encode(castPlayRequest, args)
    guard let raw = try await _rpc.request(113, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(castPlayResponse, raw)
  }

  public func onCastPlay(_ handler: @escaping (CastPlayRequest) async throws -> CastPlayResponse) {
    _handlers["@peartube/cast-play"] = handler
  }

  // Request/response — client
  public func castPause(_ args: CastPauseRequest) async throws -> CastPauseResponse {
    let encoded = try _encode(castPauseRequest, args)
    guard let raw = try await _rpc.request(114, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(castPauseResponse, raw)
  }

  public func onCastPause(_ handler: @escaping (CastPauseRequest) async throws -> CastPauseResponse) {
    _handlers["@peartube/cast-pause"] = handler
  }

  // Request/response — client
  public func castResume(_ args: CastResumeRequest) async throws -> CastResumeResponse {
    let encoded = try _encode(castResumeRequest, args)
    guard let raw = try await _rpc.request(115, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(castResumeResponse, raw)
  }

  public func onCastResume(_ handler: @escaping (CastResumeRequest) async throws -> CastResumeResponse) {
    _handlers["@peartube/cast-resume"] = handler
  }

  // Request/response — client
  public func castStop(_ args: CastStopRequest) async throws -> CastStopResponse {
    let encoded = try _encode(castStopRequest, args)
    guard let raw = try await _rpc.request(116, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(castStopResponse, raw)
  }

  public func onCastStop(_ handler: @escaping (CastStopRequest) async throws -> CastStopResponse) {
    _handlers["@peartube/cast-stop"] = handler
  }

  // Request/response — client
  public func castSeek(_ args: CastSeekRequest) async throws -> CastSeekResponse {
    let encoded = try _encode(castSeekRequest, args)
    guard let raw = try await _rpc.request(117, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(castSeekResponse, raw)
  }

  public func onCastSeek(_ handler: @escaping (CastSeekRequest) async throws -> CastSeekResponse) {
    _handlers["@peartube/cast-seek"] = handler
  }

  // Request/response — client
  public func castSetVolume(_ args: CastSetVolumeRequest) async throws -> CastSetVolumeResponse {
    let encoded = try _encode(castSetVolumeRequest, args)
    guard let raw = try await _rpc.request(118, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(castSetVolumeResponse, raw)
  }

  public func onCastSetVolume(_ handler: @escaping (CastSetVolumeRequest) async throws -> CastSetVolumeResponse) {
    _handlers["@peartube/cast-set-volume"] = handler
  }

  // Request/response — client
  public func castGetState(_ args: CastGetStateRequest) async throws -> CastGetStateResponse {
    let encoded = try _encode(castGetStateRequest, args)
    guard let raw = try await _rpc.request(119, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(castGetStateResponse, raw)
  }

  public func onCastGetState(_ handler: @escaping (CastGetStateRequest) async throws -> CastGetStateResponse) {
    _handlers["@peartube/cast-get-state"] = handler
  }

  // Request/response — client
  public func castIsConnected(_ args: CastIsConnectedRequest) async throws -> CastIsConnectedResponse {
    let encoded = try _encode(castIsConnectedRequest, args)
    guard let raw = try await _rpc.request(120, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(castIsConnectedResponse, raw)
  }

  public func onCastIsConnected(_ handler: @escaping (CastIsConnectedRequest) async throws -> CastIsConnectedResponse) {
    _handlers["@peartube/cast-is-connected"] = handler
  }

  // Send-only — client (fire and forget)
  public func eventCastDeviceFound(_ args: EventCastDeviceFound) throws {
    let encoded = try _encode(eventCastDeviceFound, args)
    _rpc.event(117, data: encoded)
  }

  public func onEventCastDeviceFound(_ handler: @escaping (EventCastDeviceFound) async -> Void) {
    _handlers["@peartube/event-cast-device-found"] = handler
  }

  // Send-only — client (fire and forget)
  public func eventCastDeviceLost(_ args: EventCastDeviceLost) throws {
    let encoded = try _encode(eventCastDeviceLost, args)
    _rpc.event(118, data: encoded)
  }

  public func onEventCastDeviceLost(_ handler: @escaping (EventCastDeviceLost) async -> Void) {
    _handlers["@peartube/event-cast-device-lost"] = handler
  }

  // Send-only — client (fire and forget)
  public func eventCastPlaybackState(_ args: EventCastPlaybackState) throws {
    let encoded = try _encode(eventCastPlaybackState, args)
    _rpc.event(119, data: encoded)
  }

  public func onEventCastPlaybackState(_ handler: @escaping (EventCastPlaybackState) async -> Void) {
    _handlers["@peartube/event-cast-playback-state"] = handler
  }

  // Send-only — client (fire and forget)
  public func eventCastTimeUpdate(_ args: EventCastTimeUpdate) throws {
    let encoded = try _encode(eventCastTimeUpdate, args)
    _rpc.event(120, data: encoded)
  }

  public func onEventCastTimeUpdate(_ handler: @escaping (EventCastTimeUpdate) async -> Void) {
    _handlers["@peartube/event-cast-time-update"] = handler
  }

  // Request/response — client
  public func searchVideos(_ args: SearchVideosRequest) async throws -> SearchVideosResponse {
    let encoded = try _encode(searchVideosRequest, args)
    guard let raw = try await _rpc.request(125, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(searchVideosResponse, raw)
  }

  public func onSearchVideos(_ handler: @escaping (SearchVideosRequest) async throws -> SearchVideosResponse) {
    _handlers["@peartube/search-videos"] = handler
  }

  // Request/response — client
  public func indexVideoVectors(_ args: IndexVideoVectorsRequest) async throws -> IndexVideoVectorsResponse {
    let encoded = try _encode(indexVideoVectorsRequest, args)
    guard let raw = try await _rpc.request(126, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(indexVideoVectorsResponse, raw)
  }

  public func onIndexVideoVectors(_ handler: @escaping (IndexVideoVectorsRequest) async throws -> IndexVideoVectorsResponse) {
    _handlers["@peartube/index-video-vectors"] = handler
  }

  // Request/response — client
  public func updateVideoMetadata(_ args: UpdateVideoMetadataRequest) async throws -> UpdateVideoMetadataResponse {
    let encoded = try _encode(updateVideoMetadataRequest, args)
    guard let raw = try await _rpc.request(127, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(updateVideoMetadataResponse, raw)
  }

  public func onUpdateVideoMetadata(_ handler: @escaping (UpdateVideoMetadataRequest) async throws -> UpdateVideoMetadataResponse) {
    _handlers["@peartube/update-video-metadata"] = handler
  }

  // Request/response — client
  public func getMediaCatalog(_ args: MediaPageRequest) async throws -> GetMediaCatalogResponse {
    let encoded = try _encode(mediaPageRequest, args)
    guard let raw = try await _rpc.request(128, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getMediaCatalogResponse, raw)
  }

  public func onGetMediaCatalog(_ handler: @escaping (MediaPageRequest) async throws -> GetMediaCatalogResponse) {
    _handlers["@peartube/get-media-catalog"] = handler
  }

  // Request/response — client
  public func getMediaEntity(_ args: GetMediaEntityRequest) async throws -> GetMediaEntityResponse {
    let encoded = try _encode(getMediaEntityRequest, args)
    guard let raw = try await _rpc.request(129, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getMediaEntityResponse, raw)
  }

  public func onGetMediaEntity(_ handler: @escaping (GetMediaEntityRequest) async throws -> GetMediaEntityResponse) {
    _handlers["@peartube/get-media-entity"] = handler
  }

  // Request/response — client
  public func getMediaCollection(_ args: GetMediaCollectionRequest) async throws -> GetMediaCollectionResponse {
    let encoded = try _encode(getMediaCollectionRequest, args)
    guard let raw = try await _rpc.request(130, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getMediaCollectionResponse, raw)
  }

  public func onGetMediaCollection(_ handler: @escaping (GetMediaCollectionRequest) async throws -> GetMediaCollectionResponse) {
    _handlers["@peartube/get-media-collection"] = handler
  }

  // Request/response — client
  public func getMediaCollectionItems(_ args: GetMediaCollectionItemsRequest) async throws -> GetMediaCollectionItemsResponse {
    let encoded = try _encode(getMediaCollectionItemsRequest, args)
    guard let raw = try await _rpc.request(131, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getMediaCollectionItemsResponse, raw)
  }

  public func onGetMediaCollectionItems(_ handler: @escaping (GetMediaCollectionItemsRequest) async throws -> GetMediaCollectionItemsResponse) {
    _handlers["@peartube/get-media-collection-items"] = handler
  }

  // Request/response — client
  public func getMediaAgent(_ args: GetMediaAgentRequest) async throws -> GetMediaAgentResponse {
    let encoded = try _encode(getMediaAgentRequest, args)
    guard let raw = try await _rpc.request(132, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getMediaAgentResponse, raw)
  }

  public func onGetMediaAgent(_ handler: @escaping (GetMediaAgentRequest) async throws -> GetMediaAgentResponse) {
    _handlers["@peartube/get-media-agent"] = handler
  }

  // Request/response — client
  public func getAgentContributions(_ args: GetAgentContributionsRequest) async throws -> GetAgentContributionsResponse {
    let encoded = try _encode(getAgentContributionsRequest, args)
    guard let raw = try await _rpc.request(133, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getAgentContributionsResponse, raw)
  }

  public func onGetAgentContributions(_ handler: @escaping (GetAgentContributionsRequest) async throws -> GetAgentContributionsResponse) {
    _handlers["@peartube/get-agent-contributions"] = handler
  }

  // Request/response — client
  public func getPublicationSources(_ args: GetPublicationSourcesRequest) async throws -> GetPublicationSourcesResponse {
    let encoded = try _encode(getPublicationSourcesRequest, args)
    guard let raw = try await _rpc.request(134, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getPublicationSourcesResponse, raw)
  }

  public func onGetPublicationSources(_ handler: @escaping (GetPublicationSourcesRequest) async throws -> GetPublicationSourcesResponse) {
    _handlers["@peartube/get-publication-sources"] = handler
  }

  // Request/response — client
  public func getClaimProvenance(_ args: GetClaimProvenanceRequest) async throws -> GetClaimProvenanceResponse {
    let encoded = try _encode(getClaimProvenanceRequest, args)
    guard let raw = try await _rpc.request(136, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(getClaimProvenanceResponse, raw)
  }

  public func onGetClaimProvenance(_ handler: @escaping (GetClaimProvenanceRequest) async throws -> GetClaimProvenanceResponse) {
    _handlers["@peartube/get-claim-provenance"] = handler
  }

  // Request/response — client
  public func setSourcePreference(_ args: SetSourcePreferenceRequest) async throws -> SetSourcePreferenceResponse {
    let encoded = try _encode(setSourcePreferenceRequest, args)
    guard let raw = try await _rpc.request(137, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(setSourcePreferenceResponse, raw)
  }

  public func onSetSourcePreference(_ handler: @escaping (SetSourcePreferenceRequest) async throws -> SetSourcePreferenceResponse) {
    _handlers["@peartube/set-source-preference"] = handler
  }

  // Request/response — client
  public func prepareMediaPlayback(_ args: PrepareMediaPlaybackRequest) async throws -> PrepareMediaPlaybackResponse {
    let encoded = try _encode(prepareMediaPlaybackRequest, args)
    guard let raw = try await _rpc.request(138, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(prepareMediaPlaybackResponse, raw)
  }

  public func onPrepareMediaPlayback(_ handler: @escaping (PrepareMediaPlaybackRequest) async throws -> PrepareMediaPlaybackResponse) {
    _handlers["@peartube/prepare-media-playback"] = handler
  }

  // Request/response — client
  public func desktopBootstrap(_ args: DesktopBootstrapRequest) async throws -> DesktopBootstrapResponse {
    let encoded = try _encode(desktopBootstrapRequest, args)
    guard let raw = try await _rpc.request(139, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(desktopBootstrapResponse, raw)
  }

  public func onDesktopBootstrap(_ handler: @escaping (DesktopBootstrapRequest) async throws -> DesktopBootstrapResponse) {
    _handlers["@peartube/desktop-bootstrap"] = handler
  }

  // Request/response — client
  public func desktopShutdown(_ args: DesktopShutdownRequest) async throws -> DesktopShutdownResponse {
    let encoded = try _encode(desktopShutdownRequest, args)
    guard let raw = try await _rpc.request(140, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(desktopShutdownResponse, raw)
  }

  public func onDesktopShutdown(_ handler: @escaping (DesktopShutdownRequest) async throws -> DesktopShutdownResponse) {
    _handlers["@peartube/desktop-shutdown"] = handler
  }

  // Request/response — client
  public func desktopRefreshBrowse(_ args: DesktopRefreshBrowseRequest) async throws -> DesktopRefreshBrowseResponse {
    let encoded = try _encode(desktopRefreshBrowseRequest, args)
    guard let raw = try await _rpc.request(141, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(desktopRefreshBrowseResponse, raw)
  }

  public func onDesktopRefreshBrowse(_ handler: @escaping (DesktopRefreshBrowseRequest) async throws -> DesktopRefreshBrowseResponse) {
    _handlers["@peartube/desktop-refresh-browse"] = handler
  }

  // Request/response — client
  public func ffmpegDecodeAvailable(_ args: FfmpegDecodeAvailableRequest) async throws -> FfmpegDecodeAvailableResponse {
    let encoded = try _encode(ffmpegDecodeAvailableRequest, args)
    guard let raw = try await _rpc.request(142, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(ffmpegDecodeAvailableResponse, raw)
  }

  public func onFfmpegDecodeAvailable(_ handler: @escaping (FfmpegDecodeAvailableRequest) async throws -> FfmpegDecodeAvailableResponse) {
    _handlers["@peartube/ffmpeg-decode-available"] = handler
  }

  // Request/response — client
  public func updateChannelAvatar(_ args: UpdateChannelAvatarRequest) async throws -> UpdateChannelAvatarResponse {
    let encoded = try _encode(updateChannelAvatarRequest, args)
    guard let raw = try await _rpc.request(143, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(updateChannelAvatarResponse, raw)
  }

  public func onUpdateChannelAvatar(_ handler: @escaping (UpdateChannelAvatarRequest) async throws -> UpdateChannelAvatarResponse) {
    _handlers["@peartube/update-channel-avatar"] = handler
  }

  // Request/response — client
  public func transcodeStart(_ args: TranscodeStartRequest) async throws -> TranscodeStartResponse {
    let encoded = try _encode(transcodeStartRequest, args)
    guard let raw = try await _rpc.request(144, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(transcodeStartResponse, raw)
  }

  public func onTranscodeStart(_ handler: @escaping (TranscodeStartRequest) async throws -> TranscodeStartResponse) {
    _handlers["@peartube/transcode-start"] = handler
  }

  // Request/response — client
  public func transcodeStop(_ args: TranscodeStopRequest) async throws -> TranscodeStopResponse {
    let encoded = try _encode(transcodeStopRequest, args)
    guard let raw = try await _rpc.request(145, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(transcodeStopResponse, raw)
  }

  public func onTranscodeStop(_ handler: @escaping (TranscodeStopRequest) async throws -> TranscodeStopResponse) {
    _handlers["@peartube/transcode-stop"] = handler
  }

  // Request/response — client
  public func transcodeStatus(_ args: TranscodeStatusRequest) async throws -> TranscodeStatusResponse {
    let encoded = try _encode(transcodeStatusRequest, args)
    guard let raw = try await _rpc.request(146, data: encoded) else {
      throw RPCRemoteError(message: "Missing response", code: "MISSING_RESPONSE")
    }
    return try _decode(transcodeStatusResponse, raw)
  }

  public func onTranscodeStatus(_ handler: @escaping (TranscodeStatusRequest) async throws -> TranscodeStatusResponse) {
    _handlers["@peartube/transcode-status"] = handler
  }

  // Send-only — client (fire and forget)
  public func eventTranscodeProgress(_ args: EventTranscodeProgress) throws {
    let encoded = try _encode(eventTranscodeProgress, args)
    _rpc.event(145, data: encoded)
  }

  public func onEventTranscodeProgress(_ handler: @escaping (EventTranscodeProgress) async -> Void) {
    _handlers["@peartube/event-transcode-progress"] = handler
  }

  private func _dispatchRequest(_ req: IncomingRequest) async {
    switch req.command {
    case 0:   // @peartube/create-identity
      guard let handler = _handlers["@peartube/create-identity"] as? (CreateIdentityRequest) async throws -> CreateIdentityResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(createIdentityRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(createIdentityResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 1:   // @peartube/get-identity
      guard let handler = _handlers["@peartube/get-identity"] as? (GetIdentityRequest) async throws -> GetIdentityResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getIdentityRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getIdentityResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 2:   // @peartube/get-identities
      guard let handler = _handlers["@peartube/get-identities"] as? (GetIdentitiesRequest) async throws -> GetIdentitiesResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getIdentitiesRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getIdentitiesResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 3:   // @peartube/set-active-identity
      guard let handler = _handlers["@peartube/set-active-identity"] as? (SetActiveIdentityRequest) async throws -> SetActiveIdentityResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(setActiveIdentityRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(setActiveIdentityResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 4:   // @peartube/recover-identity
      guard let handler = _handlers["@peartube/recover-identity"] as? (RecoverIdentityRequest) async throws -> RecoverIdentityResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(recoverIdentityRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(recoverIdentityResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 5:   // @peartube/bootstrap-device
      guard let handler = _handlers["@peartube/bootstrap-device"] as? (BootstrapDeviceRequest) async throws -> BootstrapDeviceResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(bootstrapDeviceRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(bootstrapDeviceResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 6:   // @peartube/attest-device
      guard let handler = _handlers["@peartube/attest-device"] as? (AttestDeviceRequest) async throws -> AttestDeviceResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(attestDeviceRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(attestDeviceResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 7:   // @peartube/provision-publisher-catalog
      guard let handler = _handlers["@peartube/provision-publisher-catalog"] as? (ProvisionPublisherCatalogRequest) async throws -> ProvisionPublisherCatalogResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(provisionPublisherCatalogRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(provisionPublisherCatalogResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 8:   // @peartube/verify-attestation
      guard let handler = _handlers["@peartube/verify-attestation"] as? (VerifyAttestationRequest) async throws -> VerifyAttestationResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(verifyAttestationRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(verifyAttestationResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 9:   // @peartube/prepare-publisher-root-operation
      guard let handler = _handlers["@peartube/prepare-publisher-root-operation"] as? (PreparePublisherRootOperationRequest) async throws -> PreparePublisherRootOperationResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(preparePublisherRootOperationRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(preparePublisherRootOperationResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 10:   // @peartube/submit-publisher-root-operation
      guard let handler = _handlers["@peartube/submit-publisher-root-operation"] as? (SubmitPublisherRootOperationRequest) async throws -> SubmitPublisherRootOperationResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(submitPublisherRootOperationRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(submitPublisherRootOperationResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 11:   // @peartube/get-channel
      guard let handler = _handlers["@peartube/get-channel"] as? (GetChannelRequest) async throws -> GetChannelResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getChannelRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getChannelResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 12:   // @peartube/update-channel
      guard let handler = _handlers["@peartube/update-channel"] as? (UpdateChannelRequest) async throws -> UpdateChannelResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(updateChannelRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(updateChannelResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 13:   // @peartube/get-content-catalog
      guard let handler = _handlers["@peartube/get-content-catalog"] as? (GetContentCatalogRequest) async throws -> GetContentCatalogResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getContentCatalogRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getContentCatalogResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 14:   // @peartube/get-content-items
      guard let handler = _handlers["@peartube/get-content-items"] as? (GetContentItemsRequest) async throws -> GetContentItemsResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getContentItemsRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getContentItemsResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 15:   // @peartube/list-videos
      guard let handler = _handlers["@peartube/list-videos"] as? (ListVideosRequest) async throws -> ListVideosResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(listVideosRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(listVideosResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 16:   // @peartube/get-video-url
      guard let handler = _handlers["@peartube/get-video-url"] as? (GetVideoUrlRequest) async throws -> GetVideoUrlResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getVideoUrlRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getVideoUrlResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 17:   // @peartube/prepare-playback
      guard let handler = _handlers["@peartube/prepare-playback"] as? (PreparePlaybackRequest) async throws -> PreparePlaybackResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(preparePlaybackRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(preparePlaybackResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 18:   // @peartube/web-prepare-playback
      guard let handler = _handlers["@peartube/web-prepare-playback"] as? (PreparePlaybackRequest) async throws -> WebPreparePlaybackResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(preparePlaybackRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(webPreparePlaybackResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 19:   // @peartube/get-video-data
      guard let handler = _handlers["@peartube/get-video-data"] as? (GetVideoDataRequest) async throws -> GetVideoDataResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getVideoDataRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getVideoDataResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 20:   // @peartube/upload-video
      guard let handler = _handlers["@peartube/upload-video"] as? (UploadVideoRequest) async throws -> UploadVideoResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(uploadVideoRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(uploadVideoResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 21:   // @peartube/download-video
      guard let handler = _handlers["@peartube/download-video"] as? (DownloadVideoRequest) async throws -> DownloadVideoResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(downloadVideoRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(downloadVideoResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 22:   // @peartube/delete-video
      guard let handler = _handlers["@peartube/delete-video"] as? (DeleteVideoRequest) async throws -> DeleteVideoResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(deleteVideoRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(deleteVideoResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 23:   // @peartube/start-livestream
      guard let handler = _handlers["@peartube/start-livestream"] as? (StartLivestreamRequest) async throws -> StartLivestreamResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(startLivestreamRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(startLivestreamResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 24:   // @peartube/stop-livestream
      guard let handler = _handlers["@peartube/stop-livestream"] as? (StopLivestreamRequest) async throws -> StopLivestreamResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(stopLivestreamRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(stopLivestreamResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 25:   // @peartube/get-livestream-status
      guard let handler = _handlers["@peartube/get-livestream-status"] as? (GetLivestreamStatusRequest) async throws -> GetLivestreamStatusResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getLivestreamStatusRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getLivestreamStatusResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 26:   // @peartube/prepare-live-playback
      guard let handler = _handlers["@peartube/prepare-live-playback"] as? (PrepareLivePlaybackRequest) async throws -> PrepareLivePlaybackResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(prepareLivePlaybackRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(prepareLivePlaybackResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 27:   // @peartube/subscribe-channel
      guard let handler = _handlers["@peartube/subscribe-channel"] as? (SubscribeChannelRequest) async throws -> SubscribeChannelResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(subscribeChannelRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(subscribeChannelResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 28:   // @peartube/unsubscribe-channel
      guard let handler = _handlers["@peartube/unsubscribe-channel"] as? (UnsubscribeChannelRequest) async throws -> UnsubscribeChannelResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(unsubscribeChannelRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(unsubscribeChannelResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 29:   // @peartube/get-subscriptions
      guard let handler = _handlers["@peartube/get-subscriptions"] as? (GetSubscriptionsRequest) async throws -> GetSubscriptionsResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getSubscriptionsRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getSubscriptionsResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 30:   // @peartube/get-playlists
      guard let handler = _handlers["@peartube/get-playlists"] as? (GetPlaylistsRequest) async throws -> GetPlaylistsResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getPlaylistsRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getPlaylistsResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 31:   // @peartube/get-playlist-items
      guard let handler = _handlers["@peartube/get-playlist-items"] as? (GetPlaylistItemsRequest) async throws -> GetPlaylistItemsResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getPlaylistItemsRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getPlaylistItemsResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 32:   // @peartube/create-playlist
      guard let handler = _handlers["@peartube/create-playlist"] as? (CreatePlaylistRequest) async throws -> CreatePlaylistResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(createPlaylistRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(createPlaylistResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 33:   // @peartube/update-playlist
      guard let handler = _handlers["@peartube/update-playlist"] as? (UpdatePlaylistRequest) async throws -> UpdatePlaylistResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(updatePlaylistRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(updatePlaylistResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 34:   // @peartube/delete-playlist
      guard let handler = _handlers["@peartube/delete-playlist"] as? (DeletePlaylistRequest) async throws -> DeletePlaylistResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(deletePlaylistRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(deletePlaylistResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 35:   // @peartube/add-to-playlist
      guard let handler = _handlers["@peartube/add-to-playlist"] as? (AddToPlaylistRequest) async throws -> AddToPlaylistResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(addToPlaylistRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(addToPlaylistResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 36:   // @peartube/remove-from-playlist
      guard let handler = _handlers["@peartube/remove-from-playlist"] as? (RemoveFromPlaylistRequest) async throws -> RemoveFromPlaylistResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(removeFromPlaylistRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(removeFromPlaylistResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 37:   // @peartube/log-watch-history
      guard let handler = _handlers["@peartube/log-watch-history"] as? (LogWatchHistoryRequest) async throws -> LogWatchHistoryResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(logWatchHistoryRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(logWatchHistoryResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 38:   // @peartube/get-watch-history
      guard let handler = _handlers["@peartube/get-watch-history"] as? (GetWatchHistoryRequest) async throws -> GetWatchHistoryResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getWatchHistoryRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getWatchHistoryResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 39:   // @peartube/get-resume-position
      guard let handler = _handlers["@peartube/get-resume-position"] as? (GetResumePositionRequest) async throws -> GetResumePositionResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getResumePositionRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getResumePositionResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 40:   // @peartube/list-resume-positions
      guard let handler = _handlers["@peartube/list-resume-positions"] as? (ListResumePositionsRequest) async throws -> ListResumePositionsResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(listResumePositionsRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(listResumePositionsResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 41:   // @peartube/set-personal-setting
      guard let handler = _handlers["@peartube/set-personal-setting"] as? (SetPersonalSettingRequest) async throws -> SetPersonalSettingResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(setPersonalSettingRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(setPersonalSettingResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 42:   // @peartube/get-personal-settings
      guard let handler = _handlers["@peartube/get-personal-settings"] as? (GetPersonalSettingsRequest) async throws -> GetPersonalSettingsResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getPersonalSettingsRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getPersonalSettingsResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 43:   // @peartube/provision-personal-encryption
      guard let handler = _handlers["@peartube/provision-personal-encryption"] as? (ProvisionPersonalEncryptionRequest) async throws -> ProvisionPersonalEncryptionResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(provisionPersonalEncryptionRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(provisionPersonalEncryptionResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 48:   // @peartube/join-channel
      guard let handler = _handlers["@peartube/join-channel"] as? (JoinChannelRequest) async throws -> JoinChannelResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(joinChannelRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(joinChannelResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 49:   // @peartube/hide-channel
      guard let handler = _handlers["@peartube/hide-channel"] as? (HideChannelRequest) async throws -> HideChannelResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(hideChannelRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(hideChannelResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 50:   // @peartube/get-channel-meta
      guard let handler = _handlers["@peartube/get-channel-meta"] as? (GetChannelMetaRequest) async throws -> GetChannelMetaResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getChannelMetaRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getChannelMetaResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 51:   // @peartube/get-swarm-status
      guard let handler = _handlers["@peartube/get-swarm-status"] as? (GetSwarmStatusRequest) async throws -> GetSwarmStatusResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getSwarmStatusRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getSwarmStatusResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 52:   // @peartube/create-device-invite
      guard let handler = _handlers["@peartube/create-device-invite"] as? (CreateDeviceInviteRequest) async throws -> CreateDeviceInviteResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(createDeviceInviteRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(createDeviceInviteResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 53:   // @peartube/pair-device
      guard let handler = _handlers["@peartube/pair-device"] as? (PairDeviceRequest) async throws -> PairDeviceResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(pairDeviceRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(pairDeviceResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 54:   // @peartube/list-devices
      guard let handler = _handlers["@peartube/list-devices"] as? (ListDevicesRequest) async throws -> ListDevicesResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(listDevicesRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(listDevicesResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 55:   // @peartube/retry-sync-channel
      guard let handler = _handlers["@peartube/retry-sync-channel"] as? (RetrySyncChannelRequest) async throws -> RetrySyncChannelResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(retrySyncChannelRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(retrySyncChannelResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 56:   // @peartube/prefetch-video
      guard let handler = _handlers["@peartube/prefetch-video"] as? (PrefetchVideoRequest) async throws -> PrefetchVideoResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(prefetchVideoRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(prefetchVideoResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 57:   // @peartube/get-video-stats
      guard let handler = _handlers["@peartube/get-video-stats"] as? (GetVideoStatsRequest) async throws -> GetVideoStatsResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getVideoStatsRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getVideoStatsResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 58:   // @peartube/get-seeding-status
      guard let handler = _handlers["@peartube/get-seeding-status"] as? (GetSeedingStatusRequest) async throws -> GetSeedingStatusResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getSeedingStatusRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getSeedingStatusResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 59:   // @peartube/set-seeding-config
      guard let handler = _handlers["@peartube/set-seeding-config"] as? (SetSeedingConfigRequest) async throws -> SetSeedingConfigResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(setSeedingConfigRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(setSeedingConfigResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 60:   // @peartube/get-transcode-settings
      guard let handler = _handlers["@peartube/get-transcode-settings"] as? (GetTranscodeSettingsRequest) async throws -> GetTranscodeSettingsResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getTranscodeSettingsRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getTranscodeSettingsResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 61:   // @peartube/set-transcode-settings
      guard let handler = _handlers["@peartube/set-transcode-settings"] as? (SetTranscodeSettingsRequest) async throws -> SetTranscodeSettingsResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(setTranscodeSettingsRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(setTranscodeSettingsResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 62:   // @peartube/pin-channel
      guard let handler = _handlers["@peartube/pin-channel"] as? (PinChannelRequest) async throws -> PinChannelResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(pinChannelRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(pinChannelResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 63:   // @peartube/unpin-channel
      guard let handler = _handlers["@peartube/unpin-channel"] as? (UnpinChannelRequest) async throws -> UnpinChannelResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(unpinChannelRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(unpinChannelResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 64:   // @peartube/get-pinned-channels
      guard let handler = _handlers["@peartube/get-pinned-channels"] as? (GetPinnedChannelsRequest) async throws -> GetPinnedChannelsResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getPinnedChannelsRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getPinnedChannelsResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 65:   // @peartube/get-storage-stats
      guard let handler = _handlers["@peartube/get-storage-stats"] as? (GetStorageStatsRequest) async throws -> GetStorageStatsResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getStorageStatsRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getStorageStatsResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 66:   // @peartube/set-storage-limit
      guard let handler = _handlers["@peartube/set-storage-limit"] as? (SetStorageLimitRequest) async throws -> SetStorageLimitResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(setStorageLimitRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(setStorageLimitResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 67:   // @peartube/get-migration-status
      guard let handler = _handlers["@peartube/get-migration-status"] as? (GetMigrationStatusRequest) async throws -> GetMigrationStatusResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getMigrationStatusRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getMigrationStatusResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 68:   // @peartube/retry-migration
      guard let handler = _handlers["@peartube/retry-migration"] as? (RetryMigrationRequest) async throws -> RetryMigrationResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(retryMigrationRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(retryMigrationResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 69:   // @peartube/export-migration-report
      guard let handler = _handlers["@peartube/export-migration-report"] as? (ExportMigrationReportRequest) async throws -> ExportMigrationReportResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(exportMigrationReportRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(exportMigrationReportResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 70:   // @peartube/get-publisher-device-status
      guard let handler = _handlers["@peartube/get-publisher-device-status"] as? (GetPublisherDeviceStatusRequest) async throws -> GetPublisherDeviceStatusResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getPublisherDeviceStatusRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getPublisherDeviceStatusResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 71:   // @peartube/export-portable-state
      guard let handler = _handlers["@peartube/export-portable-state"] as? (ExportPortableStateRequest) async throws -> ExportPortableStateResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(exportPortableStateRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(exportPortableStateResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 72:   // @peartube/restore-portable-state
      guard let handler = _handlers["@peartube/restore-portable-state"] as? (RestorePortableStateRequest) async throws -> RestorePortableStateResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(restorePortableStateRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(restorePortableStateResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 73:   // @peartube/preview-storage-limit
      guard let handler = _handlers["@peartube/preview-storage-limit"] as? (PreviewStorageLimitRequest) async throws -> PreviewStorageLimitResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(previewStorageLimitRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(previewStorageLimitResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 74:   // @peartube/get-archive-operator-status
      guard let handler = _handlers["@peartube/get-archive-operator-status"] as? (GetArchiveOperatorStatusRequest) async throws -> GetArchiveOperatorStatusResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getArchiveOperatorStatusRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getArchiveOperatorStatusResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 75:   // @peartube/get-archive-participation
      guard let handler = _handlers["@peartube/get-archive-participation"] as? (GetArchiveParticipationRequest) async throws -> GetArchiveParticipationResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getArchiveParticipationRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getArchiveParticipationResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 76:   // @peartube/set-archive-participation
      guard let handler = _handlers["@peartube/set-archive-participation"] as? (SetArchiveParticipationRequest) async throws -> SetArchiveParticipationResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(setArchiveParticipationRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(setArchiveParticipationResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 77:   // @peartube/request-archive-publication
      guard let handler = _handlers["@peartube/request-archive-publication"] as? (RequestArchivePublicationRequest) async throws -> RequestArchivePublicationResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(requestArchivePublicationRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(requestArchivePublicationResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 78:   // @peartube/get-network-policy
      guard let handler = _handlers["@peartube/get-network-policy"] as? (GetNetworkPolicyRequest) async throws -> GetNetworkPolicyResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getNetworkPolicyRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getNetworkPolicyResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 79:   // @peartube/set-network-policy
      guard let handler = _handlers["@peartube/set-network-policy"] as? (SetNetworkPolicyRequest) async throws -> SetNetworkPolicyResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(setNetworkPolicyRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(setNetworkPolicyResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 80:   // @peartube/clear-cache
      guard let handler = _handlers["@peartube/clear-cache"] as? (ClearCacheRequest) async throws -> ClearCacheResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(clearCacheRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(clearCacheResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 81:   // @peartube/assess-source-offload
      guard let handler = _handlers["@peartube/assess-source-offload"] as? (AssessSourceOffloadRequest) async throws -> AssessSourceOffloadResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(assessSourceOffloadRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(assessSourceOffloadResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 82:   // @peartube/confirm-source-offload
      guard let handler = _handlers["@peartube/confirm-source-offload"] as? (ConfirmSourceOffloadRequest) async throws -> ConfirmSourceOffloadResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(confirmSourceOffloadRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(confirmSourceOffloadResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 83:   // @peartube/get-video-thumbnail
      guard let handler = _handlers["@peartube/get-video-thumbnail"] as? (GetVideoThumbnailRequest) async throws -> GetVideoThumbnailResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getVideoThumbnailRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getVideoThumbnailResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 84:   // @peartube/get-video-metadata
      guard let handler = _handlers["@peartube/get-video-metadata"] as? (GetVideoMetadataRequest) async throws -> GetVideoMetadataResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getVideoMetadataRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getVideoMetadataResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 85:   // @peartube/set-video-thumbnail
      guard let handler = _handlers["@peartube/set-video-thumbnail"] as? (SetVideoThumbnailRequest) async throws -> SetVideoThumbnailResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(setVideoThumbnailRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(setVideoThumbnailResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 86:   // @peartube/set-video-thumbnail-from-file
      guard let handler = _handlers["@peartube/set-video-thumbnail-from-file"] as? (SetVideoThumbnailFromFileRequest) async throws -> SetVideoThumbnailFromFileResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(setVideoThumbnailFromFileRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(setVideoThumbnailFromFileResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 87:   // @peartube/get-status
      guard let handler = _handlers["@peartube/get-status"] as? (GetStatusRequest) async throws -> GetStatusResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getStatusRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getStatusResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 88:   // @peartube/pick-video-file
      guard let handler = _handlers["@peartube/pick-video-file"] as? (PickVideoFileRequest) async throws -> PickVideoFileResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(pickVideoFileRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(pickVideoFileResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 89:   // @peartube/pick-image-file
      guard let handler = _handlers["@peartube/pick-image-file"] as? (PickImageFileRequest) async throws -> PickImageFileResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(pickImageFileRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(pickImageFileResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 90:   // @peartube/get-blob-server-port
      guard let handler = _handlers["@peartube/get-blob-server-port"] as? (GetBlobServerPortRequest) async throws -> GetBlobServerPortResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getBlobServerPortRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getBlobServerPortResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 91:   // @peartube/global-search-videos
      guard let handler = _handlers["@peartube/global-search-videos"] as? (GlobalSearchVideosRequest) async throws -> GlobalSearchVideosResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(globalSearchVideosRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(globalSearchVideosResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 92:   // @peartube/add-comment
      guard let handler = _handlers["@peartube/add-comment"] as? (AddCommentRequest) async throws -> AddCommentResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(addCommentRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(addCommentResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 93:   // @peartube/list-comments
      guard let handler = _handlers["@peartube/list-comments"] as? (ListCommentsRequest) async throws -> ListCommentsResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(listCommentsRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(listCommentsResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 94:   // @peartube/hide-comment
      guard let handler = _handlers["@peartube/hide-comment"] as? (HideCommentRequest) async throws -> HideCommentResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(hideCommentRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(hideCommentResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 95:   // @peartube/remove-comment
      guard let handler = _handlers["@peartube/remove-comment"] as? (RemoveCommentRequest) async throws -> RemoveCommentResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(removeCommentRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(removeCommentResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 96:   // @peartube/add-reaction
      guard let handler = _handlers["@peartube/add-reaction"] as? (AddReactionRequest) async throws -> AddReactionResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(addReactionRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(addReactionResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 97:   // @peartube/remove-reaction
      guard let handler = _handlers["@peartube/remove-reaction"] as? (RemoveReactionRequest) async throws -> RemoveReactionResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(removeReactionRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(removeReactionResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 98:   // @peartube/get-reactions
      guard let handler = _handlers["@peartube/get-reactions"] as? (GetReactionsRequest) async throws -> GetReactionsResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getReactionsRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getReactionsResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 106:   // @peartube/cast-available
      guard let handler = _handlers["@peartube/cast-available"] as? (CastAvailableRequest) async throws -> CastAvailableResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(castAvailableRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(castAvailableResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 107:   // @peartube/cast-start-discovery
      guard let handler = _handlers["@peartube/cast-start-discovery"] as? (CastStartDiscoveryRequest) async throws -> CastStartDiscoveryResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(castStartDiscoveryRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(castStartDiscoveryResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 108:   // @peartube/cast-stop-discovery
      guard let handler = _handlers["@peartube/cast-stop-discovery"] as? (CastStopDiscoveryRequest) async throws -> CastStopDiscoveryResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(castStopDiscoveryRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(castStopDiscoveryResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 109:   // @peartube/cast-get-devices
      guard let handler = _handlers["@peartube/cast-get-devices"] as? (CastGetDevicesRequest) async throws -> CastGetDevicesResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(castGetDevicesRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(castGetDevicesResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 110:   // @peartube/cast-add-manual-device
      guard let handler = _handlers["@peartube/cast-add-manual-device"] as? (CastAddManualDeviceRequest) async throws -> CastAddManualDeviceResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(castAddManualDeviceRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(castAddManualDeviceResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 111:   // @peartube/cast-connect
      guard let handler = _handlers["@peartube/cast-connect"] as? (CastConnectRequest) async throws -> CastConnectResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(castConnectRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(castConnectResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 112:   // @peartube/cast-disconnect
      guard let handler = _handlers["@peartube/cast-disconnect"] as? (CastDisconnectRequest) async throws -> CastDisconnectResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(castDisconnectRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(castDisconnectResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 113:   // @peartube/cast-play
      guard let handler = _handlers["@peartube/cast-play"] as? (CastPlayRequest) async throws -> CastPlayResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(castPlayRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(castPlayResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 114:   // @peartube/cast-pause
      guard let handler = _handlers["@peartube/cast-pause"] as? (CastPauseRequest) async throws -> CastPauseResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(castPauseRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(castPauseResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 115:   // @peartube/cast-resume
      guard let handler = _handlers["@peartube/cast-resume"] as? (CastResumeRequest) async throws -> CastResumeResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(castResumeRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(castResumeResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 116:   // @peartube/cast-stop
      guard let handler = _handlers["@peartube/cast-stop"] as? (CastStopRequest) async throws -> CastStopResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(castStopRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(castStopResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 117:   // @peartube/cast-seek
      guard let handler = _handlers["@peartube/cast-seek"] as? (CastSeekRequest) async throws -> CastSeekResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(castSeekRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(castSeekResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 118:   // @peartube/cast-set-volume
      guard let handler = _handlers["@peartube/cast-set-volume"] as? (CastSetVolumeRequest) async throws -> CastSetVolumeResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(castSetVolumeRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(castSetVolumeResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 119:   // @peartube/cast-get-state
      guard let handler = _handlers["@peartube/cast-get-state"] as? (CastGetStateRequest) async throws -> CastGetStateResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(castGetStateRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(castGetStateResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 120:   // @peartube/cast-is-connected
      guard let handler = _handlers["@peartube/cast-is-connected"] as? (CastIsConnectedRequest) async throws -> CastIsConnectedResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(castIsConnectedRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(castIsConnectedResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 125:   // @peartube/search-videos
      guard let handler = _handlers["@peartube/search-videos"] as? (SearchVideosRequest) async throws -> SearchVideosResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(searchVideosRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(searchVideosResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
      }
      do {
        let args = try _decode(logWatchEventRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(logWatchEventResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 126:   // @peartube/index-video-vectors
      guard let handler = _handlers["@peartube/index-video-vectors"] as? (IndexVideoVectorsRequest) async throws -> IndexVideoVectorsResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(indexVideoVectorsRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(indexVideoVectorsResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
      }
      do {
        let args = try _decode(getRecommendationsRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getRecommendationsResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
      }
      do {
        let args = try _decode(getVideoRecommendationsRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getVideoRecommendationsResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 127:   // @peartube/update-video-metadata
      guard let handler = _handlers["@peartube/update-video-metadata"] as? (UpdateVideoMetadataRequest) async throws -> UpdateVideoMetadataResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(updateVideoMetadataRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(updateVideoMetadataResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 128:   // @peartube/get-media-catalog
      guard let handler = _handlers["@peartube/get-media-catalog"] as? (MediaPageRequest) async throws -> GetMediaCatalogResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(mediaPageRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getMediaCatalogResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 129:   // @peartube/get-media-entity
      guard let handler = _handlers["@peartube/get-media-entity"] as? (GetMediaEntityRequest) async throws -> GetMediaEntityResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getMediaEntityRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getMediaEntityResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 130:   // @peartube/get-media-collection
      guard let handler = _handlers["@peartube/get-media-collection"] as? (GetMediaCollectionRequest) async throws -> GetMediaCollectionResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getMediaCollectionRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getMediaCollectionResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 131:   // @peartube/get-media-collection-items
      guard let handler = _handlers["@peartube/get-media-collection-items"] as? (GetMediaCollectionItemsRequest) async throws -> GetMediaCollectionItemsResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getMediaCollectionItemsRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getMediaCollectionItemsResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 132:   // @peartube/get-media-agent
      guard let handler = _handlers["@peartube/get-media-agent"] as? (GetMediaAgentRequest) async throws -> GetMediaAgentResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getMediaAgentRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getMediaAgentResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 133:   // @peartube/get-agent-contributions
      guard let handler = _handlers["@peartube/get-agent-contributions"] as? (GetAgentContributionsRequest) async throws -> GetAgentContributionsResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getAgentContributionsRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getAgentContributionsResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 134:   // @peartube/get-publication-sources
      guard let handler = _handlers["@peartube/get-publication-sources"] as? (GetPublicationSourcesRequest) async throws -> GetPublicationSourcesResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getPublicationSourcesRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getPublicationSourcesResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 136:   // @peartube/get-claim-provenance
      guard let handler = _handlers["@peartube/get-claim-provenance"] as? (GetClaimProvenanceRequest) async throws -> GetClaimProvenanceResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(getClaimProvenanceRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(getClaimProvenanceResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 137:   // @peartube/set-source-preference
      guard let handler = _handlers["@peartube/set-source-preference"] as? (SetSourcePreferenceRequest) async throws -> SetSourcePreferenceResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(setSourcePreferenceRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(setSourcePreferenceResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 138:   // @peartube/prepare-media-playback
      guard let handler = _handlers["@peartube/prepare-media-playback"] as? (PrepareMediaPlaybackRequest) async throws -> PrepareMediaPlaybackResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(prepareMediaPlaybackRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(prepareMediaPlaybackResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 139:   // @peartube/desktop-bootstrap
      guard let handler = _handlers["@peartube/desktop-bootstrap"] as? (DesktopBootstrapRequest) async throws -> DesktopBootstrapResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(desktopBootstrapRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(desktopBootstrapResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 140:   // @peartube/desktop-shutdown
      guard let handler = _handlers["@peartube/desktop-shutdown"] as? (DesktopShutdownRequest) async throws -> DesktopShutdownResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(desktopShutdownRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(desktopShutdownResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 141:   // @peartube/desktop-refresh-browse
      guard let handler = _handlers["@peartube/desktop-refresh-browse"] as? (DesktopRefreshBrowseRequest) async throws -> DesktopRefreshBrowseResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(desktopRefreshBrowseRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(desktopRefreshBrowseResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 142:   // @peartube/ffmpeg-decode-available
      guard let handler = _handlers["@peartube/ffmpeg-decode-available"] as? (FfmpegDecodeAvailableRequest) async throws -> FfmpegDecodeAvailableResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(ffmpegDecodeAvailableRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(ffmpegDecodeAvailableResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 143:   // @peartube/update-channel-avatar
      guard let handler = _handlers["@peartube/update-channel-avatar"] as? (UpdateChannelAvatarRequest) async throws -> UpdateChannelAvatarResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(updateChannelAvatarRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(updateChannelAvatarResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 144:   // @peartube/transcode-start
      guard let handler = _handlers["@peartube/transcode-start"] as? (TranscodeStartRequest) async throws -> TranscodeStartResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(transcodeStartRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(transcodeStartResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 145:   // @peartube/transcode-stop
      guard let handler = _handlers["@peartube/transcode-stop"] as? (TranscodeStopRequest) async throws -> TranscodeStopResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(transcodeStopRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(transcodeStopResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 146:   // @peartube/transcode-status
      guard let handler = _handlers["@peartube/transcode-status"] as? (TranscodeStatusRequest) async throws -> TranscodeStatusResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(transcodeStatusRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(transcodeStatusResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 44:   // @peartube/create-personal-device-invite
      guard let handler = _handlers["@peartube/create-personal-device-invite"] as? (CreatePersonalDeviceInviteRequest) async throws -> CreatePersonalDeviceInviteResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(createPersonalDeviceInviteRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(createPersonalDeviceInviteResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 45:   // @peartube/redeem-personal-device-invite
      guard let handler = _handlers["@peartube/redeem-personal-device-invite"] as? (RedeemPersonalDeviceInviteRequest) async throws -> RedeemPersonalDeviceInviteResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(redeemPersonalDeviceInviteRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(redeemPersonalDeviceInviteResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 46:   // @peartube/list-personal-devices
      guard let handler = _handlers["@peartube/list-personal-devices"] as? (ListPersonalDevicesRequest) async throws -> ListPersonalDevicesResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(listPersonalDevicesRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(listPersonalDevicesResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    case 47:   // @peartube/revoke-personal-device
      guard let handler = _handlers["@peartube/revoke-personal-device"] as? (RevokePersonalDeviceRequest) async throws -> RevokePersonalDeviceResponse else { req.reject("No handler registered", code: "NO_HANDLER"); return }
      guard let rawData = req.data else {
        req.reject("Missing request data", code: "BAD_REQUEST")
        return
      }
      do {
        let args = try _decode(revokePersonalDeviceRequest, rawData)
        let response = try await handler(args)
        req.reply(try _encode(revokePersonalDeviceResponse, response))
      } catch {
        req.reject(error.localizedDescription, code: "HANDLER_ERROR")
      }
    default:
      req.reject("Unknown command", code: "UNKNOWN_COMMAND")
    }
  }
  private func _dispatchEvent(_ event: IncomingEvent) async {
    switch event.command {
    case 99:   // @peartube/event-ready
      guard let handler = _handlers["@peartube/event-ready"] as? (EventReady) async -> Void else { return }
      guard let rawData = event.data else { return }
      do {
        let args = try _decode(eventReady, rawData)
        await handler(args)
      } catch {
        _outerDelegate?.rpc(_rpc, didFailWith: error)
      }
    case 100:   // @peartube/event-error
      guard let handler = _handlers["@peartube/event-error"] as? (EventError) async -> Void else { return }
      guard let rawData = event.data else { return }
      do {
        let args = try _decode(eventError, rawData)
        await handler(args)
      } catch {
        _outerDelegate?.rpc(_rpc, didFailWith: error)
      }
    case 101:   // @peartube/event-upload-progress
      guard let handler = _handlers["@peartube/event-upload-progress"] as? (EventUploadProgress) async -> Void else { return }
      guard let rawData = event.data else { return }
      do {
        let args = try _decode(eventUploadProgress, rawData)
        await handler(args)
      } catch {
        _outerDelegate?.rpc(_rpc, didFailWith: error)
      }
    case 102:   // @peartube/event-download-progress
      guard let handler = _handlers["@peartube/event-download-progress"] as? (EventDownloadProgress) async -> Void else { return }
      guard let rawData = event.data else { return }
      do {
        let args = try _decode(eventDownloadProgress, rawData)
        await handler(args)
      } catch {
        _outerDelegate?.rpc(_rpc, didFailWith: error)
      }
    case 103:   // @peartube/event-media-graph-update
      guard let handler = _handlers["@peartube/event-media-graph-update"] as? (EventMediaGraphUpdate) async -> Void else { return }
      guard let rawData = event.data else { return }
      do {
        let args = try _decode(eventMediaGraphUpdate, rawData)
        await handler(args)
      } catch {
        _outerDelegate?.rpc(_rpc, didFailWith: error)
      }
    case 104:   // @peartube/event-log
      guard let handler = _handlers["@peartube/event-log"] as? (EventLog) async -> Void else { return }
      guard let rawData = event.data else { return }
      do {
        let args = try _decode(eventLog, rawData)
        await handler(args)
      } catch {
        _outerDelegate?.rpc(_rpc, didFailWith: error)
      }
    case 105:   // @peartube/event-video-stats
      guard let handler = _handlers["@peartube/event-video-stats"] as? (EventVideoStats) async -> Void else { return }
      guard let rawData = event.data else { return }
      do {
        let args = try _decode(eventVideoStats, rawData)
        await handler(args)
      } catch {
        _outerDelegate?.rpc(_rpc, didFailWith: error)
      }
    case 121:   // @peartube/event-cast-device-found
      guard let handler = _handlers["@peartube/event-cast-device-found"] as? (EventCastDeviceFound) async -> Void else { return }
      guard let rawData = event.data else { return }
      do {
        let args = try _decode(eventCastDeviceFound, rawData)
        await handler(args)
      } catch {
        _outerDelegate?.rpc(_rpc, didFailWith: error)
      }
    case 122:   // @peartube/event-cast-device-lost
      guard let handler = _handlers["@peartube/event-cast-device-lost"] as? (EventCastDeviceLost) async -> Void else { return }
      guard let rawData = event.data else { return }
      do {
        let args = try _decode(eventCastDeviceLost, rawData)
        await handler(args)
      } catch {
        _outerDelegate?.rpc(_rpc, didFailWith: error)
      }
    case 123:   // @peartube/event-cast-playback-state
      guard let handler = _handlers["@peartube/event-cast-playback-state"] as? (EventCastPlaybackState) async -> Void else { return }
      guard let rawData = event.data else { return }
      do {
        let args = try _decode(eventCastPlaybackState, rawData)
        await handler(args)
      } catch {
        _outerDelegate?.rpc(_rpc, didFailWith: error)
      }
    case 124:   // @peartube/event-cast-time-update
      guard let handler = _handlers["@peartube/event-cast-time-update"] as? (EventCastTimeUpdate) async -> Void else { return }
      guard let rawData = event.data else { return }
      do {
        let args = try _decode(eventCastTimeUpdate, rawData)
        await handler(args)
      } catch {
        _outerDelegate?.rpc(_rpc, didFailWith: error)
      }
    case 147:   // @peartube/event-transcode-progress
      guard let handler = _handlers["@peartube/event-transcode-progress"] as? (EventTranscodeProgress) async -> Void else { return }
      guard let rawData = event.data else { return }
      do {
        let args = try _decode(eventTranscodeProgress, rawData)
        await handler(args)
      } catch {
        _outerDelegate?.rpc(_rpc, didFailWith: error)
      }
    default:
      break
    }
  }

  private func _encode<C: Codec>(_ codec: C, _ value: C.Value) throws -> Data {
    var state = State()
    codec.preencode(&state, value)
    state.allocate()
    try codec.encode(&state, value)
    return state.buffer
  }

  private func _decode<C: Codec>(_ codec: C, _ data: Data) throws -> C.Value {
    var state = State(data)
    return try codec.decode(&state)
  }
}
