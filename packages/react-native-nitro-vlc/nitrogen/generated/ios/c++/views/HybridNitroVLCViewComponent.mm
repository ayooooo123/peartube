///
/// HybridNitroVLCViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridNitroVLCViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridNitroVLCViewSpecSwift.hpp"
#import "NitroVLC-Swift-Cxx-Umbrella.hpp"

using namespace facebook;
using namespace margelo::nitro::nitrovlc;
using namespace margelo::nitro::nitrovlc::views;

/**
 * Represents the React Native View holder for the Nitro "NitroVLCView" HybridView.
 */
@interface HybridNitroVLCViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridNitroVLCViewComponent {
  std::shared_ptr<HybridNitroVLCViewSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridNitroVLCViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridNitroVLCViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridNitroVLCViewSpec> hybridView = NitroVLC::NitroVLCAutolinking::createNitroVLCView();
    _hybridView = std::dynamic_pointer_cast<HybridNitroVLCViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroVLC::HybridNitroVLCViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridNitroVLCViewProps const>(props);
  auto& newViewProps = const_cast<HybridNitroVLCViewProps&>(newViewPropsConst);
  NitroVLC::HybridNitroVLCViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // source: struct
  if (newViewProps.source.isDirty) {
    swiftPart.setSource(newViewProps.source.value);
    newViewProps.source.isDirty = false;
  }
  // subtitleUri: optional
  if (newViewProps.subtitleUri.isDirty) {
    swiftPart.setSubtitleUri(newViewProps.subtitleUri.value);
    newViewProps.subtitleUri.isDirty = false;
  }
  // paused: optional
  if (newViewProps.paused.isDirty) {
    swiftPart.setPaused(newViewProps.paused.value);
    newViewProps.paused.isDirty = false;
  }
  // loop: optional
  if (newViewProps.loop.isDirty) {
    swiftPart.setLoop(newViewProps.loop.value);
    newViewProps.loop.isDirty = false;
  }
  // rate: optional
  if (newViewProps.rate.isDirty) {
    swiftPart.setRate(newViewProps.rate.value);
    newViewProps.rate.isDirty = false;
  }
  // seek: optional
  if (newViewProps.seek.isDirty) {
    swiftPart.setSeek(newViewProps.seek.value);
    newViewProps.seek.isDirty = false;
  }
  // volume: optional
  if (newViewProps.volume.isDirty) {
    swiftPart.setVolume(newViewProps.volume.value);
    newViewProps.volume.isDirty = false;
  }
  // muted: optional
  if (newViewProps.muted.isDirty) {
    swiftPart.setMuted(newViewProps.muted.value);
    newViewProps.muted.isDirty = false;
  }
  // audioTrack: optional
  if (newViewProps.audioTrack.isDirty) {
    swiftPart.setAudioTrack(newViewProps.audioTrack.value);
    newViewProps.audioTrack.isDirty = false;
  }
  // textTrack: optional
  if (newViewProps.textTrack.isDirty) {
    swiftPart.setTextTrack(newViewProps.textTrack.value);
    newViewProps.textTrack.isDirty = false;
  }
  // playInBackground: optional
  if (newViewProps.playInBackground.isDirty) {
    swiftPart.setPlayInBackground(newViewProps.playInBackground.value);
    newViewProps.playInBackground.isDirty = false;
  }
  // videoAspectRatio: optional
  if (newViewProps.videoAspectRatio.isDirty) {
    swiftPart.setVideoAspectRatio(newViewProps.videoAspectRatio.value);
    newViewProps.videoAspectRatio.isDirty = false;
  }
  // autoAspectRatio: optional
  if (newViewProps.autoAspectRatio.isDirty) {
    swiftPart.setAutoAspectRatio(newViewProps.autoAspectRatio.value);
    newViewProps.autoAspectRatio.isDirty = false;
  }
  // resizeMode: optional
  if (newViewProps.resizeMode.isDirty) {
    swiftPart.setResizeMode(newViewProps.resizeMode.value);
    newViewProps.resizeMode.isDirty = false;
  }
  // autoplay: optional
  if (newViewProps.autoplay.isDirty) {
    swiftPart.setAutoplay(newViewProps.autoplay.value);
    newViewProps.autoplay.isDirty = false;
  }
  // acceptInvalidCertificates: optional
  if (newViewProps.acceptInvalidCertificates.isDirty) {
    swiftPart.setAcceptInvalidCertificates(newViewProps.acceptInvalidCertificates.value);
    newViewProps.acceptInvalidCertificates.isDirty = false;
  }
  // onPlaying: optional
  if (newViewProps.onPlaying.isDirty) {
    swiftPart.setOnPlaying(newViewProps.onPlaying.value);
    newViewProps.onPlaying.isDirty = false;
  }
  // onProgress: optional
  if (newViewProps.onProgress.isDirty) {
    swiftPart.setOnProgress(newViewProps.onProgress.value);
    newViewProps.onProgress.isDirty = false;
  }
  // onPaused: optional
  if (newViewProps.onPaused.isDirty) {
    swiftPart.setOnPaused(newViewProps.onPaused.value);
    newViewProps.onPaused.isDirty = false;
  }
  // onStopped: optional
  if (newViewProps.onStopped.isDirty) {
    swiftPart.setOnStopped(newViewProps.onStopped.value);
    newViewProps.onStopped.isDirty = false;
  }
  // onBuffering: optional
  if (newViewProps.onBuffering.isDirty) {
    swiftPart.setOnBuffering(newViewProps.onBuffering.value);
    newViewProps.onBuffering.isDirty = false;
  }
  // onEnded: optional
  if (newViewProps.onEnded.isDirty) {
    swiftPart.setOnEnded(newViewProps.onEnded.value);
    newViewProps.onEnded.isDirty = false;
  }
  // onError: optional
  if (newViewProps.onError.isDirty) {
    swiftPart.setOnError(newViewProps.onError.value);
    newViewProps.onError.isDirty = false;
  }
  // onLoad: optional
  if (newViewProps.onLoad.isDirty) {
    swiftPart.setOnLoad(newViewProps.onLoad.value);
    newViewProps.onLoad.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return NitroVLC::NitroVLCAutolinking::isNitroVLCViewRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  NitroVLC::HybridNitroVLCViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

@end
