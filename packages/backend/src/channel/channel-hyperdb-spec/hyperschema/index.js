// This file is autogenerated by the hyperschema compiler
// Schema Version: 1
/* eslint-disable camelcase */
/* eslint-disable quotes */
/* eslint-disable space-before-function-paren */
/* eslint-disable @typescript-eslint/no-require-imports */

const { c } = require('hyperschema/runtime')

const VERSION = 1

// eslint-disable-next-line no-unused-vars
let version = VERSION

// @peartubeChannel/metadata
const encoding0 = {
  preencode(state, m) {
    c.string.preencode(state, m.key)
    state.end += 2 // flags are fixed size

    if (m.name) c.string.preencode(state, m.name)
    if (m.description) c.string.preencode(state, m.description)
    if (m.avatar) c.string.preencode(state, m.avatar)
    if (m.publicBeeKey) c.string.preencode(state, m.publicBeeKey)
    if (m.commentsDbKey) c.string.preencode(state, m.commentsDbKey)
    if (m.commentsAdminKey) c.string.preencode(state, m.commentsAdminKey)
    if (m.createdAt) c.uint64.preencode(state, m.createdAt)
    if (m.createdBy) c.string.preencode(state, m.createdBy)
    if (m.updatedAt) c.uint64.preencode(state, m.updatedAt)
    if (m.updatedBy) c.string.preencode(state, m.updatedBy)
    if (m.schemaVersion) c.uint64.preencode(state, m.schemaVersion)
    if (m.logicalClock) c.uint64.preencode(state, m.logicalClock)
  },
  encode(state, m) {
    const flags =
      (m.name ? 1 : 0) |
      (m.description ? 2 : 0) |
      (m.avatar ? 4 : 0) |
      (m.publicBeeKey ? 8 : 0) |
      (m.commentsDbKey ? 16 : 0) |
      (m.commentsAdminKey ? 32 : 0) |
      (m.createdAt ? 64 : 0) |
      (m.createdBy ? 128 : 0) |
      (m.updatedAt ? 256 : 0) |
      (m.updatedBy ? 512 : 0) |
      (m.schemaVersion ? 1024 : 0) |
      (m.logicalClock ? 2048 : 0)

    c.string.encode(state, m.key)
    c.uint16.encode(state, flags)

    if (m.name) c.string.encode(state, m.name)
    if (m.description) c.string.encode(state, m.description)
    if (m.avatar) c.string.encode(state, m.avatar)
    if (m.publicBeeKey) c.string.encode(state, m.publicBeeKey)
    if (m.commentsDbKey) c.string.encode(state, m.commentsDbKey)
    if (m.commentsAdminKey) c.string.encode(state, m.commentsAdminKey)
    if (m.createdAt) c.uint64.encode(state, m.createdAt)
    if (m.createdBy) c.string.encode(state, m.createdBy)
    if (m.updatedAt) c.uint64.encode(state, m.updatedAt)
    if (m.updatedBy) c.string.encode(state, m.updatedBy)
    if (m.schemaVersion) c.uint64.encode(state, m.schemaVersion)
    if (m.logicalClock) c.uint64.encode(state, m.logicalClock)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint16.decode(state)

    return {
      key: r0,
      name: (flags & 1) !== 0 ? c.string.decode(state) : null,
      description: (flags & 2) !== 0 ? c.string.decode(state) : null,
      avatar: (flags & 4) !== 0 ? c.string.decode(state) : null,
      publicBeeKey: (flags & 8) !== 0 ? c.string.decode(state) : null,
      commentsDbKey: (flags & 16) !== 0 ? c.string.decode(state) : null,
      commentsAdminKey: (flags & 32) !== 0 ? c.string.decode(state) : null,
      createdAt: (flags & 64) !== 0 ? c.uint64.decode(state) : 0,
      createdBy: (flags & 128) !== 0 ? c.string.decode(state) : null,
      updatedAt: (flags & 256) !== 0 ? c.uint64.decode(state) : 0,
      updatedBy: (flags & 512) !== 0 ? c.string.decode(state) : null,
      schemaVersion: (flags & 1024) !== 0 ? c.uint64.decode(state) : 0,
      logicalClock: (flags & 2048) !== 0 ? c.uint64.decode(state) : 0
    }
  }
}

// @peartubeChannel/writer
const encoding1 = {
  preencode(state, m) {
    c.string.preencode(state, m.keyHex)
    state.end++ // flags are fixed size

    if (m.role) c.string.preencode(state, m.role)
    if (m.deviceName) c.string.preencode(state, m.deviceName)
    if (m.blobDriveKey) c.string.preencode(state, m.blobDriveKey)
    if (m.addedAt) c.uint64.preencode(state, m.addedAt)
    if (m.updatedAt) c.uint64.preencode(state, m.updatedAt)
    if (m.removedAt) c.uint64.preencode(state, m.removedAt)
    if (m.swarmKeyHex) c.string.preencode(state, m.swarmKeyHex)
  },
  encode(state, m) {
    const flags =
      (m.role ? 1 : 0) |
      (m.deviceName ? 2 : 0) |
      (m.blobDriveKey ? 4 : 0) |
      (m.addedAt ? 8 : 0) |
      (m.updatedAt ? 16 : 0) |
      (m.removedAt ? 32 : 0) |
      (m.banned ? 64 : 0) |
      (m.swarmKeyHex ? 128 : 0)

    c.string.encode(state, m.keyHex)
    c.uint8.encode(state, flags)

    if (m.role) c.string.encode(state, m.role)
    if (m.deviceName) c.string.encode(state, m.deviceName)
    if (m.blobDriveKey) c.string.encode(state, m.blobDriveKey)
    if (m.addedAt) c.uint64.encode(state, m.addedAt)
    if (m.updatedAt) c.uint64.encode(state, m.updatedAt)
    if (m.removedAt) c.uint64.encode(state, m.removedAt)
    if (m.swarmKeyHex) c.string.encode(state, m.swarmKeyHex)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint8.decode(state)

    return {
      keyHex: r0,
      role: (flags & 1) !== 0 ? c.string.decode(state) : null,
      deviceName: (flags & 2) !== 0 ? c.string.decode(state) : null,
      blobDriveKey: (flags & 4) !== 0 ? c.string.decode(state) : null,
      addedAt: (flags & 8) !== 0 ? c.uint64.decode(state) : 0,
      updatedAt: (flags & 16) !== 0 ? c.uint64.decode(state) : 0,
      removedAt: (flags & 32) !== 0 ? c.uint64.decode(state) : 0,
      banned: (flags & 64) !== 0,
      swarmKeyHex: (flags & 128) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @peartubeChannel/video
const encoding2 = {
  preencode(state, m) {
    c.string.preencode(state, m.id)
    state.end += 3 // flags are fixed size

    if (m.title) c.string.preencode(state, m.title)
    if (m.description) c.string.preencode(state, m.description)
    if (m.path) c.string.preencode(state, m.path)
    if (m.duration) c.uint64.preencode(state, m.duration)
    if (m.thumbnail) c.string.preencode(state, m.thumbnail)
    if (m.thumbnailBlobId) c.string.preencode(state, m.thumbnailBlobId)
    if (m.thumbnailBlobsCoreKey) c.string.preencode(state, m.thumbnailBlobsCoreKey)
    if (m.thumbnailMimeType) c.string.preencode(state, m.thumbnailMimeType)
    if (m.blobId) c.string.preencode(state, m.blobId)
    if (m.blobsCoreKey) c.string.preencode(state, m.blobsCoreKey)
    if (m.blobDriveKey) c.string.preencode(state, m.blobDriveKey)
    if (m.mimeType) c.string.preencode(state, m.mimeType)
    if (m.size) c.uint64.preencode(state, m.size)
    if (m.category) c.string.preencode(state, m.category)
    if (m.views) c.uint64.preencode(state, m.views)
    if (m.uploadedAt) c.uint64.preencode(state, m.uploadedAt)
    if (m.uploadedBy) c.string.preencode(state, m.uploadedBy)
    if (m.updatedAt) c.uint64.preencode(state, m.updatedAt)
    if (m.updatedBy) c.string.preencode(state, m.updatedBy)
    if (m.schemaVersion) c.uint64.preencode(state, m.schemaVersion)
    if (m.logicalClock) c.uint64.preencode(state, m.logicalClock)
  },
  encode(state, m) {
    const flags =
      (m.title ? 1 : 0) |
      (m.description ? 2 : 0) |
      (m.path ? 4 : 0) |
      (m.duration ? 8 : 0) |
      (m.thumbnail ? 16 : 0) |
      (m.thumbnailBlobId ? 32 : 0) |
      (m.thumbnailBlobsCoreKey ? 64 : 0) |
      (m.thumbnailMimeType ? 128 : 0) |
      (m.blobId ? 256 : 0) |
      (m.blobsCoreKey ? 512 : 0) |
      (m.blobDriveKey ? 1024 : 0) |
      (m.mimeType ? 2048 : 0) |
      (m.size ? 4096 : 0) |
      (m.category ? 8192 : 0) |
      (m.views ? 16384 : 0) |
      (m.uploadedAt ? 32768 : 0) |
      (m.uploadedBy ? 65536 : 0) |
      (m.updatedAt ? 131072 : 0) |
      (m.updatedBy ? 262144 : 0) |
      (m.schemaVersion ? 524288 : 0) |
      (m.logicalClock ? 1048576 : 0)

    c.string.encode(state, m.id)
    c.uint24.encode(state, flags)

    if (m.title) c.string.encode(state, m.title)
    if (m.description) c.string.encode(state, m.description)
    if (m.path) c.string.encode(state, m.path)
    if (m.duration) c.uint64.encode(state, m.duration)
    if (m.thumbnail) c.string.encode(state, m.thumbnail)
    if (m.thumbnailBlobId) c.string.encode(state, m.thumbnailBlobId)
    if (m.thumbnailBlobsCoreKey) c.string.encode(state, m.thumbnailBlobsCoreKey)
    if (m.thumbnailMimeType) c.string.encode(state, m.thumbnailMimeType)
    if (m.blobId) c.string.encode(state, m.blobId)
    if (m.blobsCoreKey) c.string.encode(state, m.blobsCoreKey)
    if (m.blobDriveKey) c.string.encode(state, m.blobDriveKey)
    if (m.mimeType) c.string.encode(state, m.mimeType)
    if (m.size) c.uint64.encode(state, m.size)
    if (m.category) c.string.encode(state, m.category)
    if (m.views) c.uint64.encode(state, m.views)
    if (m.uploadedAt) c.uint64.encode(state, m.uploadedAt)
    if (m.uploadedBy) c.string.encode(state, m.uploadedBy)
    if (m.updatedAt) c.uint64.encode(state, m.updatedAt)
    if (m.updatedBy) c.string.encode(state, m.updatedBy)
    if (m.schemaVersion) c.uint64.encode(state, m.schemaVersion)
    if (m.logicalClock) c.uint64.encode(state, m.logicalClock)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint24.decode(state)

    return {
      id: r0,
      title: (flags & 1) !== 0 ? c.string.decode(state) : null,
      description: (flags & 2) !== 0 ? c.string.decode(state) : null,
      path: (flags & 4) !== 0 ? c.string.decode(state) : null,
      duration: (flags & 8) !== 0 ? c.uint64.decode(state) : 0,
      thumbnail: (flags & 16) !== 0 ? c.string.decode(state) : null,
      thumbnailBlobId: (flags & 32) !== 0 ? c.string.decode(state) : null,
      thumbnailBlobsCoreKey: (flags & 64) !== 0 ? c.string.decode(state) : null,
      thumbnailMimeType: (flags & 128) !== 0 ? c.string.decode(state) : null,
      blobId: (flags & 256) !== 0 ? c.string.decode(state) : null,
      blobsCoreKey: (flags & 512) !== 0 ? c.string.decode(state) : null,
      blobDriveKey: (flags & 1024) !== 0 ? c.string.decode(state) : null,
      mimeType: (flags & 2048) !== 0 ? c.string.decode(state) : null,
      size: (flags & 4096) !== 0 ? c.uint64.decode(state) : 0,
      category: (flags & 8192) !== 0 ? c.string.decode(state) : null,
      views: (flags & 16384) !== 0 ? c.uint64.decode(state) : 0,
      uploadedAt: (flags & 32768) !== 0 ? c.uint64.decode(state) : 0,
      uploadedBy: (flags & 65536) !== 0 ? c.string.decode(state) : null,
      updatedAt: (flags & 131072) !== 0 ? c.uint64.decode(state) : 0,
      updatedBy: (flags & 262144) !== 0 ? c.string.decode(state) : null,
      schemaVersion: (flags & 524288) !== 0 ? c.uint64.decode(state) : 0,
      logicalClock: (flags & 1048576) !== 0 ? c.uint64.decode(state) : 0
    }
  }
}

// @peartubeChannel/comment
const encoding3 = {
  preencode(state, m) {
    c.string.preencode(state, m.videoId)
    c.string.preencode(state, m.commentId)
    state.end += 2 // flags are fixed size

    if (m.text) c.string.preencode(state, m.text)
    if (m.authorKeyHex) c.string.preencode(state, m.authorKeyHex)
    if (m.timestamp) c.uint64.preencode(state, m.timestamp)
    if (m.parentId) c.string.preencode(state, m.parentId)
    if (m.hiddenBy) c.string.preencode(state, m.hiddenBy)
    if (m.hiddenAt) c.uint64.preencode(state, m.hiddenAt)
    if (m.removedBy) c.string.preencode(state, m.removedBy)
    if (m.removedAt) c.uint64.preencode(state, m.removedAt)
  },
  encode(state, m) {
    const flags =
      (m.text ? 1 : 0) |
      (m.authorKeyHex ? 2 : 0) |
      (m.timestamp ? 4 : 0) |
      (m.parentId ? 8 : 0) |
      (m.hidden ? 16 : 0) |
      (m.hiddenBy ? 32 : 0) |
      (m.hiddenAt ? 64 : 0) |
      (m.removed ? 128 : 0) |
      (m.removedBy ? 256 : 0) |
      (m.removedAt ? 512 : 0)

    c.string.encode(state, m.videoId)
    c.string.encode(state, m.commentId)
    c.uint16.encode(state, flags)

    if (m.text) c.string.encode(state, m.text)
    if (m.authorKeyHex) c.string.encode(state, m.authorKeyHex)
    if (m.timestamp) c.uint64.encode(state, m.timestamp)
    if (m.parentId) c.string.encode(state, m.parentId)
    if (m.hiddenBy) c.string.encode(state, m.hiddenBy)
    if (m.hiddenAt) c.uint64.encode(state, m.hiddenAt)
    if (m.removedBy) c.string.encode(state, m.removedBy)
    if (m.removedAt) c.uint64.encode(state, m.removedAt)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint16.decode(state)

    return {
      videoId: r0,
      commentId: r1,
      text: (flags & 1) !== 0 ? c.string.decode(state) : null,
      authorKeyHex: (flags & 2) !== 0 ? c.string.decode(state) : null,
      timestamp: (flags & 4) !== 0 ? c.uint64.decode(state) : 0,
      parentId: (flags & 8) !== 0 ? c.string.decode(state) : null,
      hidden: (flags & 16) !== 0,
      hiddenBy: (flags & 32) !== 0 ? c.string.decode(state) : null,
      hiddenAt: (flags & 64) !== 0 ? c.uint64.decode(state) : 0,
      removed: (flags & 128) !== 0,
      removedBy: (flags & 256) !== 0 ? c.string.decode(state) : null,
      removedAt: (flags & 512) !== 0 ? c.uint64.decode(state) : 0
    }
  }
}

// @peartubeChannel/reaction
const encoding4 = {
  preencode(state, m) {
    c.string.preencode(state, m.videoId)
    c.string.preencode(state, m.authorKeyHex)
    state.end++ // flags are fixed size

    if (m.reactionType) c.string.preencode(state, m.reactionType)
    if (m.timestamp) c.uint64.preencode(state, m.timestamp)
  },
  encode(state, m) {
    const flags = (m.reactionType ? 1 : 0) | (m.timestamp ? 2 : 0)

    c.string.encode(state, m.videoId)
    c.string.encode(state, m.authorKeyHex)
    c.uint8.encode(state, flags)

    if (m.reactionType) c.string.encode(state, m.reactionType)
    if (m.timestamp) c.uint64.encode(state, m.timestamp)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint8.decode(state)

    return {
      videoId: r0,
      authorKeyHex: r1,
      reactionType: (flags & 1) !== 0 ? c.string.decode(state) : null,
      timestamp: (flags & 2) !== 0 ? c.uint64.decode(state) : 0
    }
  }
}

// @peartubeChannel/invite
const encoding5 = {
  preencode(state, m) {
    c.string.preencode(state, m.idHex)
    state.end++ // flags are fixed size

    if (m.inviteZ32) c.string.preencode(state, m.inviteZ32)
    if (m.publicKeyHex) c.string.preencode(state, m.publicKeyHex)
    if (m.expires) c.uint64.preencode(state, m.expires)
    if (m.createdAt) c.uint64.preencode(state, m.createdAt)
  },
  encode(state, m) {
    const flags =
      (m.inviteZ32 ? 1 : 0) |
      (m.publicKeyHex ? 2 : 0) |
      (m.expires ? 4 : 0) |
      (m.createdAt ? 8 : 0) |
      (m.current ? 16 : 0)

    c.string.encode(state, m.idHex)
    c.uint8.encode(state, flags)

    if (m.inviteZ32) c.string.encode(state, m.inviteZ32)
    if (m.publicKeyHex) c.string.encode(state, m.publicKeyHex)
    if (m.expires) c.uint64.encode(state, m.expires)
    if (m.createdAt) c.uint64.encode(state, m.createdAt)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint8.decode(state)

    return {
      idHex: r0,
      inviteZ32: (flags & 1) !== 0 ? c.string.decode(state) : null,
      publicKeyHex: (flags & 2) !== 0 ? c.string.decode(state) : null,
      expires: (flags & 4) !== 0 ? c.uint64.decode(state) : 0,
      createdAt: (flags & 8) !== 0 ? c.uint64.decode(state) : 0,
      current: (flags & 16) !== 0
    }
  }
}

// @peartubeChannel/watchEvent
const encoding6 = {
  preencode(state, m) {
    c.string.preencode(state, m.videoId)
    c.string.preencode(state, m.eventId)
    state.end++ // flags are fixed size

    if (m.channelKey) c.string.preencode(state, m.channelKey)
    if (m.watcherKeyHex) c.string.preencode(state, m.watcherKeyHex)
    if (m.duration) c.uint64.preencode(state, m.duration)
    if (m.timestamp) c.uint64.preencode(state, m.timestamp)
  },
  encode(state, m) {
    const flags =
      (m.channelKey ? 1 : 0) |
      (m.watcherKeyHex ? 2 : 0) |
      (m.duration ? 4 : 0) |
      (m.completed ? 8 : 0) |
      (m.timestamp ? 16 : 0)

    c.string.encode(state, m.videoId)
    c.string.encode(state, m.eventId)
    c.uint8.encode(state, flags)

    if (m.channelKey) c.string.encode(state, m.channelKey)
    if (m.watcherKeyHex) c.string.encode(state, m.watcherKeyHex)
    if (m.duration) c.uint64.encode(state, m.duration)
    if (m.timestamp) c.uint64.encode(state, m.timestamp)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const flags = c.uint8.decode(state)

    return {
      videoId: r0,
      eventId: r1,
      channelKey: (flags & 1) !== 0 ? c.string.decode(state) : null,
      watcherKeyHex: (flags & 2) !== 0 ? c.string.decode(state) : null,
      duration: (flags & 4) !== 0 ? c.uint64.decode(state) : 0,
      completed: (flags & 8) !== 0,
      timestamp: (flags & 16) !== 0 ? c.uint64.decode(state) : 0
    }
  }
}

// @peartubeChannel/vectorIndex
const encoding7 = {
  preencode(state, m) {
    c.string.preencode(state, m.videoId)
    state.end++ // flags are fixed size

    if (m.vector) c.string.preencode(state, m.vector)
    if (m.text) c.string.preencode(state, m.text)
    if (m.metadata) c.string.preencode(state, m.metadata)
    if (m.indexedAt) c.uint64.preencode(state, m.indexedAt)
  },
  encode(state, m) {
    const flags =
      (m.vector ? 1 : 0) | (m.text ? 2 : 0) | (m.metadata ? 4 : 0) | (m.indexedAt ? 8 : 0)

    c.string.encode(state, m.videoId)
    c.uint8.encode(state, flags)

    if (m.vector) c.string.encode(state, m.vector)
    if (m.text) c.string.encode(state, m.text)
    if (m.metadata) c.string.encode(state, m.metadata)
    if (m.indexedAt) c.uint64.encode(state, m.indexedAt)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint8.decode(state)

    return {
      videoId: r0,
      vector: (flags & 1) !== 0 ? c.string.decode(state) : null,
      text: (flags & 2) !== 0 ? c.string.decode(state) : null,
      metadata: (flags & 4) !== 0 ? c.string.decode(state) : null,
      indexedAt: (flags & 8) !== 0 ? c.uint64.decode(state) : 0
    }
  }
}

function setVersion(v) {
  version = v
}

function encode(name, value, v = VERSION) {
  version = v
  return c.encode(getEncoding(name), value)
}

function decode(name, buffer, v = VERSION) {
  version = v
  return c.decode(getEncoding(name), buffer)
}

function getEnum(name) {
  switch (name) {
    default:
      throw new Error('Enum not found ' + name)
  }
}

function getEncoding(name) {
  switch (name) {
    case '@peartubeChannel/metadata':
      return encoding0
    case '@peartubeChannel/writer':
      return encoding1
    case '@peartubeChannel/video':
      return encoding2
    case '@peartubeChannel/comment':
      return encoding3
    case '@peartubeChannel/reaction':
      return encoding4
    case '@peartubeChannel/invite':
      return encoding5
    case '@peartubeChannel/watchEvent':
      return encoding6
    case '@peartubeChannel/vectorIndex':
      return encoding7
    default:
      throw new Error('Encoder not found ' + name)
  }
}

function getStruct(name, v = VERSION) {
  const enc = getEncoding(name)
  return {
    preencode(state, m) {
      version = v
      enc.preencode(state, m)
    },
    encode(state, m) {
      version = v
      enc.encode(state, m)
    },
    decode(state) {
      version = v
      return enc.decode(state)
    }
  }
}

const resolveStruct = getStruct // compat

module.exports = {
  resolveStruct,
  getStruct,
  getEnum,
  getEncoding,
  encode,
  decode,
  setVersion,
  version
}
