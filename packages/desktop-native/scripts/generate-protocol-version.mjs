import { writeFileSync } from 'node:fs'
import { dirname, join } from 'node:path'
import { fileURLToPath } from 'node:url'
import { PROTOCOL_VERSION } from '../../host/src/contracts.js'

const __dirname = dirname(fileURLToPath(import.meta.url))
const target = join(__dirname, '..', 'Sources', 'Support', 'ProtocolVersion.swift')
writeFileSync(target, `// Generated by packages/desktop-native/scripts/generate-protocol-version.mjs\nlet NativeHostProtocolVersion = ${PROTOCOL_VERSION}\n`)
