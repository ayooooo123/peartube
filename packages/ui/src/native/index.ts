// Native implementations - same as web since react-native-web makes them compatible
// These use the same React Native primitives that work on both platforms
export { Button } from '../web/Button';
export { Card } from '../web/Card';
export { Input } from '../web/Input';
export { Text } from '../web/Text';
