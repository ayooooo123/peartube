/**
 * worker-client.js no longer needs bundling.
 * Pear's renderer has native require() support.
 * The worker-client.js file is a plain CommonJS module.
 */
console.log('worker-client.js: No bundling needed - using native require() in Pear')
